# FleetFlow Legacy `index.html` Map

Generated by `scripts/analyze-legacy-app.mjs` (acorn + acorn-walk + eslint-scope — real
AST/scope resolution, not regex). Deterministic: re-running against unchanged source
produces an identical file. This document contains no runtime code and describes the
file as it exists today; it does not itself change any behavior.

- Total file lines: 20426
- Main application script: lines 1929-20382 (80 banner-delimited sections)
- Section coverage: validated — sections exactly tile lines 1929-20382 with no gaps or overlaps
- Global bindings resolved: 458
- Inline HTML event handlers cataloged: 808

## External CDN scripts

| Line | src |
|---|---|
| 94 | `https://www.gstatic.com/firebasejs/10.8.0/firebase-app-compat.js` |
| 95 | `https://www.gstatic.com/firebasejs/10.8.0/firebase-firestore-compat.js` |
| 96 | `https://www.gstatic.com/firebasejs/10.8.0/firebase-storage-compat.js` |
| 97 | `https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js` |
| 646 | `https://js.stripe.com/v3/` |

## Style blocks

| Start | End | Lines |
|---|---|---|
| 111 | 643 | 534 |
| 650 | 649 | 1 |
| 3729 | 3740 | 13 |
| 5778 | 5777 | 1 |
| 7243 | 7350 | 109 |

## Small standalone `<script>` blocks (outside the main application script)

| Start | End | Lines | Local-testing only (`file:` protocol gated) |
|---|---|---|---|
| 688 | 700 | 14 | YES |
| 99 | 109 | 12 | no |
| 647 | 651 | 6 | YES |

> Blocks flagged local-testing-only are gated behind `location.protocol === 'file:'` and
> do not run in production (Netlify serves over `https:`). Same category as the earlier
> password-bypass finding — real, but should never be mistaken for production auth/startup behavior.

## Sections (main script, in file order)

| # | Section | Start | End | Lines |
|---|---|---|---|---|
| 1 | (preamble — before first banner) | 1929 | 1929 | 1 |
| 2 | FIREBASE INIT | 1930 | 1953 | 24 |
| 3 | OWNER ACCOUNT — Cannot be deleted, deactivated, or role-changed — by anyone except the owner themselves | 1954 | 1957 | 4 |
| 4 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 1958 | 2001 | 44 |
| 5 | TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created | 2002 | 2445 | 444 |
| 6 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 2446 | 2933 | 488 |
| 7 | WAREHOUSE WORK LOGGING — Log warehouse work by employee — shows on calendar + payroll | 2934 | 3145 | 212 |
| 8 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 3146 | 3994 | 849 |
| 9 | ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard | 3995 | 4383 | 389 |
| 10 | LABOR PRODUCTIVITY SCORES — v52 — Uses gate timestamps to calculate efficiency per crew member — Crew A moves 50 cu.ft/hr vs Crew B at 35 cu.ft/hr — Visible to owner/office — used for rewards and training | 4384 | 4605 | 222 |
| 11 | CLAIMS DEFENSE AI — v51 — Pre-load damage detection using Claude vision — When driver takes a photo at pre-load gate, AI scans for — pre-existing damage, flags it, embeds in BOL as evidence. — Protects moving company from false damage claims. | 4606 | 4803 | 198 |
| 12 | SYSTEM HEALTH DASHBOARD — Platform operator view — JPG Ventures sees all licensees, sync status, revenue flow — Accessible only to creator/owner accounts | 4804 | 5288 | 485 |
| 13 | FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill | 5289 | 5312 | 24 |
| 14 | STATE & STORAGE | 5313 | 5840 | 528 |
| 15 | AUTH | 5841 | 5844 | 4 |
| 16 | FIRST RUN CHECK — runs on page load before login | 5845 | 6029 | 185 |
| 17 | PWA — SPLASH, INSTALL PROMPT, SERVICE WORKER | 6030 | 6075 | 46 |
| 18 | MIGRATION — Stamp companyId on any legacy docs missing it — Runs silently on every login — safe to run repeatedly | 6076 | 6121 | 46 |
| 19 | OFFLINE MODE — Full PWA with offline queue — Caches app shell on first load — Queues Firestore writes when offline — Syncs queue automatically when back online | 6122 | 6317 | 196 |
| 20 | USER SETTINGS — stored in localStorage per user | 6318 | 6348 | 31 |
| 21 | SESSION PERSISTENCE — remember me across refreshes/tab closes | 6349 | 6584 | 236 |
| 22 | TABS | 6585 | 6705 | 121 |
| 23 | DASHBOARD | 6706 | 7747 | 1042 |
| 24 | HISTORY | 7748 | 7771 | 24 |
| 25 | FINANCIALS | 7772 | 7840 | 69 |
| 26 | CREW LOG  ← BUG FIX: removed stray content.innerHTML = html; that was outside this function | 7841 | 7845 | 5 |
| 27 | CREW LOG HELPERS — extracted to avoid nested template literal — Chrome syntax errors from 4+ levels of backtick nesting | 7846 | 8175 | 330 |
| 28 | PAYROLL CALCULATION ENGINE — Supports: hourly (hours × rate), flat (fixed amount), daily (rate per day) | 8176 | 8180 | 5 |
| 29 | NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive | 8181 | 8213 | 33 |
| 30 | CARRIER CUBIC FEET HELPERS | 8214 | 8241 | 28 |
| 31 | ESTIMATE TAB — Per-job BOL line item editor — This is the source of truth for the BOL PDF | 8242 | 8533 | 292 |
| 32 | CLIENT MOVE PORTAL — Full transparency experience — This is what the client pays $100-150 for — Real-time gate tracking, item location, messaging, docs | 8534 | 8933 | 400 |
| 33 | 1099 / W2 YEAR-END TAX EXPORT — Aggregates all payments per contractor/employee for the year — Generates: on-screen summary + CSV export + PDF report | 8934 | 9520 | 587 |
| 34 | P&L ACCOUNTING MODULE — Kills QuickBooks — Chart of accounts, monthly P&L, period reports, export | 9521 | 9945 | 425 |
| 35 | TWILIO SMS — Real text notifications — Configure in Settings → Integrations | 9946 | 10011 | 66 |
| 36 | STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live | 10012 | 10253 | 242 |
| 37 | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 10254 | 10909 | 656 |
| 38 | JOB DISPLAY ID — broker/carrier ref number support — job.id      = FleetFlow internal ID (never changes) — job.refNum  = Broker/carrier order number (editable by office) — jobDispId() = what to show everywhere in the UI | 10910 | 10983 | 74 |
| 39 | EMPLOYEE ID SYSTEM — empId  = auto-assigned badge (EMP-001, EMP-002...) — handle = user-chosen nickname/alias — displayHandle() = handle if set, else empId | 10984 | 11012 | 29 |
| 40 | CLIENT BOOKING PORTAL — URL: fleetflowmovemasters.netlify.app/?book=1&co=COMPANY_ID — Moving company shares this link on their website — Client fills details → instant quote → pays deposit → job created | 11013 | 11921 | 909 |
| 41 | PORTAL ROUTE CHECK — runs before login screen — Matches Good Friends Movers LLC document package exactly — US DOT# 3921653 / MC# 1450729 — Structure: Interstate BOL | Legal Terms | Inventory x2 | Packing | Post Contract | Valuation | 11922 | 12759 | 838 |
| 42 | PAY SCHEDULE & UNPAID CREW CHECKER | 12760 | 12816 | 57 |
| 43 | COMPANY BRANDING ENGINE | 12817 | 12853 | 37 |
| 44 | NOTIFICATION ENGINE | 12854 | 12914 | 61 |
| 45 | SOUND ENGINE — Web Audio API, no external files needed | 12915 | 13194 | 280 |
| 46 | PAYROLL CALCULATION ENGINE v2 — pay + commission + tax | 13195 | 13242 | 48 |
| 47 | TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 13243 | 13509 | 267 |
| 48 | CALENDAR | 13510 | 13656 | 147 |
| 49 | MONTH VIEW — Google Calendar style with multi-day span bars | 13657 | 13901 | 245 |
| 50 | WEEK VIEW — time-based layout like Google Calendar | 13902 | 14209 | 308 |
| 51 | MESSAGING | 14210 | 14432 | 223 |
| 52 | WAREHOUSE | 14433 | 14725 | 293 |
| 53 | BOL TAB | 14726 | 14957 | 232 |
| 54 | SCHEDULE | 14958 | 15012 | 55 |
| 55 | RECEIPTS | 15013 | 15889 | 877 |
| 56 | JOB DETAIL MODAL | 15890 | 16296 | 407 |
| 57 | EDIT JOB  ← NEW FEATURE | 16297 | 16614 | 318 |
| 58 | function openNewJobModal(prefillDate) { — ── HARD RESET — every field to blank/zero ────────────────── — const safe = (id, val='') => { const el = document.getElementById(id); if(el) el.value = val; }; — Core job fields — safe('nj-client'); safe('nj-phone'); safe('nj-email'); — safe('nj-origin'); safe('nj-dest'); safe('nj-pickup-from'); — safe('nj-amount'); safe('nj-notes'); safe('nj-carrier'); — safe('nj-cubic-ft'); safe('nj-cubic-ft-2'); safe('nj-rate-per-cuft'); — safe('nj-weight'); safe('nj-linehaul'); — safe('nj-end-date'); safe('nj-end-time'); — safe('nj-pickup-start'); safe('nj-pickup-end'); — safe('nj-delivery-start'); safe('nj-delivery-end'); — safe('nj-pickup-driver'); safe('nj-delivery-driver'); — safe('nj-job-id'); safe('nj-ref-num'); — safe('nj-priority'); safe('nj-followup'); safe('nj-fadd'); — safe('nj-broker-ref'); safe('nj-carrier'); — ALL estimate fields — zero out completely — safe('nj-est-fuel', ''); — safe('nj-est-fuel-pct', '0'); — safe('nj-est-overload-cuft', ''); — safe('nj-est-stairs-origin', ''); — safe('nj-est-stairs-dest', ''); — safe('nj-est-longcarry-origin', ''); — safe('nj-est-longcarry-dest', ''); — safe('nj-est-shuttle', ''); — safe('nj-est-bulky', ''); — safe('nj-est-split', ''); — safe('nj-est-pgs', ''); — safe('nj-est-packing', ''); — safe('nj-est-storage-days', ''); — safe('nj-est-storage-rate', ''); — safe('nj-est-other', ''); — safe('nj-est-deposit', ''); — safe('nj-est-partial2', ''); — safe('nj-est-tip', ''); — safe('nj-est-discount', ''); — safe('nj-est-discount-label', ''); — safe('nj-est-others1', '');  safe('nj-est-others1-label', ''); — safe('nj-est-others2', '');  safe('nj-est-others2-label', ''); — safe('nj-est-others3', '');  safe('nj-est-others3-label', ''); — safe('nj-est-extra1', '');   safe('nj-est-extra1-label', ''); — safe('nj-est-extra2', '');   safe('nj-est-extra2-label', ''); — safe('nj-est-extra3', '');   safe('nj-est-extra3-label', ''); — safe('nj-est-notes', ''); — safe('nj-est-office-remarks', ''); — safe('nj-est-slip-remarks', ''); — Reset carrier total display — const ct = document.getElementById('nj-carrier-total'); if(ct) ct.textContent = '$0.00'; — Clear helper rows — const hr = document.getElementById('nj-helper-rows'); if(hr) hr.innerHTML = ''; — Reset all selects to index 0 — ['nj-job-for','nj-est-tip-method','nj-est-valuation', — 'nj-est-fuel-mode','nj-est-tip-assign'].forEach(id => { — const el = document.getElementById(id); — if (el) el.selectedIndex = 0; — }); — try { toggleCarrierFields('nj'); } catch(e) {} — Apply defaults — const s2 = loadSettings(); — safe('nj-crew', s2.defaultCrew || '3'); — safe('nj-type', s2.defaultMove || 'local'); — safe('nj-date', prefillDate || getTodayStr()); — safe('nj-time', getSetting('defaultTime', '08:00')); — safe('nj-driver', currentUser.role === 'driver' ? (currentUser.name||'') : ''); — closeModal('modal-calday'); — openModal('modal-newjob'); — setTimeout(() => { try { applyJobDefaults('nj'); } catch(e) {} }, 50); — } — async function createJob() { — const client = document.getElementById('nj-client').value.trim(); — const date   = document.getElementById('nj-date').value; — const origin = document.getElementById('nj-origin').value.trim(); — const dest   = document.getElementById('nj-dest').value.trim(); — if (!client || !date) { notify('⚠ Client name and date required', true); return; } — Custom ID or auto-generate — const customId = (document.getElementById('nj-job-id')?.value||'').trim().toUpperCase(); — const autoId   = 'JOB-' + String((STATE.jobs||[]).length + 1).padStart(3,'0'); — const jobId    = customId || autoId; — if ((STATE.jobs||[]).find(j => j.id === jobId)) { — notify('⚠ Job ID ' + jobId + ' already exists', true); return; — } — Collect helpers — const helpers = []; — document.querySelectorAll('.nj-helper-name').forEach(inp => { — const name = inp.value.trim(); — if (!name) return; — const row = inp.closest('.nj-helper-row'); — helpers.push({ — name, — rate:    parseFloat(row?.querySelector('.nj-helper-rate')?.value) || 0, — payType: row?.querySelector('.nj-helper-type')?.value || 'hourly', — hours: 0, days: 1, workerType: '1099', paid: false — }); — }); — const g = id => (document.getElementById(id)?.value || ''); — const newJob = { — id:            jobId, — refNum:        g('nj-ref-num'), — client, — phone:         g('nj-phone'), — email:         g('nj-email'), — date, — time:          g('nj-time'), — endDate:       g('nj-end-date') || g('nj-delivery-end').slice(0,10) || '', — endTime:       g('nj-end-time'), — origin, — dest, — pickupStart:   g('nj-pickup-start'), — pickupEnd:     g('nj-pickup-end'), — pickupDriver:  g('nj-pickup-driver'), — deliveryStart: g('nj-delivery-start'), — deliveryEnd:   g('nj-delivery-end'), — deliveryDriver:g('nj-delivery-driver'), — driver:        g('nj-driver'), — driverUser:    currentUser.username || '', — crewSize:      parseInt(g('nj-crew')) || 3, — crewNames:     helpers.map(h => h.name), — amount:        parseFloat(g('nj-amount')) || 0, — cubicFt:       parseFloat(g('nj-cubic-ft-2') || g('nj-cubic-ft')) || 0, — weight:        parseFloat(g('nj-weight')) || 0, — linehaul:      parseFloat(g('nj-linehaul')) || 0, — priority:      g('nj-priority'), — followUp:      g('nj-followup'), — fadd:          g('nj-fadd'), — type:          g('nj-type') || 'local', — notes:         g('nj-notes'), — companyId:     COMPANY_ID, — status:        'pending', — gates:         { preload:false, loaded:false, delivered:false, payment:false }, — messages:      { crew:[], client:[] }, — photos:        [], — signatures:    [], — payroll:       { driver:0, helpers } — }; — try { — await db.collection(COL_JOBS).doc(newJob.id).set(newJob); — closeModal('modal-newjob'); — notify('✓ Job ' + newJob.id + ' created — ' + client); — setTimeout(() => renderCalendar(), 200); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('createJob error:', e); — } — } | 16615 | 16772 | 158 |
| 59 | function renderMyPay() { — const content  = document.getElementById('main-content'); — const role     = currentUser.role; — const isHelper = role === 'helper'; — Collect all pay entries for this person — const myJobs = STATE.jobs.filter(j => { — if (role === 'driver') return nameIncludes(j.driver, currentUser.name); — if (role === 'helper') return (j.payroll?.helpers||[]).some(h => nameIncludes(h.name, currentUser.name)); — return false; — }); — let totalGross      = 0; — let totalCommission = 0; — let totalTax        = 0; — let totalTakeHome   = 0; — const payRows       = []; — myJobs.forEach(j => { — let gross = 0, commission = 0, workerType = '1099', payDetails = ''; — if (role === 'driver') { — gross       = j.payroll?.driver || 0; — commission  = j.payroll?.driverCommission || 0; — workerType  = j.payroll?.driverWorkerType || '1099'; — payDetails  = payLabel({ payType: j.payroll?.driverPayType||'flat', rate: j.payroll?.driverRate||gross, hours: j.payroll?.driverHours||0, days: j.payroll?.driverDays||1 }); — } else { — const helper = (j.payroll?.helpers||[]).find(h => — h.name?.toLowerCase().includes(currentUser.name.toLowerCase()) || — (currentUser.name.toLowerCase().split(' ')[0] && h.name?.toLowerCase().includes(currentUser.name.toLowerCase().split(' ')[0])) — ); — if (helper) { — gross      = calcPay(helper); — commission = helper.commission || 0; — workerType = helper.workerType || '1099'; — payDetails = payLabel(helper); — } — } — if (gross === 0) return; — const tax = calcTax(gross, workerType); — totalGross      += gross; — totalCommission += commission; — totalTax        += tax.totalEstTax; — totalTakeHome   += tax.takeHome; — payRows.push({ job: j, gross, commission, workerType, payDetails, tax }); — }); — let html = ` — <div class="section-header"> — <div> — <div class="section-title">📊 MY PAY SUMMARY</div> — <div class="section-sub">${currentUser.name} · ${role.toUpperCase()} · ${payRows.length} job(s)</div> — </div> — </div> — <!-- YTD SUMMARY STATS --> — <div class="grid-4" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Gross Earned</div> — <div class="stat-value">$${totalGross.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Commission</div> — <div class="stat-value" style="color:var(--amber)">$${totalCommission.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Est. Tax Owed</div> — <div class="stat-value red">$${totalTax.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Est. Take-Home</div> — <div class="stat-value">$${totalTakeHome.toFixed(2)}</div> — </div> — </div> — <!-- TAX ADVISORY BANNER --> — <div class="card" style="border-color:#664400;background:rgba(255,170,0,0.05);margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:11px;color:var(--amber);letter-spacing:2px;margin-bottom:8px">⚠ TAX ADVISORY</div> — <div class="info-text" style="line-height:1.8"> — ${totalGross > 0 ? `Based on your earnings of <strong style="color:var(--white)">$${totalGross.toFixed(2)}</strong>, you should set aside approximately <strong style="color:var(--red)">$${totalTax.toFixed(2)}</strong> (${(totalTax/totalGross*100).toFixed(1)}%) for taxes.` : 'No earnings recorded yet.'} — ${totalGross >= 600 ? ' You will likely receive a 1099 form if classified as a contractor.' : ''} — If you are a <strong style="color:var(--white)">1099 contractor</strong>, you are responsible for paying your own taxes quarterly (April, June, September, January). Consider setting aside <strong style="color:var(--red)">${totalGross > 0 ? (totalTax/totalGross*100).toFixed(0) : '~25'}%</strong> of every paycheck. — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div style="margin-top:4px"> — ${payRows.length ? payRows.map(row => ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:10px"> — <div> — <div style="display:flex;gap:8px;align-items:center;margin-bottom:4px"> — <span class="tag">${row.job.id}</span> — <span class="status-badge status-${row.job.status}">${row.job.status}</span> — <span class="tag">${row.workerType}</span> — </div> — <div style="font-size:14px;font-weight:600">${row.job.client}</div> — <div class="info-text">${row.job.date} · ${row.payDetails}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:20px;color:var(--green)">$${row.gross.toFixed(2)}</div> — ${row.commission > 0 ? `<div style="font-family:var(--font-mono);font-size:12px;color:var(--amber)">+ $${row.commission.toFixed(2)} commission</div>` : ''} — </div> — </div> — <!-- Tax breakdown per job --> — <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;margin-bottom:8px"> — <div class="stat-box" style="padding:10px"> — <div class="stat-label" style="font-size:9px">Gross Pay</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--white)">$${row.gross.toFixed(2)}</div> — </div> — <div class="stat-box" style="padding:10px"> — <div class="stat-label" style="font-size:9px">Est. Tax</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--red)">−$${row.tax.totalEstTax.toFixed(2)}</div> — </div> — <div class="stat-box" style="padding:10px;border-color:var(--green3)"> — <div class="stat-label" style="font-size:9px">Take-Home</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--green)">$${row.tax.takeHome.toFixed(2)}</div> — </div> — </div> — <!-- Detailed tax line items --> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;font-family:var(--font-mono);font-size:10px"> — ${row.tax.selfEmployTax ? `<div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>Self-Employment Tax (15.3%)</span><span style="color:var(--red)">−$${row.tax.selfEmployTax.toFixed(2)}</span></div>` : ''} — ${row.tax.ficaTax ? `<div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>FICA Employee (7.65%)</span><span style="color:var(--red)">−$${row.tax.ficaTax.toFixed(2)}</span></div>` : ''} — <div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>Federal Income (~${(row.tax.federalRate*100).toFixed(0)}%)</span><span style="color:var(--red)">−$${row.tax.federalTax.toFixed(2)}</span></div> — <div style="display:flex;justify-content:space-between;color:var(--gray)"><span>Georgia State (5.5%)</span><span style="color:var(--red)">−$${row.tax.stateTax.toFixed(2)}</span></div> — </div> — </div>`).join('') : ` — <div class="card" style="text-align:center;padding:40px"> — <p class="info-text">No pay records found for ${currentUser.name}.</p> — <p class="info-text" style="margin-top:8px">Your earnings will appear here once jobs are assigned and crew logs are saved.</p> — </div>`} — </div> — <div style="padding:12px;font-family:var(--font-mono);font-size:9px;color:var(--gray);line-height:1.8;border-top:1px solid var(--gray2);margin-top:8px"> — ⚠ DISCLAIMER: Tax estimates are for informational purposes only based on 2024 US federal rates and Georgia state rates. This is not tax advice. Individual circumstances vary. Consult a licensed tax professional or CPA for filing guidance. JPG Systems / JPG Ventures LLC is not responsible for tax obligations of independent contractors. — </div>`; — content.innerHTML = html; — } | 16773 | 16915 | 143 |
| 60 | function renderExpenses() { — const content = document.getElementById('main-content'); — const role = currentUser.role; — const isDriver = role === 'driver'; — Scope to this driver's jobs/disbursements if not office — const myJobs = isDriver — ? STATE.jobs.filter(j => nameIncludes(j.driver, currentUser.name)) — : STATE.jobs; — const myDisbursements = isDriver — ? STATE.disbursements.filter(d => — d.driverUser === currentUser.username || — (d.driver && d.driver.toLowerCase().includes(currentUser.name.toLowerCase())) — ) — : STATE.disbursements; — const myReceipts = isDriver — ? STATE.receipts.filter(r => { — const nm = r.driver && nameIncludes(r.driver, currentUser.name); — const um = r.driverUser && r.driverUser === currentUser.username; — return nm || um; — }) — : STATE.receipts; — Totals — const totalDisbursed = myDisbursements.reduce((s,d) => s + (d.amount||0), 0); — const totalReceipts  = myReceipts.reduce((s,r) => s + (r.amount||0), 0); — const netOwed        = totalReceipts - totalDisbursed; // positive = driver owes, negative = company owes driver — let html = ` — <div class="section-header"> — <div> — <div class="section-title">EXPENSE TRACKER</div> — <div class="section-sub">${isDriver ? 'Your funds & receipts · ' + currentUser.name : 'All driver funds & expense reconciliation'}</div> — </div> — ${isAdmin() ? `<button class="btn-green" onclick="openDisbursementModal()">💰 SEND FUNDS</button>` : ''} — </div> — <!-- SUMMARY STATS --> — <div class="grid-3" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Funds Disbursed</div> — <div class="stat-value">$${totalDisbursed.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Receipts Submitted</div> — <div class="stat-value amber">$${totalReceipts.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">${netOwed > 0 ? 'Driver Owes Back' : netOwed < 0 ? 'Company Owes Driver' : 'Balanced'}</div> — <div class="stat-value ${netOwed > 0 ? 'red' : netOwed < 0 ? 'amber' : ''}"> — ${netOwed === 0 ? '✓ $0.00' : (netOwed > 0 ? '+' : '') + '$' + Math.abs(netOwed).toFixed(2)} — </div> — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div class="card" style="margin-bottom:16px"> — <div class="card-header"><div class="card-title">JOB EXPENSE BREAKDOWN</div></div> — <table> — <thead><tr> — <th>Job ID</th><th>Client</th><th>Driver</th> — <th style="text-align:right">Funds Sent</th> — <th style="text-align:right">Receipts</th> — <th style="text-align:right">Net</th> — <th>Status</th> — </tr></thead> — <tbody> — ${myJobs.length ? myJobs.map(j => { — const jobDisb = myDisbursements.filter(d => d.jobId === j.id); — const jobRec  = myReceipts.filter(r => r.jobId === j.id); — const sent    = jobDisb.reduce((s,d) => s + (d.amount||0), 0); — const spent   = jobRec.reduce((s,r) => s + (r.amount||0), 0); — const net     = spent - sent; — return `<tr onclick="openJobExpenseDetail('${j.id}')" style="cursor:pointer"> — <td><span class="tag">${j.id}</span></td> — <td>${j.client}</td> — <td class="info-text">${j.driver || '—'}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--green)">$${sent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--amber)">$${spent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);font-weight:700;color:${net > 0 ? 'var(--red)' : net < 0 ? 'var(--amber)' : 'var(--green)'}"> — ${net === 0 ? '✓ $0.00' : (net > 0 ? '+' : '') + '$' + Math.abs(net).toFixed(2)} — </td> — <td><span class="status-badge status-${j.status}">${j.status}</span></td> — </tr>`; — }).join('') : `<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--gray);font-family:var(--font-mono)">No jobs found</td></tr>`} — </tbody> — </table> — </div> — <!-- DISBURSEMENTS LOG --> — <div class="grid-2"> — <div class="card"> — <div class="card-header"> — <div class="card-title">💰 FUNDS DISBURSED</div> — ${isAdmin() ? `<button class="btn-green" style="padding:7px 14px;font-size:11px" onclick="openDisbursementModal()">+ SEND FUNDS</button>` : ''} — </div> — ${myDisbursements.length ? myDisbursements.map(d => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${d.driver} <span class="tag">${d.jobId}</span></div> — <div class="info-text">${d.date} · ${d.method || 'Cash'} · ${d.notes || ''}</div> — <div class="info-text" style="color:var(--gray)">${d.sentBy ? 'Sent by: ' + d.sentBy : ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--green)">$${(d.amount||0).toFixed(2)}</div> — ${role === 'office' ? `<button class="btn-sm" style="margin-top:4px;color:var(--red);border-color:#552222;font-size:10px" onclick="event.stopPropagation();deleteDisbursementEntry('${d.id}')">DELETE</button>` : ''} — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No funds disbursed yet.</p>`} — ${myDisbursements.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Disbursed</span> — <span style="font-family:var(--font-mono);color:var(--green);font-size:14px">$${totalDisbursed.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="card"> — <div class="card-header"><div class="card-title">🧾 RECEIPTS SUBMITTED</div></div> — ${myReceipts.length ? myReceipts.map(r => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${r.type} <span class="tag">${r.jobId}</span></div> — <div class="info-text">${r.driver} · ${r.date} · ${r.notes || ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--amber)">$${(r.amount||0).toFixed(2)}</div> — <div style="margin-top:2px">${r.approved — ? '<span style="color:var(--green);font-size:10px;font-family:var(--font-mono)">✓ APPROVED</span>' — : '<span style="color:var(--amber);font-size:10px;font-family:var(--font-mono)">⏳ PENDING</span>'}</div> — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No receipts submitted yet.</p>`} — ${myReceipts.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Receipts</span> — <span style="font-family:var(--font-mono);color:var(--amber);font-size:14px">$${totalReceipts.toFixed(2)}</span> — </div>` : ''} — </div> — </div>`; — content.innerHTML = html; — } — function openJobExpenseDetail(jobId) { — Switch to jobs tab and open the job detail — closeModal('modal-calday'); — openJobDetail(jobId); — } — function openDisbursementModal(prefillJobId) { — Pre-fill job ID if provided — document.getElementById('disb-jobid').value = prefillJobId || ''; — document.getElementById('disb-driver').value = ''; — document.getElementById('disb-amount').value = ''; — document.getElementById('disb-method').value = getSetting('defaultPay', 'Cash'); — document.getElementById('disb-notes').value = ''; — Populate driver dropdown from users — populateDisbDriverDropdown(); — openModal('modal-disbursement'); — } — async function populateDisbDriverDropdown() { — const sel = document.getElementById('disb-driver'); — sel.innerHTML = '<option value="">-- Select Driver --</option>'; — try { — const snap = await db.collection(COL_USERS) — .where('role', 'in', ['driver','helper']) — .get(); — snap.docs.forEach(d => { — const u = d.data(); — if (u.active) { — const opt = document.createElement('option'); — opt.value = JSON.stringify({ name: u.displayName, username: u.username }); — opt.textContent = u.displayName + ' (' + u.role + ')'; — sel.appendChild(opt); — } — }); — } catch(e) { — Fallback — type name manually — sel.innerHTML = '<option value="">Type driver name in notes</option>'; — } — } — async function saveDisbursementEntry() { — const jobId   = document.getElementById('disb-jobid').value.trim().toUpperCase(); — const driverRaw = document.getElementById('disb-driver').value; — const amount  = parseFloat(document.getElementById('disb-amount').value); — const method  = document.getElementById('disb-method').value; — const notes   = document.getElementById('disb-notes').value.trim(); — const btn     = document.getElementById('disb-submit-btn'); — if (!jobId)   { notify('⚠ Job ID is required', true); return; } — if (!driverRaw) { notify('⚠ Select a driver', true); return; } — if (isNaN(amount) || amount <= 0) { notify('⚠ Enter a valid amount', true); return; } — if (btn.disabled) return; — btn.disabled = true; — btn.textContent = 'SAVING...'; — try { — let driverName = driverRaw, driverUser = ''; — try { — const parsed = JSON.parse(driverRaw); — driverName = parsed.name; — driverUser = parsed.username; — } catch(e) { driverName = driverRaw; } — const disb = { — id:         'DISB-' + Date.now(), — jobId, — driver:     driverName, — driverUser: driverUser, — amount, — method, — notes, — sentBy:     currentUser.name, — date:       getTodayStr(), — createdAt:  new Date().toISOString() — }; — await saveDisbursement(disb); — closeModal('modal-disbursement'); — notify('$' + amount.toFixed(2) + ' disbursed to ' + driverName + ' ✓'); — Notify the driver — try { — if (driverUser) { — await sendNotification(driverUser, driverName, 'funds_sent', — 'Funds Sent 💰', — '$' + amount.toFixed(2) + ' sent via ' + method + ' for ' + jobId + (notes ? ' · ' + notes : '') + ' — from ' + currentUser.name, — { jobId, amount } — ); — } — } catch(e) {} — } catch(e) { — notify('⚠ Error: ' + e.message, true); — } finally { — btn.disabled = false; — btn.textContent = 'SEND FUNDS'; — } — } — async function deleteDisbursementEntry(disbId) { — if (!confirm('Delete this disbursement? This cannot be undone.')) return; — try { — await db.collection(COL_DISBURSEMENTS).doc(disbId).delete(); — notify('Disbursement deleted ✓'); — } catch(e) { notify('⚠ ' + e.message, true); } — } | 16916 | 17169 | 254 |
| 61 | function renderFleet() { — const content = document.getElementById('main-content'); — const role    = currentUser.role; — const canEdit = role === 'office'; — let html = ` — <div class="section-header"> — <div> — <div class="section-title">🚚 FLEET MANAGEMENT</div> — <div class="section-sub">${STATE.fleet.length} vehicle(s) registered · Move Masters OS</div> — </div> — ${canEdit ? `<button class="btn-green" onclick="openFleetModal()">+ ADD VEHICLE</button>` : ''} — </div>`; — if (!STATE.fleet.length) { — html += `<div class="card" style="text-align:center;padding:40px"> — <div style="font-size:48px;margin-bottom:12px">🚚</div> — <p class="info-text">No vehicles registered yet.</p> — ${canEdit ? `<button class="btn-green" style="margin-top:16px" onclick="openFleetModal()">+ ADD FIRST VEHICLE</button>` : ''} — </div>`; — } else { — Summary stats — const active  = STATE.fleet.filter(v => v.status === 'active').length; — const maint   = STATE.fleet.filter(v => v.status === 'maintenance').length; — const offline = STATE.fleet.filter(v => v.status === 'offline').length; — html += `<div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Active</div><div class="stat-value">${active}</div></div> — <div class="stat-box"><div class="stat-label">In Maintenance</div><div class="stat-value amber">${maint}</div></div> — <div class="stat-box"><div class="stat-label">Offline</div><div class="stat-value red">${offline}</div></div> — </div>`; — STATE.fleet.forEach(v => { — const assignedJob = STATE.jobs.find(j => j.vehicleId === v.id && j.status === 'active'); — const statusColor = v.status === 'active' ? 'var(--green)' : v.status === 'maintenance' ? 'var(--amber)' : 'var(--red)'; — const maintDue    = v.nextMaint ? new Date(v.nextMaint) : null; — const maintOverdue = maintDue && maintDue < new Date(); — html += ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:12px"> — <div> — <div style="display:flex;align-items:center;gap:10px;margin-bottom:6px"> — <div style="font-size:28px">${v.type === 'box_truck' ? '🚛' : v.type === 'cargo_van' ? '🚐' : '🚚'}</div> — <div> — <div style="font-family:var(--font-mono);font-size:14px;font-weight:700;color:var(--white)">${v.name || v.id}</div> — <div class="info-text">${v.make||''} ${v.model||''} ${v.year||''} · ${v.plate||'No plate'}</div> — </div> — </div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:11px;color:${statusColor};letter-spacing:2px">${(v.status||'active').toUpperCase()}</div> — ${canEdit ? `<button class="btn-sm" style="margin-top:6px" onclick="openFleetModal('${v.id}')">✎ EDIT</button>` : ''} — </div> — </div> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(110px,1fr));gap:8px;margin-bottom:12px"> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Capacity</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.capacity || '—'}</div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Mileage</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.mileage ? v.mileage.toLocaleString() + ' mi' : '—'}</div> — </div> — <div class="stat-box" style="padding:8px;${maintOverdue ? 'border-color:var(--red)' : ''}"> — <div class="stat-label" style="font-size:9px">Next Service</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${maintOverdue ? 'var(--red)' : 'var(--white)'}"> — ${v.nextMaint || 'Not set'}${maintOverdue ? ' ⚠' : ''} — </div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Assigned Job</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${assignedJob ? 'var(--green)' : 'var(--gray)'}"> — ${assignedJob ? assignedJob.id : 'Available'} — </div> — </div> — </div> — ${v.notes ? `<div class="info-text" style="margin-bottom:10px">📝 ${v.notes}</div>` : ''} — <!-- MAINTENANCE LOG --> — ${v.maintLog && v.maintLog.length ? ` — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:6px">MAINTENANCE LOG</div> — ${v.maintLog.slice(-3).reverse().map(m => ` — <div style="display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px solid var(--gray2);font-size:12px"> — <div><span class="info-text">${m.date}</span> · ${m.work}</div> — <div style="font-family:var(--font-mono);color:var(--amber)">$${(m.cost||0).toFixed(0)}</div> — </div>`).join('')}` : ''} — ${canEdit ? ` — <div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap"> — <button class="btn-sm" onclick="logMaintenance('${v.id}')">🔧 LOG SERVICE</button> — <button class="btn-sm" onclick="updateMileage('${v.id}')">📍 UPDATE MILEAGE</button> — <select style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:5px 8px;font-family:var(--font-mono);font-size:10px;border-radius:3px" — onchange="updateVehicleStatus('${v.id}',this.value)"> — <option value="active"      ${v.status==='active'      ?'selected':''}>● Active</option> — <option value="maintenance" ${v.status==='maintenance' ?'selected':''}>🔧 Maintenance</option> — <option value="offline"     ${v.status==='offline'     ?'selected':''}>○ Offline</option> — </select> — </div>` : ''} — </div>`; — }); — } — content.innerHTML = html; — } — function openFleetModal(vehicleId) { — const v = vehicleId ? STATE.fleet.find(x => x.id === vehicleId) : null; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">${v ? 'EDIT VEHICLE — ' + v.id : 'ADD VEHICLE'}</div> — <div class="grid-2"> — <div class="form-group"><label>Vehicle Name / Unit #</label> — <input type="text" id="flt-name" value="${(v&&v.name)||''}" placeholder="e.g. Truck 1 · Unit 24" /></div> — <div class="form-group"><label>Type</label> — <select id="flt-type"> — <option value="box_truck" ${v?.type==='box_truck'?'selected':''}>🚛 Box Truck</option> — <option value="cargo_van" ${v?.type==='cargo_van'?'selected':''}>🚐 Cargo Van</option> — <option value="flatbed"   ${v?.type==='flatbed'  ?'selected':''}>🚚 Flatbed</option> — <option value="other"     ${v?.type==='other'    ?'selected':''}>📋 Other</option> — </select> — </div> — </div> — <div class="grid-3"> — <div class="form-group"><label>Make</label><input type="text" id="flt-make" value="${(v&&v.make)||''}" placeholder="Ford" /></div> — <div class="form-group"><label>Model</label><input type="text" id="flt-model" value="${(v&&v.model)||''}" placeholder="E-450" /></div> — <div class="form-group"><label>Year</label><input type="number" id="flt-year" value="${(v&&v.year)||''}" placeholder="2019" /></div> — </div> — <div class="grid-2"> — <div class="form-group"><label>License Plate</label><input type="text" id="flt-plate" value="${(v&&v.plate)||''}" placeholder="ABC-1234" style="text-transform:uppercase"/></div> — <div class="form-group"><label>Capacity (text label)</label><input type="text" id="flt-cap" value="${(v&&v.capacity)||''}" placeholder="26 ft · 1500 lbs" /></div> — </div> — <div class="grid-3"> — <div class="form-group"> — <label>Max Cubic Feet <span class="info-text">(for load optimization)</span></label> — <input type="number" id="flt-maxcuft" value="${(v&&v.maxCuFt)||''}" placeholder="e.g. 1200" min="0" step="50" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Fuel Efficiency (MPG) <span class="info-text">(for cost calc)</span></label> — <input type="number" id="flt-mpg" value="${(v&&v.mpg)||''}" placeholder="e.g. 10" min="1" step="0.5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Load Efficiency % <span class="info-text">(usable space)</span></label> — <input type="number" id="flt-loadeff" value="${(v&&v.loadEff)||85}" placeholder="85" min="50" max="100" step="5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — </div> — <div class="grid-2"> — <div class="form-group"><label>Current Mileage</label><input type="number" id="flt-miles" value="${(v&&v.mileage)||''}" placeholder="85000" /></div> — <div class="form-group"><label>Next Service Due</label><input type="date" id="flt-maint" value="${(v&&v.nextMaint)||''}" /></div> — </div> — <div class="form-group"><label>Notes</label> — <input type="text" id="flt-notes" value="${(v&&v.notes)||''}" placeholder="Insurance, registration, special equipment..." /></div> — <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:8px"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" onclick="saveVehicle('${vehicleId||''}')">SAVE VEHICLE</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveVehicle(vehicleId) { — const vehicle = { — id:        vehicleId || 'VEH-' + Date.now(), — name:      document.getElementById('flt-name').value.trim(), — type:      document.getElementById('flt-type').value, — make:      document.getElementById('flt-make').value.trim(), — model:     document.getElementById('flt-model').value.trim(), — year:      document.getElementById('flt-year').value, — plate:     document.getElementById('flt-plate').value.trim().toUpperCase(), — capacity:  document.getElementById('flt-cap').value.trim(), — maxCuFt:   parseFloat(document.getElementById('flt-maxcuft')?.value)||0, — mpg:       parseFloat(document.getElementById('flt-mpg')?.value)||0, — loadEff:   parseFloat(document.getElementById('flt-loadeff')?.value)||85, — mileage:   parseInt(document.getElementById('flt-miles').value)||0, — nextMaint: document.getElementById('flt-maint').value, — notes:     document.getElementById('flt-notes').value.trim(), — status:    vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.status||'active') : 'active', — maintLog:  vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.maintLog||[]) : [], — updatedAt: new Date().toISOString() — }; — if (!vehicle.name) { notify('⚠ Vehicle name is required', true); return; } — try { — await db.collection(COL_FLEET).doc(vehicle.id).set(vehicle); — notify((vehicleId ? 'Vehicle updated' : 'Vehicle added') + ' ✓'); — closeModal('modal-jobdetail'); — } catch(e) { notify('⚠ ' + e.message, true); } — } — async function logMaintenance(vehicleId) { — const work = prompt('Service performed:'); if (!work) return; — const cost = parseFloat(prompt('Cost ($):') || '0'); — const v    = STATE.fleet.find(x => x.id === vehicleId); — if (!v) return; — if (!v.maintLog) v.maintLog = []; — v.maintLog.push({ date: getTodayStr(), work, cost, loggedBy: currentUser.name }); — v.updatedAt = new Date().toISOString(); — await db.collection(COL_FLEET).doc(vehicleId).set(v); — notify('Maintenance logged ✓'); — } — async function updateMileage(vehicleId) { — const miles = prompt('Enter current mileage:'); if (!miles) return; — await db.collection(COL_FLEET).doc(vehicleId).update({ mileage: parseInt(miles)||0 }); — notify('Mileage updated ✓'); — } — async function updateVehicleStatus(vehicleId, status) { — await db.collection(COL_FLEET).doc(vehicleId).update({ status }); — notify('Vehicle status: ' + status.toUpperCase()); — } | 17170 | 17391 | 222 |
| 62 | function renderPayrollSummary() { — This is the office-facing payroll output — all crew, all jobs — const content = document.getElementById('main-content'); — const role    = currentUser.role; — if (role !== 'office') { switchTab('mypay'); return; } — Aggregate per person — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — Driver — const dKey = j.driver; — if (!payMap[dKey]) payMap[dKey] = { name: dKey, role: 'Driver', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[dKey].jobs++; — payMap[dKey].gross      += j.payroll.driver || 0; — payMap[dKey].commission += j.payroll.driverCommission || 0; — payMap[dKey].workerType  = j.payroll.driverWorkerType || '1099'; — Helpers — (j.payroll.helpers||[]).forEach(h => { — if (!h.name) return; — const hKey = h.name; — if (!payMap[hKey]) payMap[hKey] = { name: hKey, role: 'Helper', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[hKey].jobs++; — payMap[hKey].gross      += calcPay(h); — payMap[hKey].commission += h.commission || 0; — payMap[hKey].workerType  = h.workerType || '1099'; — }); — }); — const people    = Object.values(payMap).sort((a,b) => b.gross - a.gross); — const totalGross = people.reduce((s,p) => s + p.gross + p.commission, 0); — let html = ` — <div class="section-header"> — <div> — <div class="section-title">💵 PAYROLL SUMMARY</div> — <div class="section-sub">All crew · All jobs · JPG Systems</div> — </div> — <div style="display:flex;gap:8px"> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportPayrollCSV()">⬇ EXPORT CSV</button> — </div> — </div> — <div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Total Crew</div><div class="stat-value">${people.length}</div></div> — <div class="stat-box"><div class="stat-label">Total Payroll</div><div class="stat-value amber">$${totalGross.toFixed(2)}</div></div> — <div class="stat-box"><div class="stat-label">Jobs w/ Payroll</div><div class="stat-value">${STATE.jobs.filter(j=>j.payroll).length}</div></div> — </div> — <div class="card"> — <table> — <thead><tr> — <th>Name</th><th>Role</th><th>Type</th><th>Jobs</th> — <th style="text-align:right">Base Pay</th> — <th style="text-align:right">Commission</th> — <th style="text-align:right">Total Gross</th> — <th style="text-align:right">Est. Tax</th> — <th style="text-align:right">Take-Home</th> — </tr></thead> — <tbody> — ${people.map(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — return `<tr> — <td style="font-weight:600">${p.name}</td> — <td><span class="tag">${p.role}</span></td> — <td><span class="tag">${p.workerType}</span></td> — <td style="font-family:var(--font-mono);text-align:center">${p.jobs}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${p.gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">${p.commission>0?'$'+p.commission.toFixed(2):'—'}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green);font-weight:700">$${gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${tax.totalEstTax.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${tax.takeHome.toFixed(2)}</td> — </tr>`; — }).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="4" style="font-family:var(--font-mono);color:var(--green)">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${people.reduce((s,p)=>s+p.gross,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">$${people.reduce((s,p)=>s+p.commission,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${totalGross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).totalEstTax,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).takeHome,0).toFixed(2)}</td> — </tr> — </tbody> — </table> — </div>`; — content.innerHTML = html; — } | 17392 | 17485 | 94 |
| 63 | function exportPayrollCSV() { — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — const addPerson = (name, role, gross, commission, workerType) => { — if (!name) return; — if (!payMap[name]) payMap[name] = { name, role, jobs: 0, gross: 0, commission: 0, workerType }; — payMap[name].jobs++; — payMap[name].gross      += gross; — payMap[name].commission += commission; — }; — addPerson(j.driver, 'Driver', j.payroll.driver||0, j.payroll.driverCommission||0, j.payroll.driverWorkerType||'1099'); — (j.payroll.helpers||[]).forEach(h => addPerson(h.name, 'Helper', calcPay(h), h.commission||0, h.workerType||'1099')); — }); — const rows = [['Name','Role','Worker Type','Jobs','Base Pay','Commission','Total Gross','Est Tax','Est Take-Home']]; — Object.values(payMap).forEach(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — rows.push([p.name, p.role, p.workerType, p.jobs, p.gross.toFixed(2), p.commission.toFixed(2), — gross.toFixed(2), tax.totalEstTax.toFixed(2), tax.takeHome.toFixed(2)]); — }); — downloadCSV(rows, 'jpgsystems-payroll-' + getTodayStr() + '.csv'); — notify('Payroll CSV exported ✓'); — } — function exportJobsCSV() { — const rows = [['Job ID','Client','Date','Time','Driver','Type','Status','Amount','Gates Passed','Origin','Destination']]; — STATE.jobs.forEach(j => { — const gates = Object.values(j.gates||{}).filter(Boolean).length; — rows.push([j.id, j.client, j.date, j.time||'', j.driver||'', j.type||'', — j.status, (j.amount||0).toFixed(2), gates + '/' + Object.keys(j.gates||{}).length, — j.origin||'', j.dest||'']); — }); — downloadCSV(rows, 'jpgsystems-jobs-' + getTodayStr() + '.csv'); — notify('Jobs CSV exported ✓'); — } — function exportReceiptsCSV() { — const rows = [['Receipt ID','Job ID','Driver','Date','Type','Amount','Status','Notes']]; — STATE.receipts.forEach(r => { — rows.push([r.id, r.jobId, r.driver||'', r.date||'', r.type||'', — (r.amount||0).toFixed(2), r.approved ? 'APPROVED' : r.rejected ? 'REJECTED' : 'PENDING', r.notes||'']); — }); — downloadCSV(rows, 'jpgsystems-receipts-' + getTodayStr() + '.csv'); — notify('Receipts CSV exported ✓'); — } — function downloadCSV(rows, filename) { — const csv  = rows.map(r => r.map(c => '"' + String(c).replace(/"/g,'""') + '"').join(',')).join('\n'); — const blob = new Blob([csv], { type: 'text/csv' }); — const url  = URL.createObjectURL(blob); — const a    = document.createElement('a'); — a.href     = url; — a.download = filename; — a.click(); — URL.revokeObjectURL(url); — } — Add export buttons to financials tab — function renderExportBar() { — return ` — <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;align-self:center">EXPORT:</div> — <button class="btn-sm" onclick="exportJobsCSV()">⬇ Jobs CSV</button> — <button class="btn-sm" onclick="exportReceiptsCSV()">⬇ Receipts CSV</button> — <button class="btn-sm" onclick="exportPayrollCSV()">⬇ Payroll CSV</button> — <button class="btn-sm" onclick="renderPayrollSummary()">💵 Payroll Report</button> — </div>`; — } | 17486 | 17561 | 76 |
| 64 | function renderMargins() { — const content = document.getElementById('main-content'); — Build margin data per job — const jobMargins = STATE.jobs — .filter(j => j.amount > 0 || j.payroll) — .map(j => { — const revenue   = j.amount || 0; — const driverAccBonus = parseFloat(j.payroll?.accessorialBonus) || 0; — const crewCost  = (j.payroll?.driver || 0) + — driverAccBonus + — (j.payroll?.helpers || []).reduce((s,h) => s + calcPay(h), 0) + — (j.payroll?.driverCommission || 0); — Company revenue excludes driver accessorial bonuses (passed through to driver) — const expenses  = STATE.receipts — .filter(r => r.jobId === j.id && r.approved !== false) — .reduce((s,r) => s + (r.amount||0), 0); — const net       = revenue - crewCost - expenses; — const margin    = revenue > 0 ? (net / revenue * 100) : 0; — return { job: j, revenue, crewCost, expenses, net, margin }; — }) — .sort((a,b) => { — Default: worst margin first (ascending) — const mA = a.revenue > 0 ? a.profit/a.revenue : 0; — const mB = b.revenue > 0 ? b.profit/b.revenue : 0; — return mA - mB; — }); — Sort control — if (window._marginSort === 'date') jobMargins.sort((a,b) => b.job.date > a.job.date ? 1 : -1); — else if (window._marginSort === 'revenue') jobMargins.sort((a,b) => b.revenue - a.revenue); — ── Margin intelligence ── — const overpayJobs   = jobMargins.filter(m => m.revenue > 0 && (m.crewCost/m.revenue) > 0.40); — const lowProfitJobs = jobMargins.filter(m => m.revenue > 0 && (m.profit/m.revenue) < 0.20); — const bestJob       = [...jobMargins].sort((a,b) => (b.profit/b.revenue||0)-(a.profit/a.revenue||0))[0]; — const worstJob      = jobMargins[0]; // already sorted worst first — Overall stats — const totalRevenue  = jobMargins.reduce((s,m) => s + m.revenue, 0); — const totalCrew     = jobMargins.reduce((s,m) => s + m.crewCost, 0); — const totalExpenses = jobMargins.reduce((s,m) => s + m.expenses, 0); — const totalNet      = jobMargins.reduce((s,m) => s + m.net, 0); — const avgMargin     = totalRevenue > 0 ? (totalNet / totalRevenue * 100) : 0; — const profitable    = jobMargins.filter(m => m.net > 0).length; — const losing        = jobMargins.filter(m => m.net < 0).length; — Best and worst job — const best  = jobMargins.reduce((a,b) => b.margin > a.margin ? b : a, jobMargins[0]); — const worst = jobMargins.reduce((a,b) => b.margin < a.margin ? b : a, jobMargins[0]); — const marginColor = (n) => n >= 40 ? 'var(--green)' : n >= 20 ? 'var(--amber)' : n >= 0 ? '#ff8c00' : 'var(--red)'; — const marginBg    = (n) => n >= 40 ? 'rgba(0,255,65,0.08)' : n >= 20 ? 'rgba(255,170,0,0.08)' : n >= 0 ? 'rgba(255,140,0,0.08)' : 'rgba(255,51,51,0.08)'; — let html = ` — ${overpayJobs.length ? `<div style="background:rgba(255,30,30,0.06);border:1px solid rgba(255,30,30,0.3);border-radius:6px;padding:10px 14px;margin-bottom:12px;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px"> — <div> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;font-weight:700">⚠ LABOR OVERPAY ALERT</div> — <div style="font-size:12px;color:var(--white);margin-top:3px">${overpayJobs.length} job${overpayJobs.length>1?'s':''} with crew cost >40% of revenue</div> — <div style="font-size:11px;color:var(--gray);margin-top:2px">${overpayJobs.slice(0,3).map(m=>m.job.client+' ('+Math.round(m.crewCost/m.revenue*100)+'%)').join(' · ')}</div> — </div> — </div>` : ''} — <div style="display:flex;gap:8px;margin-bottom:12px;flex-wrap:wrap"> — <span style="font-family:var(--font-mono);font-size:10px;color:var(--gray);align-self:center">Sort:</span> — ${['worst:⬇ Worst Margin','revenue:$ Revenue','date:📅 Date'].map(o=>{const[v,l]=o.split(':');return `<button onclick="window._marginSort='${v}';renderMargins()" style="padding:5px 12px;font-family:var(--font-mono);font-size:10px;border-radius:3px;cursor:pointer;background:${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--green)':'transparent'};color:${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--navy)':'var(--gray)'};border:1px solid ${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--green)':'var(--gray2)'}">${l}</button>`;}).join('')} — </div> — <div class="section-header"> — <div> — <div class="section-title">📈 JOB MARGIN DASHBOARD</div> — <div class="section-sub">Revenue · Crew Cost · Expenses · Net per Job</div> — </div> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportJobsCSV()">⬇ EXPORT</button> — </div> — <!-- OVERALL STATS --> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(130px,1fr));gap:8px;margin-bottom:16px"> — <div class="stat-box"> — <div class="stat-label">Total Revenue</div> — <div class="stat-value">$${totalRevenue.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${jobMargins.length} jobs</div> — </div> — <div class="stat-box"> — <div class="stat-label">Crew Cost</div> — <div class="stat-value red">$${totalCrew.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${totalRevenue>0?(totalCrew/totalRevenue*100).toFixed(1):'0'}% of revenue</div> — </div> — <div class="stat-box"> — <div class="stat-label">Expenses</div> — <div class="stat-value red">$${totalExpenses.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${totalRevenue>0?(totalExpenses/totalRevenue*100).toFixed(1):'0'}% of revenue</div> — </div> — <div class="stat-box" style="border-color:${marginColor(avgMargin)}"> — <div class="stat-label">Net Profit</div> — <div class="stat-value" style="color:${marginColor(avgMargin)}">$${totalNet.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px;color:${marginColor(avgMargin)}">${avgMargin.toFixed(1)}% avg margin</div> — </div> — <div class="stat-box"> — <div class="stat-label">Profitable Jobs</div> — <div class="stat-value" style="color:var(--green)">${profitable}</div> — <div class="info-text" style="margin-top:4px;color:var(--red)">${losing} losing</div> — </div> — </div> — <!-- MARGIN BREAKDOWN BAR --> — ${totalRevenue > 0 ? ` — <div class="card" style="margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;margin-bottom:10px">REVENUE BREAKDOWN</div> — <div style="height:32px;border-radius:4px;overflow:hidden;display:flex;margin-bottom:8px"> — <div style="width:${(totalCrew/totalRevenue*100).toFixed(1)}%;background:rgba(255,51,51,0.7);transition:width 0.5s" title="Crew Cost"></div> — <div style="width:${(totalExpenses/totalRevenue*100).toFixed(1)}%;background:rgba(255,140,0,0.7);transition:width 0.5s" title="Expenses"></div> — <div style="flex:1;background:rgba(0,255,65,0.4);transition:width 0.5s" title="Net Profit"></div> — </div> — <div style="display:flex;gap:16px;flex-wrap:wrap"> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(255,51,51,0.7);border-radius:2px"></div><span class="info-text">Crew ${(totalCrew/totalRevenue*100).toFixed(1)}%</span></div> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(255,140,0,0.7);border-radius:2px"></div><span class="info-text">Expenses ${(totalExpenses/totalRevenue*100).toFixed(1)}%</span></div> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(0,255,65,0.4);border-radius:2px"></div><span class="info-text">Net ${avgMargin.toFixed(1)}%</span></div> — </div> — </div>` : ''} — <!-- BEST / WORST --> — ${jobMargins.length >= 2 ? ` — <div class="grid-2" style="margin-bottom:16px"> — ${best ? `<div class="card" style="border-color:var(--green3)"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--green);letter-spacing:2px;margin-bottom:6px">🏆 BEST MARGIN JOB</div> — <div style="font-size:15px;font-weight:700">${best.job.client} <span class="tag">${best.job.id}</span></div> — <div class="info-text">${best.job.date}</div> — <div style="font-family:var(--font-mono);font-size:22px;color:var(--green);margin-top:6px">${best.margin.toFixed(1)}%</div> — <div class="info-text">$${best.net.toFixed(2)} net on $${best.revenue.toLocaleString()}</div> — </div>` : ''} — ${worst ? `<div class="card" style="border-color:${marginColor(worst.margin)}"> — <div style="font-family:var(--font-mono);font-size:9px;color:${marginColor(worst.margin)};letter-spacing:2px;margin-bottom:6px">${worst.net < 0 ? '⚠ LOSING JOB' : '📉 LOWEST MARGIN'}</div> — <div style="font-size:15px;font-weight:700">${worst.job.client} <span class="tag">${worst.job.id}</span></div> — <div class="info-text">${worst.job.date}</div> — <div style="font-family:var(--font-mono);font-size:22px;color:${marginColor(worst.margin)};margin-top:6px">${worst.margin.toFixed(1)}%</div> — <div class="info-text">$${worst.net.toFixed(2)} net on $${worst.revenue.toLocaleString()}</div> — </div>` : ''} — </div>` : ''} — <!-- PER-JOB TABLE --> — <div class="card" style="padding:0;overflow:hidden"> — <div style="padding:12px 16px;border-bottom:1px solid var(--gray2)"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--green);letter-spacing:2px">JOB BY JOB BREAKDOWN</div> — </div> — ${jobMargins.length ? ` — <div style="overflow-x:auto"> — <table style="min-width:700px"> — <thead><tr> — <th>Job</th> — <th>Client</th> — <th>Date</th> — <th style="text-align:right">Revenue</th> — <th style="text-align:right">Crew Cost</th> — <th style="text-align:right">Expenses</th> — <th style="text-align:right">Net</th> — <th style="text-align:center">Margin</th> — <th>Status</th> — </tr></thead> — <tbody> — ${jobMargins.map(m => ` — <tr style="cursor:pointer;background:${marginBg(m.margin)}" onclick="switchTab('jobs');setTimeout(()=>openJobDetail('${m.job.id}'),300)"> — <td><span class="tag">${m.job.id}</span></td> — <td style="font-weight:600">${m.job.client}</td> — <td class="info-text">${m.job.date}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${m.revenue.toLocaleString()}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${m.crewCost.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:#ff8c00">$${m.expenses.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;font-weight:700;color:${marginColor(m.margin)}"> — ${m.net >= 0 ? '+' : ''}$${m.net.toFixed(2)} — </td> — <td style="text-align:center"> — <div style="display:inline-flex;align-items:center;justify-content:center;width:52px;height:24px;border-radius:12px;background:${marginBg(m.margin)};border:1px solid ${marginColor(m.margin)};font-family:var(--font-mono);font-size:10px;font-weight:700;color:${marginColor(m.margin)}"> — ${m.margin.toFixed(0)}% — </div> — </td> — <td><span class="status-badge status-${m.job.status}">${m.job.status}</span></td> — </tr>`).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="3" style="font-family:var(--font-mono);color:var(--green);font-size:11px">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${totalRevenue.toLocaleString()}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${totalCrew.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:#ff8c00">$${totalExpenses.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;font-weight:700;color:${marginColor(avgMargin)}"> — ${totalNet >= 0 ? '+' : ''}$${totalNet.toFixed(2)} — </td> — <td style="text-align:center"> — <div style="display:inline-flex;align-items:center;justify-content:center;width:52px;height:24px;border-radius:12px;background:${marginBg(avgMargin)};border:1px solid ${marginColor(avgMargin)};font-family:var(--font-mono);font-size:10px;font-weight:700;color:${marginColor(avgMargin)}"> — ${avgMargin.toFixed(0)}% — </div> — </td> — <td></td> — </tr> — </tbody> — </table> — </div>` : ` — <div style="text-align:center;padding:48px"> — <div style="font-size:40px;margin-bottom:12px">📈</div> — <p class="info-text">No job data yet. Create jobs with quoted amounts and crew logs to see margins.</p> — </div>`} — </div> — <!-- MARGIN LEGEND --> — <div style="display:flex;gap:16px;padding:12px 4px;flex-wrap:wrap;align-items:center"> — <span class="info-text">Margin health:</span> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(0,255,65,0.2);border:1px solid var(--green);border-radius:2px"></div><span class="info-text" style="color:var(--green)">40%+ Excellent</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,170,0,0.2);border:1px solid var(--amber);border-radius:2px"></div><span class="info-text" style="color:var(--amber)">20–39% Good</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,140,0,0.2);border:1px solid #ff8c00;border-radius:2px"></div><span class="info-text" style="color:#ff8c00">0–19% Thin</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,51,51,0.15);border:1px solid var(--red);border-radius:2px"></div><span class="info-text" style="color:var(--red)">Negative — Losing money</span></div> — </div>`; — content.innerHTML = html; — } | 17562 | 17777 | 216 |
| 65 | Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — } | 17778 | 18194 | 417 |
| 66 | PAYROLL LEDGER — Running balance per crew member — Balance = earned - paid. When paid → resets to 0 → new cycle | 18195 | 18355 | 161 |
| 67 | PAYROLL LEDGER — Build running balance from jobs + payments | 18356 | 18552 | 197 |
| 68 | PAYROLL LEDGER — Mark Paid modal | 18553 | 18832 | 280 |
| 69 | SETTINGS TAB | 18833 | 19438 | 606 |
| 70 | SETTINGS ACTION FUNCTIONS | 19439 | 19453 | 15 |
| 71 | ROLE SWITCHER — instant role switch without logout | 19454 | 19646 | 193 |
| 72 | USER MANAGEMENT (Office only) | 19647 | 19966 | 320 |
| 73 | MODAL HELPERS | 19967 | 20007 | 41 |
| 74 | NOTIFICATION | 20008 | 20032 | 25 |
| 75 | PAYWALL — Company activation gate | 20033 | 20036 | 4 |
| 76 | TRIAL + TIER SYSTEM | 20037 | 20195 | 159 |
| 77 | PHOTO VIEWER | 20196 | 20285 | 90 |
| 78 | SIGNATURE MODAL | 20286 | 20331 | 46 |
| 79 | FIELD NOTES | 20332 | 20344 | 13 |
| 80 | THEME TOGGLE | 20345 | 20382 | 38 |

## Safe extraction order (lowest coupling first)

Coupling score = number of distinct other sections this section depends on, plus number of
distinct other sections that depend on it (via resolved global-variable read/write references,
including inline HTML event handlers). Lower is safer to extract first.

| Section | Lines | Coupling | Depends on | Depended on by |
|---|---|---|---|---|
| (preamble — before first banner) | 1929-1929 | 0 | — | —
| OWNER ACCOUNT — Cannot be deleted, deactivated, or role-changed — by anyone except the owner themselves | 1954-1957 | 0 | — | —
| AUTH | 5841-5844 | 0 | — | —
| CREW LOG  ← BUG FIX: removed stray content.innerHTML = html; that was outside this function | 7841-7845 | 0 | — | —
| PAYROLL CALCULATION ENGINE — Supports: hourly (hours × rate), flat (fixed amount), daily (rate per day) | 8176-8180 | 0 | — | —
| PAYWALL — Company activation gate | 20033-20036 | 0 | — | —
| THEME TOGGLE | 20345-20382 | 0 | — | —
| PWA — SPLASH, INSTALL PROMPT, SERVICE WORKER | 6030-6075 | 1 | NOTIFICATION | —
| CARRIER CUBIC FEET HELPERS | 8214-8241 | 1 | — | function openNewJobModal(prefillDate) { — ── HARD RESET — every field to blank/zero ────────────────── — const safe = (id, val='') => { const el = document.getElementById(id); if(el) el.value = val; }; — Core job fields — safe('nj-client'); safe('nj-phone'); safe('nj-email'); — safe('nj-origin'); safe('nj-dest'); safe('nj-pickup-from'); — safe('nj-amount'); safe('nj-notes'); safe('nj-carrier'); — safe('nj-cubic-ft'); safe('nj-cubic-ft-2'); safe('nj-rate-per-cuft'); — safe('nj-weight'); safe('nj-linehaul'); — safe('nj-end-date'); safe('nj-end-time'); — safe('nj-pickup-start'); safe('nj-pickup-end'); — safe('nj-delivery-start'); safe('nj-delivery-end'); — safe('nj-pickup-driver'); safe('nj-delivery-driver'); — safe('nj-job-id'); safe('nj-ref-num'); — safe('nj-priority'); safe('nj-followup'); safe('nj-fadd'); — safe('nj-broker-ref'); safe('nj-carrier'); — ALL estimate fields — zero out completely — safe('nj-est-fuel', ''); — safe('nj-est-fuel-pct', '0'); — safe('nj-est-overload-cuft', ''); — safe('nj-est-stairs-origin', ''); — safe('nj-est-stairs-dest', ''); — safe('nj-est-longcarry-origin', ''); — safe('nj-est-longcarry-dest', ''); — safe('nj-est-shuttle', ''); — safe('nj-est-bulky', ''); — safe('nj-est-split', ''); — safe('nj-est-pgs', ''); — safe('nj-est-packing', ''); — safe('nj-est-storage-days', ''); — safe('nj-est-storage-rate', ''); — safe('nj-est-other', ''); — safe('nj-est-deposit', ''); — safe('nj-est-partial2', ''); — safe('nj-est-tip', ''); — safe('nj-est-discount', ''); — safe('nj-est-discount-label', ''); — safe('nj-est-others1', '');  safe('nj-est-others1-label', ''); — safe('nj-est-others2', '');  safe('nj-est-others2-label', ''); — safe('nj-est-others3', '');  safe('nj-est-others3-label', ''); — safe('nj-est-extra1', '');   safe('nj-est-extra1-label', ''); — safe('nj-est-extra2', '');   safe('nj-est-extra2-label', ''); — safe('nj-est-extra3', '');   safe('nj-est-extra3-label', ''); — safe('nj-est-notes', ''); — safe('nj-est-office-remarks', ''); — safe('nj-est-slip-remarks', ''); — Reset carrier total display — const ct = document.getElementById('nj-carrier-total'); if(ct) ct.textContent = '$0.00'; — Clear helper rows — const hr = document.getElementById('nj-helper-rows'); if(hr) hr.innerHTML = ''; — Reset all selects to index 0 — ['nj-job-for','nj-est-tip-method','nj-est-valuation', — 'nj-est-fuel-mode','nj-est-tip-assign'].forEach(id => { — const el = document.getElementById(id); — if (el) el.selectedIndex = 0; — }); — try { toggleCarrierFields('nj'); } catch(e) {} — Apply defaults — const s2 = loadSettings(); — safe('nj-crew', s2.defaultCrew || '3'); — safe('nj-type', s2.defaultMove || 'local'); — safe('nj-date', prefillDate || getTodayStr()); — safe('nj-time', getSetting('defaultTime', '08:00')); — safe('nj-driver', currentUser.role === 'driver' ? (currentUser.name||'') : ''); — closeModal('modal-calday'); — openModal('modal-newjob'); — setTimeout(() => { try { applyJobDefaults('nj'); } catch(e) {} }, 50); — } — async function createJob() { — const client = document.getElementById('nj-client').value.trim(); — const date   = document.getElementById('nj-date').value; — const origin = document.getElementById('nj-origin').value.trim(); — const dest   = document.getElementById('nj-dest').value.trim(); — if (!client || !date) { notify('⚠ Client name and date required', true); return; } — Custom ID or auto-generate — const customId = (document.getElementById('nj-job-id')?.value||'').trim().toUpperCase(); — const autoId   = 'JOB-' + String((STATE.jobs||[]).length + 1).padStart(3,'0'); — const jobId    = customId || autoId; — if ((STATE.jobs||[]).find(j => j.id === jobId)) { — notify('⚠ Job ID ' + jobId + ' already exists', true); return; — } — Collect helpers — const helpers = []; — document.querySelectorAll('.nj-helper-name').forEach(inp => { — const name = inp.value.trim(); — if (!name) return; — const row = inp.closest('.nj-helper-row'); — helpers.push({ — name, — rate:    parseFloat(row?.querySelector('.nj-helper-rate')?.value) || 0, — payType: row?.querySelector('.nj-helper-type')?.value || 'hourly', — hours: 0, days: 1, workerType: '1099', paid: false — }); — }); — const g = id => (document.getElementById(id)?.value || ''); — const newJob = { — id:            jobId, — refNum:        g('nj-ref-num'), — client, — phone:         g('nj-phone'), — email:         g('nj-email'), — date, — time:          g('nj-time'), — endDate:       g('nj-end-date') || g('nj-delivery-end').slice(0,10) || '', — endTime:       g('nj-end-time'), — origin, — dest, — pickupStart:   g('nj-pickup-start'), — pickupEnd:     g('nj-pickup-end'), — pickupDriver:  g('nj-pickup-driver'), — deliveryStart: g('nj-delivery-start'), — deliveryEnd:   g('nj-delivery-end'), — deliveryDriver:g('nj-delivery-driver'), — driver:        g('nj-driver'), — driverUser:    currentUser.username || '', — crewSize:      parseInt(g('nj-crew')) || 3, — crewNames:     helpers.map(h => h.name), — amount:        parseFloat(g('nj-amount')) || 0, — cubicFt:       parseFloat(g('nj-cubic-ft-2') || g('nj-cubic-ft')) || 0, — weight:        parseFloat(g('nj-weight')) || 0, — linehaul:      parseFloat(g('nj-linehaul')) || 0, — priority:      g('nj-priority'), — followUp:      g('nj-followup'), — fadd:          g('nj-fadd'), — type:          g('nj-type') || 'local', — notes:         g('nj-notes'), — companyId:     COMPANY_ID, — status:        'pending', — gates:         { preload:false, loaded:false, delivered:false, payment:false }, — messages:      { crew:[], client:[] }, — photos:        [], — signatures:    [], — payroll:       { driver:0, helpers } — }; — try { — await db.collection(COL_JOBS).doc(newJob.id).set(newJob); — closeModal('modal-newjob'); — notify('✓ Job ' + newJob.id + ' created — ' + client); — setTimeout(() => renderCalendar(), 200); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('createJob error:', e); — } — }
| EMPLOYEE ID SYSTEM — empId  = auto-assigned badge (EMP-001, EMP-002...) — handle = user-chosen nickname/alias — displayHandle() = handle if set, else empId | 10984-11012 | 2 | FIREBASE INIT, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | —
| FIELD NOTES | 20332-20344 | 2 | NOTIFICATION, STATE & STORAGE | —
| LABOR PRODUCTIVITY SCORES — v52 — Uses gate timestamps to calculate efficiency per crew member — Crew A moves 50 cu.ft/hr vs Crew B at 35 cu.ft/hr — Visible to owner/office — used for rewards and training | 4384-4605 | 3 | NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, STATE & STORAGE | TABS
| OFFLINE MODE — Full PWA with offline queue — Caches app shell on first load — Queues Firestore writes when offline — Syncs queue automatically when back online | 6122-6317 | 3 | FIREBASE INIT, NOTIFICATION | STATE & STORAGE
| HISTORY | 7748-7771 | 3 | JOB DETAIL MODAL, STATE & STORAGE | TABS
| TWILIO SMS — Real text notifications — Configure in Settings → Integrations | 9946-10011 | 3 | NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, STATE & STORAGE | JOB DETAIL MODAL
| ESTIMATE TAB — Per-job BOL line item editor — This is the source of truth for the BOL PDF | 8242-8533 | 4 | BOL TAB, PORTAL ROUTE CHECK — runs before login screen — Matches Good Friends Movers LLC document package exactly — US DOT# 3921653 / MC# 1450729 — Structure: Interstate BOL | Legal Terms | Inventory x2 | Packing | Post Contract | Valuation, STATE & STORAGE | TABS
| P&L ACCOUNTING MODULE — Kills QuickBooks — Chart of accounts, monthly P&L, period reports, export | 9521-9945 | 4 | NOTIFICATION, PAYROLL CALCULATION ENGINE v2 — pay + commission + tax, STATE & STORAGE | TABS
| SCHEDULE | 14958-15012 | 4 | JOB DETAIL MODAL, NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, STATE & STORAGE | TABS
| SETTINGS ACTION FUNCTIONS | 19439-19453 | 4 | NOTIFICATION, SETTINGS TAB, USER SETTINGS — stored in localStorage per user | SETTINGS TAB
| JOB DISPLAY ID — broker/carrier ref number support — job.id      = FleetFlow internal ID (never changes) — job.refNum  = Broker/carrier order number (editable by office) — jobDispId() = what to show everywhere in the UI | 10910-10983 | 5 | JOB DETAIL MODAL, MODAL HELPERS, NOTIFICATION, STATE & STORAGE | JOB DETAIL MODAL
| CLIENT BOOKING PORTAL — URL: fleetflowmovemasters.netlify.app/?book=1&co=COMPANY_ID — Moving company shares this link on their website — Client fills details → instant quote → pays deposit → job created | 11013-11921 | 5 | FIREBASE INIT, FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill, NOTIFICATION, STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live | SESSION PERSISTENCE — remember me across refreshes/tab closes
| PORTAL ROUTE CHECK — runs before login screen — Matches Good Friends Movers LLC document package exactly — US DOT# 3921653 / MC# 1450729 — Structure: Interstate BOL | Legal Terms | Inventory x2 | Packing | Post Contract | Valuation | 11922-12759 | 5 | NOTIFICATION, STATE & STORAGE, USER SETTINGS — stored in localStorage per user | BOL TAB, ESTIMATE TAB — Per-job BOL line item editor — This is the source of truth for the BOL PDF
| function renderMyPay() { — const content  = document.getElementById('main-content'); — const role     = currentUser.role; — const isHelper = role === 'helper'; — Collect all pay entries for this person — const myJobs = STATE.jobs.filter(j => { — if (role === 'driver') return nameIncludes(j.driver, currentUser.name); — if (role === 'helper') return (j.payroll?.helpers||[]).some(h => nameIncludes(h.name, currentUser.name)); — return false; — }); — let totalGross      = 0; — let totalCommission = 0; — let totalTax        = 0; — let totalTakeHome   = 0; — const payRows       = []; — myJobs.forEach(j => { — let gross = 0, commission = 0, workerType = '1099', payDetails = ''; — if (role === 'driver') { — gross       = j.payroll?.driver || 0; — commission  = j.payroll?.driverCommission || 0; — workerType  = j.payroll?.driverWorkerType || '1099'; — payDetails  = payLabel({ payType: j.payroll?.driverPayType||'flat', rate: j.payroll?.driverRate||gross, hours: j.payroll?.driverHours||0, days: j.payroll?.driverDays||1 }); — } else { — const helper = (j.payroll?.helpers||[]).find(h => — h.name?.toLowerCase().includes(currentUser.name.toLowerCase()) || — (currentUser.name.toLowerCase().split(' ')[0] && h.name?.toLowerCase().includes(currentUser.name.toLowerCase().split(' ')[0])) — ); — if (helper) { — gross      = calcPay(helper); — commission = helper.commission || 0; — workerType = helper.workerType || '1099'; — payDetails = payLabel(helper); — } — } — if (gross === 0) return; — const tax = calcTax(gross, workerType); — totalGross      += gross; — totalCommission += commission; — totalTax        += tax.totalEstTax; — totalTakeHome   += tax.takeHome; — payRows.push({ job: j, gross, commission, workerType, payDetails, tax }); — }); — let html = ` — <div class="section-header"> — <div> — <div class="section-title">📊 MY PAY SUMMARY</div> — <div class="section-sub">${currentUser.name} · ${role.toUpperCase()} · ${payRows.length} job(s)</div> — </div> — </div> — <!-- YTD SUMMARY STATS --> — <div class="grid-4" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Gross Earned</div> — <div class="stat-value">$${totalGross.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Commission</div> — <div class="stat-value" style="color:var(--amber)">$${totalCommission.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Est. Tax Owed</div> — <div class="stat-value red">$${totalTax.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Est. Take-Home</div> — <div class="stat-value">$${totalTakeHome.toFixed(2)}</div> — </div> — </div> — <!-- TAX ADVISORY BANNER --> — <div class="card" style="border-color:#664400;background:rgba(255,170,0,0.05);margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:11px;color:var(--amber);letter-spacing:2px;margin-bottom:8px">⚠ TAX ADVISORY</div> — <div class="info-text" style="line-height:1.8"> — ${totalGross > 0 ? `Based on your earnings of <strong style="color:var(--white)">$${totalGross.toFixed(2)}</strong>, you should set aside approximately <strong style="color:var(--red)">$${totalTax.toFixed(2)}</strong> (${(totalTax/totalGross*100).toFixed(1)}%) for taxes.` : 'No earnings recorded yet.'} — ${totalGross >= 600 ? ' You will likely receive a 1099 form if classified as a contractor.' : ''} — If you are a <strong style="color:var(--white)">1099 contractor</strong>, you are responsible for paying your own taxes quarterly (April, June, September, January). Consider setting aside <strong style="color:var(--red)">${totalGross > 0 ? (totalTax/totalGross*100).toFixed(0) : '~25'}%</strong> of every paycheck. — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div style="margin-top:4px"> — ${payRows.length ? payRows.map(row => ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:10px"> — <div> — <div style="display:flex;gap:8px;align-items:center;margin-bottom:4px"> — <span class="tag">${row.job.id}</span> — <span class="status-badge status-${row.job.status}">${row.job.status}</span> — <span class="tag">${row.workerType}</span> — </div> — <div style="font-size:14px;font-weight:600">${row.job.client}</div> — <div class="info-text">${row.job.date} · ${row.payDetails}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:20px;color:var(--green)">$${row.gross.toFixed(2)}</div> — ${row.commission > 0 ? `<div style="font-family:var(--font-mono);font-size:12px;color:var(--amber)">+ $${row.commission.toFixed(2)} commission</div>` : ''} — </div> — </div> — <!-- Tax breakdown per job --> — <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;margin-bottom:8px"> — <div class="stat-box" style="padding:10px"> — <div class="stat-label" style="font-size:9px">Gross Pay</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--white)">$${row.gross.toFixed(2)}</div> — </div> — <div class="stat-box" style="padding:10px"> — <div class="stat-label" style="font-size:9px">Est. Tax</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--red)">−$${row.tax.totalEstTax.toFixed(2)}</div> — </div> — <div class="stat-box" style="padding:10px;border-color:var(--green3)"> — <div class="stat-label" style="font-size:9px">Take-Home</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--green)">$${row.tax.takeHome.toFixed(2)}</div> — </div> — </div> — <!-- Detailed tax line items --> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;font-family:var(--font-mono);font-size:10px"> — ${row.tax.selfEmployTax ? `<div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>Self-Employment Tax (15.3%)</span><span style="color:var(--red)">−$${row.tax.selfEmployTax.toFixed(2)}</span></div>` : ''} — ${row.tax.ficaTax ? `<div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>FICA Employee (7.65%)</span><span style="color:var(--red)">−$${row.tax.ficaTax.toFixed(2)}</span></div>` : ''} — <div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>Federal Income (~${(row.tax.federalRate*100).toFixed(0)}%)</span><span style="color:var(--red)">−$${row.tax.federalTax.toFixed(2)}</span></div> — <div style="display:flex;justify-content:space-between;color:var(--gray)"><span>Georgia State (5.5%)</span><span style="color:var(--red)">−$${row.tax.stateTax.toFixed(2)}</span></div> — </div> — </div>`).join('') : ` — <div class="card" style="text-align:center;padding:40px"> — <p class="info-text">No pay records found for ${currentUser.name}.</p> — <p class="info-text" style="margin-top:8px">Your earnings will appear here once jobs are assigned and crew logs are saved.</p> — </div>`} — </div> — <div style="padding:12px;font-family:var(--font-mono);font-size:9px;color:var(--gray);line-height:1.8;border-top:1px solid var(--gray2);margin-top:8px"> — ⚠ DISCLAIMER: Tax estimates are for informational purposes only based on 2024 US federal rates and Georgia state rates. This is not tax advice. Individual circumstances vary. Consult a licensed tax professional or CPA for filing guidance. JPG Systems / JPG Ventures LLC is not responsible for tax obligations of independent contractors. — </div>`; — content.innerHTML = html; — } | 16773-16915 | 5 | NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, PAYROLL CALCULATION ENGINE v2 — pay + commission + tax, STATE & STORAGE, TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | TABS
| PAYROLL LEDGER — Build running balance from jobs + payments | 18356-18552 | 5 | PAYROLL CALCULATION ENGINE v2 — pay + commission + tax, STATE & STORAGE | Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — }, PAYROLL LEDGER — Mark Paid modal, PAYROLL LEDGER — Running balance per crew member — Balance = earned - paid. When paid → resets to 0 → new cycle
| PHOTO VIEWER | 20196-20285 | 5 | NOTIFICATION, STATE & STORAGE | CLAIMS DEFENSE AI — v51 — Pre-load damage detection using Claude vision — When driver takes a photo at pre-load gate, AI scans for — pre-existing damage, flags it, embeds in BOL as evidence. — Protects moving company from false damage claims., CLIENT MOVE PORTAL — Full transparency experience — This is what the client pays $100-150 for — Real-time gate tracking, item location, messaging, docs, LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool
| SIGNATURE MODAL | 20286-20331 | 5 | MODAL HELPERS, NOTIFICATION, STATE & STORAGE | BOL TAB, CLIENT MOVE PORTAL — Full transparency experience — This is what the client pays $100-150 for — Real-time gate tracking, item location, messaging, docs
| ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard | 3995-4383 | 6 | COMPANY BRANDING ENGINE, MODAL HELPERS, NOTIFICATION, STATE & STORAGE, TABS | TABS
| CLAIMS DEFENSE AI — v51 — Pre-load damage detection using Claude vision — When driver takes a photo at pre-load gate, AI scans for — pre-existing damage, flags it, embeds in BOL as evidence. — Protects moving company from false damage claims. | 4606-4803 | 6 | JOB DETAIL MODAL, MODAL HELPERS, NOTIFICATION, PHOTO VIEWER, STATE & STORAGE | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool
| PAY SCHEDULE & UNPAID CREW CHECKER | 12760-12816 | 6 | NOTIFICATION, NOTIFICATION ENGINE, PAYROLL CALCULATION ENGINE v2 — pay + commission + tax, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, USER SETTINGS — stored in localStorage per user | —
| function renderFleet() { — const content = document.getElementById('main-content'); — const role    = currentUser.role; — const canEdit = role === 'office'; — let html = ` — <div class="section-header"> — <div> — <div class="section-title">🚚 FLEET MANAGEMENT</div> — <div class="section-sub">${STATE.fleet.length} vehicle(s) registered · Move Masters OS</div> — </div> — ${canEdit ? `<button class="btn-green" onclick="openFleetModal()">+ ADD VEHICLE</button>` : ''} — </div>`; — if (!STATE.fleet.length) { — html += `<div class="card" style="text-align:center;padding:40px"> — <div style="font-size:48px;margin-bottom:12px">🚚</div> — <p class="info-text">No vehicles registered yet.</p> — ${canEdit ? `<button class="btn-green" style="margin-top:16px" onclick="openFleetModal()">+ ADD FIRST VEHICLE</button>` : ''} — </div>`; — } else { — Summary stats — const active  = STATE.fleet.filter(v => v.status === 'active').length; — const maint   = STATE.fleet.filter(v => v.status === 'maintenance').length; — const offline = STATE.fleet.filter(v => v.status === 'offline').length; — html += `<div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Active</div><div class="stat-value">${active}</div></div> — <div class="stat-box"><div class="stat-label">In Maintenance</div><div class="stat-value amber">${maint}</div></div> — <div class="stat-box"><div class="stat-label">Offline</div><div class="stat-value red">${offline}</div></div> — </div>`; — STATE.fleet.forEach(v => { — const assignedJob = STATE.jobs.find(j => j.vehicleId === v.id && j.status === 'active'); — const statusColor = v.status === 'active' ? 'var(--green)' : v.status === 'maintenance' ? 'var(--amber)' : 'var(--red)'; — const maintDue    = v.nextMaint ? new Date(v.nextMaint) : null; — const maintOverdue = maintDue && maintDue < new Date(); — html += ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:12px"> — <div> — <div style="display:flex;align-items:center;gap:10px;margin-bottom:6px"> — <div style="font-size:28px">${v.type === 'box_truck' ? '🚛' : v.type === 'cargo_van' ? '🚐' : '🚚'}</div> — <div> — <div style="font-family:var(--font-mono);font-size:14px;font-weight:700;color:var(--white)">${v.name || v.id}</div> — <div class="info-text">${v.make||''} ${v.model||''} ${v.year||''} · ${v.plate||'No plate'}</div> — </div> — </div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:11px;color:${statusColor};letter-spacing:2px">${(v.status||'active').toUpperCase()}</div> — ${canEdit ? `<button class="btn-sm" style="margin-top:6px" onclick="openFleetModal('${v.id}')">✎ EDIT</button>` : ''} — </div> — </div> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(110px,1fr));gap:8px;margin-bottom:12px"> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Capacity</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.capacity || '—'}</div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Mileage</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.mileage ? v.mileage.toLocaleString() + ' mi' : '—'}</div> — </div> — <div class="stat-box" style="padding:8px;${maintOverdue ? 'border-color:var(--red)' : ''}"> — <div class="stat-label" style="font-size:9px">Next Service</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${maintOverdue ? 'var(--red)' : 'var(--white)'}"> — ${v.nextMaint || 'Not set'}${maintOverdue ? ' ⚠' : ''} — </div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Assigned Job</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${assignedJob ? 'var(--green)' : 'var(--gray)'}"> — ${assignedJob ? assignedJob.id : 'Available'} — </div> — </div> — </div> — ${v.notes ? `<div class="info-text" style="margin-bottom:10px">📝 ${v.notes}</div>` : ''} — <!-- MAINTENANCE LOG --> — ${v.maintLog && v.maintLog.length ? ` — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:6px">MAINTENANCE LOG</div> — ${v.maintLog.slice(-3).reverse().map(m => ` — <div style="display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px solid var(--gray2);font-size:12px"> — <div><span class="info-text">${m.date}</span> · ${m.work}</div> — <div style="font-family:var(--font-mono);color:var(--amber)">$${(m.cost||0).toFixed(0)}</div> — </div>`).join('')}` : ''} — ${canEdit ? ` — <div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap"> — <button class="btn-sm" onclick="logMaintenance('${v.id}')">🔧 LOG SERVICE</button> — <button class="btn-sm" onclick="updateMileage('${v.id}')">📍 UPDATE MILEAGE</button> — <select style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:5px 8px;font-family:var(--font-mono);font-size:10px;border-radius:3px" — onchange="updateVehicleStatus('${v.id}',this.value)"> — <option value="active"      ${v.status==='active'      ?'selected':''}>● Active</option> — <option value="maintenance" ${v.status==='maintenance' ?'selected':''}>🔧 Maintenance</option> — <option value="offline"     ${v.status==='offline'     ?'selected':''}>○ Offline</option> — </select> — </div>` : ''} — </div>`; — }); — } — content.innerHTML = html; — } — function openFleetModal(vehicleId) { — const v = vehicleId ? STATE.fleet.find(x => x.id === vehicleId) : null; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">${v ? 'EDIT VEHICLE — ' + v.id : 'ADD VEHICLE'}</div> — <div class="grid-2"> — <div class="form-group"><label>Vehicle Name / Unit #</label> — <input type="text" id="flt-name" value="${(v&&v.name)||''}" placeholder="e.g. Truck 1 · Unit 24" /></div> — <div class="form-group"><label>Type</label> — <select id="flt-type"> — <option value="box_truck" ${v?.type==='box_truck'?'selected':''}>🚛 Box Truck</option> — <option value="cargo_van" ${v?.type==='cargo_van'?'selected':''}>🚐 Cargo Van</option> — <option value="flatbed"   ${v?.type==='flatbed'  ?'selected':''}>🚚 Flatbed</option> — <option value="other"     ${v?.type==='other'    ?'selected':''}>📋 Other</option> — </select> — </div> — </div> — <div class="grid-3"> — <div class="form-group"><label>Make</label><input type="text" id="flt-make" value="${(v&&v.make)||''}" placeholder="Ford" /></div> — <div class="form-group"><label>Model</label><input type="text" id="flt-model" value="${(v&&v.model)||''}" placeholder="E-450" /></div> — <div class="form-group"><label>Year</label><input type="number" id="flt-year" value="${(v&&v.year)||''}" placeholder="2019" /></div> — </div> — <div class="grid-2"> — <div class="form-group"><label>License Plate</label><input type="text" id="flt-plate" value="${(v&&v.plate)||''}" placeholder="ABC-1234" style="text-transform:uppercase"/></div> — <div class="form-group"><label>Capacity (text label)</label><input type="text" id="flt-cap" value="${(v&&v.capacity)||''}" placeholder="26 ft · 1500 lbs" /></div> — </div> — <div class="grid-3"> — <div class="form-group"> — <label>Max Cubic Feet <span class="info-text">(for load optimization)</span></label> — <input type="number" id="flt-maxcuft" value="${(v&&v.maxCuFt)||''}" placeholder="e.g. 1200" min="0" step="50" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Fuel Efficiency (MPG) <span class="info-text">(for cost calc)</span></label> — <input type="number" id="flt-mpg" value="${(v&&v.mpg)||''}" placeholder="e.g. 10" min="1" step="0.5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Load Efficiency % <span class="info-text">(usable space)</span></label> — <input type="number" id="flt-loadeff" value="${(v&&v.loadEff)||85}" placeholder="85" min="50" max="100" step="5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — </div> — <div class="grid-2"> — <div class="form-group"><label>Current Mileage</label><input type="number" id="flt-miles" value="${(v&&v.mileage)||''}" placeholder="85000" /></div> — <div class="form-group"><label>Next Service Due</label><input type="date" id="flt-maint" value="${(v&&v.nextMaint)||''}" /></div> — </div> — <div class="form-group"><label>Notes</label> — <input type="text" id="flt-notes" value="${(v&&v.notes)||''}" placeholder="Insurance, registration, special equipment..." /></div> — <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:8px"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" onclick="saveVehicle('${vehicleId||''}')">SAVE VEHICLE</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveVehicle(vehicleId) { — const vehicle = { — id:        vehicleId || 'VEH-' + Date.now(), — name:      document.getElementById('flt-name').value.trim(), — type:      document.getElementById('flt-type').value, — make:      document.getElementById('flt-make').value.trim(), — model:     document.getElementById('flt-model').value.trim(), — year:      document.getElementById('flt-year').value, — plate:     document.getElementById('flt-plate').value.trim().toUpperCase(), — capacity:  document.getElementById('flt-cap').value.trim(), — maxCuFt:   parseFloat(document.getElementById('flt-maxcuft')?.value)||0, — mpg:       parseFloat(document.getElementById('flt-mpg')?.value)||0, — loadEff:   parseFloat(document.getElementById('flt-loadeff')?.value)||85, — mileage:   parseInt(document.getElementById('flt-miles').value)||0, — nextMaint: document.getElementById('flt-maint').value, — notes:     document.getElementById('flt-notes').value.trim(), — status:    vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.status||'active') : 'active', — maintLog:  vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.maintLog||[]) : [], — updatedAt: new Date().toISOString() — }; — if (!vehicle.name) { notify('⚠ Vehicle name is required', true); return; } — try { — await db.collection(COL_FLEET).doc(vehicle.id).set(vehicle); — notify((vehicleId ? 'Vehicle updated' : 'Vehicle added') + ' ✓'); — closeModal('modal-jobdetail'); — } catch(e) { notify('⚠ ' + e.message, true); } — } — async function logMaintenance(vehicleId) { — const work = prompt('Service performed:'); if (!work) return; — const cost = parseFloat(prompt('Cost ($):') || '0'); — const v    = STATE.fleet.find(x => x.id === vehicleId); — if (!v) return; — if (!v.maintLog) v.maintLog = []; — v.maintLog.push({ date: getTodayStr(), work, cost, loggedBy: currentUser.name }); — v.updatedAt = new Date().toISOString(); — await db.collection(COL_FLEET).doc(vehicleId).set(v); — notify('Maintenance logged ✓'); — } — async function updateMileage(vehicleId) { — const miles = prompt('Enter current mileage:'); if (!miles) return; — await db.collection(COL_FLEET).doc(vehicleId).update({ mileage: parseInt(miles)||0 }); — notify('Mileage updated ✓'); — } — async function updateVehicleStatus(vehicleId, status) { — await db.collection(COL_FLEET).doc(vehicleId).update({ status }); — notify('Vehicle status: ' + status.toUpperCase()); — } | 17170-17391 | 6 | FIREBASE INIT, MODAL HELPERS, NOTIFICATION, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE | TABS
| function renderMargins() { — const content = document.getElementById('main-content'); — Build margin data per job — const jobMargins = STATE.jobs — .filter(j => j.amount > 0 || j.payroll) — .map(j => { — const revenue   = j.amount || 0; — const driverAccBonus = parseFloat(j.payroll?.accessorialBonus) || 0; — const crewCost  = (j.payroll?.driver || 0) + — driverAccBonus + — (j.payroll?.helpers || []).reduce((s,h) => s + calcPay(h), 0) + — (j.payroll?.driverCommission || 0); — Company revenue excludes driver accessorial bonuses (passed through to driver) — const expenses  = STATE.receipts — .filter(r => r.jobId === j.id && r.approved !== false) — .reduce((s,r) => s + (r.amount||0), 0); — const net       = revenue - crewCost - expenses; — const margin    = revenue > 0 ? (net / revenue * 100) : 0; — return { job: j, revenue, crewCost, expenses, net, margin }; — }) — .sort((a,b) => { — Default: worst margin first (ascending) — const mA = a.revenue > 0 ? a.profit/a.revenue : 0; — const mB = b.revenue > 0 ? b.profit/b.revenue : 0; — return mA - mB; — }); — Sort control — if (window._marginSort === 'date') jobMargins.sort((a,b) => b.job.date > a.job.date ? 1 : -1); — else if (window._marginSort === 'revenue') jobMargins.sort((a,b) => b.revenue - a.revenue); — ── Margin intelligence ── — const overpayJobs   = jobMargins.filter(m => m.revenue > 0 && (m.crewCost/m.revenue) > 0.40); — const lowProfitJobs = jobMargins.filter(m => m.revenue > 0 && (m.profit/m.revenue) < 0.20); — const bestJob       = [...jobMargins].sort((a,b) => (b.profit/b.revenue||0)-(a.profit/a.revenue||0))[0]; — const worstJob      = jobMargins[0]; // already sorted worst first — Overall stats — const totalRevenue  = jobMargins.reduce((s,m) => s + m.revenue, 0); — const totalCrew     = jobMargins.reduce((s,m) => s + m.crewCost, 0); — const totalExpenses = jobMargins.reduce((s,m) => s + m.expenses, 0); — const totalNet      = jobMargins.reduce((s,m) => s + m.net, 0); — const avgMargin     = totalRevenue > 0 ? (totalNet / totalRevenue * 100) : 0; — const profitable    = jobMargins.filter(m => m.net > 0).length; — const losing        = jobMargins.filter(m => m.net < 0).length; — Best and worst job — const best  = jobMargins.reduce((a,b) => b.margin > a.margin ? b : a, jobMargins[0]); — const worst = jobMargins.reduce((a,b) => b.margin < a.margin ? b : a, jobMargins[0]); — const marginColor = (n) => n >= 40 ? 'var(--green)' : n >= 20 ? 'var(--amber)' : n >= 0 ? '#ff8c00' : 'var(--red)'; — const marginBg    = (n) => n >= 40 ? 'rgba(0,255,65,0.08)' : n >= 20 ? 'rgba(255,170,0,0.08)' : n >= 0 ? 'rgba(255,140,0,0.08)' : 'rgba(255,51,51,0.08)'; — let html = ` — ${overpayJobs.length ? `<div style="background:rgba(255,30,30,0.06);border:1px solid rgba(255,30,30,0.3);border-radius:6px;padding:10px 14px;margin-bottom:12px;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px"> — <div> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;font-weight:700">⚠ LABOR OVERPAY ALERT</div> — <div style="font-size:12px;color:var(--white);margin-top:3px">${overpayJobs.length} job${overpayJobs.length>1?'s':''} with crew cost >40% of revenue</div> — <div style="font-size:11px;color:var(--gray);margin-top:2px">${overpayJobs.slice(0,3).map(m=>m.job.client+' ('+Math.round(m.crewCost/m.revenue*100)+'%)').join(' · ')}</div> — </div> — </div>` : ''} — <div style="display:flex;gap:8px;margin-bottom:12px;flex-wrap:wrap"> — <span style="font-family:var(--font-mono);font-size:10px;color:var(--gray);align-self:center">Sort:</span> — ${['worst:⬇ Worst Margin','revenue:$ Revenue','date:📅 Date'].map(o=>{const[v,l]=o.split(':');return `<button onclick="window._marginSort='${v}';renderMargins()" style="padding:5px 12px;font-family:var(--font-mono);font-size:10px;border-radius:3px;cursor:pointer;background:${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--green)':'transparent'};color:${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--navy)':'var(--gray)'};border:1px solid ${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--green)':'var(--gray2)'}">${l}</button>`;}).join('')} — </div> — <div class="section-header"> — <div> — <div class="section-title">📈 JOB MARGIN DASHBOARD</div> — <div class="section-sub">Revenue · Crew Cost · Expenses · Net per Job</div> — </div> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportJobsCSV()">⬇ EXPORT</button> — </div> — <!-- OVERALL STATS --> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(130px,1fr));gap:8px;margin-bottom:16px"> — <div class="stat-box"> — <div class="stat-label">Total Revenue</div> — <div class="stat-value">$${totalRevenue.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${jobMargins.length} jobs</div> — </div> — <div class="stat-box"> — <div class="stat-label">Crew Cost</div> — <div class="stat-value red">$${totalCrew.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${totalRevenue>0?(totalCrew/totalRevenue*100).toFixed(1):'0'}% of revenue</div> — </div> — <div class="stat-box"> — <div class="stat-label">Expenses</div> — <div class="stat-value red">$${totalExpenses.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${totalRevenue>0?(totalExpenses/totalRevenue*100).toFixed(1):'0'}% of revenue</div> — </div> — <div class="stat-box" style="border-color:${marginColor(avgMargin)}"> — <div class="stat-label">Net Profit</div> — <div class="stat-value" style="color:${marginColor(avgMargin)}">$${totalNet.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px;color:${marginColor(avgMargin)}">${avgMargin.toFixed(1)}% avg margin</div> — </div> — <div class="stat-box"> — <div class="stat-label">Profitable Jobs</div> — <div class="stat-value" style="color:var(--green)">${profitable}</div> — <div class="info-text" style="margin-top:4px;color:var(--red)">${losing} losing</div> — </div> — </div> — <!-- MARGIN BREAKDOWN BAR --> — ${totalRevenue > 0 ? ` — <div class="card" style="margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;margin-bottom:10px">REVENUE BREAKDOWN</div> — <div style="height:32px;border-radius:4px;overflow:hidden;display:flex;margin-bottom:8px"> — <div style="width:${(totalCrew/totalRevenue*100).toFixed(1)}%;background:rgba(255,51,51,0.7);transition:width 0.5s" title="Crew Cost"></div> — <div style="width:${(totalExpenses/totalRevenue*100).toFixed(1)}%;background:rgba(255,140,0,0.7);transition:width 0.5s" title="Expenses"></div> — <div style="flex:1;background:rgba(0,255,65,0.4);transition:width 0.5s" title="Net Profit"></div> — </div> — <div style="display:flex;gap:16px;flex-wrap:wrap"> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(255,51,51,0.7);border-radius:2px"></div><span class="info-text">Crew ${(totalCrew/totalRevenue*100).toFixed(1)}%</span></div> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(255,140,0,0.7);border-radius:2px"></div><span class="info-text">Expenses ${(totalExpenses/totalRevenue*100).toFixed(1)}%</span></div> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(0,255,65,0.4);border-radius:2px"></div><span class="info-text">Net ${avgMargin.toFixed(1)}%</span></div> — </div> — </div>` : ''} — <!-- BEST / WORST --> — ${jobMargins.length >= 2 ? ` — <div class="grid-2" style="margin-bottom:16px"> — ${best ? `<div class="card" style="border-color:var(--green3)"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--green);letter-spacing:2px;margin-bottom:6px">🏆 BEST MARGIN JOB</div> — <div style="font-size:15px;font-weight:700">${best.job.client} <span class="tag">${best.job.id}</span></div> — <div class="info-text">${best.job.date}</div> — <div style="font-family:var(--font-mono);font-size:22px;color:var(--green);margin-top:6px">${best.margin.toFixed(1)}%</div> — <div class="info-text">$${best.net.toFixed(2)} net on $${best.revenue.toLocaleString()}</div> — </div>` : ''} — ${worst ? `<div class="card" style="border-color:${marginColor(worst.margin)}"> — <div style="font-family:var(--font-mono);font-size:9px;color:${marginColor(worst.margin)};letter-spacing:2px;margin-bottom:6px">${worst.net < 0 ? '⚠ LOSING JOB' : '📉 LOWEST MARGIN'}</div> — <div style="font-size:15px;font-weight:700">${worst.job.client} <span class="tag">${worst.job.id}</span></div> — <div class="info-text">${worst.job.date}</div> — <div style="font-family:var(--font-mono);font-size:22px;color:${marginColor(worst.margin)};margin-top:6px">${worst.margin.toFixed(1)}%</div> — <div class="info-text">$${worst.net.toFixed(2)} net on $${worst.revenue.toLocaleString()}</div> — </div>` : ''} — </div>` : ''} — <!-- PER-JOB TABLE --> — <div class="card" style="padding:0;overflow:hidden"> — <div style="padding:12px 16px;border-bottom:1px solid var(--gray2)"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--green);letter-spacing:2px">JOB BY JOB BREAKDOWN</div> — </div> — ${jobMargins.length ? ` — <div style="overflow-x:auto"> — <table style="min-width:700px"> — <thead><tr> — <th>Job</th> — <th>Client</th> — <th>Date</th> — <th style="text-align:right">Revenue</th> — <th style="text-align:right">Crew Cost</th> — <th style="text-align:right">Expenses</th> — <th style="text-align:right">Net</th> — <th style="text-align:center">Margin</th> — <th>Status</th> — </tr></thead> — <tbody> — ${jobMargins.map(m => ` — <tr style="cursor:pointer;background:${marginBg(m.margin)}" onclick="switchTab('jobs');setTimeout(()=>openJobDetail('${m.job.id}'),300)"> — <td><span class="tag">${m.job.id}</span></td> — <td style="font-weight:600">${m.job.client}</td> — <td class="info-text">${m.job.date}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${m.revenue.toLocaleString()}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${m.crewCost.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:#ff8c00">$${m.expenses.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;font-weight:700;color:${marginColor(m.margin)}"> — ${m.net >= 0 ? '+' : ''}$${m.net.toFixed(2)} — </td> — <td style="text-align:center"> — <div style="display:inline-flex;align-items:center;justify-content:center;width:52px;height:24px;border-radius:12px;background:${marginBg(m.margin)};border:1px solid ${marginColor(m.margin)};font-family:var(--font-mono);font-size:10px;font-weight:700;color:${marginColor(m.margin)}"> — ${m.margin.toFixed(0)}% — </div> — </td> — <td><span class="status-badge status-${m.job.status}">${m.job.status}</span></td> — </tr>`).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="3" style="font-family:var(--font-mono);color:var(--green);font-size:11px">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${totalRevenue.toLocaleString()}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${totalCrew.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:#ff8c00">$${totalExpenses.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;font-weight:700;color:${marginColor(avgMargin)}"> — ${totalNet >= 0 ? '+' : ''}$${totalNet.toFixed(2)} — </td> — <td style="text-align:center"> — <div style="display:inline-flex;align-items:center;justify-content:center;width:52px;height:24px;border-radius:12px;background:${marginBg(avgMargin)};border:1px solid ${marginColor(avgMargin)};font-family:var(--font-mono);font-size:10px;font-weight:700;color:${marginColor(avgMargin)}"> — ${avgMargin.toFixed(0)}% — </div> — </td> — <td></td> — </tr> — </tbody> — </table> — </div>` : ` — <div style="text-align:center;padding:48px"> — <div style="font-size:40px;margin-bottom:12px">📈</div> — <p class="info-text">No job data yet. Create jobs with quoted amounts and crew logs to see margins.</p> — </div>`} — </div> — <!-- MARGIN LEGEND --> — <div style="display:flex;gap:16px;padding:12px 4px;flex-wrap:wrap;align-items:center"> — <span class="info-text">Margin health:</span> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(0,255,65,0.2);border:1px solid var(--green);border-radius:2px"></div><span class="info-text" style="color:var(--green)">40%+ Excellent</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,170,0,0.2);border:1px solid var(--amber);border-radius:2px"></div><span class="info-text" style="color:var(--amber)">20–39% Good</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,140,0,0.2);border:1px solid #ff8c00;border-radius:2px"></div><span class="info-text" style="color:#ff8c00">0–19% Thin</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,51,51,0.15);border:1px solid var(--red);border-radius:2px"></div><span class="info-text" style="color:var(--red)">Negative — Losing money</span></div> — </div>`; — content.innerHTML = html; — } | 17562-17777 | 6 | JOB DETAIL MODAL, PAYROLL CALCULATION ENGINE v2 — pay + commission + tax, STATE & STORAGE, TABS, function exportPayrollCSV() { — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — const addPerson = (name, role, gross, commission, workerType) => { — if (!name) return; — if (!payMap[name]) payMap[name] = { name, role, jobs: 0, gross: 0, commission: 0, workerType }; — payMap[name].jobs++; — payMap[name].gross      += gross; — payMap[name].commission += commission; — }; — addPerson(j.driver, 'Driver', j.payroll.driver||0, j.payroll.driverCommission||0, j.payroll.driverWorkerType||'1099'); — (j.payroll.helpers||[]).forEach(h => addPerson(h.name, 'Helper', calcPay(h), h.commission||0, h.workerType||'1099')); — }); — const rows = [['Name','Role','Worker Type','Jobs','Base Pay','Commission','Total Gross','Est Tax','Est Take-Home']]; — Object.values(payMap).forEach(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — rows.push([p.name, p.role, p.workerType, p.jobs, p.gross.toFixed(2), p.commission.toFixed(2), — gross.toFixed(2), tax.totalEstTax.toFixed(2), tax.takeHome.toFixed(2)]); — }); — downloadCSV(rows, 'jpgsystems-payroll-' + getTodayStr() + '.csv'); — notify('Payroll CSV exported ✓'); — } — function exportJobsCSV() { — const rows = [['Job ID','Client','Date','Time','Driver','Type','Status','Amount','Gates Passed','Origin','Destination']]; — STATE.jobs.forEach(j => { — const gates = Object.values(j.gates||{}).filter(Boolean).length; — rows.push([j.id, j.client, j.date, j.time||'', j.driver||'', j.type||'', — j.status, (j.amount||0).toFixed(2), gates + '/' + Object.keys(j.gates||{}).length, — j.origin||'', j.dest||'']); — }); — downloadCSV(rows, 'jpgsystems-jobs-' + getTodayStr() + '.csv'); — notify('Jobs CSV exported ✓'); — } — function exportReceiptsCSV() { — const rows = [['Receipt ID','Job ID','Driver','Date','Type','Amount','Status','Notes']]; — STATE.receipts.forEach(r => { — rows.push([r.id, r.jobId, r.driver||'', r.date||'', r.type||'', — (r.amount||0).toFixed(2), r.approved ? 'APPROVED' : r.rejected ? 'REJECTED' : 'PENDING', r.notes||'']); — }); — downloadCSV(rows, 'jpgsystems-receipts-' + getTodayStr() + '.csv'); — notify('Receipts CSV exported ✓'); — } — function downloadCSV(rows, filename) { — const csv  = rows.map(r => r.map(c => '"' + String(c).replace(/"/g,'""') + '"').join(',')).join('\n'); — const blob = new Blob([csv], { type: 'text/csv' }); — const url  = URL.createObjectURL(blob); — const a    = document.createElement('a'); — a.href     = url; — a.download = filename; — a.click(); — URL.revokeObjectURL(url); — } — Add export buttons to financials tab — function renderExportBar() { — return ` — <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;align-self:center">EXPORT:</div> — <button class="btn-sm" onclick="exportJobsCSV()">⬇ Jobs CSV</button> — <button class="btn-sm" onclick="exportReceiptsCSV()">⬇ Receipts CSV</button> — <button class="btn-sm" onclick="exportPayrollCSV()">⬇ Payroll CSV</button> — <button class="btn-sm" onclick="renderPayrollSummary()">💵 Payroll Report</button> — </div>`; — } | TABS
| WAREHOUSE WORK LOGGING — Log warehouse work by employee — shows on calendar + payroll | 2934-3145 | 7 | FIREBASE INIT, FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill, MODAL HELPERS, NOTIFICATION, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE | WEEK VIEW — time-based layout like Google Calendar
| MIGRATION — Stamp companyId on any legacy docs missing it — Runs silently on every login — safe to run repeatedly | 6076-6121 | 7 | FIREBASE INIT, FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill, LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, TABS | STATE & STORAGE
| FINANCIALS | 7772-7840 | 7 | JOB DETAIL MODAL, NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, PAYROLL CALCULATION ENGINE v2 — pay + commission + tax, STATE & STORAGE, function exportPayrollCSV() { — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — const addPerson = (name, role, gross, commission, workerType) => { — if (!name) return; — if (!payMap[name]) payMap[name] = { name, role, jobs: 0, gross: 0, commission: 0, workerType }; — payMap[name].jobs++; — payMap[name].gross      += gross; — payMap[name].commission += commission; — }; — addPerson(j.driver, 'Driver', j.payroll.driver||0, j.payroll.driverCommission||0, j.payroll.driverWorkerType||'1099'); — (j.payroll.helpers||[]).forEach(h => addPerson(h.name, 'Helper', calcPay(h), h.commission||0, h.workerType||'1099')); — }); — const rows = [['Name','Role','Worker Type','Jobs','Base Pay','Commission','Total Gross','Est Tax','Est Take-Home']]; — Object.values(payMap).forEach(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — rows.push([p.name, p.role, p.workerType, p.jobs, p.gross.toFixed(2), p.commission.toFixed(2), — gross.toFixed(2), tax.totalEstTax.toFixed(2), tax.takeHome.toFixed(2)]); — }); — downloadCSV(rows, 'jpgsystems-payroll-' + getTodayStr() + '.csv'); — notify('Payroll CSV exported ✓'); — } — function exportJobsCSV() { — const rows = [['Job ID','Client','Date','Time','Driver','Type','Status','Amount','Gates Passed','Origin','Destination']]; — STATE.jobs.forEach(j => { — const gates = Object.values(j.gates||{}).filter(Boolean).length; — rows.push([j.id, j.client, j.date, j.time||'', j.driver||'', j.type||'', — j.status, (j.amount||0).toFixed(2), gates + '/' + Object.keys(j.gates||{}).length, — j.origin||'', j.dest||'']); — }); — downloadCSV(rows, 'jpgsystems-jobs-' + getTodayStr() + '.csv'); — notify('Jobs CSV exported ✓'); — } — function exportReceiptsCSV() { — const rows = [['Receipt ID','Job ID','Driver','Date','Type','Amount','Status','Notes']]; — STATE.receipts.forEach(r => { — rows.push([r.id, r.jobId, r.driver||'', r.date||'', r.type||'', — (r.amount||0).toFixed(2), r.approved ? 'APPROVED' : r.rejected ? 'REJECTED' : 'PENDING', r.notes||'']); — }); — downloadCSV(rows, 'jpgsystems-receipts-' + getTodayStr() + '.csv'); — notify('Receipts CSV exported ✓'); — } — function downloadCSV(rows, filename) { — const csv  = rows.map(r => r.map(c => '"' + String(c).replace(/"/g,'""') + '"').join(',')).join('\n'); — const blob = new Blob([csv], { type: 'text/csv' }); — const url  = URL.createObjectURL(blob); — const a    = document.createElement('a'); — a.href     = url; — a.download = filename; — a.click(); — URL.revokeObjectURL(url); — } — Add export buttons to financials tab — function renderExportBar() { — return ` — <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;align-self:center">EXPORT:</div> — <button class="btn-sm" onclick="exportJobsCSV()">⬇ Jobs CSV</button> — <button class="btn-sm" onclick="exportReceiptsCSV()">⬇ Receipts CSV</button> — <button class="btn-sm" onclick="exportPayrollCSV()">⬇ Payroll CSV</button> — <button class="btn-sm" onclick="renderPayrollSummary()">💵 Payroll Report</button> — </div>`; — }, function renderPayrollSummary() { — This is the office-facing payroll output — all crew, all jobs — const content = document.getElementById('main-content'); — const role    = currentUser.role; — if (role !== 'office') { switchTab('mypay'); return; } — Aggregate per person — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — Driver — const dKey = j.driver; — if (!payMap[dKey]) payMap[dKey] = { name: dKey, role: 'Driver', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[dKey].jobs++; — payMap[dKey].gross      += j.payroll.driver || 0; — payMap[dKey].commission += j.payroll.driverCommission || 0; — payMap[dKey].workerType  = j.payroll.driverWorkerType || '1099'; — Helpers — (j.payroll.helpers||[]).forEach(h => { — if (!h.name) return; — const hKey = h.name; — if (!payMap[hKey]) payMap[hKey] = { name: hKey, role: 'Helper', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[hKey].jobs++; — payMap[hKey].gross      += calcPay(h); — payMap[hKey].commission += h.commission || 0; — payMap[hKey].workerType  = h.workerType || '1099'; — }); — }); — const people    = Object.values(payMap).sort((a,b) => b.gross - a.gross); — const totalGross = people.reduce((s,p) => s + p.gross + p.commission, 0); — let html = ` — <div class="section-header"> — <div> — <div class="section-title">💵 PAYROLL SUMMARY</div> — <div class="section-sub">All crew · All jobs · JPG Systems</div> — </div> — <div style="display:flex;gap:8px"> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportPayrollCSV()">⬇ EXPORT CSV</button> — </div> — </div> — <div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Total Crew</div><div class="stat-value">${people.length}</div></div> — <div class="stat-box"><div class="stat-label">Total Payroll</div><div class="stat-value amber">$${totalGross.toFixed(2)}</div></div> — <div class="stat-box"><div class="stat-label">Jobs w/ Payroll</div><div class="stat-value">${STATE.jobs.filter(j=>j.payroll).length}</div></div> — </div> — <div class="card"> — <table> — <thead><tr> — <th>Name</th><th>Role</th><th>Type</th><th>Jobs</th> — <th style="text-align:right">Base Pay</th> — <th style="text-align:right">Commission</th> — <th style="text-align:right">Total Gross</th> — <th style="text-align:right">Est. Tax</th> — <th style="text-align:right">Take-Home</th> — </tr></thead> — <tbody> — ${people.map(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — return `<tr> — <td style="font-weight:600">${p.name}</td> — <td><span class="tag">${p.role}</span></td> — <td><span class="tag">${p.workerType}</span></td> — <td style="font-family:var(--font-mono);text-align:center">${p.jobs}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${p.gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">${p.commission>0?'$'+p.commission.toFixed(2):'—'}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green);font-weight:700">$${gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${tax.totalEstTax.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${tax.takeHome.toFixed(2)}</td> — </tr>`; — }).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="4" style="font-family:var(--font-mono);color:var(--green)">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${people.reduce((s,p)=>s+p.gross,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">$${people.reduce((s,p)=>s+p.commission,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${totalGross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).totalEstTax,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).takeHome,0).toFixed(2)}</td> — </tr> — </tbody> — </table> — </div>`; — content.innerHTML = html; — } | TABS
| CREW LOG HELPERS — extracted to avoid nested template literal — Chrome syntax errors from 4+ levels of backtick nesting | 7846-8175 | 7 | NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, PAYROLL CALCULATION ENGINE v2 — pay + commission + tax, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | TABS, TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate
| CLIENT MOVE PORTAL — Full transparency experience — This is what the client pays $100-150 for — Real-time gate tracking, item location, messaging, docs | 8534-8933 | 7 | BOL TAB, MODAL HELPERS, PHOTO VIEWER, SIGNATURE MODAL, STATE & STORAGE, TABS | DASHBOARD
| MESSAGING | 14210-14432 | 7 | FIREBASE INIT, NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, NOTIFICATION, NOTIFICATION ENGINE, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE | TABS
| function renderPayrollSummary() { — This is the office-facing payroll output — all crew, all jobs — const content = document.getElementById('main-content'); — const role    = currentUser.role; — if (role !== 'office') { switchTab('mypay'); return; } — Aggregate per person — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — Driver — const dKey = j.driver; — if (!payMap[dKey]) payMap[dKey] = { name: dKey, role: 'Driver', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[dKey].jobs++; — payMap[dKey].gross      += j.payroll.driver || 0; — payMap[dKey].commission += j.payroll.driverCommission || 0; — payMap[dKey].workerType  = j.payroll.driverWorkerType || '1099'; — Helpers — (j.payroll.helpers||[]).forEach(h => { — if (!h.name) return; — const hKey = h.name; — if (!payMap[hKey]) payMap[hKey] = { name: hKey, role: 'Helper', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[hKey].jobs++; — payMap[hKey].gross      += calcPay(h); — payMap[hKey].commission += h.commission || 0; — payMap[hKey].workerType  = h.workerType || '1099'; — }); — }); — const people    = Object.values(payMap).sort((a,b) => b.gross - a.gross); — const totalGross = people.reduce((s,p) => s + p.gross + p.commission, 0); — let html = ` — <div class="section-header"> — <div> — <div class="section-title">💵 PAYROLL SUMMARY</div> — <div class="section-sub">All crew · All jobs · JPG Systems</div> — </div> — <div style="display:flex;gap:8px"> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportPayrollCSV()">⬇ EXPORT CSV</button> — </div> — </div> — <div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Total Crew</div><div class="stat-value">${people.length}</div></div> — <div class="stat-box"><div class="stat-label">Total Payroll</div><div class="stat-value amber">$${totalGross.toFixed(2)}</div></div> — <div class="stat-box"><div class="stat-label">Jobs w/ Payroll</div><div class="stat-value">${STATE.jobs.filter(j=>j.payroll).length}</div></div> — </div> — <div class="card"> — <table> — <thead><tr> — <th>Name</th><th>Role</th><th>Type</th><th>Jobs</th> — <th style="text-align:right">Base Pay</th> — <th style="text-align:right">Commission</th> — <th style="text-align:right">Total Gross</th> — <th style="text-align:right">Est. Tax</th> — <th style="text-align:right">Take-Home</th> — </tr></thead> — <tbody> — ${people.map(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — return `<tr> — <td style="font-weight:600">${p.name}</td> — <td><span class="tag">${p.role}</span></td> — <td><span class="tag">${p.workerType}</span></td> — <td style="font-family:var(--font-mono);text-align:center">${p.jobs}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${p.gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">${p.commission>0?'$'+p.commission.toFixed(2):'—'}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green);font-weight:700">$${gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${tax.totalEstTax.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${tax.takeHome.toFixed(2)}</td> — </tr>`; — }).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="4" style="font-family:var(--font-mono);color:var(--green)">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${people.reduce((s,p)=>s+p.gross,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">$${people.reduce((s,p)=>s+p.commission,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${totalGross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).totalEstTax,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).takeHome,0).toFixed(2)}</td> — </tr> — </tbody> — </table> — </div>`; — content.innerHTML = html; — } | 17392-17485 | 7 | PAYROLL CALCULATION ENGINE v2 — pay + commission + tax, STATE & STORAGE, TABS, TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate, function exportPayrollCSV() { — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — const addPerson = (name, role, gross, commission, workerType) => { — if (!name) return; — if (!payMap[name]) payMap[name] = { name, role, jobs: 0, gross: 0, commission: 0, workerType }; — payMap[name].jobs++; — payMap[name].gross      += gross; — payMap[name].commission += commission; — }; — addPerson(j.driver, 'Driver', j.payroll.driver||0, j.payroll.driverCommission||0, j.payroll.driverWorkerType||'1099'); — (j.payroll.helpers||[]).forEach(h => addPerson(h.name, 'Helper', calcPay(h), h.commission||0, h.workerType||'1099')); — }); — const rows = [['Name','Role','Worker Type','Jobs','Base Pay','Commission','Total Gross','Est Tax','Est Take-Home']]; — Object.values(payMap).forEach(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — rows.push([p.name, p.role, p.workerType, p.jobs, p.gross.toFixed(2), p.commission.toFixed(2), — gross.toFixed(2), tax.totalEstTax.toFixed(2), tax.takeHome.toFixed(2)]); — }); — downloadCSV(rows, 'jpgsystems-payroll-' + getTodayStr() + '.csv'); — notify('Payroll CSV exported ✓'); — } — function exportJobsCSV() { — const rows = [['Job ID','Client','Date','Time','Driver','Type','Status','Amount','Gates Passed','Origin','Destination']]; — STATE.jobs.forEach(j => { — const gates = Object.values(j.gates||{}).filter(Boolean).length; — rows.push([j.id, j.client, j.date, j.time||'', j.driver||'', j.type||'', — j.status, (j.amount||0).toFixed(2), gates + '/' + Object.keys(j.gates||{}).length, — j.origin||'', j.dest||'']); — }); — downloadCSV(rows, 'jpgsystems-jobs-' + getTodayStr() + '.csv'); — notify('Jobs CSV exported ✓'); — } — function exportReceiptsCSV() { — const rows = [['Receipt ID','Job ID','Driver','Date','Type','Amount','Status','Notes']]; — STATE.receipts.forEach(r => { — rows.push([r.id, r.jobId, r.driver||'', r.date||'', r.type||'', — (r.amount||0).toFixed(2), r.approved ? 'APPROVED' : r.rejected ? 'REJECTED' : 'PENDING', r.notes||'']); — }); — downloadCSV(rows, 'jpgsystems-receipts-' + getTodayStr() + '.csv'); — notify('Receipts CSV exported ✓'); — } — function downloadCSV(rows, filename) { — const csv  = rows.map(r => r.map(c => '"' + String(c).replace(/"/g,'""') + '"').join(',')).join('\n'); — const blob = new Blob([csv], { type: 'text/csv' }); — const url  = URL.createObjectURL(blob); — const a    = document.createElement('a'); — a.href     = url; — a.download = filename; — a.click(); — URL.revokeObjectURL(url); — } — Add export buttons to financials tab — function renderExportBar() { — return ` — <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;align-self:center">EXPORT:</div> — <button class="btn-sm" onclick="exportJobsCSV()">⬇ Jobs CSV</button> — <button class="btn-sm" onclick="exportReceiptsCSV()">⬇ Receipts CSV</button> — <button class="btn-sm" onclick="exportPayrollCSV()">⬇ Payroll CSV</button> — <button class="btn-sm" onclick="renderPayrollSummary()">💵 Payroll Report</button> — </div>`; — } | FINANCIALS, function exportPayrollCSV() { — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — const addPerson = (name, role, gross, commission, workerType) => { — if (!name) return; — if (!payMap[name]) payMap[name] = { name, role, jobs: 0, gross: 0, commission: 0, workerType }; — payMap[name].jobs++; — payMap[name].gross      += gross; — payMap[name].commission += commission; — }; — addPerson(j.driver, 'Driver', j.payroll.driver||0, j.payroll.driverCommission||0, j.payroll.driverWorkerType||'1099'); — (j.payroll.helpers||[]).forEach(h => addPerson(h.name, 'Helper', calcPay(h), h.commission||0, h.workerType||'1099')); — }); — const rows = [['Name','Role','Worker Type','Jobs','Base Pay','Commission','Total Gross','Est Tax','Est Take-Home']]; — Object.values(payMap).forEach(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — rows.push([p.name, p.role, p.workerType, p.jobs, p.gross.toFixed(2), p.commission.toFixed(2), — gross.toFixed(2), tax.totalEstTax.toFixed(2), tax.takeHome.toFixed(2)]); — }); — downloadCSV(rows, 'jpgsystems-payroll-' + getTodayStr() + '.csv'); — notify('Payroll CSV exported ✓'); — } — function exportJobsCSV() { — const rows = [['Job ID','Client','Date','Time','Driver','Type','Status','Amount','Gates Passed','Origin','Destination']]; — STATE.jobs.forEach(j => { — const gates = Object.values(j.gates||{}).filter(Boolean).length; — rows.push([j.id, j.client, j.date, j.time||'', j.driver||'', j.type||'', — j.status, (j.amount||0).toFixed(2), gates + '/' + Object.keys(j.gates||{}).length, — j.origin||'', j.dest||'']); — }); — downloadCSV(rows, 'jpgsystems-jobs-' + getTodayStr() + '.csv'); — notify('Jobs CSV exported ✓'); — } — function exportReceiptsCSV() { — const rows = [['Receipt ID','Job ID','Driver','Date','Type','Amount','Status','Notes']]; — STATE.receipts.forEach(r => { — rows.push([r.id, r.jobId, r.driver||'', r.date||'', r.type||'', — (r.amount||0).toFixed(2), r.approved ? 'APPROVED' : r.rejected ? 'REJECTED' : 'PENDING', r.notes||'']); — }); — downloadCSV(rows, 'jpgsystems-receipts-' + getTodayStr() + '.csv'); — notify('Receipts CSV exported ✓'); — } — function downloadCSV(rows, filename) { — const csv  = rows.map(r => r.map(c => '"' + String(c).replace(/"/g,'""') + '"').join(',')).join('\n'); — const blob = new Blob([csv], { type: 'text/csv' }); — const url  = URL.createObjectURL(blob); — const a    = document.createElement('a'); — a.href     = url; — a.download = filename; — a.click(); — URL.revokeObjectURL(url); — } — Add export buttons to financials tab — function renderExportBar() { — return ` — <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;align-self:center">EXPORT:</div> — <button class="btn-sm" onclick="exportJobsCSV()">⬇ Jobs CSV</button> — <button class="btn-sm" onclick="exportReceiptsCSV()">⬇ Receipts CSV</button> — <button class="btn-sm" onclick="exportPayrollCSV()">⬇ Payroll CSV</button> — <button class="btn-sm" onclick="renderPayrollSummary()">💵 Payroll Report</button> — </div>`; — }
| USER MANAGEMENT (Office only) | 19647-19966 | 7 | FIREBASE INIT, FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill, MODAL HELPERS, NOTIFICATION, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE | TABS
| SYSTEM HEALTH DASHBOARD — Platform operator view — JPG Ventures sees all licensees, sync status, revenue flow — Accessible only to creator/owner accounts | 4804-5288 | 8 | 1099 / W2 YEAR-END TAX EXPORT — Aggregates all payments per contractor/employee for the year — Generates: on-screen summary + CSV export + PDF report, FIREBASE INIT, FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill, MODAL HELPERS, NOTIFICATION, TRIAL + TIER SYSTEM | TABS, TRIAL + TIER SYSTEM
| COMPANY BRANDING ENGINE | 12817-12853 | 8 | FIREBASE INIT, NOTIFICATION, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE | ROLE SWITCHER — instant role switch without logout, ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard, STATE & STORAGE, STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live
| MONTH VIEW — Google Calendar style with multi-day span bars | 13657-13901 | 8 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore, CALENDAR, NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, PAYROLL CALCULATION ENGINE v2 — pay + commission + tax, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, WEEK VIEW — time-based layout like Google Calendar | CALENDAR
| WAREHOUSE | 14433-14725 | 8 | FIREBASE INIT, JOB DETAIL MODAL, NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, NOTIFICATION, NOTIFICATION ENGINE, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE | TABS
| TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 3146-3994 | 9 | FIREBASE INIT, FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill, MODAL HELPERS, NOTIFICATION, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, TABS | EDIT JOB  ← NEW FEATURE, TABS
| FIRST RUN CHECK — runs on page load before login | 5845-6029 | 9 | FIREBASE INIT, FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill, LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool, NOTIFICATION, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, SESSION PERSISTENCE — remember me across refreshes/tab closes, STATE & STORAGE, TABS | SESSION PERSISTENCE — remember me across refreshes/tab closes
| 1099 / W2 YEAR-END TAX EXPORT — Aggregates all payments per contractor/employee for the year — Generates: on-screen summary + CSV export + PDF report | 8934-9520 | 9 | FIREBASE INIT, NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, NOTIFICATION, PAYROLL CALCULATION ENGINE v2 — pay + commission + tax, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE | SETTINGS TAB, SYSTEM HEALTH DASHBOARD — Platform operator view — JPG Ventures sees all licensees, sync status, revenue flow — Accessible only to creator/owner accounts, TABS
| SOUND ENGINE — Web Audio API, no external files needed | 12915-13194 | 9 | FIREBASE INIT, JOB DETAIL MODAL, NOTIFICATION, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, TABS, USER SETTINGS — stored in localStorage per user | SETTINGS TAB, STATE & STORAGE
| TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 13243-13509 | 9 | CREW LOG HELPERS — extracted to avoid nested template literal — Chrome syntax errors from 4+ levels of backtick nesting, NOTIFICATION, PAYROLL CALCULATION ENGINE v2 — pay + commission + tax, STATE & STORAGE | CREW LOG HELPERS — extracted to avoid nested template literal — Chrome syntax errors from 4+ levels of backtick nesting, EDIT JOB  ← NEW FEATURE, function exportPayrollCSV() { — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — const addPerson = (name, role, gross, commission, workerType) => { — if (!name) return; — if (!payMap[name]) payMap[name] = { name, role, jobs: 0, gross: 0, commission: 0, workerType }; — payMap[name].jobs++; — payMap[name].gross      += gross; — payMap[name].commission += commission; — }; — addPerson(j.driver, 'Driver', j.payroll.driver||0, j.payroll.driverCommission||0, j.payroll.driverWorkerType||'1099'); — (j.payroll.helpers||[]).forEach(h => addPerson(h.name, 'Helper', calcPay(h), h.commission||0, h.workerType||'1099')); — }); — const rows = [['Name','Role','Worker Type','Jobs','Base Pay','Commission','Total Gross','Est Tax','Est Take-Home']]; — Object.values(payMap).forEach(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — rows.push([p.name, p.role, p.workerType, p.jobs, p.gross.toFixed(2), p.commission.toFixed(2), — gross.toFixed(2), tax.totalEstTax.toFixed(2), tax.takeHome.toFixed(2)]); — }); — downloadCSV(rows, 'jpgsystems-payroll-' + getTodayStr() + '.csv'); — notify('Payroll CSV exported ✓'); — } — function exportJobsCSV() { — const rows = [['Job ID','Client','Date','Time','Driver','Type','Status','Amount','Gates Passed','Origin','Destination']]; — STATE.jobs.forEach(j => { — const gates = Object.values(j.gates||{}).filter(Boolean).length; — rows.push([j.id, j.client, j.date, j.time||'', j.driver||'', j.type||'', — j.status, (j.amount||0).toFixed(2), gates + '/' + Object.keys(j.gates||{}).length, — j.origin||'', j.dest||'']); — }); — downloadCSV(rows, 'jpgsystems-jobs-' + getTodayStr() + '.csv'); — notify('Jobs CSV exported ✓'); — } — function exportReceiptsCSV() { — const rows = [['Receipt ID','Job ID','Driver','Date','Type','Amount','Status','Notes']]; — STATE.receipts.forEach(r => { — rows.push([r.id, r.jobId, r.driver||'', r.date||'', r.type||'', — (r.amount||0).toFixed(2), r.approved ? 'APPROVED' : r.rejected ? 'REJECTED' : 'PENDING', r.notes||'']); — }); — downloadCSV(rows, 'jpgsystems-receipts-' + getTodayStr() + '.csv'); — notify('Receipts CSV exported ✓'); — } — function downloadCSV(rows, filename) { — const csv  = rows.map(r => r.map(c => '"' + String(c).replace(/"/g,'""') + '"').join(',')).join('\n'); — const blob = new Blob([csv], { type: 'text/csv' }); — const url  = URL.createObjectURL(blob); — const a    = document.createElement('a'); — a.href     = url; — a.download = filename; — a.click(); — URL.revokeObjectURL(url); — } — Add export buttons to financials tab — function renderExportBar() { — return ` — <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;align-self:center">EXPORT:</div> — <button class="btn-sm" onclick="exportJobsCSV()">⬇ Jobs CSV</button> — <button class="btn-sm" onclick="exportReceiptsCSV()">⬇ Receipts CSV</button> — <button class="btn-sm" onclick="exportPayrollCSV()">⬇ Payroll CSV</button> — <button class="btn-sm" onclick="renderPayrollSummary()">💵 Payroll Report</button> — </div>`; — }, function renderMyPay() { — const content  = document.getElementById('main-content'); — const role     = currentUser.role; — const isHelper = role === 'helper'; — Collect all pay entries for this person — const myJobs = STATE.jobs.filter(j => { — if (role === 'driver') return nameIncludes(j.driver, currentUser.name); — if (role === 'helper') return (j.payroll?.helpers||[]).some(h => nameIncludes(h.name, currentUser.name)); — return false; — }); — let totalGross      = 0; — let totalCommission = 0; — let totalTax        = 0; — let totalTakeHome   = 0; — const payRows       = []; — myJobs.forEach(j => { — let gross = 0, commission = 0, workerType = '1099', payDetails = ''; — if (role === 'driver') { — gross       = j.payroll?.driver || 0; — commission  = j.payroll?.driverCommission || 0; — workerType  = j.payroll?.driverWorkerType || '1099'; — payDetails  = payLabel({ payType: j.payroll?.driverPayType||'flat', rate: j.payroll?.driverRate||gross, hours: j.payroll?.driverHours||0, days: j.payroll?.driverDays||1 }); — } else { — const helper = (j.payroll?.helpers||[]).find(h => — h.name?.toLowerCase().includes(currentUser.name.toLowerCase()) || — (currentUser.name.toLowerCase().split(' ')[0] && h.name?.toLowerCase().includes(currentUser.name.toLowerCase().split(' ')[0])) — ); — if (helper) { — gross      = calcPay(helper); — commission = helper.commission || 0; — workerType = helper.workerType || '1099'; — payDetails = payLabel(helper); — } — } — if (gross === 0) return; — const tax = calcTax(gross, workerType); — totalGross      += gross; — totalCommission += commission; — totalTax        += tax.totalEstTax; — totalTakeHome   += tax.takeHome; — payRows.push({ job: j, gross, commission, workerType, payDetails, tax }); — }); — let html = ` — <div class="section-header"> — <div> — <div class="section-title">📊 MY PAY SUMMARY</div> — <div class="section-sub">${currentUser.name} · ${role.toUpperCase()} · ${payRows.length} job(s)</div> — </div> — </div> — <!-- YTD SUMMARY STATS --> — <div class="grid-4" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Gross Earned</div> — <div class="stat-value">$${totalGross.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Commission</div> — <div class="stat-value" style="color:var(--amber)">$${totalCommission.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Est. Tax Owed</div> — <div class="stat-value red">$${totalTax.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Est. Take-Home</div> — <div class="stat-value">$${totalTakeHome.toFixed(2)}</div> — </div> — </div> — <!-- TAX ADVISORY BANNER --> — <div class="card" style="border-color:#664400;background:rgba(255,170,0,0.05);margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:11px;color:var(--amber);letter-spacing:2px;margin-bottom:8px">⚠ TAX ADVISORY</div> — <div class="info-text" style="line-height:1.8"> — ${totalGross > 0 ? `Based on your earnings of <strong style="color:var(--white)">$${totalGross.toFixed(2)}</strong>, you should set aside approximately <strong style="color:var(--red)">$${totalTax.toFixed(2)}</strong> (${(totalTax/totalGross*100).toFixed(1)}%) for taxes.` : 'No earnings recorded yet.'} — ${totalGross >= 600 ? ' You will likely receive a 1099 form if classified as a contractor.' : ''} — If you are a <strong style="color:var(--white)">1099 contractor</strong>, you are responsible for paying your own taxes quarterly (April, June, September, January). Consider setting aside <strong style="color:var(--red)">${totalGross > 0 ? (totalTax/totalGross*100).toFixed(0) : '~25'}%</strong> of every paycheck. — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div style="margin-top:4px"> — ${payRows.length ? payRows.map(row => ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:10px"> — <div> — <div style="display:flex;gap:8px;align-items:center;margin-bottom:4px"> — <span class="tag">${row.job.id}</span> — <span class="status-badge status-${row.job.status}">${row.job.status}</span> — <span class="tag">${row.workerType}</span> — </div> — <div style="font-size:14px;font-weight:600">${row.job.client}</div> — <div class="info-text">${row.job.date} · ${row.payDetails}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:20px;color:var(--green)">$${row.gross.toFixed(2)}</div> — ${row.commission > 0 ? `<div style="font-family:var(--font-mono);font-size:12px;color:var(--amber)">+ $${row.commission.toFixed(2)} commission</div>` : ''} — </div> — </div> — <!-- Tax breakdown per job --> — <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;margin-bottom:8px"> — <div class="stat-box" style="padding:10px"> — <div class="stat-label" style="font-size:9px">Gross Pay</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--white)">$${row.gross.toFixed(2)}</div> — </div> — <div class="stat-box" style="padding:10px"> — <div class="stat-label" style="font-size:9px">Est. Tax</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--red)">−$${row.tax.totalEstTax.toFixed(2)}</div> — </div> — <div class="stat-box" style="padding:10px;border-color:var(--green3)"> — <div class="stat-label" style="font-size:9px">Take-Home</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--green)">$${row.tax.takeHome.toFixed(2)}</div> — </div> — </div> — <!-- Detailed tax line items --> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;font-family:var(--font-mono);font-size:10px"> — ${row.tax.selfEmployTax ? `<div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>Self-Employment Tax (15.3%)</span><span style="color:var(--red)">−$${row.tax.selfEmployTax.toFixed(2)}</span></div>` : ''} — ${row.tax.ficaTax ? `<div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>FICA Employee (7.65%)</span><span style="color:var(--red)">−$${row.tax.ficaTax.toFixed(2)}</span></div>` : ''} — <div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>Federal Income (~${(row.tax.federalRate*100).toFixed(0)}%)</span><span style="color:var(--red)">−$${row.tax.federalTax.toFixed(2)}</span></div> — <div style="display:flex;justify-content:space-between;color:var(--gray)"><span>Georgia State (5.5%)</span><span style="color:var(--red)">−$${row.tax.stateTax.toFixed(2)}</span></div> — </div> — </div>`).join('') : ` — <div class="card" style="text-align:center;padding:40px"> — <p class="info-text">No pay records found for ${currentUser.name}.</p> — <p class="info-text" style="margin-top:8px">Your earnings will appear here once jobs are assigned and crew logs are saved.</p> — </div>`} — </div> — <div style="padding:12px;font-family:var(--font-mono);font-size:9px;color:var(--gray);line-height:1.8;border-top:1px solid var(--gray2);margin-top:8px"> — ⚠ DISCLAIMER: Tax estimates are for informational purposes only based on 2024 US federal rates and Georgia state rates. This is not tax advice. Individual circumstances vary. Consult a licensed tax professional or CPA for filing guidance. JPG Systems / JPG Ventures LLC is not responsible for tax obligations of independent contractors. — </div>`; — content.innerHTML = html; — }, function renderPayrollSummary() { — This is the office-facing payroll output — all crew, all jobs — const content = document.getElementById('main-content'); — const role    = currentUser.role; — if (role !== 'office') { switchTab('mypay'); return; } — Aggregate per person — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — Driver — const dKey = j.driver; — if (!payMap[dKey]) payMap[dKey] = { name: dKey, role: 'Driver', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[dKey].jobs++; — payMap[dKey].gross      += j.payroll.driver || 0; — payMap[dKey].commission += j.payroll.driverCommission || 0; — payMap[dKey].workerType  = j.payroll.driverWorkerType || '1099'; — Helpers — (j.payroll.helpers||[]).forEach(h => { — if (!h.name) return; — const hKey = h.name; — if (!payMap[hKey]) payMap[hKey] = { name: hKey, role: 'Helper', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[hKey].jobs++; — payMap[hKey].gross      += calcPay(h); — payMap[hKey].commission += h.commission || 0; — payMap[hKey].workerType  = h.workerType || '1099'; — }); — }); — const people    = Object.values(payMap).sort((a,b) => b.gross - a.gross); — const totalGross = people.reduce((s,p) => s + p.gross + p.commission, 0); — let html = ` — <div class="section-header"> — <div> — <div class="section-title">💵 PAYROLL SUMMARY</div> — <div class="section-sub">All crew · All jobs · JPG Systems</div> — </div> — <div style="display:flex;gap:8px"> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportPayrollCSV()">⬇ EXPORT CSV</button> — </div> — </div> — <div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Total Crew</div><div class="stat-value">${people.length}</div></div> — <div class="stat-box"><div class="stat-label">Total Payroll</div><div class="stat-value amber">$${totalGross.toFixed(2)}</div></div> — <div class="stat-box"><div class="stat-label">Jobs w/ Payroll</div><div class="stat-value">${STATE.jobs.filter(j=>j.payroll).length}</div></div> — </div> — <div class="card"> — <table> — <thead><tr> — <th>Name</th><th>Role</th><th>Type</th><th>Jobs</th> — <th style="text-align:right">Base Pay</th> — <th style="text-align:right">Commission</th> — <th style="text-align:right">Total Gross</th> — <th style="text-align:right">Est. Tax</th> — <th style="text-align:right">Take-Home</th> — </tr></thead> — <tbody> — ${people.map(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — return `<tr> — <td style="font-weight:600">${p.name}</td> — <td><span class="tag">${p.role}</span></td> — <td><span class="tag">${p.workerType}</span></td> — <td style="font-family:var(--font-mono);text-align:center">${p.jobs}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${p.gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">${p.commission>0?'$'+p.commission.toFixed(2):'—'}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green);font-weight:700">$${gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${tax.totalEstTax.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${tax.takeHome.toFixed(2)}</td> — </tr>`; — }).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="4" style="font-family:var(--font-mono);color:var(--green)">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${people.reduce((s,p)=>s+p.gross,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">$${people.reduce((s,p)=>s+p.commission,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${totalGross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).totalEstTax,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).takeHome,0).toFixed(2)}</td> — </tr> — </tbody> — </table> — </div>`; — content.innerHTML = html; — }
| BOL TAB | 14726-14957 | 9 | MODAL HELPERS, NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, NOTIFICATION, PORTAL ROUTE CHECK — runs before login screen — Matches Good Friends Movers LLC document package exactly — US DOT# 3921653 / MC# 1450729 — Structure: Interstate BOL | Legal Terms | Inventory x2 | Packing | Post Contract | Valuation, SIGNATURE MODAL, STATE & STORAGE | CLIENT MOVE PORTAL — Full transparency experience — This is what the client pays $100-150 for — Real-time gate tracking, item location, messaging, docs, ESTIMATE TAB — Per-job BOL line item editor — This is the source of truth for the BOL PDF, TABS
| function exportPayrollCSV() { — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — const addPerson = (name, role, gross, commission, workerType) => { — if (!name) return; — if (!payMap[name]) payMap[name] = { name, role, jobs: 0, gross: 0, commission: 0, workerType }; — payMap[name].jobs++; — payMap[name].gross      += gross; — payMap[name].commission += commission; — }; — addPerson(j.driver, 'Driver', j.payroll.driver||0, j.payroll.driverCommission||0, j.payroll.driverWorkerType||'1099'); — (j.payroll.helpers||[]).forEach(h => addPerson(h.name, 'Helper', calcPay(h), h.commission||0, h.workerType||'1099')); — }); — const rows = [['Name','Role','Worker Type','Jobs','Base Pay','Commission','Total Gross','Est Tax','Est Take-Home']]; — Object.values(payMap).forEach(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — rows.push([p.name, p.role, p.workerType, p.jobs, p.gross.toFixed(2), p.commission.toFixed(2), — gross.toFixed(2), tax.totalEstTax.toFixed(2), tax.takeHome.toFixed(2)]); — }); — downloadCSV(rows, 'jpgsystems-payroll-' + getTodayStr() + '.csv'); — notify('Payroll CSV exported ✓'); — } — function exportJobsCSV() { — const rows = [['Job ID','Client','Date','Time','Driver','Type','Status','Amount','Gates Passed','Origin','Destination']]; — STATE.jobs.forEach(j => { — const gates = Object.values(j.gates||{}).filter(Boolean).length; — rows.push([j.id, j.client, j.date, j.time||'', j.driver||'', j.type||'', — j.status, (j.amount||0).toFixed(2), gates + '/' + Object.keys(j.gates||{}).length, — j.origin||'', j.dest||'']); — }); — downloadCSV(rows, 'jpgsystems-jobs-' + getTodayStr() + '.csv'); — notify('Jobs CSV exported ✓'); — } — function exportReceiptsCSV() { — const rows = [['Receipt ID','Job ID','Driver','Date','Type','Amount','Status','Notes']]; — STATE.receipts.forEach(r => { — rows.push([r.id, r.jobId, r.driver||'', r.date||'', r.type||'', — (r.amount||0).toFixed(2), r.approved ? 'APPROVED' : r.rejected ? 'REJECTED' : 'PENDING', r.notes||'']); — }); — downloadCSV(rows, 'jpgsystems-receipts-' + getTodayStr() + '.csv'); — notify('Receipts CSV exported ✓'); — } — function downloadCSV(rows, filename) { — const csv  = rows.map(r => r.map(c => '"' + String(c).replace(/"/g,'""') + '"').join(',')).join('\n'); — const blob = new Blob([csv], { type: 'text/csv' }); — const url  = URL.createObjectURL(blob); — const a    = document.createElement('a'); — a.href     = url; — a.download = filename; — a.click(); — URL.revokeObjectURL(url); — } — Add export buttons to financials tab — function renderExportBar() { — return ` — <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;align-self:center">EXPORT:</div> — <button class="btn-sm" onclick="exportJobsCSV()">⬇ Jobs CSV</button> — <button class="btn-sm" onclick="exportReceiptsCSV()">⬇ Receipts CSV</button> — <button class="btn-sm" onclick="exportPayrollCSV()">⬇ Payroll CSV</button> — <button class="btn-sm" onclick="renderPayrollSummary()">💵 Payroll Report</button> — </div>`; — } | 17486-17561 | 9 | NOTIFICATION, PAYROLL CALCULATION ENGINE v2 — pay + commission + tax, STATE & STORAGE, TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate, function renderPayrollSummary() { — This is the office-facing payroll output — all crew, all jobs — const content = document.getElementById('main-content'); — const role    = currentUser.role; — if (role !== 'office') { switchTab('mypay'); return; } — Aggregate per person — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — Driver — const dKey = j.driver; — if (!payMap[dKey]) payMap[dKey] = { name: dKey, role: 'Driver', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[dKey].jobs++; — payMap[dKey].gross      += j.payroll.driver || 0; — payMap[dKey].commission += j.payroll.driverCommission || 0; — payMap[dKey].workerType  = j.payroll.driverWorkerType || '1099'; — Helpers — (j.payroll.helpers||[]).forEach(h => { — if (!h.name) return; — const hKey = h.name; — if (!payMap[hKey]) payMap[hKey] = { name: hKey, role: 'Helper', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[hKey].jobs++; — payMap[hKey].gross      += calcPay(h); — payMap[hKey].commission += h.commission || 0; — payMap[hKey].workerType  = h.workerType || '1099'; — }); — }); — const people    = Object.values(payMap).sort((a,b) => b.gross - a.gross); — const totalGross = people.reduce((s,p) => s + p.gross + p.commission, 0); — let html = ` — <div class="section-header"> — <div> — <div class="section-title">💵 PAYROLL SUMMARY</div> — <div class="section-sub">All crew · All jobs · JPG Systems</div> — </div> — <div style="display:flex;gap:8px"> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportPayrollCSV()">⬇ EXPORT CSV</button> — </div> — </div> — <div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Total Crew</div><div class="stat-value">${people.length}</div></div> — <div class="stat-box"><div class="stat-label">Total Payroll</div><div class="stat-value amber">$${totalGross.toFixed(2)}</div></div> — <div class="stat-box"><div class="stat-label">Jobs w/ Payroll</div><div class="stat-value">${STATE.jobs.filter(j=>j.payroll).length}</div></div> — </div> — <div class="card"> — <table> — <thead><tr> — <th>Name</th><th>Role</th><th>Type</th><th>Jobs</th> — <th style="text-align:right">Base Pay</th> — <th style="text-align:right">Commission</th> — <th style="text-align:right">Total Gross</th> — <th style="text-align:right">Est. Tax</th> — <th style="text-align:right">Take-Home</th> — </tr></thead> — <tbody> — ${people.map(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — return `<tr> — <td style="font-weight:600">${p.name}</td> — <td><span class="tag">${p.role}</span></td> — <td><span class="tag">${p.workerType}</span></td> — <td style="font-family:var(--font-mono);text-align:center">${p.jobs}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${p.gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">${p.commission>0?'$'+p.commission.toFixed(2):'—'}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green);font-weight:700">$${gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${tax.totalEstTax.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${tax.takeHome.toFixed(2)}</td> — </tr>`; — }).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="4" style="font-family:var(--font-mono);color:var(--green)">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${people.reduce((s,p)=>s+p.gross,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">$${people.reduce((s,p)=>s+p.commission,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${totalGross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).totalEstTax,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).takeHome,0).toFixed(2)}</td> — </tr> — </tbody> — </table> — </div>`; — content.innerHTML = html; — } | FINANCIALS, PAYROLL LEDGER — Running balance per crew member — Balance = earned - paid. When paid → resets to 0 → new cycle, function renderMargins() { — const content = document.getElementById('main-content'); — Build margin data per job — const jobMargins = STATE.jobs — .filter(j => j.amount > 0 || j.payroll) — .map(j => { — const revenue   = j.amount || 0; — const driverAccBonus = parseFloat(j.payroll?.accessorialBonus) || 0; — const crewCost  = (j.payroll?.driver || 0) + — driverAccBonus + — (j.payroll?.helpers || []).reduce((s,h) => s + calcPay(h), 0) + — (j.payroll?.driverCommission || 0); — Company revenue excludes driver accessorial bonuses (passed through to driver) — const expenses  = STATE.receipts — .filter(r => r.jobId === j.id && r.approved !== false) — .reduce((s,r) => s + (r.amount||0), 0); — const net       = revenue - crewCost - expenses; — const margin    = revenue > 0 ? (net / revenue * 100) : 0; — return { job: j, revenue, crewCost, expenses, net, margin }; — }) — .sort((a,b) => { — Default: worst margin first (ascending) — const mA = a.revenue > 0 ? a.profit/a.revenue : 0; — const mB = b.revenue > 0 ? b.profit/b.revenue : 0; — return mA - mB; — }); — Sort control — if (window._marginSort === 'date') jobMargins.sort((a,b) => b.job.date > a.job.date ? 1 : -1); — else if (window._marginSort === 'revenue') jobMargins.sort((a,b) => b.revenue - a.revenue); — ── Margin intelligence ── — const overpayJobs   = jobMargins.filter(m => m.revenue > 0 && (m.crewCost/m.revenue) > 0.40); — const lowProfitJobs = jobMargins.filter(m => m.revenue > 0 && (m.profit/m.revenue) < 0.20); — const bestJob       = [...jobMargins].sort((a,b) => (b.profit/b.revenue||0)-(a.profit/a.revenue||0))[0]; — const worstJob      = jobMargins[0]; // already sorted worst first — Overall stats — const totalRevenue  = jobMargins.reduce((s,m) => s + m.revenue, 0); — const totalCrew     = jobMargins.reduce((s,m) => s + m.crewCost, 0); — const totalExpenses = jobMargins.reduce((s,m) => s + m.expenses, 0); — const totalNet      = jobMargins.reduce((s,m) => s + m.net, 0); — const avgMargin     = totalRevenue > 0 ? (totalNet / totalRevenue * 100) : 0; — const profitable    = jobMargins.filter(m => m.net > 0).length; — const losing        = jobMargins.filter(m => m.net < 0).length; — Best and worst job — const best  = jobMargins.reduce((a,b) => b.margin > a.margin ? b : a, jobMargins[0]); — const worst = jobMargins.reduce((a,b) => b.margin < a.margin ? b : a, jobMargins[0]); — const marginColor = (n) => n >= 40 ? 'var(--green)' : n >= 20 ? 'var(--amber)' : n >= 0 ? '#ff8c00' : 'var(--red)'; — const marginBg    = (n) => n >= 40 ? 'rgba(0,255,65,0.08)' : n >= 20 ? 'rgba(255,170,0,0.08)' : n >= 0 ? 'rgba(255,140,0,0.08)' : 'rgba(255,51,51,0.08)'; — let html = ` — ${overpayJobs.length ? `<div style="background:rgba(255,30,30,0.06);border:1px solid rgba(255,30,30,0.3);border-radius:6px;padding:10px 14px;margin-bottom:12px;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px"> — <div> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;font-weight:700">⚠ LABOR OVERPAY ALERT</div> — <div style="font-size:12px;color:var(--white);margin-top:3px">${overpayJobs.length} job${overpayJobs.length>1?'s':''} with crew cost >40% of revenue</div> — <div style="font-size:11px;color:var(--gray);margin-top:2px">${overpayJobs.slice(0,3).map(m=>m.job.client+' ('+Math.round(m.crewCost/m.revenue*100)+'%)').join(' · ')}</div> — </div> — </div>` : ''} — <div style="display:flex;gap:8px;margin-bottom:12px;flex-wrap:wrap"> — <span style="font-family:var(--font-mono);font-size:10px;color:var(--gray);align-self:center">Sort:</span> — ${['worst:⬇ Worst Margin','revenue:$ Revenue','date:📅 Date'].map(o=>{const[v,l]=o.split(':');return `<button onclick="window._marginSort='${v}';renderMargins()" style="padding:5px 12px;font-family:var(--font-mono);font-size:10px;border-radius:3px;cursor:pointer;background:${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--green)':'transparent'};color:${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--navy)':'var(--gray)'};border:1px solid ${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--green)':'var(--gray2)'}">${l}</button>`;}).join('')} — </div> — <div class="section-header"> — <div> — <div class="section-title">📈 JOB MARGIN DASHBOARD</div> — <div class="section-sub">Revenue · Crew Cost · Expenses · Net per Job</div> — </div> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportJobsCSV()">⬇ EXPORT</button> — </div> — <!-- OVERALL STATS --> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(130px,1fr));gap:8px;margin-bottom:16px"> — <div class="stat-box"> — <div class="stat-label">Total Revenue</div> — <div class="stat-value">$${totalRevenue.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${jobMargins.length} jobs</div> — </div> — <div class="stat-box"> — <div class="stat-label">Crew Cost</div> — <div class="stat-value red">$${totalCrew.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${totalRevenue>0?(totalCrew/totalRevenue*100).toFixed(1):'0'}% of revenue</div> — </div> — <div class="stat-box"> — <div class="stat-label">Expenses</div> — <div class="stat-value red">$${totalExpenses.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${totalRevenue>0?(totalExpenses/totalRevenue*100).toFixed(1):'0'}% of revenue</div> — </div> — <div class="stat-box" style="border-color:${marginColor(avgMargin)}"> — <div class="stat-label">Net Profit</div> — <div class="stat-value" style="color:${marginColor(avgMargin)}">$${totalNet.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px;color:${marginColor(avgMargin)}">${avgMargin.toFixed(1)}% avg margin</div> — </div> — <div class="stat-box"> — <div class="stat-label">Profitable Jobs</div> — <div class="stat-value" style="color:var(--green)">${profitable}</div> — <div class="info-text" style="margin-top:4px;color:var(--red)">${losing} losing</div> — </div> — </div> — <!-- MARGIN BREAKDOWN BAR --> — ${totalRevenue > 0 ? ` — <div class="card" style="margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;margin-bottom:10px">REVENUE BREAKDOWN</div> — <div style="height:32px;border-radius:4px;overflow:hidden;display:flex;margin-bottom:8px"> — <div style="width:${(totalCrew/totalRevenue*100).toFixed(1)}%;background:rgba(255,51,51,0.7);transition:width 0.5s" title="Crew Cost"></div> — <div style="width:${(totalExpenses/totalRevenue*100).toFixed(1)}%;background:rgba(255,140,0,0.7);transition:width 0.5s" title="Expenses"></div> — <div style="flex:1;background:rgba(0,255,65,0.4);transition:width 0.5s" title="Net Profit"></div> — </div> — <div style="display:flex;gap:16px;flex-wrap:wrap"> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(255,51,51,0.7);border-radius:2px"></div><span class="info-text">Crew ${(totalCrew/totalRevenue*100).toFixed(1)}%</span></div> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(255,140,0,0.7);border-radius:2px"></div><span class="info-text">Expenses ${(totalExpenses/totalRevenue*100).toFixed(1)}%</span></div> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(0,255,65,0.4);border-radius:2px"></div><span class="info-text">Net ${avgMargin.toFixed(1)}%</span></div> — </div> — </div>` : ''} — <!-- BEST / WORST --> — ${jobMargins.length >= 2 ? ` — <div class="grid-2" style="margin-bottom:16px"> — ${best ? `<div class="card" style="border-color:var(--green3)"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--green);letter-spacing:2px;margin-bottom:6px">🏆 BEST MARGIN JOB</div> — <div style="font-size:15px;font-weight:700">${best.job.client} <span class="tag">${best.job.id}</span></div> — <div class="info-text">${best.job.date}</div> — <div style="font-family:var(--font-mono);font-size:22px;color:var(--green);margin-top:6px">${best.margin.toFixed(1)}%</div> — <div class="info-text">$${best.net.toFixed(2)} net on $${best.revenue.toLocaleString()}</div> — </div>` : ''} — ${worst ? `<div class="card" style="border-color:${marginColor(worst.margin)}"> — <div style="font-family:var(--font-mono);font-size:9px;color:${marginColor(worst.margin)};letter-spacing:2px;margin-bottom:6px">${worst.net < 0 ? '⚠ LOSING JOB' : '📉 LOWEST MARGIN'}</div> — <div style="font-size:15px;font-weight:700">${worst.job.client} <span class="tag">${worst.job.id}</span></div> — <div class="info-text">${worst.job.date}</div> — <div style="font-family:var(--font-mono);font-size:22px;color:${marginColor(worst.margin)};margin-top:6px">${worst.margin.toFixed(1)}%</div> — <div class="info-text">$${worst.net.toFixed(2)} net on $${worst.revenue.toLocaleString()}</div> — </div>` : ''} — </div>` : ''} — <!-- PER-JOB TABLE --> — <div class="card" style="padding:0;overflow:hidden"> — <div style="padding:12px 16px;border-bottom:1px solid var(--gray2)"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--green);letter-spacing:2px">JOB BY JOB BREAKDOWN</div> — </div> — ${jobMargins.length ? ` — <div style="overflow-x:auto"> — <table style="min-width:700px"> — <thead><tr> — <th>Job</th> — <th>Client</th> — <th>Date</th> — <th style="text-align:right">Revenue</th> — <th style="text-align:right">Crew Cost</th> — <th style="text-align:right">Expenses</th> — <th style="text-align:right">Net</th> — <th style="text-align:center">Margin</th> — <th>Status</th> — </tr></thead> — <tbody> — ${jobMargins.map(m => ` — <tr style="cursor:pointer;background:${marginBg(m.margin)}" onclick="switchTab('jobs');setTimeout(()=>openJobDetail('${m.job.id}'),300)"> — <td><span class="tag">${m.job.id}</span></td> — <td style="font-weight:600">${m.job.client}</td> — <td class="info-text">${m.job.date}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${m.revenue.toLocaleString()}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${m.crewCost.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:#ff8c00">$${m.expenses.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;font-weight:700;color:${marginColor(m.margin)}"> — ${m.net >= 0 ? '+' : ''}$${m.net.toFixed(2)} — </td> — <td style="text-align:center"> — <div style="display:inline-flex;align-items:center;justify-content:center;width:52px;height:24px;border-radius:12px;background:${marginBg(m.margin)};border:1px solid ${marginColor(m.margin)};font-family:var(--font-mono);font-size:10px;font-weight:700;color:${marginColor(m.margin)}"> — ${m.margin.toFixed(0)}% — </div> — </td> — <td><span class="status-badge status-${m.job.status}">${m.job.status}</span></td> — </tr>`).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="3" style="font-family:var(--font-mono);color:var(--green);font-size:11px">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${totalRevenue.toLocaleString()}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${totalCrew.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:#ff8c00">$${totalExpenses.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;font-weight:700;color:${marginColor(avgMargin)}"> — ${totalNet >= 0 ? '+' : ''}$${totalNet.toFixed(2)} — </td> — <td style="text-align:center"> — <div style="display:inline-flex;align-items:center;justify-content:center;width:52px;height:24px;border-radius:12px;background:${marginBg(avgMargin)};border:1px solid ${marginColor(avgMargin)};font-family:var(--font-mono);font-size:10px;font-weight:700;color:${marginColor(avgMargin)}"> — ${avgMargin.toFixed(0)}% — </div> — </td> — <td></td> — </tr> — </tbody> — </table> — </div>` : ` — <div style="text-align:center;padding:48px"> — <div style="font-size:40px;margin-bottom:12px">📈</div> — <p class="info-text">No job data yet. Create jobs with quoted amounts and crew logs to see margins.</p> — </div>`} — </div> — <!-- MARGIN LEGEND --> — <div style="display:flex;gap:16px;padding:12px 4px;flex-wrap:wrap;align-items:center"> — <span class="info-text">Margin health:</span> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(0,255,65,0.2);border:1px solid var(--green);border-radius:2px"></div><span class="info-text" style="color:var(--green)">40%+ Excellent</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,170,0,0.2);border:1px solid var(--amber);border-radius:2px"></div><span class="info-text" style="color:var(--amber)">20–39% Good</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,140,0,0.2);border:1px solid #ff8c00;border-radius:2px"></div><span class="info-text" style="color:#ff8c00">0–19% Thin</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,51,51,0.15);border:1px solid var(--red);border-radius:2px"></div><span class="info-text" style="color:var(--red)">Negative — Losing money</span></div> — </div>`; — content.innerHTML = html; — }, function renderPayrollSummary() { — This is the office-facing payroll output — all crew, all jobs — const content = document.getElementById('main-content'); — const role    = currentUser.role; — if (role !== 'office') { switchTab('mypay'); return; } — Aggregate per person — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — Driver — const dKey = j.driver; — if (!payMap[dKey]) payMap[dKey] = { name: dKey, role: 'Driver', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[dKey].jobs++; — payMap[dKey].gross      += j.payroll.driver || 0; — payMap[dKey].commission += j.payroll.driverCommission || 0; — payMap[dKey].workerType  = j.payroll.driverWorkerType || '1099'; — Helpers — (j.payroll.helpers||[]).forEach(h => { — if (!h.name) return; — const hKey = h.name; — if (!payMap[hKey]) payMap[hKey] = { name: hKey, role: 'Helper', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[hKey].jobs++; — payMap[hKey].gross      += calcPay(h); — payMap[hKey].commission += h.commission || 0; — payMap[hKey].workerType  = h.workerType || '1099'; — }); — }); — const people    = Object.values(payMap).sort((a,b) => b.gross - a.gross); — const totalGross = people.reduce((s,p) => s + p.gross + p.commission, 0); — let html = ` — <div class="section-header"> — <div> — <div class="section-title">💵 PAYROLL SUMMARY</div> — <div class="section-sub">All crew · All jobs · JPG Systems</div> — </div> — <div style="display:flex;gap:8px"> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportPayrollCSV()">⬇ EXPORT CSV</button> — </div> — </div> — <div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Total Crew</div><div class="stat-value">${people.length}</div></div> — <div class="stat-box"><div class="stat-label">Total Payroll</div><div class="stat-value amber">$${totalGross.toFixed(2)}</div></div> — <div class="stat-box"><div class="stat-label">Jobs w/ Payroll</div><div class="stat-value">${STATE.jobs.filter(j=>j.payroll).length}</div></div> — </div> — <div class="card"> — <table> — <thead><tr> — <th>Name</th><th>Role</th><th>Type</th><th>Jobs</th> — <th style="text-align:right">Base Pay</th> — <th style="text-align:right">Commission</th> — <th style="text-align:right">Total Gross</th> — <th style="text-align:right">Est. Tax</th> — <th style="text-align:right">Take-Home</th> — </tr></thead> — <tbody> — ${people.map(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — return `<tr> — <td style="font-weight:600">${p.name}</td> — <td><span class="tag">${p.role}</span></td> — <td><span class="tag">${p.workerType}</span></td> — <td style="font-family:var(--font-mono);text-align:center">${p.jobs}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${p.gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">${p.commission>0?'$'+p.commission.toFixed(2):'—'}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green);font-weight:700">$${gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${tax.totalEstTax.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${tax.takeHome.toFixed(2)}</td> — </tr>`; — }).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="4" style="font-family:var(--font-mono);color:var(--green)">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${people.reduce((s,p)=>s+p.gross,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">$${people.reduce((s,p)=>s+p.commission,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${totalGross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).totalEstTax,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).takeHome,0).toFixed(2)}</td> — </tr> — </tbody> — </table> — </div>`; — content.innerHTML = html; — }
| TRIAL + TIER SYSTEM | 20037-20195 | 9 | FIREBASE INIT, NOTIFICATION, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, SYSTEM HEALTH DASHBOARD — Platform operator view — JPG Ventures sees all licensees, sync status, revenue flow — Accessible only to creator/owner accounts | SESSION PERSISTENCE — remember me across refreshes/tab closes, STATE & STORAGE, SYSTEM HEALTH DASHBOARD — Platform operator view — JPG Ventures sees all licensees, sync status, revenue flow — Accessible only to creator/owner accounts, TABS
| BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 2446-2933 | 10 | FIREBASE INIT, FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill, MODAL HELPERS, NOTIFICATION, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, TABS | MONTH VIEW — Google Calendar style with multi-day span bars, SETTINGS TAB, STATE & STORAGE
| SESSION PERSISTENCE — remember me across refreshes/tab closes | 6349-6584 | 10 | CLIENT BOOKING PORTAL — URL: fleetflowmovemasters.netlify.app/?book=1&co=COMPANY_ID — Moving company shares this link on their website — Client fills details → instant quote → pays deposit → job created, FIREBASE INIT, FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill, FIRST RUN CHECK — runs on page load before login, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, TABS, TRIAL + TIER SYSTEM | FIRST RUN CHECK — runs on page load before login, ROLE SWITCHER — instant role switch without logout
| STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live | 10012-10253 | 10 | COMPANY BRANDING ENGINE, FIREBASE INIT, JOB DETAIL MODAL, MODAL HELPERS, NOTIFICATION, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, SETTINGS TAB, STATE & STORAGE | CLIENT BOOKING PORTAL — URL: fleetflowmovemasters.netlify.app/?book=1&co=COMPANY_ID — Moving company shares this link on their website — Client fills details → instant quote → pays deposit → job created, SETTINGS TAB
| NOTIFICATION ENGINE | 12854-12914 | 10 | FIREBASE INIT, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE | JOB DETAIL MODAL, MESSAGING, PAY SCHEDULE & UNPAID CREW CHECKER, PAYROLL LEDGER — Mark Paid modal, RECEIPTS, WAREHOUSE, function renderExpenses() { — const content = document.getElementById('main-content'); — const role = currentUser.role; — const isDriver = role === 'driver'; — Scope to this driver's jobs/disbursements if not office — const myJobs = isDriver — ? STATE.jobs.filter(j => nameIncludes(j.driver, currentUser.name)) — : STATE.jobs; — const myDisbursements = isDriver — ? STATE.disbursements.filter(d => — d.driverUser === currentUser.username || — (d.driver && d.driver.toLowerCase().includes(currentUser.name.toLowerCase())) — ) — : STATE.disbursements; — const myReceipts = isDriver — ? STATE.receipts.filter(r => { — const nm = r.driver && nameIncludes(r.driver, currentUser.name); — const um = r.driverUser && r.driverUser === currentUser.username; — return nm || um; — }) — : STATE.receipts; — Totals — const totalDisbursed = myDisbursements.reduce((s,d) => s + (d.amount||0), 0); — const totalReceipts  = myReceipts.reduce((s,r) => s + (r.amount||0), 0); — const netOwed        = totalReceipts - totalDisbursed; // positive = driver owes, negative = company owes driver — let html = ` — <div class="section-header"> — <div> — <div class="section-title">EXPENSE TRACKER</div> — <div class="section-sub">${isDriver ? 'Your funds & receipts · ' + currentUser.name : 'All driver funds & expense reconciliation'}</div> — </div> — ${isAdmin() ? `<button class="btn-green" onclick="openDisbursementModal()">💰 SEND FUNDS</button>` : ''} — </div> — <!-- SUMMARY STATS --> — <div class="grid-3" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Funds Disbursed</div> — <div class="stat-value">$${totalDisbursed.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Receipts Submitted</div> — <div class="stat-value amber">$${totalReceipts.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">${netOwed > 0 ? 'Driver Owes Back' : netOwed < 0 ? 'Company Owes Driver' : 'Balanced'}</div> — <div class="stat-value ${netOwed > 0 ? 'red' : netOwed < 0 ? 'amber' : ''}"> — ${netOwed === 0 ? '✓ $0.00' : (netOwed > 0 ? '+' : '') + '$' + Math.abs(netOwed).toFixed(2)} — </div> — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div class="card" style="margin-bottom:16px"> — <div class="card-header"><div class="card-title">JOB EXPENSE BREAKDOWN</div></div> — <table> — <thead><tr> — <th>Job ID</th><th>Client</th><th>Driver</th> — <th style="text-align:right">Funds Sent</th> — <th style="text-align:right">Receipts</th> — <th style="text-align:right">Net</th> — <th>Status</th> — </tr></thead> — <tbody> — ${myJobs.length ? myJobs.map(j => { — const jobDisb = myDisbursements.filter(d => d.jobId === j.id); — const jobRec  = myReceipts.filter(r => r.jobId === j.id); — const sent    = jobDisb.reduce((s,d) => s + (d.amount||0), 0); — const spent   = jobRec.reduce((s,r) => s + (r.amount||0), 0); — const net     = spent - sent; — return `<tr onclick="openJobExpenseDetail('${j.id}')" style="cursor:pointer"> — <td><span class="tag">${j.id}</span></td> — <td>${j.client}</td> — <td class="info-text">${j.driver || '—'}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--green)">$${sent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--amber)">$${spent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);font-weight:700;color:${net > 0 ? 'var(--red)' : net < 0 ? 'var(--amber)' : 'var(--green)'}"> — ${net === 0 ? '✓ $0.00' : (net > 0 ? '+' : '') + '$' + Math.abs(net).toFixed(2)} — </td> — <td><span class="status-badge status-${j.status}">${j.status}</span></td> — </tr>`; — }).join('') : `<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--gray);font-family:var(--font-mono)">No jobs found</td></tr>`} — </tbody> — </table> — </div> — <!-- DISBURSEMENTS LOG --> — <div class="grid-2"> — <div class="card"> — <div class="card-header"> — <div class="card-title">💰 FUNDS DISBURSED</div> — ${isAdmin() ? `<button class="btn-green" style="padding:7px 14px;font-size:11px" onclick="openDisbursementModal()">+ SEND FUNDS</button>` : ''} — </div> — ${myDisbursements.length ? myDisbursements.map(d => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${d.driver} <span class="tag">${d.jobId}</span></div> — <div class="info-text">${d.date} · ${d.method || 'Cash'} · ${d.notes || ''}</div> — <div class="info-text" style="color:var(--gray)">${d.sentBy ? 'Sent by: ' + d.sentBy : ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--green)">$${(d.amount||0).toFixed(2)}</div> — ${role === 'office' ? `<button class="btn-sm" style="margin-top:4px;color:var(--red);border-color:#552222;font-size:10px" onclick="event.stopPropagation();deleteDisbursementEntry('${d.id}')">DELETE</button>` : ''} — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No funds disbursed yet.</p>`} — ${myDisbursements.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Disbursed</span> — <span style="font-family:var(--font-mono);color:var(--green);font-size:14px">$${totalDisbursed.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="card"> — <div class="card-header"><div class="card-title">🧾 RECEIPTS SUBMITTED</div></div> — ${myReceipts.length ? myReceipts.map(r => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${r.type} <span class="tag">${r.jobId}</span></div> — <div class="info-text">${r.driver} · ${r.date} · ${r.notes || ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--amber)">$${(r.amount||0).toFixed(2)}</div> — <div style="margin-top:2px">${r.approved — ? '<span style="color:var(--green);font-size:10px;font-family:var(--font-mono)">✓ APPROVED</span>' — : '<span style="color:var(--amber);font-size:10px;font-family:var(--font-mono)">⏳ PENDING</span>'}</div> — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No receipts submitted yet.</p>`} — ${myReceipts.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Receipts</span> — <span style="font-family:var(--font-mono);color:var(--amber);font-size:14px">$${totalReceipts.toFixed(2)}</span> — </div>` : ''} — </div> — </div>`; — content.innerHTML = html; — } — function openJobExpenseDetail(jobId) { — Switch to jobs tab and open the job detail — closeModal('modal-calday'); — openJobDetail(jobId); — } — function openDisbursementModal(prefillJobId) { — Pre-fill job ID if provided — document.getElementById('disb-jobid').value = prefillJobId || ''; — document.getElementById('disb-driver').value = ''; — document.getElementById('disb-amount').value = ''; — document.getElementById('disb-method').value = getSetting('defaultPay', 'Cash'); — document.getElementById('disb-notes').value = ''; — Populate driver dropdown from users — populateDisbDriverDropdown(); — openModal('modal-disbursement'); — } — async function populateDisbDriverDropdown() { — const sel = document.getElementById('disb-driver'); — sel.innerHTML = '<option value="">-- Select Driver --</option>'; — try { — const snap = await db.collection(COL_USERS) — .where('role', 'in', ['driver','helper']) — .get(); — snap.docs.forEach(d => { — const u = d.data(); — if (u.active) { — const opt = document.createElement('option'); — opt.value = JSON.stringify({ name: u.displayName, username: u.username }); — opt.textContent = u.displayName + ' (' + u.role + ')'; — sel.appendChild(opt); — } — }); — } catch(e) { — Fallback — type name manually — sel.innerHTML = '<option value="">Type driver name in notes</option>'; — } — } — async function saveDisbursementEntry() { — const jobId   = document.getElementById('disb-jobid').value.trim().toUpperCase(); — const driverRaw = document.getElementById('disb-driver').value; — const amount  = parseFloat(document.getElementById('disb-amount').value); — const method  = document.getElementById('disb-method').value; — const notes   = document.getElementById('disb-notes').value.trim(); — const btn     = document.getElementById('disb-submit-btn'); — if (!jobId)   { notify('⚠ Job ID is required', true); return; } — if (!driverRaw) { notify('⚠ Select a driver', true); return; } — if (isNaN(amount) || amount <= 0) { notify('⚠ Enter a valid amount', true); return; } — if (btn.disabled) return; — btn.disabled = true; — btn.textContent = 'SAVING...'; — try { — let driverName = driverRaw, driverUser = ''; — try { — const parsed = JSON.parse(driverRaw); — driverName = parsed.name; — driverUser = parsed.username; — } catch(e) { driverName = driverRaw; } — const disb = { — id:         'DISB-' + Date.now(), — jobId, — driver:     driverName, — driverUser: driverUser, — amount, — method, — notes, — sentBy:     currentUser.name, — date:       getTodayStr(), — createdAt:  new Date().toISOString() — }; — await saveDisbursement(disb); — closeModal('modal-disbursement'); — notify('$' + amount.toFixed(2) + ' disbursed to ' + driverName + ' ✓'); — Notify the driver — try { — if (driverUser) { — await sendNotification(driverUser, driverName, 'funds_sent', — 'Funds Sent 💰', — '$' + amount.toFixed(2) + ' sent via ' + method + ' for ' + jobId + (notes ? ' · ' + notes : '') + ' — from ' + currentUser.name, — { jobId, amount } — ); — } — } catch(e) {} — } catch(e) { — notify('⚠ Error: ' + e.message, true); — } finally { — btn.disabled = false; — btn.textContent = 'SEND FUNDS'; — } — } — async function deleteDisbursementEntry(disbId) { — if (!confirm('Delete this disbursement? This cannot be undone.')) return; — try { — await db.collection(COL_DISBURSEMENTS).doc(disbId).delete(); — notify('Disbursement deleted ✓'); — } catch(e) { notify('⚠ ' + e.message, true); } — }
| EDIT JOB  ← NEW FEATURE | 16297-16614 | 10 | FIREBASE INIT, FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill, MODAL HELPERS, NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, NOTIFICATION, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate, TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | MODAL HELPERS
| function renderExpenses() { — const content = document.getElementById('main-content'); — const role = currentUser.role; — const isDriver = role === 'driver'; — Scope to this driver's jobs/disbursements if not office — const myJobs = isDriver — ? STATE.jobs.filter(j => nameIncludes(j.driver, currentUser.name)) — : STATE.jobs; — const myDisbursements = isDriver — ? STATE.disbursements.filter(d => — d.driverUser === currentUser.username || — (d.driver && d.driver.toLowerCase().includes(currentUser.name.toLowerCase())) — ) — : STATE.disbursements; — const myReceipts = isDriver — ? STATE.receipts.filter(r => { — const nm = r.driver && nameIncludes(r.driver, currentUser.name); — const um = r.driverUser && r.driverUser === currentUser.username; — return nm || um; — }) — : STATE.receipts; — Totals — const totalDisbursed = myDisbursements.reduce((s,d) => s + (d.amount||0), 0); — const totalReceipts  = myReceipts.reduce((s,r) => s + (r.amount||0), 0); — const netOwed        = totalReceipts - totalDisbursed; // positive = driver owes, negative = company owes driver — let html = ` — <div class="section-header"> — <div> — <div class="section-title">EXPENSE TRACKER</div> — <div class="section-sub">${isDriver ? 'Your funds & receipts · ' + currentUser.name : 'All driver funds & expense reconciliation'}</div> — </div> — ${isAdmin() ? `<button class="btn-green" onclick="openDisbursementModal()">💰 SEND FUNDS</button>` : ''} — </div> — <!-- SUMMARY STATS --> — <div class="grid-3" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Funds Disbursed</div> — <div class="stat-value">$${totalDisbursed.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Receipts Submitted</div> — <div class="stat-value amber">$${totalReceipts.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">${netOwed > 0 ? 'Driver Owes Back' : netOwed < 0 ? 'Company Owes Driver' : 'Balanced'}</div> — <div class="stat-value ${netOwed > 0 ? 'red' : netOwed < 0 ? 'amber' : ''}"> — ${netOwed === 0 ? '✓ $0.00' : (netOwed > 0 ? '+' : '') + '$' + Math.abs(netOwed).toFixed(2)} — </div> — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div class="card" style="margin-bottom:16px"> — <div class="card-header"><div class="card-title">JOB EXPENSE BREAKDOWN</div></div> — <table> — <thead><tr> — <th>Job ID</th><th>Client</th><th>Driver</th> — <th style="text-align:right">Funds Sent</th> — <th style="text-align:right">Receipts</th> — <th style="text-align:right">Net</th> — <th>Status</th> — </tr></thead> — <tbody> — ${myJobs.length ? myJobs.map(j => { — const jobDisb = myDisbursements.filter(d => d.jobId === j.id); — const jobRec  = myReceipts.filter(r => r.jobId === j.id); — const sent    = jobDisb.reduce((s,d) => s + (d.amount||0), 0); — const spent   = jobRec.reduce((s,r) => s + (r.amount||0), 0); — const net     = spent - sent; — return `<tr onclick="openJobExpenseDetail('${j.id}')" style="cursor:pointer"> — <td><span class="tag">${j.id}</span></td> — <td>${j.client}</td> — <td class="info-text">${j.driver || '—'}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--green)">$${sent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--amber)">$${spent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);font-weight:700;color:${net > 0 ? 'var(--red)' : net < 0 ? 'var(--amber)' : 'var(--green)'}"> — ${net === 0 ? '✓ $0.00' : (net > 0 ? '+' : '') + '$' + Math.abs(net).toFixed(2)} — </td> — <td><span class="status-badge status-${j.status}">${j.status}</span></td> — </tr>`; — }).join('') : `<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--gray);font-family:var(--font-mono)">No jobs found</td></tr>`} — </tbody> — </table> — </div> — <!-- DISBURSEMENTS LOG --> — <div class="grid-2"> — <div class="card"> — <div class="card-header"> — <div class="card-title">💰 FUNDS DISBURSED</div> — ${isAdmin() ? `<button class="btn-green" style="padding:7px 14px;font-size:11px" onclick="openDisbursementModal()">+ SEND FUNDS</button>` : ''} — </div> — ${myDisbursements.length ? myDisbursements.map(d => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${d.driver} <span class="tag">${d.jobId}</span></div> — <div class="info-text">${d.date} · ${d.method || 'Cash'} · ${d.notes || ''}</div> — <div class="info-text" style="color:var(--gray)">${d.sentBy ? 'Sent by: ' + d.sentBy : ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--green)">$${(d.amount||0).toFixed(2)}</div> — ${role === 'office' ? `<button class="btn-sm" style="margin-top:4px;color:var(--red);border-color:#552222;font-size:10px" onclick="event.stopPropagation();deleteDisbursementEntry('${d.id}')">DELETE</button>` : ''} — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No funds disbursed yet.</p>`} — ${myDisbursements.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Disbursed</span> — <span style="font-family:var(--font-mono);color:var(--green);font-size:14px">$${totalDisbursed.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="card"> — <div class="card-header"><div class="card-title">🧾 RECEIPTS SUBMITTED</div></div> — ${myReceipts.length ? myReceipts.map(r => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${r.type} <span class="tag">${r.jobId}</span></div> — <div class="info-text">${r.driver} · ${r.date} · ${r.notes || ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--amber)">$${(r.amount||0).toFixed(2)}</div> — <div style="margin-top:2px">${r.approved — ? '<span style="color:var(--green);font-size:10px;font-family:var(--font-mono)">✓ APPROVED</span>' — : '<span style="color:var(--amber);font-size:10px;font-family:var(--font-mono)">⏳ PENDING</span>'}</div> — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No receipts submitted yet.</p>`} — ${myReceipts.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Receipts</span> — <span style="font-family:var(--font-mono);color:var(--amber);font-size:14px">$${totalReceipts.toFixed(2)}</span> — </div>` : ''} — </div> — </div>`; — content.innerHTML = html; — } — function openJobExpenseDetail(jobId) { — Switch to jobs tab and open the job detail — closeModal('modal-calday'); — openJobDetail(jobId); — } — function openDisbursementModal(prefillJobId) { — Pre-fill job ID if provided — document.getElementById('disb-jobid').value = prefillJobId || ''; — document.getElementById('disb-driver').value = ''; — document.getElementById('disb-amount').value = ''; — document.getElementById('disb-method').value = getSetting('defaultPay', 'Cash'); — document.getElementById('disb-notes').value = ''; — Populate driver dropdown from users — populateDisbDriverDropdown(); — openModal('modal-disbursement'); — } — async function populateDisbDriverDropdown() { — const sel = document.getElementById('disb-driver'); — sel.innerHTML = '<option value="">-- Select Driver --</option>'; — try { — const snap = await db.collection(COL_USERS) — .where('role', 'in', ['driver','helper']) — .get(); — snap.docs.forEach(d => { — const u = d.data(); — if (u.active) { — const opt = document.createElement('option'); — opt.value = JSON.stringify({ name: u.displayName, username: u.username }); — opt.textContent = u.displayName + ' (' + u.role + ')'; — sel.appendChild(opt); — } — }); — } catch(e) { — Fallback — type name manually — sel.innerHTML = '<option value="">Type driver name in notes</option>'; — } — } — async function saveDisbursementEntry() { — const jobId   = document.getElementById('disb-jobid').value.trim().toUpperCase(); — const driverRaw = document.getElementById('disb-driver').value; — const amount  = parseFloat(document.getElementById('disb-amount').value); — const method  = document.getElementById('disb-method').value; — const notes   = document.getElementById('disb-notes').value.trim(); — const btn     = document.getElementById('disb-submit-btn'); — if (!jobId)   { notify('⚠ Job ID is required', true); return; } — if (!driverRaw) { notify('⚠ Select a driver', true); return; } — if (isNaN(amount) || amount <= 0) { notify('⚠ Enter a valid amount', true); return; } — if (btn.disabled) return; — btn.disabled = true; — btn.textContent = 'SAVING...'; — try { — let driverName = driverRaw, driverUser = ''; — try { — const parsed = JSON.parse(driverRaw); — driverName = parsed.name; — driverUser = parsed.username; — } catch(e) { driverName = driverRaw; } — const disb = { — id:         'DISB-' + Date.now(), — jobId, — driver:     driverName, — driverUser: driverUser, — amount, — method, — notes, — sentBy:     currentUser.name, — date:       getTodayStr(), — createdAt:  new Date().toISOString() — }; — await saveDisbursement(disb); — closeModal('modal-disbursement'); — notify('$' + amount.toFixed(2) + ' disbursed to ' + driverName + ' ✓'); — Notify the driver — try { — if (driverUser) { — await sendNotification(driverUser, driverName, 'funds_sent', — 'Funds Sent 💰', — '$' + amount.toFixed(2) + ' sent via ' + method + ' for ' + jobId + (notes ? ' · ' + notes : '') + ' — from ' + currentUser.name, — { jobId, amount } — ); — } — } catch(e) {} — } catch(e) { — notify('⚠ Error: ' + e.message, true); — } finally { — btn.disabled = false; — btn.textContent = 'SEND FUNDS'; — } — } — async function deleteDisbursementEntry(disbId) { — if (!confirm('Delete this disbursement? This cannot be undone.')) return; — try { — await db.collection(COL_DISBURSEMENTS).doc(disbId).delete(); — notify('Disbursement deleted ✓'); — } catch(e) { notify('⚠ ' + e.message, true); } — } | 16916-17169 | 10 | FIREBASE INIT, JOB DETAIL MODAL, MODAL HELPERS, NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, NOTIFICATION, NOTIFICATION ENGINE, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, USER SETTINGS — stored in localStorage per user | TABS
| PAYROLL LEDGER — Running balance per crew member — Balance = earned - paid. When paid → resets to 0 → new cycle | 18195-18355 | 10 | NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — }, PAYROLL LEDGER — Build running balance from jobs + payments, PAYROLL LEDGER — Mark Paid modal, STATE & STORAGE, USER SETTINGS — stored in localStorage per user, function exportPayrollCSV() { — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — const addPerson = (name, role, gross, commission, workerType) => { — if (!name) return; — if (!payMap[name]) payMap[name] = { name, role, jobs: 0, gross: 0, commission: 0, workerType }; — payMap[name].jobs++; — payMap[name].gross      += gross; — payMap[name].commission += commission; — }; — addPerson(j.driver, 'Driver', j.payroll.driver||0, j.payroll.driverCommission||0, j.payroll.driverWorkerType||'1099'); — (j.payroll.helpers||[]).forEach(h => addPerson(h.name, 'Helper', calcPay(h), h.commission||0, h.workerType||'1099')); — }); — const rows = [['Name','Role','Worker Type','Jobs','Base Pay','Commission','Total Gross','Est Tax','Est Take-Home']]; — Object.values(payMap).forEach(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — rows.push([p.name, p.role, p.workerType, p.jobs, p.gross.toFixed(2), p.commission.toFixed(2), — gross.toFixed(2), tax.totalEstTax.toFixed(2), tax.takeHome.toFixed(2)]); — }); — downloadCSV(rows, 'jpgsystems-payroll-' + getTodayStr() + '.csv'); — notify('Payroll CSV exported ✓'); — } — function exportJobsCSV() { — const rows = [['Job ID','Client','Date','Time','Driver','Type','Status','Amount','Gates Passed','Origin','Destination']]; — STATE.jobs.forEach(j => { — const gates = Object.values(j.gates||{}).filter(Boolean).length; — rows.push([j.id, j.client, j.date, j.time||'', j.driver||'', j.type||'', — j.status, (j.amount||0).toFixed(2), gates + '/' + Object.keys(j.gates||{}).length, — j.origin||'', j.dest||'']); — }); — downloadCSV(rows, 'jpgsystems-jobs-' + getTodayStr() + '.csv'); — notify('Jobs CSV exported ✓'); — } — function exportReceiptsCSV() { — const rows = [['Receipt ID','Job ID','Driver','Date','Type','Amount','Status','Notes']]; — STATE.receipts.forEach(r => { — rows.push([r.id, r.jobId, r.driver||'', r.date||'', r.type||'', — (r.amount||0).toFixed(2), r.approved ? 'APPROVED' : r.rejected ? 'REJECTED' : 'PENDING', r.notes||'']); — }); — downloadCSV(rows, 'jpgsystems-receipts-' + getTodayStr() + '.csv'); — notify('Receipts CSV exported ✓'); — } — function downloadCSV(rows, filename) { — const csv  = rows.map(r => r.map(c => '"' + String(c).replace(/"/g,'""') + '"').join(',')).join('\n'); — const blob = new Blob([csv], { type: 'text/csv' }); — const url  = URL.createObjectURL(blob); — const a    = document.createElement('a'); — a.href     = url; — a.download = filename; — a.click(); — URL.revokeObjectURL(url); — } — Add export buttons to financials tab — function renderExportBar() { — return ` — <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;align-self:center">EXPORT:</div> — <button class="btn-sm" onclick="exportJobsCSV()">⬇ Jobs CSV</button> — <button class="btn-sm" onclick="exportReceiptsCSV()">⬇ Receipts CSV</button> — <button class="btn-sm" onclick="exportPayrollCSV()">⬇ Payroll CSV</button> — <button class="btn-sm" onclick="renderPayrollSummary()">💵 Payroll Report</button> — </div>`; — } | Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — }, PAYROLL LEDGER — Mark Paid modal, TABS
| ROLE SWITCHER — instant role switch without logout | 19454-19646 | 10 | COMPANY BRANDING ENGINE, FIREBASE INIT, NOTIFICATION, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, SESSION PERSISTENCE — remember me across refreshes/tab closes, SETTINGS TAB, STATE & STORAGE, TABS, USER SETTINGS — stored in localStorage per user | SETTINGS TAB
| TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created | 2002-2445 | 11 | CALENDAR, FIREBASE INIT, FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill, LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool, MODAL HELPERS, NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, NOTIFICATION, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, WEEK VIEW — time-based layout like Google Calendar | WEEK VIEW — time-based layout like Google Calendar
| USER SETTINGS — stored in localStorage per user | 6318-6348 | 11 | STATE & STORAGE | PAY SCHEDULE & UNPAID CREW CHECKER, PAYROLL LEDGER — Running balance per crew member — Balance = earned - paid. When paid → resets to 0 → new cycle, PORTAL ROUTE CHECK — runs before login screen — Matches Good Friends Movers LLC document package exactly — US DOT# 3921653 / MC# 1450729 — Structure: Interstate BOL | Legal Terms | Inventory x2 | Packing | Post Contract | Valuation, RECEIPTS, ROLE SWITCHER — instant role switch without logout, SETTINGS ACTION FUNCTIONS, SETTINGS TAB, SOUND ENGINE — Web Audio API, no external files needed, function openNewJobModal(prefillDate) { — ── HARD RESET — every field to blank/zero ────────────────── — const safe = (id, val='') => { const el = document.getElementById(id); if(el) el.value = val; }; — Core job fields — safe('nj-client'); safe('nj-phone'); safe('nj-email'); — safe('nj-origin'); safe('nj-dest'); safe('nj-pickup-from'); — safe('nj-amount'); safe('nj-notes'); safe('nj-carrier'); — safe('nj-cubic-ft'); safe('nj-cubic-ft-2'); safe('nj-rate-per-cuft'); — safe('nj-weight'); safe('nj-linehaul'); — safe('nj-end-date'); safe('nj-end-time'); — safe('nj-pickup-start'); safe('nj-pickup-end'); — safe('nj-delivery-start'); safe('nj-delivery-end'); — safe('nj-pickup-driver'); safe('nj-delivery-driver'); — safe('nj-job-id'); safe('nj-ref-num'); — safe('nj-priority'); safe('nj-followup'); safe('nj-fadd'); — safe('nj-broker-ref'); safe('nj-carrier'); — ALL estimate fields — zero out completely — safe('nj-est-fuel', ''); — safe('nj-est-fuel-pct', '0'); — safe('nj-est-overload-cuft', ''); — safe('nj-est-stairs-origin', ''); — safe('nj-est-stairs-dest', ''); — safe('nj-est-longcarry-origin', ''); — safe('nj-est-longcarry-dest', ''); — safe('nj-est-shuttle', ''); — safe('nj-est-bulky', ''); — safe('nj-est-split', ''); — safe('nj-est-pgs', ''); — safe('nj-est-packing', ''); — safe('nj-est-storage-days', ''); — safe('nj-est-storage-rate', ''); — safe('nj-est-other', ''); — safe('nj-est-deposit', ''); — safe('nj-est-partial2', ''); — safe('nj-est-tip', ''); — safe('nj-est-discount', ''); — safe('nj-est-discount-label', ''); — safe('nj-est-others1', '');  safe('nj-est-others1-label', ''); — safe('nj-est-others2', '');  safe('nj-est-others2-label', ''); — safe('nj-est-others3', '');  safe('nj-est-others3-label', ''); — safe('nj-est-extra1', '');   safe('nj-est-extra1-label', ''); — safe('nj-est-extra2', '');   safe('nj-est-extra2-label', ''); — safe('nj-est-extra3', '');   safe('nj-est-extra3-label', ''); — safe('nj-est-notes', ''); — safe('nj-est-office-remarks', ''); — safe('nj-est-slip-remarks', ''); — Reset carrier total display — const ct = document.getElementById('nj-carrier-total'); if(ct) ct.textContent = '$0.00'; — Clear helper rows — const hr = document.getElementById('nj-helper-rows'); if(hr) hr.innerHTML = ''; — Reset all selects to index 0 — ['nj-job-for','nj-est-tip-method','nj-est-valuation', — 'nj-est-fuel-mode','nj-est-tip-assign'].forEach(id => { — const el = document.getElementById(id); — if (el) el.selectedIndex = 0; — }); — try { toggleCarrierFields('nj'); } catch(e) {} — Apply defaults — const s2 = loadSettings(); — safe('nj-crew', s2.defaultCrew || '3'); — safe('nj-type', s2.defaultMove || 'local'); — safe('nj-date', prefillDate || getTodayStr()); — safe('nj-time', getSetting('defaultTime', '08:00')); — safe('nj-driver', currentUser.role === 'driver' ? (currentUser.name||'') : ''); — closeModal('modal-calday'); — openModal('modal-newjob'); — setTimeout(() => { try { applyJobDefaults('nj'); } catch(e) {} }, 50); — } — async function createJob() { — const client = document.getElementById('nj-client').value.trim(); — const date   = document.getElementById('nj-date').value; — const origin = document.getElementById('nj-origin').value.trim(); — const dest   = document.getElementById('nj-dest').value.trim(); — if (!client || !date) { notify('⚠ Client name and date required', true); return; } — Custom ID or auto-generate — const customId = (document.getElementById('nj-job-id')?.value||'').trim().toUpperCase(); — const autoId   = 'JOB-' + String((STATE.jobs||[]).length + 1).padStart(3,'0'); — const jobId    = customId || autoId; — if ((STATE.jobs||[]).find(j => j.id === jobId)) { — notify('⚠ Job ID ' + jobId + ' already exists', true); return; — } — Collect helpers — const helpers = []; — document.querySelectorAll('.nj-helper-name').forEach(inp => { — const name = inp.value.trim(); — if (!name) return; — const row = inp.closest('.nj-helper-row'); — helpers.push({ — name, — rate:    parseFloat(row?.querySelector('.nj-helper-rate')?.value) || 0, — payType: row?.querySelector('.nj-helper-type')?.value || 'hourly', — hours: 0, days: 1, workerType: '1099', paid: false — }); — }); — const g = id => (document.getElementById(id)?.value || ''); — const newJob = { — id:            jobId, — refNum:        g('nj-ref-num'), — client, — phone:         g('nj-phone'), — email:         g('nj-email'), — date, — time:          g('nj-time'), — endDate:       g('nj-end-date') || g('nj-delivery-end').slice(0,10) || '', — endTime:       g('nj-end-time'), — origin, — dest, — pickupStart:   g('nj-pickup-start'), — pickupEnd:     g('nj-pickup-end'), — pickupDriver:  g('nj-pickup-driver'), — deliveryStart: g('nj-delivery-start'), — deliveryEnd:   g('nj-delivery-end'), — deliveryDriver:g('nj-delivery-driver'), — driver:        g('nj-driver'), — driverUser:    currentUser.username || '', — crewSize:      parseInt(g('nj-crew')) || 3, — crewNames:     helpers.map(h => h.name), — amount:        parseFloat(g('nj-amount')) || 0, — cubicFt:       parseFloat(g('nj-cubic-ft-2') || g('nj-cubic-ft')) || 0, — weight:        parseFloat(g('nj-weight')) || 0, — linehaul:      parseFloat(g('nj-linehaul')) || 0, — priority:      g('nj-priority'), — followUp:      g('nj-followup'), — fadd:          g('nj-fadd'), — type:          g('nj-type') || 'local', — notes:         g('nj-notes'), — companyId:     COMPANY_ID, — status:        'pending', — gates:         { preload:false, loaded:false, delivered:false, payment:false }, — messages:      { crew:[], client:[] }, — photos:        [], — signatures:    [], — payroll:       { driver:0, helpers } — }; — try { — await db.collection(COL_JOBS).doc(newJob.id).set(newJob); — closeModal('modal-newjob'); — notify('✓ Job ' + newJob.id + ' created — ' + client); — setTimeout(() => renderCalendar(), 200); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('createJob error:', e); — } — }, function renderExpenses() { — const content = document.getElementById('main-content'); — const role = currentUser.role; — const isDriver = role === 'driver'; — Scope to this driver's jobs/disbursements if not office — const myJobs = isDriver — ? STATE.jobs.filter(j => nameIncludes(j.driver, currentUser.name)) — : STATE.jobs; — const myDisbursements = isDriver — ? STATE.disbursements.filter(d => — d.driverUser === currentUser.username || — (d.driver && d.driver.toLowerCase().includes(currentUser.name.toLowerCase())) — ) — : STATE.disbursements; — const myReceipts = isDriver — ? STATE.receipts.filter(r => { — const nm = r.driver && nameIncludes(r.driver, currentUser.name); — const um = r.driverUser && r.driverUser === currentUser.username; — return nm || um; — }) — : STATE.receipts; — Totals — const totalDisbursed = myDisbursements.reduce((s,d) => s + (d.amount||0), 0); — const totalReceipts  = myReceipts.reduce((s,r) => s + (r.amount||0), 0); — const netOwed        = totalReceipts - totalDisbursed; // positive = driver owes, negative = company owes driver — let html = ` — <div class="section-header"> — <div> — <div class="section-title">EXPENSE TRACKER</div> — <div class="section-sub">${isDriver ? 'Your funds & receipts · ' + currentUser.name : 'All driver funds & expense reconciliation'}</div> — </div> — ${isAdmin() ? `<button class="btn-green" onclick="openDisbursementModal()">💰 SEND FUNDS</button>` : ''} — </div> — <!-- SUMMARY STATS --> — <div class="grid-3" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Funds Disbursed</div> — <div class="stat-value">$${totalDisbursed.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Receipts Submitted</div> — <div class="stat-value amber">$${totalReceipts.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">${netOwed > 0 ? 'Driver Owes Back' : netOwed < 0 ? 'Company Owes Driver' : 'Balanced'}</div> — <div class="stat-value ${netOwed > 0 ? 'red' : netOwed < 0 ? 'amber' : ''}"> — ${netOwed === 0 ? '✓ $0.00' : (netOwed > 0 ? '+' : '') + '$' + Math.abs(netOwed).toFixed(2)} — </div> — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div class="card" style="margin-bottom:16px"> — <div class="card-header"><div class="card-title">JOB EXPENSE BREAKDOWN</div></div> — <table> — <thead><tr> — <th>Job ID</th><th>Client</th><th>Driver</th> — <th style="text-align:right">Funds Sent</th> — <th style="text-align:right">Receipts</th> — <th style="text-align:right">Net</th> — <th>Status</th> — </tr></thead> — <tbody> — ${myJobs.length ? myJobs.map(j => { — const jobDisb = myDisbursements.filter(d => d.jobId === j.id); — const jobRec  = myReceipts.filter(r => r.jobId === j.id); — const sent    = jobDisb.reduce((s,d) => s + (d.amount||0), 0); — const spent   = jobRec.reduce((s,r) => s + (r.amount||0), 0); — const net     = spent - sent; — return `<tr onclick="openJobExpenseDetail('${j.id}')" style="cursor:pointer"> — <td><span class="tag">${j.id}</span></td> — <td>${j.client}</td> — <td class="info-text">${j.driver || '—'}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--green)">$${sent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--amber)">$${spent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);font-weight:700;color:${net > 0 ? 'var(--red)' : net < 0 ? 'var(--amber)' : 'var(--green)'}"> — ${net === 0 ? '✓ $0.00' : (net > 0 ? '+' : '') + '$' + Math.abs(net).toFixed(2)} — </td> — <td><span class="status-badge status-${j.status}">${j.status}</span></td> — </tr>`; — }).join('') : `<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--gray);font-family:var(--font-mono)">No jobs found</td></tr>`} — </tbody> — </table> — </div> — <!-- DISBURSEMENTS LOG --> — <div class="grid-2"> — <div class="card"> — <div class="card-header"> — <div class="card-title">💰 FUNDS DISBURSED</div> — ${isAdmin() ? `<button class="btn-green" style="padding:7px 14px;font-size:11px" onclick="openDisbursementModal()">+ SEND FUNDS</button>` : ''} — </div> — ${myDisbursements.length ? myDisbursements.map(d => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${d.driver} <span class="tag">${d.jobId}</span></div> — <div class="info-text">${d.date} · ${d.method || 'Cash'} · ${d.notes || ''}</div> — <div class="info-text" style="color:var(--gray)">${d.sentBy ? 'Sent by: ' + d.sentBy : ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--green)">$${(d.amount||0).toFixed(2)}</div> — ${role === 'office' ? `<button class="btn-sm" style="margin-top:4px;color:var(--red);border-color:#552222;font-size:10px" onclick="event.stopPropagation();deleteDisbursementEntry('${d.id}')">DELETE</button>` : ''} — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No funds disbursed yet.</p>`} — ${myDisbursements.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Disbursed</span> — <span style="font-family:var(--font-mono);color:var(--green);font-size:14px">$${totalDisbursed.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="card"> — <div class="card-header"><div class="card-title">🧾 RECEIPTS SUBMITTED</div></div> — ${myReceipts.length ? myReceipts.map(r => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${r.type} <span class="tag">${r.jobId}</span></div> — <div class="info-text">${r.driver} · ${r.date} · ${r.notes || ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--amber)">$${(r.amount||0).toFixed(2)}</div> — <div style="margin-top:2px">${r.approved — ? '<span style="color:var(--green);font-size:10px;font-family:var(--font-mono)">✓ APPROVED</span>' — : '<span style="color:var(--amber);font-size:10px;font-family:var(--font-mono)">⏳ PENDING</span>'}</div> — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No receipts submitted yet.</p>`} — ${myReceipts.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Receipts</span> — <span style="font-family:var(--font-mono);color:var(--amber);font-size:14px">$${totalReceipts.toFixed(2)}</span> — </div>` : ''} — </div> — </div>`; — content.innerHTML = html; — } — function openJobExpenseDetail(jobId) { — Switch to jobs tab and open the job detail — closeModal('modal-calday'); — openJobDetail(jobId); — } — function openDisbursementModal(prefillJobId) { — Pre-fill job ID if provided — document.getElementById('disb-jobid').value = prefillJobId || ''; — document.getElementById('disb-driver').value = ''; — document.getElementById('disb-amount').value = ''; — document.getElementById('disb-method').value = getSetting('defaultPay', 'Cash'); — document.getElementById('disb-notes').value = ''; — Populate driver dropdown from users — populateDisbDriverDropdown(); — openModal('modal-disbursement'); — } — async function populateDisbDriverDropdown() { — const sel = document.getElementById('disb-driver'); — sel.innerHTML = '<option value="">-- Select Driver --</option>'; — try { — const snap = await db.collection(COL_USERS) — .where('role', 'in', ['driver','helper']) — .get(); — snap.docs.forEach(d => { — const u = d.data(); — if (u.active) { — const opt = document.createElement('option'); — opt.value = JSON.stringify({ name: u.displayName, username: u.username }); — opt.textContent = u.displayName + ' (' + u.role + ')'; — sel.appendChild(opt); — } — }); — } catch(e) { — Fallback — type name manually — sel.innerHTML = '<option value="">Type driver name in notes</option>'; — } — } — async function saveDisbursementEntry() { — const jobId   = document.getElementById('disb-jobid').value.trim().toUpperCase(); — const driverRaw = document.getElementById('disb-driver').value; — const amount  = parseFloat(document.getElementById('disb-amount').value); — const method  = document.getElementById('disb-method').value; — const notes   = document.getElementById('disb-notes').value.trim(); — const btn     = document.getElementById('disb-submit-btn'); — if (!jobId)   { notify('⚠ Job ID is required', true); return; } — if (!driverRaw) { notify('⚠ Select a driver', true); return; } — if (isNaN(amount) || amount <= 0) { notify('⚠ Enter a valid amount', true); return; } — if (btn.disabled) return; — btn.disabled = true; — btn.textContent = 'SAVING...'; — try { — let driverName = driverRaw, driverUser = ''; — try { — const parsed = JSON.parse(driverRaw); — driverName = parsed.name; — driverUser = parsed.username; — } catch(e) { driverName = driverRaw; } — const disb = { — id:         'DISB-' + Date.now(), — jobId, — driver:     driverName, — driverUser: driverUser, — amount, — method, — notes, — sentBy:     currentUser.name, — date:       getTodayStr(), — createdAt:  new Date().toISOString() — }; — await saveDisbursement(disb); — closeModal('modal-disbursement'); — notify('$' + amount.toFixed(2) + ' disbursed to ' + driverName + ' ✓'); — Notify the driver — try { — if (driverUser) { — await sendNotification(driverUser, driverName, 'funds_sent', — 'Funds Sent 💰', — '$' + amount.toFixed(2) + ' sent via ' + method + ' for ' + jobId + (notes ? ' · ' + notes : '') + ' — from ' + currentUser.name, — { jobId, amount } — ); — } — } catch(e) {} — } catch(e) { — notify('⚠ Error: ' + e.message, true); — } finally { — btn.disabled = false; — btn.textContent = 'SEND FUNDS'; — } — } — async function deleteDisbursementEntry(disbId) { — if (!confirm('Delete this disbursement? This cannot be undone.')) return; — try { — await db.collection(COL_DISBURSEMENTS).doc(disbId).delete(); — notify('Disbursement deleted ✓'); — } catch(e) { notify('⚠ ' + e.message, true); } — }
| Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — } | 17778-18194 | 11 | FIREBASE INIT, MODAL HELPERS, NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, NOTIFICATION, PAYROLL CALCULATION ENGINE v2 — pay + commission + tax, PAYROLL LEDGER — Build running balance from jobs + payments, PAYROLL LEDGER — Running balance per crew member — Balance = earned - paid. When paid → resets to 0 → new cycle, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE | PAYROLL LEDGER — Mark Paid modal, PAYROLL LEDGER — Running balance per crew member — Balance = earned - paid. When paid → resets to 0 → new cycle
| DASHBOARD | 6706-7747 | 12 | CLIENT MOVE PORTAL — Full transparency experience — This is what the client pays $100-150 for — Real-time gate tracking, item location, messaging, docs, FIREBASE INIT, JOB DETAIL MODAL, LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool, NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, NOTIFICATION, PAYROLL CALCULATION ENGINE v2 — pay + commission + tax, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, TABS, function openNewJobModal(prefillDate) { — ── HARD RESET — every field to blank/zero ────────────────── — const safe = (id, val='') => { const el = document.getElementById(id); if(el) el.value = val; }; — Core job fields — safe('nj-client'); safe('nj-phone'); safe('nj-email'); — safe('nj-origin'); safe('nj-dest'); safe('nj-pickup-from'); — safe('nj-amount'); safe('nj-notes'); safe('nj-carrier'); — safe('nj-cubic-ft'); safe('nj-cubic-ft-2'); safe('nj-rate-per-cuft'); — safe('nj-weight'); safe('nj-linehaul'); — safe('nj-end-date'); safe('nj-end-time'); — safe('nj-pickup-start'); safe('nj-pickup-end'); — safe('nj-delivery-start'); safe('nj-delivery-end'); — safe('nj-pickup-driver'); safe('nj-delivery-driver'); — safe('nj-job-id'); safe('nj-ref-num'); — safe('nj-priority'); safe('nj-followup'); safe('nj-fadd'); — safe('nj-broker-ref'); safe('nj-carrier'); — ALL estimate fields — zero out completely — safe('nj-est-fuel', ''); — safe('nj-est-fuel-pct', '0'); — safe('nj-est-overload-cuft', ''); — safe('nj-est-stairs-origin', ''); — safe('nj-est-stairs-dest', ''); — safe('nj-est-longcarry-origin', ''); — safe('nj-est-longcarry-dest', ''); — safe('nj-est-shuttle', ''); — safe('nj-est-bulky', ''); — safe('nj-est-split', ''); — safe('nj-est-pgs', ''); — safe('nj-est-packing', ''); — safe('nj-est-storage-days', ''); — safe('nj-est-storage-rate', ''); — safe('nj-est-other', ''); — safe('nj-est-deposit', ''); — safe('nj-est-partial2', ''); — safe('nj-est-tip', ''); — safe('nj-est-discount', ''); — safe('nj-est-discount-label', ''); — safe('nj-est-others1', '');  safe('nj-est-others1-label', ''); — safe('nj-est-others2', '');  safe('nj-est-others2-label', ''); — safe('nj-est-others3', '');  safe('nj-est-others3-label', ''); — safe('nj-est-extra1', '');   safe('nj-est-extra1-label', ''); — safe('nj-est-extra2', '');   safe('nj-est-extra2-label', ''); — safe('nj-est-extra3', '');   safe('nj-est-extra3-label', ''); — safe('nj-est-notes', ''); — safe('nj-est-office-remarks', ''); — safe('nj-est-slip-remarks', ''); — Reset carrier total display — const ct = document.getElementById('nj-carrier-total'); if(ct) ct.textContent = '$0.00'; — Clear helper rows — const hr = document.getElementById('nj-helper-rows'); if(hr) hr.innerHTML = ''; — Reset all selects to index 0 — ['nj-job-for','nj-est-tip-method','nj-est-valuation', — 'nj-est-fuel-mode','nj-est-tip-assign'].forEach(id => { — const el = document.getElementById(id); — if (el) el.selectedIndex = 0; — }); — try { toggleCarrierFields('nj'); } catch(e) {} — Apply defaults — const s2 = loadSettings(); — safe('nj-crew', s2.defaultCrew || '3'); — safe('nj-type', s2.defaultMove || 'local'); — safe('nj-date', prefillDate || getTodayStr()); — safe('nj-time', getSetting('defaultTime', '08:00')); — safe('nj-driver', currentUser.role === 'driver' ? (currentUser.name||'') : ''); — closeModal('modal-calday'); — openModal('modal-newjob'); — setTimeout(() => { try { applyJobDefaults('nj'); } catch(e) {} }, 50); — } — async function createJob() { — const client = document.getElementById('nj-client').value.trim(); — const date   = document.getElementById('nj-date').value; — const origin = document.getElementById('nj-origin').value.trim(); — const dest   = document.getElementById('nj-dest').value.trim(); — if (!client || !date) { notify('⚠ Client name and date required', true); return; } — Custom ID or auto-generate — const customId = (document.getElementById('nj-job-id')?.value||'').trim().toUpperCase(); — const autoId   = 'JOB-' + String((STATE.jobs||[]).length + 1).padStart(3,'0'); — const jobId    = customId || autoId; — if ((STATE.jobs||[]).find(j => j.id === jobId)) { — notify('⚠ Job ID ' + jobId + ' already exists', true); return; — } — Collect helpers — const helpers = []; — document.querySelectorAll('.nj-helper-name').forEach(inp => { — const name = inp.value.trim(); — if (!name) return; — const row = inp.closest('.nj-helper-row'); — helpers.push({ — name, — rate:    parseFloat(row?.querySelector('.nj-helper-rate')?.value) || 0, — payType: row?.querySelector('.nj-helper-type')?.value || 'hourly', — hours: 0, days: 1, workerType: '1099', paid: false — }); — }); — const g = id => (document.getElementById(id)?.value || ''); — const newJob = { — id:            jobId, — refNum:        g('nj-ref-num'), — client, — phone:         g('nj-phone'), — email:         g('nj-email'), — date, — time:          g('nj-time'), — endDate:       g('nj-end-date') || g('nj-delivery-end').slice(0,10) || '', — endTime:       g('nj-end-time'), — origin, — dest, — pickupStart:   g('nj-pickup-start'), — pickupEnd:     g('nj-pickup-end'), — pickupDriver:  g('nj-pickup-driver'), — deliveryStart: g('nj-delivery-start'), — deliveryEnd:   g('nj-delivery-end'), — deliveryDriver:g('nj-delivery-driver'), — driver:        g('nj-driver'), — driverUser:    currentUser.username || '', — crewSize:      parseInt(g('nj-crew')) || 3, — crewNames:     helpers.map(h => h.name), — amount:        parseFloat(g('nj-amount')) || 0, — cubicFt:       parseFloat(g('nj-cubic-ft-2') || g('nj-cubic-ft')) || 0, — weight:        parseFloat(g('nj-weight')) || 0, — linehaul:      parseFloat(g('nj-linehaul')) || 0, — priority:      g('nj-priority'), — followUp:      g('nj-followup'), — fadd:          g('nj-fadd'), — type:          g('nj-type') || 'local', — notes:         g('nj-notes'), — companyId:     COMPANY_ID, — status:        'pending', — gates:         { preload:false, loaded:false, delivered:false, payment:false }, — messages:      { crew:[], client:[] }, — photos:        [], — signatures:    [], — payroll:       { driver:0, helpers } — }; — try { — await db.collection(COL_JOBS).doc(newJob.id).set(newJob); — closeModal('modal-newjob'); — notify('✓ Job ' + newJob.id + ' created — ' + client); — setTimeout(() => renderCalendar(), 200); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('createJob error:', e); — } — } | TABS
| CALENDAR | 13510-13656 | 12 | MONTH VIEW — Google Calendar style with multi-day span bars, NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, RECEIPTS, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, WEEK VIEW — time-based layout like Google Calendar, function openNewJobModal(prefillDate) { — ── HARD RESET — every field to blank/zero ────────────────── — const safe = (id, val='') => { const el = document.getElementById(id); if(el) el.value = val; }; — Core job fields — safe('nj-client'); safe('nj-phone'); safe('nj-email'); — safe('nj-origin'); safe('nj-dest'); safe('nj-pickup-from'); — safe('nj-amount'); safe('nj-notes'); safe('nj-carrier'); — safe('nj-cubic-ft'); safe('nj-cubic-ft-2'); safe('nj-rate-per-cuft'); — safe('nj-weight'); safe('nj-linehaul'); — safe('nj-end-date'); safe('nj-end-time'); — safe('nj-pickup-start'); safe('nj-pickup-end'); — safe('nj-delivery-start'); safe('nj-delivery-end'); — safe('nj-pickup-driver'); safe('nj-delivery-driver'); — safe('nj-job-id'); safe('nj-ref-num'); — safe('nj-priority'); safe('nj-followup'); safe('nj-fadd'); — safe('nj-broker-ref'); safe('nj-carrier'); — ALL estimate fields — zero out completely — safe('nj-est-fuel', ''); — safe('nj-est-fuel-pct', '0'); — safe('nj-est-overload-cuft', ''); — safe('nj-est-stairs-origin', ''); — safe('nj-est-stairs-dest', ''); — safe('nj-est-longcarry-origin', ''); — safe('nj-est-longcarry-dest', ''); — safe('nj-est-shuttle', ''); — safe('nj-est-bulky', ''); — safe('nj-est-split', ''); — safe('nj-est-pgs', ''); — safe('nj-est-packing', ''); — safe('nj-est-storage-days', ''); — safe('nj-est-storage-rate', ''); — safe('nj-est-other', ''); — safe('nj-est-deposit', ''); — safe('nj-est-partial2', ''); — safe('nj-est-tip', ''); — safe('nj-est-discount', ''); — safe('nj-est-discount-label', ''); — safe('nj-est-others1', '');  safe('nj-est-others1-label', ''); — safe('nj-est-others2', '');  safe('nj-est-others2-label', ''); — safe('nj-est-others3', '');  safe('nj-est-others3-label', ''); — safe('nj-est-extra1', '');   safe('nj-est-extra1-label', ''); — safe('nj-est-extra2', '');   safe('nj-est-extra2-label', ''); — safe('nj-est-extra3', '');   safe('nj-est-extra3-label', ''); — safe('nj-est-notes', ''); — safe('nj-est-office-remarks', ''); — safe('nj-est-slip-remarks', ''); — Reset carrier total display — const ct = document.getElementById('nj-carrier-total'); if(ct) ct.textContent = '$0.00'; — Clear helper rows — const hr = document.getElementById('nj-helper-rows'); if(hr) hr.innerHTML = ''; — Reset all selects to index 0 — ['nj-job-for','nj-est-tip-method','nj-est-valuation', — 'nj-est-fuel-mode','nj-est-tip-assign'].forEach(id => { — const el = document.getElementById(id); — if (el) el.selectedIndex = 0; — }); — try { toggleCarrierFields('nj'); } catch(e) {} — Apply defaults — const s2 = loadSettings(); — safe('nj-crew', s2.defaultCrew || '3'); — safe('nj-type', s2.defaultMove || 'local'); — safe('nj-date', prefillDate || getTodayStr()); — safe('nj-time', getSetting('defaultTime', '08:00')); — safe('nj-driver', currentUser.role === 'driver' ? (currentUser.name||'') : ''); — closeModal('modal-calday'); — openModal('modal-newjob'); — setTimeout(() => { try { applyJobDefaults('nj'); } catch(e) {} }, 50); — } — async function createJob() { — const client = document.getElementById('nj-client').value.trim(); — const date   = document.getElementById('nj-date').value; — const origin = document.getElementById('nj-origin').value.trim(); — const dest   = document.getElementById('nj-dest').value.trim(); — if (!client || !date) { notify('⚠ Client name and date required', true); return; } — Custom ID or auto-generate — const customId = (document.getElementById('nj-job-id')?.value||'').trim().toUpperCase(); — const autoId   = 'JOB-' + String((STATE.jobs||[]).length + 1).padStart(3,'0'); — const jobId    = customId || autoId; — if ((STATE.jobs||[]).find(j => j.id === jobId)) { — notify('⚠ Job ID ' + jobId + ' already exists', true); return; — } — Collect helpers — const helpers = []; — document.querySelectorAll('.nj-helper-name').forEach(inp => { — const name = inp.value.trim(); — if (!name) return; — const row = inp.closest('.nj-helper-row'); — helpers.push({ — name, — rate:    parseFloat(row?.querySelector('.nj-helper-rate')?.value) || 0, — payType: row?.querySelector('.nj-helper-type')?.value || 'hourly', — hours: 0, days: 1, workerType: '1099', paid: false — }); — }); — const g = id => (document.getElementById(id)?.value || ''); — const newJob = { — id:            jobId, — refNum:        g('nj-ref-num'), — client, — phone:         g('nj-phone'), — email:         g('nj-email'), — date, — time:          g('nj-time'), — endDate:       g('nj-end-date') || g('nj-delivery-end').slice(0,10) || '', — endTime:       g('nj-end-time'), — origin, — dest, — pickupStart:   g('nj-pickup-start'), — pickupEnd:     g('nj-pickup-end'), — pickupDriver:  g('nj-pickup-driver'), — deliveryStart: g('nj-delivery-start'), — deliveryEnd:   g('nj-delivery-end'), — deliveryDriver:g('nj-delivery-driver'), — driver:        g('nj-driver'), — driverUser:    currentUser.username || '', — crewSize:      parseInt(g('nj-crew')) || 3, — crewNames:     helpers.map(h => h.name), — amount:        parseFloat(g('nj-amount')) || 0, — cubicFt:       parseFloat(g('nj-cubic-ft-2') || g('nj-cubic-ft')) || 0, — weight:        parseFloat(g('nj-weight')) || 0, — linehaul:      parseFloat(g('nj-linehaul')) || 0, — priority:      g('nj-priority'), — followUp:      g('nj-followup'), — fadd:          g('nj-fadd'), — type:          g('nj-type') || 'local', — notes:         g('nj-notes'), — companyId:     COMPANY_ID, — status:        'pending', — gates:         { preload:false, loaded:false, delivered:false, payment:false }, — messages:      { crew:[], client:[] }, — photos:        [], — signatures:    [], — payroll:       { driver:0, helpers } — }; — try { — await db.collection(COL_JOBS).doc(newJob.id).set(newJob); — closeModal('modal-newjob'); — notify('✓ Job ' + newJob.id + ' created — ' + client); — setTimeout(() => renderCalendar(), 200); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('createJob error:', e); — } — } | MONTH VIEW — Google Calendar style with multi-day span bars, TABS, TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created, WEEK VIEW — time-based layout like Google Calendar, function openNewJobModal(prefillDate) { — ── HARD RESET — every field to blank/zero ────────────────── — const safe = (id, val='') => { const el = document.getElementById(id); if(el) el.value = val; }; — Core job fields — safe('nj-client'); safe('nj-phone'); safe('nj-email'); — safe('nj-origin'); safe('nj-dest'); safe('nj-pickup-from'); — safe('nj-amount'); safe('nj-notes'); safe('nj-carrier'); — safe('nj-cubic-ft'); safe('nj-cubic-ft-2'); safe('nj-rate-per-cuft'); — safe('nj-weight'); safe('nj-linehaul'); — safe('nj-end-date'); safe('nj-end-time'); — safe('nj-pickup-start'); safe('nj-pickup-end'); — safe('nj-delivery-start'); safe('nj-delivery-end'); — safe('nj-pickup-driver'); safe('nj-delivery-driver'); — safe('nj-job-id'); safe('nj-ref-num'); — safe('nj-priority'); safe('nj-followup'); safe('nj-fadd'); — safe('nj-broker-ref'); safe('nj-carrier'); — ALL estimate fields — zero out completely — safe('nj-est-fuel', ''); — safe('nj-est-fuel-pct', '0'); — safe('nj-est-overload-cuft', ''); — safe('nj-est-stairs-origin', ''); — safe('nj-est-stairs-dest', ''); — safe('nj-est-longcarry-origin', ''); — safe('nj-est-longcarry-dest', ''); — safe('nj-est-shuttle', ''); — safe('nj-est-bulky', ''); — safe('nj-est-split', ''); — safe('nj-est-pgs', ''); — safe('nj-est-packing', ''); — safe('nj-est-storage-days', ''); — safe('nj-est-storage-rate', ''); — safe('nj-est-other', ''); — safe('nj-est-deposit', ''); — safe('nj-est-partial2', ''); — safe('nj-est-tip', ''); — safe('nj-est-discount', ''); — safe('nj-est-discount-label', ''); — safe('nj-est-others1', '');  safe('nj-est-others1-label', ''); — safe('nj-est-others2', '');  safe('nj-est-others2-label', ''); — safe('nj-est-others3', '');  safe('nj-est-others3-label', ''); — safe('nj-est-extra1', '');   safe('nj-est-extra1-label', ''); — safe('nj-est-extra2', '');   safe('nj-est-extra2-label', ''); — safe('nj-est-extra3', '');   safe('nj-est-extra3-label', ''); — safe('nj-est-notes', ''); — safe('nj-est-office-remarks', ''); — safe('nj-est-slip-remarks', ''); — Reset carrier total display — const ct = document.getElementById('nj-carrier-total'); if(ct) ct.textContent = '$0.00'; — Clear helper rows — const hr = document.getElementById('nj-helper-rows'); if(hr) hr.innerHTML = ''; — Reset all selects to index 0 — ['nj-job-for','nj-est-tip-method','nj-est-valuation', — 'nj-est-fuel-mode','nj-est-tip-assign'].forEach(id => { — const el = document.getElementById(id); — if (el) el.selectedIndex = 0; — }); — try { toggleCarrierFields('nj'); } catch(e) {} — Apply defaults — const s2 = loadSettings(); — safe('nj-crew', s2.defaultCrew || '3'); — safe('nj-type', s2.defaultMove || 'local'); — safe('nj-date', prefillDate || getTodayStr()); — safe('nj-time', getSetting('defaultTime', '08:00')); — safe('nj-driver', currentUser.role === 'driver' ? (currentUser.name||'') : ''); — closeModal('modal-calday'); — openModal('modal-newjob'); — setTimeout(() => { try { applyJobDefaults('nj'); } catch(e) {} }, 50); — } — async function createJob() { — const client = document.getElementById('nj-client').value.trim(); — const date   = document.getElementById('nj-date').value; — const origin = document.getElementById('nj-origin').value.trim(); — const dest   = document.getElementById('nj-dest').value.trim(); — if (!client || !date) { notify('⚠ Client name and date required', true); return; } — Custom ID or auto-generate — const customId = (document.getElementById('nj-job-id')?.value||'').trim().toUpperCase(); — const autoId   = 'JOB-' + String((STATE.jobs||[]).length + 1).padStart(3,'0'); — const jobId    = customId || autoId; — if ((STATE.jobs||[]).find(j => j.id === jobId)) { — notify('⚠ Job ID ' + jobId + ' already exists', true); return; — } — Collect helpers — const helpers = []; — document.querySelectorAll('.nj-helper-name').forEach(inp => { — const name = inp.value.trim(); — if (!name) return; — const row = inp.closest('.nj-helper-row'); — helpers.push({ — name, — rate:    parseFloat(row?.querySelector('.nj-helper-rate')?.value) || 0, — payType: row?.querySelector('.nj-helper-type')?.value || 'hourly', — hours: 0, days: 1, workerType: '1099', paid: false — }); — }); — const g = id => (document.getElementById(id)?.value || ''); — const newJob = { — id:            jobId, — refNum:        g('nj-ref-num'), — client, — phone:         g('nj-phone'), — email:         g('nj-email'), — date, — time:          g('nj-time'), — endDate:       g('nj-end-date') || g('nj-delivery-end').slice(0,10) || '', — endTime:       g('nj-end-time'), — origin, — dest, — pickupStart:   g('nj-pickup-start'), — pickupEnd:     g('nj-pickup-end'), — pickupDriver:  g('nj-pickup-driver'), — deliveryStart: g('nj-delivery-start'), — deliveryEnd:   g('nj-delivery-end'), — deliveryDriver:g('nj-delivery-driver'), — driver:        g('nj-driver'), — driverUser:    currentUser.username || '', — crewSize:      parseInt(g('nj-crew')) || 3, — crewNames:     helpers.map(h => h.name), — amount:        parseFloat(g('nj-amount')) || 0, — cubicFt:       parseFloat(g('nj-cubic-ft-2') || g('nj-cubic-ft')) || 0, — weight:        parseFloat(g('nj-weight')) || 0, — linehaul:      parseFloat(g('nj-linehaul')) || 0, — priority:      g('nj-priority'), — followUp:      g('nj-followup'), — fadd:          g('nj-fadd'), — type:          g('nj-type') || 'local', — notes:         g('nj-notes'), — companyId:     COMPANY_ID, — status:        'pending', — gates:         { preload:false, loaded:false, delivered:false, payment:false }, — messages:      { crew:[], client:[] }, — photos:        [], — signatures:    [], — payroll:       { driver:0, helpers } — }; — try { — await db.collection(COL_JOBS).doc(newJob.id).set(newJob); — closeModal('modal-newjob'); — notify('✓ Job ' + newJob.id + ' created — ' + client); — setTimeout(() => renderCalendar(), 200); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('createJob error:', e); — } — }
| RECEIPTS | 15013-15889 | 12 | FIREBASE INIT, FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill, MODAL HELPERS, NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, NOTIFICATION, NOTIFICATION ENGINE, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, USER SETTINGS — stored in localStorage per user | CALENDAR, TABS, WEEK VIEW — time-based layout like Google Calendar
| PAYROLL LEDGER — Mark Paid modal | 18553-18832 | 12 | FIREBASE INIT, FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill, MODAL HELPERS, NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, NOTIFICATION, NOTIFICATION ENGINE, Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — }, PAYROLL LEDGER — Build running balance from jobs + payments, PAYROLL LEDGER — Running balance per crew member — Balance = earned - paid. When paid → resets to 0 → new cycle, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE | PAYROLL LEDGER — Running balance per crew member — Balance = earned - paid. When paid → resets to 0 → new cycle
| WEEK VIEW — time-based layout like Google Calendar | 13902-14209 | 13 | CALENDAR, JOB DETAIL MODAL, MODAL HELPERS, NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, RECEIPTS, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created, WAREHOUSE WORK LOGGING — Log warehouse work by employee — shows on calendar + payroll, function openNewJobModal(prefillDate) { — ── HARD RESET — every field to blank/zero ────────────────── — const safe = (id, val='') => { const el = document.getElementById(id); if(el) el.value = val; }; — Core job fields — safe('nj-client'); safe('nj-phone'); safe('nj-email'); — safe('nj-origin'); safe('nj-dest'); safe('nj-pickup-from'); — safe('nj-amount'); safe('nj-notes'); safe('nj-carrier'); — safe('nj-cubic-ft'); safe('nj-cubic-ft-2'); safe('nj-rate-per-cuft'); — safe('nj-weight'); safe('nj-linehaul'); — safe('nj-end-date'); safe('nj-end-time'); — safe('nj-pickup-start'); safe('nj-pickup-end'); — safe('nj-delivery-start'); safe('nj-delivery-end'); — safe('nj-pickup-driver'); safe('nj-delivery-driver'); — safe('nj-job-id'); safe('nj-ref-num'); — safe('nj-priority'); safe('nj-followup'); safe('nj-fadd'); — safe('nj-broker-ref'); safe('nj-carrier'); — ALL estimate fields — zero out completely — safe('nj-est-fuel', ''); — safe('nj-est-fuel-pct', '0'); — safe('nj-est-overload-cuft', ''); — safe('nj-est-stairs-origin', ''); — safe('nj-est-stairs-dest', ''); — safe('nj-est-longcarry-origin', ''); — safe('nj-est-longcarry-dest', ''); — safe('nj-est-shuttle', ''); — safe('nj-est-bulky', ''); — safe('nj-est-split', ''); — safe('nj-est-pgs', ''); — safe('nj-est-packing', ''); — safe('nj-est-storage-days', ''); — safe('nj-est-storage-rate', ''); — safe('nj-est-other', ''); — safe('nj-est-deposit', ''); — safe('nj-est-partial2', ''); — safe('nj-est-tip', ''); — safe('nj-est-discount', ''); — safe('nj-est-discount-label', ''); — safe('nj-est-others1', '');  safe('nj-est-others1-label', ''); — safe('nj-est-others2', '');  safe('nj-est-others2-label', ''); — safe('nj-est-others3', '');  safe('nj-est-others3-label', ''); — safe('nj-est-extra1', '');   safe('nj-est-extra1-label', ''); — safe('nj-est-extra2', '');   safe('nj-est-extra2-label', ''); — safe('nj-est-extra3', '');   safe('nj-est-extra3-label', ''); — safe('nj-est-notes', ''); — safe('nj-est-office-remarks', ''); — safe('nj-est-slip-remarks', ''); — Reset carrier total display — const ct = document.getElementById('nj-carrier-total'); if(ct) ct.textContent = '$0.00'; — Clear helper rows — const hr = document.getElementById('nj-helper-rows'); if(hr) hr.innerHTML = ''; — Reset all selects to index 0 — ['nj-job-for','nj-est-tip-method','nj-est-valuation', — 'nj-est-fuel-mode','nj-est-tip-assign'].forEach(id => { — const el = document.getElementById(id); — if (el) el.selectedIndex = 0; — }); — try { toggleCarrierFields('nj'); } catch(e) {} — Apply defaults — const s2 = loadSettings(); — safe('nj-crew', s2.defaultCrew || '3'); — safe('nj-type', s2.defaultMove || 'local'); — safe('nj-date', prefillDate || getTodayStr()); — safe('nj-time', getSetting('defaultTime', '08:00')); — safe('nj-driver', currentUser.role === 'driver' ? (currentUser.name||'') : ''); — closeModal('modal-calday'); — openModal('modal-newjob'); — setTimeout(() => { try { applyJobDefaults('nj'); } catch(e) {} }, 50); — } — async function createJob() { — const client = document.getElementById('nj-client').value.trim(); — const date   = document.getElementById('nj-date').value; — const origin = document.getElementById('nj-origin').value.trim(); — const dest   = document.getElementById('nj-dest').value.trim(); — if (!client || !date) { notify('⚠ Client name and date required', true); return; } — Custom ID or auto-generate — const customId = (document.getElementById('nj-job-id')?.value||'').trim().toUpperCase(); — const autoId   = 'JOB-' + String((STATE.jobs||[]).length + 1).padStart(3,'0'); — const jobId    = customId || autoId; — if ((STATE.jobs||[]).find(j => j.id === jobId)) { — notify('⚠ Job ID ' + jobId + ' already exists', true); return; — } — Collect helpers — const helpers = []; — document.querySelectorAll('.nj-helper-name').forEach(inp => { — const name = inp.value.trim(); — if (!name) return; — const row = inp.closest('.nj-helper-row'); — helpers.push({ — name, — rate:    parseFloat(row?.querySelector('.nj-helper-rate')?.value) || 0, — payType: row?.querySelector('.nj-helper-type')?.value || 'hourly', — hours: 0, days: 1, workerType: '1099', paid: false — }); — }); — const g = id => (document.getElementById(id)?.value || ''); — const newJob = { — id:            jobId, — refNum:        g('nj-ref-num'), — client, — phone:         g('nj-phone'), — email:         g('nj-email'), — date, — time:          g('nj-time'), — endDate:       g('nj-end-date') || g('nj-delivery-end').slice(0,10) || '', — endTime:       g('nj-end-time'), — origin, — dest, — pickupStart:   g('nj-pickup-start'), — pickupEnd:     g('nj-pickup-end'), — pickupDriver:  g('nj-pickup-driver'), — deliveryStart: g('nj-delivery-start'), — deliveryEnd:   g('nj-delivery-end'), — deliveryDriver:g('nj-delivery-driver'), — driver:        g('nj-driver'), — driverUser:    currentUser.username || '', — crewSize:      parseInt(g('nj-crew')) || 3, — crewNames:     helpers.map(h => h.name), — amount:        parseFloat(g('nj-amount')) || 0, — cubicFt:       parseFloat(g('nj-cubic-ft-2') || g('nj-cubic-ft')) || 0, — weight:        parseFloat(g('nj-weight')) || 0, — linehaul:      parseFloat(g('nj-linehaul')) || 0, — priority:      g('nj-priority'), — followUp:      g('nj-followup'), — fadd:          g('nj-fadd'), — type:          g('nj-type') || 'local', — notes:         g('nj-notes'), — companyId:     COMPANY_ID, — status:        'pending', — gates:         { preload:false, loaded:false, delivered:false, payment:false }, — messages:      { crew:[], client:[] }, — photos:        [], — signatures:    [], — payroll:       { driver:0, helpers } — }; — try { — await db.collection(COL_JOBS).doc(newJob.id).set(newJob); — closeModal('modal-newjob'); — notify('✓ Job ' + newJob.id + ' created — ' + client); — setTimeout(() => renderCalendar(), 200); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('createJob error:', e); — } — } | CALENDAR, MONTH VIEW — Google Calendar style with multi-day span bars, TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created
| function openNewJobModal(prefillDate) { — ── HARD RESET — every field to blank/zero ────────────────── — const safe = (id, val='') => { const el = document.getElementById(id); if(el) el.value = val; }; — Core job fields — safe('nj-client'); safe('nj-phone'); safe('nj-email'); — safe('nj-origin'); safe('nj-dest'); safe('nj-pickup-from'); — safe('nj-amount'); safe('nj-notes'); safe('nj-carrier'); — safe('nj-cubic-ft'); safe('nj-cubic-ft-2'); safe('nj-rate-per-cuft'); — safe('nj-weight'); safe('nj-linehaul'); — safe('nj-end-date'); safe('nj-end-time'); — safe('nj-pickup-start'); safe('nj-pickup-end'); — safe('nj-delivery-start'); safe('nj-delivery-end'); — safe('nj-pickup-driver'); safe('nj-delivery-driver'); — safe('nj-job-id'); safe('nj-ref-num'); — safe('nj-priority'); safe('nj-followup'); safe('nj-fadd'); — safe('nj-broker-ref'); safe('nj-carrier'); — ALL estimate fields — zero out completely — safe('nj-est-fuel', ''); — safe('nj-est-fuel-pct', '0'); — safe('nj-est-overload-cuft', ''); — safe('nj-est-stairs-origin', ''); — safe('nj-est-stairs-dest', ''); — safe('nj-est-longcarry-origin', ''); — safe('nj-est-longcarry-dest', ''); — safe('nj-est-shuttle', ''); — safe('nj-est-bulky', ''); — safe('nj-est-split', ''); — safe('nj-est-pgs', ''); — safe('nj-est-packing', ''); — safe('nj-est-storage-days', ''); — safe('nj-est-storage-rate', ''); — safe('nj-est-other', ''); — safe('nj-est-deposit', ''); — safe('nj-est-partial2', ''); — safe('nj-est-tip', ''); — safe('nj-est-discount', ''); — safe('nj-est-discount-label', ''); — safe('nj-est-others1', '');  safe('nj-est-others1-label', ''); — safe('nj-est-others2', '');  safe('nj-est-others2-label', ''); — safe('nj-est-others3', '');  safe('nj-est-others3-label', ''); — safe('nj-est-extra1', '');   safe('nj-est-extra1-label', ''); — safe('nj-est-extra2', '');   safe('nj-est-extra2-label', ''); — safe('nj-est-extra3', '');   safe('nj-est-extra3-label', ''); — safe('nj-est-notes', ''); — safe('nj-est-office-remarks', ''); — safe('nj-est-slip-remarks', ''); — Reset carrier total display — const ct = document.getElementById('nj-carrier-total'); if(ct) ct.textContent = '$0.00'; — Clear helper rows — const hr = document.getElementById('nj-helper-rows'); if(hr) hr.innerHTML = ''; — Reset all selects to index 0 — ['nj-job-for','nj-est-tip-method','nj-est-valuation', — 'nj-est-fuel-mode','nj-est-tip-assign'].forEach(id => { — const el = document.getElementById(id); — if (el) el.selectedIndex = 0; — }); — try { toggleCarrierFields('nj'); } catch(e) {} — Apply defaults — const s2 = loadSettings(); — safe('nj-crew', s2.defaultCrew || '3'); — safe('nj-type', s2.defaultMove || 'local'); — safe('nj-date', prefillDate || getTodayStr()); — safe('nj-time', getSetting('defaultTime', '08:00')); — safe('nj-driver', currentUser.role === 'driver' ? (currentUser.name||'') : ''); — closeModal('modal-calday'); — openModal('modal-newjob'); — setTimeout(() => { try { applyJobDefaults('nj'); } catch(e) {} }, 50); — } — async function createJob() { — const client = document.getElementById('nj-client').value.trim(); — const date   = document.getElementById('nj-date').value; — const origin = document.getElementById('nj-origin').value.trim(); — const dest   = document.getElementById('nj-dest').value.trim(); — if (!client || !date) { notify('⚠ Client name and date required', true); return; } — Custom ID or auto-generate — const customId = (document.getElementById('nj-job-id')?.value||'').trim().toUpperCase(); — const autoId   = 'JOB-' + String((STATE.jobs||[]).length + 1).padStart(3,'0'); — const jobId    = customId || autoId; — if ((STATE.jobs||[]).find(j => j.id === jobId)) { — notify('⚠ Job ID ' + jobId + ' already exists', true); return; — } — Collect helpers — const helpers = []; — document.querySelectorAll('.nj-helper-name').forEach(inp => { — const name = inp.value.trim(); — if (!name) return; — const row = inp.closest('.nj-helper-row'); — helpers.push({ — name, — rate:    parseFloat(row?.querySelector('.nj-helper-rate')?.value) || 0, — payType: row?.querySelector('.nj-helper-type')?.value || 'hourly', — hours: 0, days: 1, workerType: '1099', paid: false — }); — }); — const g = id => (document.getElementById(id)?.value || ''); — const newJob = { — id:            jobId, — refNum:        g('nj-ref-num'), — client, — phone:         g('nj-phone'), — email:         g('nj-email'), — date, — time:          g('nj-time'), — endDate:       g('nj-end-date') || g('nj-delivery-end').slice(0,10) || '', — endTime:       g('nj-end-time'), — origin, — dest, — pickupStart:   g('nj-pickup-start'), — pickupEnd:     g('nj-pickup-end'), — pickupDriver:  g('nj-pickup-driver'), — deliveryStart: g('nj-delivery-start'), — deliveryEnd:   g('nj-delivery-end'), — deliveryDriver:g('nj-delivery-driver'), — driver:        g('nj-driver'), — driverUser:    currentUser.username || '', — crewSize:      parseInt(g('nj-crew')) || 3, — crewNames:     helpers.map(h => h.name), — amount:        parseFloat(g('nj-amount')) || 0, — cubicFt:       parseFloat(g('nj-cubic-ft-2') || g('nj-cubic-ft')) || 0, — weight:        parseFloat(g('nj-weight')) || 0, — linehaul:      parseFloat(g('nj-linehaul')) || 0, — priority:      g('nj-priority'), — followUp:      g('nj-followup'), — fadd:          g('nj-fadd'), — type:          g('nj-type') || 'local', — notes:         g('nj-notes'), — companyId:     COMPANY_ID, — status:        'pending', — gates:         { preload:false, loaded:false, delivered:false, payment:false }, — messages:      { crew:[], client:[] }, — photos:        [], — signatures:    [], — payroll:       { driver:0, helpers } — }; — try { — await db.collection(COL_JOBS).doc(newJob.id).set(newJob); — closeModal('modal-newjob'); — notify('✓ Job ' + newJob.id + ' created — ' + client); — setTimeout(() => renderCalendar(), 200); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('createJob error:', e); — } — } | 16615-16772 | 13 | CALENDAR, CARRIER CUBIC FEET HELPERS, FIREBASE INIT, FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill, MODAL HELPERS, NOTIFICATION, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, USER SETTINGS — stored in localStorage per user | CALENDAR, DASHBOARD, LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool, WEEK VIEW — time-based layout like Google Calendar
| PAYROLL CALCULATION ENGINE v2 — pay + commission + tax | 13195-13242 | 14 | — | 1099 / W2 YEAR-END TAX EXPORT — Aggregates all payments per contractor/employee for the year — Generates: on-screen summary + CSV export + PDF report, CREW LOG HELPERS — extracted to avoid nested template literal — Chrome syntax errors from 4+ levels of backtick nesting, DASHBOARD, FINANCIALS, MONTH VIEW — Google Calendar style with multi-day span bars, Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — }, P&L ACCOUNTING MODULE — Kills QuickBooks — Chart of accounts, monthly P&L, period reports, export, PAY SCHEDULE & UNPAID CREW CHECKER, PAYROLL LEDGER — Build running balance from jobs + payments, TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate, function exportPayrollCSV() { — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — const addPerson = (name, role, gross, commission, workerType) => { — if (!name) return; — if (!payMap[name]) payMap[name] = { name, role, jobs: 0, gross: 0, commission: 0, workerType }; — payMap[name].jobs++; — payMap[name].gross      += gross; — payMap[name].commission += commission; — }; — addPerson(j.driver, 'Driver', j.payroll.driver||0, j.payroll.driverCommission||0, j.payroll.driverWorkerType||'1099'); — (j.payroll.helpers||[]).forEach(h => addPerson(h.name, 'Helper', calcPay(h), h.commission||0, h.workerType||'1099')); — }); — const rows = [['Name','Role','Worker Type','Jobs','Base Pay','Commission','Total Gross','Est Tax','Est Take-Home']]; — Object.values(payMap).forEach(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — rows.push([p.name, p.role, p.workerType, p.jobs, p.gross.toFixed(2), p.commission.toFixed(2), — gross.toFixed(2), tax.totalEstTax.toFixed(2), tax.takeHome.toFixed(2)]); — }); — downloadCSV(rows, 'jpgsystems-payroll-' + getTodayStr() + '.csv'); — notify('Payroll CSV exported ✓'); — } — function exportJobsCSV() { — const rows = [['Job ID','Client','Date','Time','Driver','Type','Status','Amount','Gates Passed','Origin','Destination']]; — STATE.jobs.forEach(j => { — const gates = Object.values(j.gates||{}).filter(Boolean).length; — rows.push([j.id, j.client, j.date, j.time||'', j.driver||'', j.type||'', — j.status, (j.amount||0).toFixed(2), gates + '/' + Object.keys(j.gates||{}).length, — j.origin||'', j.dest||'']); — }); — downloadCSV(rows, 'jpgsystems-jobs-' + getTodayStr() + '.csv'); — notify('Jobs CSV exported ✓'); — } — function exportReceiptsCSV() { — const rows = [['Receipt ID','Job ID','Driver','Date','Type','Amount','Status','Notes']]; — STATE.receipts.forEach(r => { — rows.push([r.id, r.jobId, r.driver||'', r.date||'', r.type||'', — (r.amount||0).toFixed(2), r.approved ? 'APPROVED' : r.rejected ? 'REJECTED' : 'PENDING', r.notes||'']); — }); — downloadCSV(rows, 'jpgsystems-receipts-' + getTodayStr() + '.csv'); — notify('Receipts CSV exported ✓'); — } — function downloadCSV(rows, filename) { — const csv  = rows.map(r => r.map(c => '"' + String(c).replace(/"/g,'""') + '"').join(',')).join('\n'); — const blob = new Blob([csv], { type: 'text/csv' }); — const url  = URL.createObjectURL(blob); — const a    = document.createElement('a'); — a.href     = url; — a.download = filename; — a.click(); — URL.revokeObjectURL(url); — } — Add export buttons to financials tab — function renderExportBar() { — return ` — <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;align-self:center">EXPORT:</div> — <button class="btn-sm" onclick="exportJobsCSV()">⬇ Jobs CSV</button> — <button class="btn-sm" onclick="exportReceiptsCSV()">⬇ Receipts CSV</button> — <button class="btn-sm" onclick="exportPayrollCSV()">⬇ Payroll CSV</button> — <button class="btn-sm" onclick="renderPayrollSummary()">💵 Payroll Report</button> — </div>`; — }, function renderMargins() { — const content = document.getElementById('main-content'); — Build margin data per job — const jobMargins = STATE.jobs — .filter(j => j.amount > 0 || j.payroll) — .map(j => { — const revenue   = j.amount || 0; — const driverAccBonus = parseFloat(j.payroll?.accessorialBonus) || 0; — const crewCost  = (j.payroll?.driver || 0) + — driverAccBonus + — (j.payroll?.helpers || []).reduce((s,h) => s + calcPay(h), 0) + — (j.payroll?.driverCommission || 0); — Company revenue excludes driver accessorial bonuses (passed through to driver) — const expenses  = STATE.receipts — .filter(r => r.jobId === j.id && r.approved !== false) — .reduce((s,r) => s + (r.amount||0), 0); — const net       = revenue - crewCost - expenses; — const margin    = revenue > 0 ? (net / revenue * 100) : 0; — return { job: j, revenue, crewCost, expenses, net, margin }; — }) — .sort((a,b) => { — Default: worst margin first (ascending) — const mA = a.revenue > 0 ? a.profit/a.revenue : 0; — const mB = b.revenue > 0 ? b.profit/b.revenue : 0; — return mA - mB; — }); — Sort control — if (window._marginSort === 'date') jobMargins.sort((a,b) => b.job.date > a.job.date ? 1 : -1); — else if (window._marginSort === 'revenue') jobMargins.sort((a,b) => b.revenue - a.revenue); — ── Margin intelligence ── — const overpayJobs   = jobMargins.filter(m => m.revenue > 0 && (m.crewCost/m.revenue) > 0.40); — const lowProfitJobs = jobMargins.filter(m => m.revenue > 0 && (m.profit/m.revenue) < 0.20); — const bestJob       = [...jobMargins].sort((a,b) => (b.profit/b.revenue||0)-(a.profit/a.revenue||0))[0]; — const worstJob      = jobMargins[0]; // already sorted worst first — Overall stats — const totalRevenue  = jobMargins.reduce((s,m) => s + m.revenue, 0); — const totalCrew     = jobMargins.reduce((s,m) => s + m.crewCost, 0); — const totalExpenses = jobMargins.reduce((s,m) => s + m.expenses, 0); — const totalNet      = jobMargins.reduce((s,m) => s + m.net, 0); — const avgMargin     = totalRevenue > 0 ? (totalNet / totalRevenue * 100) : 0; — const profitable    = jobMargins.filter(m => m.net > 0).length; — const losing        = jobMargins.filter(m => m.net < 0).length; — Best and worst job — const best  = jobMargins.reduce((a,b) => b.margin > a.margin ? b : a, jobMargins[0]); — const worst = jobMargins.reduce((a,b) => b.margin < a.margin ? b : a, jobMargins[0]); — const marginColor = (n) => n >= 40 ? 'var(--green)' : n >= 20 ? 'var(--amber)' : n >= 0 ? '#ff8c00' : 'var(--red)'; — const marginBg    = (n) => n >= 40 ? 'rgba(0,255,65,0.08)' : n >= 20 ? 'rgba(255,170,0,0.08)' : n >= 0 ? 'rgba(255,140,0,0.08)' : 'rgba(255,51,51,0.08)'; — let html = ` — ${overpayJobs.length ? `<div style="background:rgba(255,30,30,0.06);border:1px solid rgba(255,30,30,0.3);border-radius:6px;padding:10px 14px;margin-bottom:12px;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px"> — <div> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;font-weight:700">⚠ LABOR OVERPAY ALERT</div> — <div style="font-size:12px;color:var(--white);margin-top:3px">${overpayJobs.length} job${overpayJobs.length>1?'s':''} with crew cost >40% of revenue</div> — <div style="font-size:11px;color:var(--gray);margin-top:2px">${overpayJobs.slice(0,3).map(m=>m.job.client+' ('+Math.round(m.crewCost/m.revenue*100)+'%)').join(' · ')}</div> — </div> — </div>` : ''} — <div style="display:flex;gap:8px;margin-bottom:12px;flex-wrap:wrap"> — <span style="font-family:var(--font-mono);font-size:10px;color:var(--gray);align-self:center">Sort:</span> — ${['worst:⬇ Worst Margin','revenue:$ Revenue','date:📅 Date'].map(o=>{const[v,l]=o.split(':');return `<button onclick="window._marginSort='${v}';renderMargins()" style="padding:5px 12px;font-family:var(--font-mono);font-size:10px;border-radius:3px;cursor:pointer;background:${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--green)':'transparent'};color:${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--navy)':'var(--gray)'};border:1px solid ${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--green)':'var(--gray2)'}">${l}</button>`;}).join('')} — </div> — <div class="section-header"> — <div> — <div class="section-title">📈 JOB MARGIN DASHBOARD</div> — <div class="section-sub">Revenue · Crew Cost · Expenses · Net per Job</div> — </div> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportJobsCSV()">⬇ EXPORT</button> — </div> — <!-- OVERALL STATS --> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(130px,1fr));gap:8px;margin-bottom:16px"> — <div class="stat-box"> — <div class="stat-label">Total Revenue</div> — <div class="stat-value">$${totalRevenue.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${jobMargins.length} jobs</div> — </div> — <div class="stat-box"> — <div class="stat-label">Crew Cost</div> — <div class="stat-value red">$${totalCrew.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${totalRevenue>0?(totalCrew/totalRevenue*100).toFixed(1):'0'}% of revenue</div> — </div> — <div class="stat-box"> — <div class="stat-label">Expenses</div> — <div class="stat-value red">$${totalExpenses.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${totalRevenue>0?(totalExpenses/totalRevenue*100).toFixed(1):'0'}% of revenue</div> — </div> — <div class="stat-box" style="border-color:${marginColor(avgMargin)}"> — <div class="stat-label">Net Profit</div> — <div class="stat-value" style="color:${marginColor(avgMargin)}">$${totalNet.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px;color:${marginColor(avgMargin)}">${avgMargin.toFixed(1)}% avg margin</div> — </div> — <div class="stat-box"> — <div class="stat-label">Profitable Jobs</div> — <div class="stat-value" style="color:var(--green)">${profitable}</div> — <div class="info-text" style="margin-top:4px;color:var(--red)">${losing} losing</div> — </div> — </div> — <!-- MARGIN BREAKDOWN BAR --> — ${totalRevenue > 0 ? ` — <div class="card" style="margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;margin-bottom:10px">REVENUE BREAKDOWN</div> — <div style="height:32px;border-radius:4px;overflow:hidden;display:flex;margin-bottom:8px"> — <div style="width:${(totalCrew/totalRevenue*100).toFixed(1)}%;background:rgba(255,51,51,0.7);transition:width 0.5s" title="Crew Cost"></div> — <div style="width:${(totalExpenses/totalRevenue*100).toFixed(1)}%;background:rgba(255,140,0,0.7);transition:width 0.5s" title="Expenses"></div> — <div style="flex:1;background:rgba(0,255,65,0.4);transition:width 0.5s" title="Net Profit"></div> — </div> — <div style="display:flex;gap:16px;flex-wrap:wrap"> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(255,51,51,0.7);border-radius:2px"></div><span class="info-text">Crew ${(totalCrew/totalRevenue*100).toFixed(1)}%</span></div> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(255,140,0,0.7);border-radius:2px"></div><span class="info-text">Expenses ${(totalExpenses/totalRevenue*100).toFixed(1)}%</span></div> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(0,255,65,0.4);border-radius:2px"></div><span class="info-text">Net ${avgMargin.toFixed(1)}%</span></div> — </div> — </div>` : ''} — <!-- BEST / WORST --> — ${jobMargins.length >= 2 ? ` — <div class="grid-2" style="margin-bottom:16px"> — ${best ? `<div class="card" style="border-color:var(--green3)"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--green);letter-spacing:2px;margin-bottom:6px">🏆 BEST MARGIN JOB</div> — <div style="font-size:15px;font-weight:700">${best.job.client} <span class="tag">${best.job.id}</span></div> — <div class="info-text">${best.job.date}</div> — <div style="font-family:var(--font-mono);font-size:22px;color:var(--green);margin-top:6px">${best.margin.toFixed(1)}%</div> — <div class="info-text">$${best.net.toFixed(2)} net on $${best.revenue.toLocaleString()}</div> — </div>` : ''} — ${worst ? `<div class="card" style="border-color:${marginColor(worst.margin)}"> — <div style="font-family:var(--font-mono);font-size:9px;color:${marginColor(worst.margin)};letter-spacing:2px;margin-bottom:6px">${worst.net < 0 ? '⚠ LOSING JOB' : '📉 LOWEST MARGIN'}</div> — <div style="font-size:15px;font-weight:700">${worst.job.client} <span class="tag">${worst.job.id}</span></div> — <div class="info-text">${worst.job.date}</div> — <div style="font-family:var(--font-mono);font-size:22px;color:${marginColor(worst.margin)};margin-top:6px">${worst.margin.toFixed(1)}%</div> — <div class="info-text">$${worst.net.toFixed(2)} net on $${worst.revenue.toLocaleString()}</div> — </div>` : ''} — </div>` : ''} — <!-- PER-JOB TABLE --> — <div class="card" style="padding:0;overflow:hidden"> — <div style="padding:12px 16px;border-bottom:1px solid var(--gray2)"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--green);letter-spacing:2px">JOB BY JOB BREAKDOWN</div> — </div> — ${jobMargins.length ? ` — <div style="overflow-x:auto"> — <table style="min-width:700px"> — <thead><tr> — <th>Job</th> — <th>Client</th> — <th>Date</th> — <th style="text-align:right">Revenue</th> — <th style="text-align:right">Crew Cost</th> — <th style="text-align:right">Expenses</th> — <th style="text-align:right">Net</th> — <th style="text-align:center">Margin</th> — <th>Status</th> — </tr></thead> — <tbody> — ${jobMargins.map(m => ` — <tr style="cursor:pointer;background:${marginBg(m.margin)}" onclick="switchTab('jobs');setTimeout(()=>openJobDetail('${m.job.id}'),300)"> — <td><span class="tag">${m.job.id}</span></td> — <td style="font-weight:600">${m.job.client}</td> — <td class="info-text">${m.job.date}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${m.revenue.toLocaleString()}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${m.crewCost.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:#ff8c00">$${m.expenses.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;font-weight:700;color:${marginColor(m.margin)}"> — ${m.net >= 0 ? '+' : ''}$${m.net.toFixed(2)} — </td> — <td style="text-align:center"> — <div style="display:inline-flex;align-items:center;justify-content:center;width:52px;height:24px;border-radius:12px;background:${marginBg(m.margin)};border:1px solid ${marginColor(m.margin)};font-family:var(--font-mono);font-size:10px;font-weight:700;color:${marginColor(m.margin)}"> — ${m.margin.toFixed(0)}% — </div> — </td> — <td><span class="status-badge status-${m.job.status}">${m.job.status}</span></td> — </tr>`).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="3" style="font-family:var(--font-mono);color:var(--green);font-size:11px">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${totalRevenue.toLocaleString()}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${totalCrew.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:#ff8c00">$${totalExpenses.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;font-weight:700;color:${marginColor(avgMargin)}"> — ${totalNet >= 0 ? '+' : ''}$${totalNet.toFixed(2)} — </td> — <td style="text-align:center"> — <div style="display:inline-flex;align-items:center;justify-content:center;width:52px;height:24px;border-radius:12px;background:${marginBg(avgMargin)};border:1px solid ${marginColor(avgMargin)};font-family:var(--font-mono);font-size:10px;font-weight:700;color:${marginColor(avgMargin)}"> — ${avgMargin.toFixed(0)}% — </div> — </td> — <td></td> — </tr> — </tbody> — </table> — </div>` : ` — <div style="text-align:center;padding:48px"> — <div style="font-size:40px;margin-bottom:12px">📈</div> — <p class="info-text">No job data yet. Create jobs with quoted amounts and crew logs to see margins.</p> — </div>`} — </div> — <!-- MARGIN LEGEND --> — <div style="display:flex;gap:16px;padding:12px 4px;flex-wrap:wrap;align-items:center"> — <span class="info-text">Margin health:</span> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(0,255,65,0.2);border:1px solid var(--green);border-radius:2px"></div><span class="info-text" style="color:var(--green)">40%+ Excellent</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,170,0,0.2);border:1px solid var(--amber);border-radius:2px"></div><span class="info-text" style="color:var(--amber)">20–39% Good</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,140,0,0.2);border:1px solid #ff8c00;border-radius:2px"></div><span class="info-text" style="color:#ff8c00">0–19% Thin</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,51,51,0.15);border:1px solid var(--red);border-radius:2px"></div><span class="info-text" style="color:var(--red)">Negative — Losing money</span></div> — </div>`; — content.innerHTML = html; — }, function renderMyPay() { — const content  = document.getElementById('main-content'); — const role     = currentUser.role; — const isHelper = role === 'helper'; — Collect all pay entries for this person — const myJobs = STATE.jobs.filter(j => { — if (role === 'driver') return nameIncludes(j.driver, currentUser.name); — if (role === 'helper') return (j.payroll?.helpers||[]).some(h => nameIncludes(h.name, currentUser.name)); — return false; — }); — let totalGross      = 0; — let totalCommission = 0; — let totalTax        = 0; — let totalTakeHome   = 0; — const payRows       = []; — myJobs.forEach(j => { — let gross = 0, commission = 0, workerType = '1099', payDetails = ''; — if (role === 'driver') { — gross       = j.payroll?.driver || 0; — commission  = j.payroll?.driverCommission || 0; — workerType  = j.payroll?.driverWorkerType || '1099'; — payDetails  = payLabel({ payType: j.payroll?.driverPayType||'flat', rate: j.payroll?.driverRate||gross, hours: j.payroll?.driverHours||0, days: j.payroll?.driverDays||1 }); — } else { — const helper = (j.payroll?.helpers||[]).find(h => — h.name?.toLowerCase().includes(currentUser.name.toLowerCase()) || — (currentUser.name.toLowerCase().split(' ')[0] && h.name?.toLowerCase().includes(currentUser.name.toLowerCase().split(' ')[0])) — ); — if (helper) { — gross      = calcPay(helper); — commission = helper.commission || 0; — workerType = helper.workerType || '1099'; — payDetails = payLabel(helper); — } — } — if (gross === 0) return; — const tax = calcTax(gross, workerType); — totalGross      += gross; — totalCommission += commission; — totalTax        += tax.totalEstTax; — totalTakeHome   += tax.takeHome; — payRows.push({ job: j, gross, commission, workerType, payDetails, tax }); — }); — let html = ` — <div class="section-header"> — <div> — <div class="section-title">📊 MY PAY SUMMARY</div> — <div class="section-sub">${currentUser.name} · ${role.toUpperCase()} · ${payRows.length} job(s)</div> — </div> — </div> — <!-- YTD SUMMARY STATS --> — <div class="grid-4" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Gross Earned</div> — <div class="stat-value">$${totalGross.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Commission</div> — <div class="stat-value" style="color:var(--amber)">$${totalCommission.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Est. Tax Owed</div> — <div class="stat-value red">$${totalTax.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Est. Take-Home</div> — <div class="stat-value">$${totalTakeHome.toFixed(2)}</div> — </div> — </div> — <!-- TAX ADVISORY BANNER --> — <div class="card" style="border-color:#664400;background:rgba(255,170,0,0.05);margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:11px;color:var(--amber);letter-spacing:2px;margin-bottom:8px">⚠ TAX ADVISORY</div> — <div class="info-text" style="line-height:1.8"> — ${totalGross > 0 ? `Based on your earnings of <strong style="color:var(--white)">$${totalGross.toFixed(2)}</strong>, you should set aside approximately <strong style="color:var(--red)">$${totalTax.toFixed(2)}</strong> (${(totalTax/totalGross*100).toFixed(1)}%) for taxes.` : 'No earnings recorded yet.'} — ${totalGross >= 600 ? ' You will likely receive a 1099 form if classified as a contractor.' : ''} — If you are a <strong style="color:var(--white)">1099 contractor</strong>, you are responsible for paying your own taxes quarterly (April, June, September, January). Consider setting aside <strong style="color:var(--red)">${totalGross > 0 ? (totalTax/totalGross*100).toFixed(0) : '~25'}%</strong> of every paycheck. — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div style="margin-top:4px"> — ${payRows.length ? payRows.map(row => ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:10px"> — <div> — <div style="display:flex;gap:8px;align-items:center;margin-bottom:4px"> — <span class="tag">${row.job.id}</span> — <span class="status-badge status-${row.job.status}">${row.job.status}</span> — <span class="tag">${row.workerType}</span> — </div> — <div style="font-size:14px;font-weight:600">${row.job.client}</div> — <div class="info-text">${row.job.date} · ${row.payDetails}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:20px;color:var(--green)">$${row.gross.toFixed(2)}</div> — ${row.commission > 0 ? `<div style="font-family:var(--font-mono);font-size:12px;color:var(--amber)">+ $${row.commission.toFixed(2)} commission</div>` : ''} — </div> — </div> — <!-- Tax breakdown per job --> — <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;margin-bottom:8px"> — <div class="stat-box" style="padding:10px"> — <div class="stat-label" style="font-size:9px">Gross Pay</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--white)">$${row.gross.toFixed(2)}</div> — </div> — <div class="stat-box" style="padding:10px"> — <div class="stat-label" style="font-size:9px">Est. Tax</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--red)">−$${row.tax.totalEstTax.toFixed(2)}</div> — </div> — <div class="stat-box" style="padding:10px;border-color:var(--green3)"> — <div class="stat-label" style="font-size:9px">Take-Home</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--green)">$${row.tax.takeHome.toFixed(2)}</div> — </div> — </div> — <!-- Detailed tax line items --> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;font-family:var(--font-mono);font-size:10px"> — ${row.tax.selfEmployTax ? `<div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>Self-Employment Tax (15.3%)</span><span style="color:var(--red)">−$${row.tax.selfEmployTax.toFixed(2)}</span></div>` : ''} — ${row.tax.ficaTax ? `<div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>FICA Employee (7.65%)</span><span style="color:var(--red)">−$${row.tax.ficaTax.toFixed(2)}</span></div>` : ''} — <div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>Federal Income (~${(row.tax.federalRate*100).toFixed(0)}%)</span><span style="color:var(--red)">−$${row.tax.federalTax.toFixed(2)}</span></div> — <div style="display:flex;justify-content:space-between;color:var(--gray)"><span>Georgia State (5.5%)</span><span style="color:var(--red)">−$${row.tax.stateTax.toFixed(2)}</span></div> — </div> — </div>`).join('') : ` — <div class="card" style="text-align:center;padding:40px"> — <p class="info-text">No pay records found for ${currentUser.name}.</p> — <p class="info-text" style="margin-top:8px">Your earnings will appear here once jobs are assigned and crew logs are saved.</p> — </div>`} — </div> — <div style="padding:12px;font-family:var(--font-mono);font-size:9px;color:var(--gray);line-height:1.8;border-top:1px solid var(--gray2);margin-top:8px"> — ⚠ DISCLAIMER: Tax estimates are for informational purposes only based on 2024 US federal rates and Georgia state rates. This is not tax advice. Individual circumstances vary. Consult a licensed tax professional or CPA for filing guidance. JPG Systems / JPG Ventures LLC is not responsible for tax obligations of independent contractors. — </div>`; — content.innerHTML = html; — }, function renderPayrollSummary() { — This is the office-facing payroll output — all crew, all jobs — const content = document.getElementById('main-content'); — const role    = currentUser.role; — if (role !== 'office') { switchTab('mypay'); return; } — Aggregate per person — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — Driver — const dKey = j.driver; — if (!payMap[dKey]) payMap[dKey] = { name: dKey, role: 'Driver', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[dKey].jobs++; — payMap[dKey].gross      += j.payroll.driver || 0; — payMap[dKey].commission += j.payroll.driverCommission || 0; — payMap[dKey].workerType  = j.payroll.driverWorkerType || '1099'; — Helpers — (j.payroll.helpers||[]).forEach(h => { — if (!h.name) return; — const hKey = h.name; — if (!payMap[hKey]) payMap[hKey] = { name: hKey, role: 'Helper', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[hKey].jobs++; — payMap[hKey].gross      += calcPay(h); — payMap[hKey].commission += h.commission || 0; — payMap[hKey].workerType  = h.workerType || '1099'; — }); — }); — const people    = Object.values(payMap).sort((a,b) => b.gross - a.gross); — const totalGross = people.reduce((s,p) => s + p.gross + p.commission, 0); — let html = ` — <div class="section-header"> — <div> — <div class="section-title">💵 PAYROLL SUMMARY</div> — <div class="section-sub">All crew · All jobs · JPG Systems</div> — </div> — <div style="display:flex;gap:8px"> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportPayrollCSV()">⬇ EXPORT CSV</button> — </div> — </div> — <div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Total Crew</div><div class="stat-value">${people.length}</div></div> — <div class="stat-box"><div class="stat-label">Total Payroll</div><div class="stat-value amber">$${totalGross.toFixed(2)}</div></div> — <div class="stat-box"><div class="stat-label">Jobs w/ Payroll</div><div class="stat-value">${STATE.jobs.filter(j=>j.payroll).length}</div></div> — </div> — <div class="card"> — <table> — <thead><tr> — <th>Name</th><th>Role</th><th>Type</th><th>Jobs</th> — <th style="text-align:right">Base Pay</th> — <th style="text-align:right">Commission</th> — <th style="text-align:right">Total Gross</th> — <th style="text-align:right">Est. Tax</th> — <th style="text-align:right">Take-Home</th> — </tr></thead> — <tbody> — ${people.map(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — return `<tr> — <td style="font-weight:600">${p.name}</td> — <td><span class="tag">${p.role}</span></td> — <td><span class="tag">${p.workerType}</span></td> — <td style="font-family:var(--font-mono);text-align:center">${p.jobs}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${p.gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">${p.commission>0?'$'+p.commission.toFixed(2):'—'}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green);font-weight:700">$${gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${tax.totalEstTax.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${tax.takeHome.toFixed(2)}</td> — </tr>`; — }).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="4" style="font-family:var(--font-mono);color:var(--green)">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${people.reduce((s,p)=>s+p.gross,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">$${people.reduce((s,p)=>s+p.commission,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${totalGross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).totalEstTax,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).takeHome,0).toFixed(2)}</td> — </tr> — </tbody> — </table> — </div>`; — content.innerHTML = html; — }
| SETTINGS TAB | 18833-19438 | 14 | 1099 / W2 YEAR-END TAX EXPORT — Aggregates all payments per contractor/employee for the year — Generates: on-screen summary + CSV export + PDF report, BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore, NOTIFICATION, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, ROLE SWITCHER — instant role switch without logout, SETTINGS ACTION FUNCTIONS, SOUND ENGINE — Web Audio API, no external files needed, STATE & STORAGE, STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live, USER SETTINGS — stored in localStorage per user | ROLE SWITCHER — instant role switch without logout, SETTINGS ACTION FUNCTIONS, STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live, TABS
| LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 10254-10909 | 15 | CLAIMS DEFENSE AI — v51 — Pre-load damage detection using Claude vision — When driver takes a photo at pre-load gate, AI scans for — pre-existing damage, flags it, embeds in BOL as evidence. — Protects moving company from false damage claims., FIREBASE INIT, FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill, MODAL HELPERS, NOTIFICATION, PHOTO VIEWER, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, function openNewJobModal(prefillDate) { — ── HARD RESET — every field to blank/zero ────────────────── — const safe = (id, val='') => { const el = document.getElementById(id); if(el) el.value = val; }; — Core job fields — safe('nj-client'); safe('nj-phone'); safe('nj-email'); — safe('nj-origin'); safe('nj-dest'); safe('nj-pickup-from'); — safe('nj-amount'); safe('nj-notes'); safe('nj-carrier'); — safe('nj-cubic-ft'); safe('nj-cubic-ft-2'); safe('nj-rate-per-cuft'); — safe('nj-weight'); safe('nj-linehaul'); — safe('nj-end-date'); safe('nj-end-time'); — safe('nj-pickup-start'); safe('nj-pickup-end'); — safe('nj-delivery-start'); safe('nj-delivery-end'); — safe('nj-pickup-driver'); safe('nj-delivery-driver'); — safe('nj-job-id'); safe('nj-ref-num'); — safe('nj-priority'); safe('nj-followup'); safe('nj-fadd'); — safe('nj-broker-ref'); safe('nj-carrier'); — ALL estimate fields — zero out completely — safe('nj-est-fuel', ''); — safe('nj-est-fuel-pct', '0'); — safe('nj-est-overload-cuft', ''); — safe('nj-est-stairs-origin', ''); — safe('nj-est-stairs-dest', ''); — safe('nj-est-longcarry-origin', ''); — safe('nj-est-longcarry-dest', ''); — safe('nj-est-shuttle', ''); — safe('nj-est-bulky', ''); — safe('nj-est-split', ''); — safe('nj-est-pgs', ''); — safe('nj-est-packing', ''); — safe('nj-est-storage-days', ''); — safe('nj-est-storage-rate', ''); — safe('nj-est-other', ''); — safe('nj-est-deposit', ''); — safe('nj-est-partial2', ''); — safe('nj-est-tip', ''); — safe('nj-est-discount', ''); — safe('nj-est-discount-label', ''); — safe('nj-est-others1', '');  safe('nj-est-others1-label', ''); — safe('nj-est-others2', '');  safe('nj-est-others2-label', ''); — safe('nj-est-others3', '');  safe('nj-est-others3-label', ''); — safe('nj-est-extra1', '');   safe('nj-est-extra1-label', ''); — safe('nj-est-extra2', '');   safe('nj-est-extra2-label', ''); — safe('nj-est-extra3', '');   safe('nj-est-extra3-label', ''); — safe('nj-est-notes', ''); — safe('nj-est-office-remarks', ''); — safe('nj-est-slip-remarks', ''); — Reset carrier total display — const ct = document.getElementById('nj-carrier-total'); if(ct) ct.textContent = '$0.00'; — Clear helper rows — const hr = document.getElementById('nj-helper-rows'); if(hr) hr.innerHTML = ''; — Reset all selects to index 0 — ['nj-job-for','nj-est-tip-method','nj-est-valuation', — 'nj-est-fuel-mode','nj-est-tip-assign'].forEach(id => { — const el = document.getElementById(id); — if (el) el.selectedIndex = 0; — }); — try { toggleCarrierFields('nj'); } catch(e) {} — Apply defaults — const s2 = loadSettings(); — safe('nj-crew', s2.defaultCrew || '3'); — safe('nj-type', s2.defaultMove || 'local'); — safe('nj-date', prefillDate || getTodayStr()); — safe('nj-time', getSetting('defaultTime', '08:00')); — safe('nj-driver', currentUser.role === 'driver' ? (currentUser.name||'') : ''); — closeModal('modal-calday'); — openModal('modal-newjob'); — setTimeout(() => { try { applyJobDefaults('nj'); } catch(e) {} }, 50); — } — async function createJob() { — const client = document.getElementById('nj-client').value.trim(); — const date   = document.getElementById('nj-date').value; — const origin = document.getElementById('nj-origin').value.trim(); — const dest   = document.getElementById('nj-dest').value.trim(); — if (!client || !date) { notify('⚠ Client name and date required', true); return; } — Custom ID or auto-generate — const customId = (document.getElementById('nj-job-id')?.value||'').trim().toUpperCase(); — const autoId   = 'JOB-' + String((STATE.jobs||[]).length + 1).padStart(3,'0'); — const jobId    = customId || autoId; — if ((STATE.jobs||[]).find(j => j.id === jobId)) { — notify('⚠ Job ID ' + jobId + ' already exists', true); return; — } — Collect helpers — const helpers = []; — document.querySelectorAll('.nj-helper-name').forEach(inp => { — const name = inp.value.trim(); — if (!name) return; — const row = inp.closest('.nj-helper-row'); — helpers.push({ — name, — rate:    parseFloat(row?.querySelector('.nj-helper-rate')?.value) || 0, — payType: row?.querySelector('.nj-helper-type')?.value || 'hourly', — hours: 0, days: 1, workerType: '1099', paid: false — }); — }); — const g = id => (document.getElementById(id)?.value || ''); — const newJob = { — id:            jobId, — refNum:        g('nj-ref-num'), — client, — phone:         g('nj-phone'), — email:         g('nj-email'), — date, — time:          g('nj-time'), — endDate:       g('nj-end-date') || g('nj-delivery-end').slice(0,10) || '', — endTime:       g('nj-end-time'), — origin, — dest, — pickupStart:   g('nj-pickup-start'), — pickupEnd:     g('nj-pickup-end'), — pickupDriver:  g('nj-pickup-driver'), — deliveryStart: g('nj-delivery-start'), — deliveryEnd:   g('nj-delivery-end'), — deliveryDriver:g('nj-delivery-driver'), — driver:        g('nj-driver'), — driverUser:    currentUser.username || '', — crewSize:      parseInt(g('nj-crew')) || 3, — crewNames:     helpers.map(h => h.name), — amount:        parseFloat(g('nj-amount')) || 0, — cubicFt:       parseFloat(g('nj-cubic-ft-2') || g('nj-cubic-ft')) || 0, — weight:        parseFloat(g('nj-weight')) || 0, — linehaul:      parseFloat(g('nj-linehaul')) || 0, — priority:      g('nj-priority'), — followUp:      g('nj-followup'), — fadd:          g('nj-fadd'), — type:          g('nj-type') || 'local', — notes:         g('nj-notes'), — companyId:     COMPANY_ID, — status:        'pending', — gates:         { preload:false, loaded:false, delivered:false, payment:false }, — messages:      { crew:[], client:[] }, — photos:        [], — signatures:    [], — payroll:       { driver:0, helpers } — }; — try { — await db.collection(COL_JOBS).doc(newJob.id).set(newJob); — closeModal('modal-newjob'); — notify('✓ Job ' + newJob.id + ' created — ' + client); — setTimeout(() => renderCalendar(), 200); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('createJob error:', e); — } — } | DASHBOARD, FIRST RUN CHECK — runs on page load before login, MIGRATION — Stamp companyId on any legacy docs missing it — Runs silently on every login — safe to run repeatedly, STATE & STORAGE, TABS, TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created
| FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill | 5289-5312 | 16 | — | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore, CLIENT BOOKING PORTAL — URL: fleetflowmovemasters.netlify.app/?book=1&co=COMPANY_ID — Moving company shares this link on their website — Client fills details → instant quote → pays deposit → job created, EDIT JOB  ← NEW FEATURE, FIRST RUN CHECK — runs on page load before login, LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool, MIGRATION — Stamp companyId on any legacy docs missing it — Runs silently on every login — safe to run repeatedly, PAYROLL LEDGER — Mark Paid modal, RECEIPTS, SESSION PERSISTENCE — remember me across refreshes/tab closes, STATE & STORAGE, SYSTEM HEALTH DASHBOARD — Platform operator view — JPG Ventures sees all licensees, sync status, revenue flow — Accessible only to creator/owner accounts, TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created, TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order, USER MANAGEMENT (Office only), WAREHOUSE WORK LOGGING — Log warehouse work by employee — shows on calendar + payroll, function openNewJobModal(prefillDate) { — ── HARD RESET — every field to blank/zero ────────────────── — const safe = (id, val='') => { const el = document.getElementById(id); if(el) el.value = val; }; — Core job fields — safe('nj-client'); safe('nj-phone'); safe('nj-email'); — safe('nj-origin'); safe('nj-dest'); safe('nj-pickup-from'); — safe('nj-amount'); safe('nj-notes'); safe('nj-carrier'); — safe('nj-cubic-ft'); safe('nj-cubic-ft-2'); safe('nj-rate-per-cuft'); — safe('nj-weight'); safe('nj-linehaul'); — safe('nj-end-date'); safe('nj-end-time'); — safe('nj-pickup-start'); safe('nj-pickup-end'); — safe('nj-delivery-start'); safe('nj-delivery-end'); — safe('nj-pickup-driver'); safe('nj-delivery-driver'); — safe('nj-job-id'); safe('nj-ref-num'); — safe('nj-priority'); safe('nj-followup'); safe('nj-fadd'); — safe('nj-broker-ref'); safe('nj-carrier'); — ALL estimate fields — zero out completely — safe('nj-est-fuel', ''); — safe('nj-est-fuel-pct', '0'); — safe('nj-est-overload-cuft', ''); — safe('nj-est-stairs-origin', ''); — safe('nj-est-stairs-dest', ''); — safe('nj-est-longcarry-origin', ''); — safe('nj-est-longcarry-dest', ''); — safe('nj-est-shuttle', ''); — safe('nj-est-bulky', ''); — safe('nj-est-split', ''); — safe('nj-est-pgs', ''); — safe('nj-est-packing', ''); — safe('nj-est-storage-days', ''); — safe('nj-est-storage-rate', ''); — safe('nj-est-other', ''); — safe('nj-est-deposit', ''); — safe('nj-est-partial2', ''); — safe('nj-est-tip', ''); — safe('nj-est-discount', ''); — safe('nj-est-discount-label', ''); — safe('nj-est-others1', '');  safe('nj-est-others1-label', ''); — safe('nj-est-others2', '');  safe('nj-est-others2-label', ''); — safe('nj-est-others3', '');  safe('nj-est-others3-label', ''); — safe('nj-est-extra1', '');   safe('nj-est-extra1-label', ''); — safe('nj-est-extra2', '');   safe('nj-est-extra2-label', ''); — safe('nj-est-extra3', '');   safe('nj-est-extra3-label', ''); — safe('nj-est-notes', ''); — safe('nj-est-office-remarks', ''); — safe('nj-est-slip-remarks', ''); — Reset carrier total display — const ct = document.getElementById('nj-carrier-total'); if(ct) ct.textContent = '$0.00'; — Clear helper rows — const hr = document.getElementById('nj-helper-rows'); if(hr) hr.innerHTML = ''; — Reset all selects to index 0 — ['nj-job-for','nj-est-tip-method','nj-est-valuation', — 'nj-est-fuel-mode','nj-est-tip-assign'].forEach(id => { — const el = document.getElementById(id); — if (el) el.selectedIndex = 0; — }); — try { toggleCarrierFields('nj'); } catch(e) {} — Apply defaults — const s2 = loadSettings(); — safe('nj-crew', s2.defaultCrew || '3'); — safe('nj-type', s2.defaultMove || 'local'); — safe('nj-date', prefillDate || getTodayStr()); — safe('nj-time', getSetting('defaultTime', '08:00')); — safe('nj-driver', currentUser.role === 'driver' ? (currentUser.name||'') : ''); — closeModal('modal-calday'); — openModal('modal-newjob'); — setTimeout(() => { try { applyJobDefaults('nj'); } catch(e) {} }, 50); — } — async function createJob() { — const client = document.getElementById('nj-client').value.trim(); — const date   = document.getElementById('nj-date').value; — const origin = document.getElementById('nj-origin').value.trim(); — const dest   = document.getElementById('nj-dest').value.trim(); — if (!client || !date) { notify('⚠ Client name and date required', true); return; } — Custom ID or auto-generate — const customId = (document.getElementById('nj-job-id')?.value||'').trim().toUpperCase(); — const autoId   = 'JOB-' + String((STATE.jobs||[]).length + 1).padStart(3,'0'); — const jobId    = customId || autoId; — if ((STATE.jobs||[]).find(j => j.id === jobId)) { — notify('⚠ Job ID ' + jobId + ' already exists', true); return; — } — Collect helpers — const helpers = []; — document.querySelectorAll('.nj-helper-name').forEach(inp => { — const name = inp.value.trim(); — if (!name) return; — const row = inp.closest('.nj-helper-row'); — helpers.push({ — name, — rate:    parseFloat(row?.querySelector('.nj-helper-rate')?.value) || 0, — payType: row?.querySelector('.nj-helper-type')?.value || 'hourly', — hours: 0, days: 1, workerType: '1099', paid: false — }); — }); — const g = id => (document.getElementById(id)?.value || ''); — const newJob = { — id:            jobId, — refNum:        g('nj-ref-num'), — client, — phone:         g('nj-phone'), — email:         g('nj-email'), — date, — time:          g('nj-time'), — endDate:       g('nj-end-date') || g('nj-delivery-end').slice(0,10) || '', — endTime:       g('nj-end-time'), — origin, — dest, — pickupStart:   g('nj-pickup-start'), — pickupEnd:     g('nj-pickup-end'), — pickupDriver:  g('nj-pickup-driver'), — deliveryStart: g('nj-delivery-start'), — deliveryEnd:   g('nj-delivery-end'), — deliveryDriver:g('nj-delivery-driver'), — driver:        g('nj-driver'), — driverUser:    currentUser.username || '', — crewSize:      parseInt(g('nj-crew')) || 3, — crewNames:     helpers.map(h => h.name), — amount:        parseFloat(g('nj-amount')) || 0, — cubicFt:       parseFloat(g('nj-cubic-ft-2') || g('nj-cubic-ft')) || 0, — weight:        parseFloat(g('nj-weight')) || 0, — linehaul:      parseFloat(g('nj-linehaul')) || 0, — priority:      g('nj-priority'), — followUp:      g('nj-followup'), — fadd:          g('nj-fadd'), — type:          g('nj-type') || 'local', — notes:         g('nj-notes'), — companyId:     COMPANY_ID, — status:        'pending', — gates:         { preload:false, loaded:false, delivered:false, payment:false }, — messages:      { crew:[], client:[] }, — photos:        [], — signatures:    [], — payroll:       { driver:0, helpers } — }; — try { — await db.collection(COL_JOBS).doc(newJob.id).set(newJob); — closeModal('modal-newjob'); — notify('✓ Job ' + newJob.id + ' created — ' + client); — setTimeout(() => renderCalendar(), 200); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('createJob error:', e); — } — }
| JOB DETAIL MODAL | 15890-16296 | 21 | FIREBASE INIT, JOB DISPLAY ID — broker/carrier ref number support — job.id      = FleetFlow internal ID (never changes) — job.refNum  = Broker/carrier order number (editable by office) — jobDispId() = what to show everywhere in the UI, MODAL HELPERS, NOTIFICATION, NOTIFICATION ENGINE, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, STATE & STORAGE, TWILIO SMS — Real text notifications — Configure in Settings → Integrations | CLAIMS DEFENSE AI — v51 — Pre-load damage detection using Claude vision — When driver takes a photo at pre-load gate, AI scans for — pre-existing damage, flags it, embeds in BOL as evidence. — Protects moving company from false damage claims., DASHBOARD, FINANCIALS, HISTORY, JOB DISPLAY ID — broker/carrier ref number support — job.id      = FleetFlow internal ID (never changes) — job.refNum  = Broker/carrier order number (editable by office) — jobDispId() = what to show everywhere in the UI, MODAL HELPERS, SCHEDULE, SOUND ENGINE — Web Audio API, no external files needed, STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live, WAREHOUSE, WEEK VIEW — time-based layout like Google Calendar, function renderExpenses() { — const content = document.getElementById('main-content'); — const role = currentUser.role; — const isDriver = role === 'driver'; — Scope to this driver's jobs/disbursements if not office — const myJobs = isDriver — ? STATE.jobs.filter(j => nameIncludes(j.driver, currentUser.name)) — : STATE.jobs; — const myDisbursements = isDriver — ? STATE.disbursements.filter(d => — d.driverUser === currentUser.username || — (d.driver && d.driver.toLowerCase().includes(currentUser.name.toLowerCase())) — ) — : STATE.disbursements; — const myReceipts = isDriver — ? STATE.receipts.filter(r => { — const nm = r.driver && nameIncludes(r.driver, currentUser.name); — const um = r.driverUser && r.driverUser === currentUser.username; — return nm || um; — }) — : STATE.receipts; — Totals — const totalDisbursed = myDisbursements.reduce((s,d) => s + (d.amount||0), 0); — const totalReceipts  = myReceipts.reduce((s,r) => s + (r.amount||0), 0); — const netOwed        = totalReceipts - totalDisbursed; // positive = driver owes, negative = company owes driver — let html = ` — <div class="section-header"> — <div> — <div class="section-title">EXPENSE TRACKER</div> — <div class="section-sub">${isDriver ? 'Your funds & receipts · ' + currentUser.name : 'All driver funds & expense reconciliation'}</div> — </div> — ${isAdmin() ? `<button class="btn-green" onclick="openDisbursementModal()">💰 SEND FUNDS</button>` : ''} — </div> — <!-- SUMMARY STATS --> — <div class="grid-3" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Funds Disbursed</div> — <div class="stat-value">$${totalDisbursed.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Receipts Submitted</div> — <div class="stat-value amber">$${totalReceipts.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">${netOwed > 0 ? 'Driver Owes Back' : netOwed < 0 ? 'Company Owes Driver' : 'Balanced'}</div> — <div class="stat-value ${netOwed > 0 ? 'red' : netOwed < 0 ? 'amber' : ''}"> — ${netOwed === 0 ? '✓ $0.00' : (netOwed > 0 ? '+' : '') + '$' + Math.abs(netOwed).toFixed(2)} — </div> — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div class="card" style="margin-bottom:16px"> — <div class="card-header"><div class="card-title">JOB EXPENSE BREAKDOWN</div></div> — <table> — <thead><tr> — <th>Job ID</th><th>Client</th><th>Driver</th> — <th style="text-align:right">Funds Sent</th> — <th style="text-align:right">Receipts</th> — <th style="text-align:right">Net</th> — <th>Status</th> — </tr></thead> — <tbody> — ${myJobs.length ? myJobs.map(j => { — const jobDisb = myDisbursements.filter(d => d.jobId === j.id); — const jobRec  = myReceipts.filter(r => r.jobId === j.id); — const sent    = jobDisb.reduce((s,d) => s + (d.amount||0), 0); — const spent   = jobRec.reduce((s,r) => s + (r.amount||0), 0); — const net     = spent - sent; — return `<tr onclick="openJobExpenseDetail('${j.id}')" style="cursor:pointer"> — <td><span class="tag">${j.id}</span></td> — <td>${j.client}</td> — <td class="info-text">${j.driver || '—'}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--green)">$${sent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--amber)">$${spent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);font-weight:700;color:${net > 0 ? 'var(--red)' : net < 0 ? 'var(--amber)' : 'var(--green)'}"> — ${net === 0 ? '✓ $0.00' : (net > 0 ? '+' : '') + '$' + Math.abs(net).toFixed(2)} — </td> — <td><span class="status-badge status-${j.status}">${j.status}</span></td> — </tr>`; — }).join('') : `<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--gray);font-family:var(--font-mono)">No jobs found</td></tr>`} — </tbody> — </table> — </div> — <!-- DISBURSEMENTS LOG --> — <div class="grid-2"> — <div class="card"> — <div class="card-header"> — <div class="card-title">💰 FUNDS DISBURSED</div> — ${isAdmin() ? `<button class="btn-green" style="padding:7px 14px;font-size:11px" onclick="openDisbursementModal()">+ SEND FUNDS</button>` : ''} — </div> — ${myDisbursements.length ? myDisbursements.map(d => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${d.driver} <span class="tag">${d.jobId}</span></div> — <div class="info-text">${d.date} · ${d.method || 'Cash'} · ${d.notes || ''}</div> — <div class="info-text" style="color:var(--gray)">${d.sentBy ? 'Sent by: ' + d.sentBy : ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--green)">$${(d.amount||0).toFixed(2)}</div> — ${role === 'office' ? `<button class="btn-sm" style="margin-top:4px;color:var(--red);border-color:#552222;font-size:10px" onclick="event.stopPropagation();deleteDisbursementEntry('${d.id}')">DELETE</button>` : ''} — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No funds disbursed yet.</p>`} — ${myDisbursements.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Disbursed</span> — <span style="font-family:var(--font-mono);color:var(--green);font-size:14px">$${totalDisbursed.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="card"> — <div class="card-header"><div class="card-title">🧾 RECEIPTS SUBMITTED</div></div> — ${myReceipts.length ? myReceipts.map(r => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${r.type} <span class="tag">${r.jobId}</span></div> — <div class="info-text">${r.driver} · ${r.date} · ${r.notes || ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--amber)">$${(r.amount||0).toFixed(2)}</div> — <div style="margin-top:2px">${r.approved — ? '<span style="color:var(--green);font-size:10px;font-family:var(--font-mono)">✓ APPROVED</span>' — : '<span style="color:var(--amber);font-size:10px;font-family:var(--font-mono)">⏳ PENDING</span>'}</div> — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No receipts submitted yet.</p>`} — ${myReceipts.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Receipts</span> — <span style="font-family:var(--font-mono);color:var(--amber);font-size:14px">$${totalReceipts.toFixed(2)}</span> — </div>` : ''} — </div> — </div>`; — content.innerHTML = html; — } — function openJobExpenseDetail(jobId) { — Switch to jobs tab and open the job detail — closeModal('modal-calday'); — openJobDetail(jobId); — } — function openDisbursementModal(prefillJobId) { — Pre-fill job ID if provided — document.getElementById('disb-jobid').value = prefillJobId || ''; — document.getElementById('disb-driver').value = ''; — document.getElementById('disb-amount').value = ''; — document.getElementById('disb-method').value = getSetting('defaultPay', 'Cash'); — document.getElementById('disb-notes').value = ''; — Populate driver dropdown from users — populateDisbDriverDropdown(); — openModal('modal-disbursement'); — } — async function populateDisbDriverDropdown() { — const sel = document.getElementById('disb-driver'); — sel.innerHTML = '<option value="">-- Select Driver --</option>'; — try { — const snap = await db.collection(COL_USERS) — .where('role', 'in', ['driver','helper']) — .get(); — snap.docs.forEach(d => { — const u = d.data(); — if (u.active) { — const opt = document.createElement('option'); — opt.value = JSON.stringify({ name: u.displayName, username: u.username }); — opt.textContent = u.displayName + ' (' + u.role + ')'; — sel.appendChild(opt); — } — }); — } catch(e) { — Fallback — type name manually — sel.innerHTML = '<option value="">Type driver name in notes</option>'; — } — } — async function saveDisbursementEntry() { — const jobId   = document.getElementById('disb-jobid').value.trim().toUpperCase(); — const driverRaw = document.getElementById('disb-driver').value; — const amount  = parseFloat(document.getElementById('disb-amount').value); — const method  = document.getElementById('disb-method').value; — const notes   = document.getElementById('disb-notes').value.trim(); — const btn     = document.getElementById('disb-submit-btn'); — if (!jobId)   { notify('⚠ Job ID is required', true); return; } — if (!driverRaw) { notify('⚠ Select a driver', true); return; } — if (isNaN(amount) || amount <= 0) { notify('⚠ Enter a valid amount', true); return; } — if (btn.disabled) return; — btn.disabled = true; — btn.textContent = 'SAVING...'; — try { — let driverName = driverRaw, driverUser = ''; — try { — const parsed = JSON.parse(driverRaw); — driverName = parsed.name; — driverUser = parsed.username; — } catch(e) { driverName = driverRaw; } — const disb = { — id:         'DISB-' + Date.now(), — jobId, — driver:     driverName, — driverUser: driverUser, — amount, — method, — notes, — sentBy:     currentUser.name, — date:       getTodayStr(), — createdAt:  new Date().toISOString() — }; — await saveDisbursement(disb); — closeModal('modal-disbursement'); — notify('$' + amount.toFixed(2) + ' disbursed to ' + driverName + ' ✓'); — Notify the driver — try { — if (driverUser) { — await sendNotification(driverUser, driverName, 'funds_sent', — 'Funds Sent 💰', — '$' + amount.toFixed(2) + ' sent via ' + method + ' for ' + jobId + (notes ? ' · ' + notes : '') + ' — from ' + currentUser.name, — { jobId, amount } — ); — } — } catch(e) {} — } catch(e) { — notify('⚠ Error: ' + e.message, true); — } finally { — btn.disabled = false; — btn.textContent = 'SEND FUNDS'; — } — } — async function deleteDisbursementEntry(disbId) { — if (!confirm('Delete this disbursement? This cannot be undone.')) return; — try { — await db.collection(COL_DISBURSEMENTS).doc(disbId).delete(); — notify('Disbursement deleted ✓'); — } catch(e) { notify('⚠ ' + e.message, true); } — }, function renderMargins() { — const content = document.getElementById('main-content'); — Build margin data per job — const jobMargins = STATE.jobs — .filter(j => j.amount > 0 || j.payroll) — .map(j => { — const revenue   = j.amount || 0; — const driverAccBonus = parseFloat(j.payroll?.accessorialBonus) || 0; — const crewCost  = (j.payroll?.driver || 0) + — driverAccBonus + — (j.payroll?.helpers || []).reduce((s,h) => s + calcPay(h), 0) + — (j.payroll?.driverCommission || 0); — Company revenue excludes driver accessorial bonuses (passed through to driver) — const expenses  = STATE.receipts — .filter(r => r.jobId === j.id && r.approved !== false) — .reduce((s,r) => s + (r.amount||0), 0); — const net       = revenue - crewCost - expenses; — const margin    = revenue > 0 ? (net / revenue * 100) : 0; — return { job: j, revenue, crewCost, expenses, net, margin }; — }) — .sort((a,b) => { — Default: worst margin first (ascending) — const mA = a.revenue > 0 ? a.profit/a.revenue : 0; — const mB = b.revenue > 0 ? b.profit/b.revenue : 0; — return mA - mB; — }); — Sort control — if (window._marginSort === 'date') jobMargins.sort((a,b) => b.job.date > a.job.date ? 1 : -1); — else if (window._marginSort === 'revenue') jobMargins.sort((a,b) => b.revenue - a.revenue); — ── Margin intelligence ── — const overpayJobs   = jobMargins.filter(m => m.revenue > 0 && (m.crewCost/m.revenue) > 0.40); — const lowProfitJobs = jobMargins.filter(m => m.revenue > 0 && (m.profit/m.revenue) < 0.20); — const bestJob       = [...jobMargins].sort((a,b) => (b.profit/b.revenue||0)-(a.profit/a.revenue||0))[0]; — const worstJob      = jobMargins[0]; // already sorted worst first — Overall stats — const totalRevenue  = jobMargins.reduce((s,m) => s + m.revenue, 0); — const totalCrew     = jobMargins.reduce((s,m) => s + m.crewCost, 0); — const totalExpenses = jobMargins.reduce((s,m) => s + m.expenses, 0); — const totalNet      = jobMargins.reduce((s,m) => s + m.net, 0); — const avgMargin     = totalRevenue > 0 ? (totalNet / totalRevenue * 100) : 0; — const profitable    = jobMargins.filter(m => m.net > 0).length; — const losing        = jobMargins.filter(m => m.net < 0).length; — Best and worst job — const best  = jobMargins.reduce((a,b) => b.margin > a.margin ? b : a, jobMargins[0]); — const worst = jobMargins.reduce((a,b) => b.margin < a.margin ? b : a, jobMargins[0]); — const marginColor = (n) => n >= 40 ? 'var(--green)' : n >= 20 ? 'var(--amber)' : n >= 0 ? '#ff8c00' : 'var(--red)'; — const marginBg    = (n) => n >= 40 ? 'rgba(0,255,65,0.08)' : n >= 20 ? 'rgba(255,170,0,0.08)' : n >= 0 ? 'rgba(255,140,0,0.08)' : 'rgba(255,51,51,0.08)'; — let html = ` — ${overpayJobs.length ? `<div style="background:rgba(255,30,30,0.06);border:1px solid rgba(255,30,30,0.3);border-radius:6px;padding:10px 14px;margin-bottom:12px;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px"> — <div> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;font-weight:700">⚠ LABOR OVERPAY ALERT</div> — <div style="font-size:12px;color:var(--white);margin-top:3px">${overpayJobs.length} job${overpayJobs.length>1?'s':''} with crew cost >40% of revenue</div> — <div style="font-size:11px;color:var(--gray);margin-top:2px">${overpayJobs.slice(0,3).map(m=>m.job.client+' ('+Math.round(m.crewCost/m.revenue*100)+'%)').join(' · ')}</div> — </div> — </div>` : ''} — <div style="display:flex;gap:8px;margin-bottom:12px;flex-wrap:wrap"> — <span style="font-family:var(--font-mono);font-size:10px;color:var(--gray);align-self:center">Sort:</span> — ${['worst:⬇ Worst Margin','revenue:$ Revenue','date:📅 Date'].map(o=>{const[v,l]=o.split(':');return `<button onclick="window._marginSort='${v}';renderMargins()" style="padding:5px 12px;font-family:var(--font-mono);font-size:10px;border-radius:3px;cursor:pointer;background:${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--green)':'transparent'};color:${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--navy)':'var(--gray)'};border:1px solid ${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--green)':'var(--gray2)'}">${l}</button>`;}).join('')} — </div> — <div class="section-header"> — <div> — <div class="section-title">📈 JOB MARGIN DASHBOARD</div> — <div class="section-sub">Revenue · Crew Cost · Expenses · Net per Job</div> — </div> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportJobsCSV()">⬇ EXPORT</button> — </div> — <!-- OVERALL STATS --> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(130px,1fr));gap:8px;margin-bottom:16px"> — <div class="stat-box"> — <div class="stat-label">Total Revenue</div> — <div class="stat-value">$${totalRevenue.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${jobMargins.length} jobs</div> — </div> — <div class="stat-box"> — <div class="stat-label">Crew Cost</div> — <div class="stat-value red">$${totalCrew.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${totalRevenue>0?(totalCrew/totalRevenue*100).toFixed(1):'0'}% of revenue</div> — </div> — <div class="stat-box"> — <div class="stat-label">Expenses</div> — <div class="stat-value red">$${totalExpenses.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${totalRevenue>0?(totalExpenses/totalRevenue*100).toFixed(1):'0'}% of revenue</div> — </div> — <div class="stat-box" style="border-color:${marginColor(avgMargin)}"> — <div class="stat-label">Net Profit</div> — <div class="stat-value" style="color:${marginColor(avgMargin)}">$${totalNet.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px;color:${marginColor(avgMargin)}">${avgMargin.toFixed(1)}% avg margin</div> — </div> — <div class="stat-box"> — <div class="stat-label">Profitable Jobs</div> — <div class="stat-value" style="color:var(--green)">${profitable}</div> — <div class="info-text" style="margin-top:4px;color:var(--red)">${losing} losing</div> — </div> — </div> — <!-- MARGIN BREAKDOWN BAR --> — ${totalRevenue > 0 ? ` — <div class="card" style="margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;margin-bottom:10px">REVENUE BREAKDOWN</div> — <div style="height:32px;border-radius:4px;overflow:hidden;display:flex;margin-bottom:8px"> — <div style="width:${(totalCrew/totalRevenue*100).toFixed(1)}%;background:rgba(255,51,51,0.7);transition:width 0.5s" title="Crew Cost"></div> — <div style="width:${(totalExpenses/totalRevenue*100).toFixed(1)}%;background:rgba(255,140,0,0.7);transition:width 0.5s" title="Expenses"></div> — <div style="flex:1;background:rgba(0,255,65,0.4);transition:width 0.5s" title="Net Profit"></div> — </div> — <div style="display:flex;gap:16px;flex-wrap:wrap"> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(255,51,51,0.7);border-radius:2px"></div><span class="info-text">Crew ${(totalCrew/totalRevenue*100).toFixed(1)}%</span></div> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(255,140,0,0.7);border-radius:2px"></div><span class="info-text">Expenses ${(totalExpenses/totalRevenue*100).toFixed(1)}%</span></div> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(0,255,65,0.4);border-radius:2px"></div><span class="info-text">Net ${avgMargin.toFixed(1)}%</span></div> — </div> — </div>` : ''} — <!-- BEST / WORST --> — ${jobMargins.length >= 2 ? ` — <div class="grid-2" style="margin-bottom:16px"> — ${best ? `<div class="card" style="border-color:var(--green3)"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--green);letter-spacing:2px;margin-bottom:6px">🏆 BEST MARGIN JOB</div> — <div style="font-size:15px;font-weight:700">${best.job.client} <span class="tag">${best.job.id}</span></div> — <div class="info-text">${best.job.date}</div> — <div style="font-family:var(--font-mono);font-size:22px;color:var(--green);margin-top:6px">${best.margin.toFixed(1)}%</div> — <div class="info-text">$${best.net.toFixed(2)} net on $${best.revenue.toLocaleString()}</div> — </div>` : ''} — ${worst ? `<div class="card" style="border-color:${marginColor(worst.margin)}"> — <div style="font-family:var(--font-mono);font-size:9px;color:${marginColor(worst.margin)};letter-spacing:2px;margin-bottom:6px">${worst.net < 0 ? '⚠ LOSING JOB' : '📉 LOWEST MARGIN'}</div> — <div style="font-size:15px;font-weight:700">${worst.job.client} <span class="tag">${worst.job.id}</span></div> — <div class="info-text">${worst.job.date}</div> — <div style="font-family:var(--font-mono);font-size:22px;color:${marginColor(worst.margin)};margin-top:6px">${worst.margin.toFixed(1)}%</div> — <div class="info-text">$${worst.net.toFixed(2)} net on $${worst.revenue.toLocaleString()}</div> — </div>` : ''} — </div>` : ''} — <!-- PER-JOB TABLE --> — <div class="card" style="padding:0;overflow:hidden"> — <div style="padding:12px 16px;border-bottom:1px solid var(--gray2)"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--green);letter-spacing:2px">JOB BY JOB BREAKDOWN</div> — </div> — ${jobMargins.length ? ` — <div style="overflow-x:auto"> — <table style="min-width:700px"> — <thead><tr> — <th>Job</th> — <th>Client</th> — <th>Date</th> — <th style="text-align:right">Revenue</th> — <th style="text-align:right">Crew Cost</th> — <th style="text-align:right">Expenses</th> — <th style="text-align:right">Net</th> — <th style="text-align:center">Margin</th> — <th>Status</th> — </tr></thead> — <tbody> — ${jobMargins.map(m => ` — <tr style="cursor:pointer;background:${marginBg(m.margin)}" onclick="switchTab('jobs');setTimeout(()=>openJobDetail('${m.job.id}'),300)"> — <td><span class="tag">${m.job.id}</span></td> — <td style="font-weight:600">${m.job.client}</td> — <td class="info-text">${m.job.date}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${m.revenue.toLocaleString()}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${m.crewCost.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:#ff8c00">$${m.expenses.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;font-weight:700;color:${marginColor(m.margin)}"> — ${m.net >= 0 ? '+' : ''}$${m.net.toFixed(2)} — </td> — <td style="text-align:center"> — <div style="display:inline-flex;align-items:center;justify-content:center;width:52px;height:24px;border-radius:12px;background:${marginBg(m.margin)};border:1px solid ${marginColor(m.margin)};font-family:var(--font-mono);font-size:10px;font-weight:700;color:${marginColor(m.margin)}"> — ${m.margin.toFixed(0)}% — </div> — </td> — <td><span class="status-badge status-${m.job.status}">${m.job.status}</span></td> — </tr>`).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="3" style="font-family:var(--font-mono);color:var(--green);font-size:11px">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${totalRevenue.toLocaleString()}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${totalCrew.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:#ff8c00">$${totalExpenses.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;font-weight:700;color:${marginColor(avgMargin)}"> — ${totalNet >= 0 ? '+' : ''}$${totalNet.toFixed(2)} — </td> — <td style="text-align:center"> — <div style="display:inline-flex;align-items:center;justify-content:center;width:52px;height:24px;border-radius:12px;background:${marginBg(avgMargin)};border:1px solid ${marginColor(avgMargin)};font-family:var(--font-mono);font-size:10px;font-weight:700;color:${marginColor(avgMargin)}"> — ${avgMargin.toFixed(0)}% — </div> — </td> — <td></td> — </tr> — </tbody> — </table> — </div>` : ` — <div style="text-align:center;padding:48px"> — <div style="font-size:40px;margin-bottom:12px">📈</div> — <p class="info-text">No job data yet. Create jobs with quoted amounts and crew logs to see margins.</p> — </div>`} — </div> — <!-- MARGIN LEGEND --> — <div style="display:flex;gap:16px;padding:12px 4px;flex-wrap:wrap;align-items:center"> — <span class="info-text">Margin health:</span> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(0,255,65,0.2);border:1px solid var(--green);border-radius:2px"></div><span class="info-text" style="color:var(--green)">40%+ Excellent</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,170,0,0.2);border:1px solid var(--amber);border-radius:2px"></div><span class="info-text" style="color:var(--amber)">20–39% Good</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,140,0,0.2);border:1px solid #ff8c00;border-radius:2px"></div><span class="info-text" style="color:#ff8c00">0–19% Thin</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,51,51,0.15);border:1px solid var(--red);border-radius:2px"></div><span class="info-text" style="color:var(--red)">Negative — Losing money</span></div> — </div>`; — content.innerHTML = html; — }
| NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive | 8181-8213 | 22 | — | 1099 / W2 YEAR-END TAX EXPORT — Aggregates all payments per contractor/employee for the year — Generates: on-screen summary + CSV export + PDF report, BOL TAB, CALENDAR, CREW LOG HELPERS — extracted to avoid nested template literal — Chrome syntax errors from 4+ levels of backtick nesting, DASHBOARD, EDIT JOB  ← NEW FEATURE, FINANCIALS, LABOR PRODUCTIVITY SCORES — v52 — Uses gate timestamps to calculate efficiency per crew member — Crew A moves 50 cu.ft/hr vs Crew B at 35 cu.ft/hr — Visible to owner/office — used for rewards and training, MESSAGING, MONTH VIEW — Google Calendar style with multi-day span bars, Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — }, PAYROLL LEDGER — Mark Paid modal, PAYROLL LEDGER — Running balance per crew member — Balance = earned - paid. When paid → resets to 0 → new cycle, RECEIPTS, SCHEDULE, STATE & STORAGE, TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created, TWILIO SMS — Real text notifications — Configure in Settings → Integrations, WAREHOUSE, WEEK VIEW — time-based layout like Google Calendar, function renderExpenses() { — const content = document.getElementById('main-content'); — const role = currentUser.role; — const isDriver = role === 'driver'; — Scope to this driver's jobs/disbursements if not office — const myJobs = isDriver — ? STATE.jobs.filter(j => nameIncludes(j.driver, currentUser.name)) — : STATE.jobs; — const myDisbursements = isDriver — ? STATE.disbursements.filter(d => — d.driverUser === currentUser.username || — (d.driver && d.driver.toLowerCase().includes(currentUser.name.toLowerCase())) — ) — : STATE.disbursements; — const myReceipts = isDriver — ? STATE.receipts.filter(r => { — const nm = r.driver && nameIncludes(r.driver, currentUser.name); — const um = r.driverUser && r.driverUser === currentUser.username; — return nm || um; — }) — : STATE.receipts; — Totals — const totalDisbursed = myDisbursements.reduce((s,d) => s + (d.amount||0), 0); — const totalReceipts  = myReceipts.reduce((s,r) => s + (r.amount||0), 0); — const netOwed        = totalReceipts - totalDisbursed; // positive = driver owes, negative = company owes driver — let html = ` — <div class="section-header"> — <div> — <div class="section-title">EXPENSE TRACKER</div> — <div class="section-sub">${isDriver ? 'Your funds & receipts · ' + currentUser.name : 'All driver funds & expense reconciliation'}</div> — </div> — ${isAdmin() ? `<button class="btn-green" onclick="openDisbursementModal()">💰 SEND FUNDS</button>` : ''} — </div> — <!-- SUMMARY STATS --> — <div class="grid-3" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Funds Disbursed</div> — <div class="stat-value">$${totalDisbursed.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Receipts Submitted</div> — <div class="stat-value amber">$${totalReceipts.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">${netOwed > 0 ? 'Driver Owes Back' : netOwed < 0 ? 'Company Owes Driver' : 'Balanced'}</div> — <div class="stat-value ${netOwed > 0 ? 'red' : netOwed < 0 ? 'amber' : ''}"> — ${netOwed === 0 ? '✓ $0.00' : (netOwed > 0 ? '+' : '') + '$' + Math.abs(netOwed).toFixed(2)} — </div> — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div class="card" style="margin-bottom:16px"> — <div class="card-header"><div class="card-title">JOB EXPENSE BREAKDOWN</div></div> — <table> — <thead><tr> — <th>Job ID</th><th>Client</th><th>Driver</th> — <th style="text-align:right">Funds Sent</th> — <th style="text-align:right">Receipts</th> — <th style="text-align:right">Net</th> — <th>Status</th> — </tr></thead> — <tbody> — ${myJobs.length ? myJobs.map(j => { — const jobDisb = myDisbursements.filter(d => d.jobId === j.id); — const jobRec  = myReceipts.filter(r => r.jobId === j.id); — const sent    = jobDisb.reduce((s,d) => s + (d.amount||0), 0); — const spent   = jobRec.reduce((s,r) => s + (r.amount||0), 0); — const net     = spent - sent; — return `<tr onclick="openJobExpenseDetail('${j.id}')" style="cursor:pointer"> — <td><span class="tag">${j.id}</span></td> — <td>${j.client}</td> — <td class="info-text">${j.driver || '—'}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--green)">$${sent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--amber)">$${spent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);font-weight:700;color:${net > 0 ? 'var(--red)' : net < 0 ? 'var(--amber)' : 'var(--green)'}"> — ${net === 0 ? '✓ $0.00' : (net > 0 ? '+' : '') + '$' + Math.abs(net).toFixed(2)} — </td> — <td><span class="status-badge status-${j.status}">${j.status}</span></td> — </tr>`; — }).join('') : `<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--gray);font-family:var(--font-mono)">No jobs found</td></tr>`} — </tbody> — </table> — </div> — <!-- DISBURSEMENTS LOG --> — <div class="grid-2"> — <div class="card"> — <div class="card-header"> — <div class="card-title">💰 FUNDS DISBURSED</div> — ${isAdmin() ? `<button class="btn-green" style="padding:7px 14px;font-size:11px" onclick="openDisbursementModal()">+ SEND FUNDS</button>` : ''} — </div> — ${myDisbursements.length ? myDisbursements.map(d => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${d.driver} <span class="tag">${d.jobId}</span></div> — <div class="info-text">${d.date} · ${d.method || 'Cash'} · ${d.notes || ''}</div> — <div class="info-text" style="color:var(--gray)">${d.sentBy ? 'Sent by: ' + d.sentBy : ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--green)">$${(d.amount||0).toFixed(2)}</div> — ${role === 'office' ? `<button class="btn-sm" style="margin-top:4px;color:var(--red);border-color:#552222;font-size:10px" onclick="event.stopPropagation();deleteDisbursementEntry('${d.id}')">DELETE</button>` : ''} — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No funds disbursed yet.</p>`} — ${myDisbursements.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Disbursed</span> — <span style="font-family:var(--font-mono);color:var(--green);font-size:14px">$${totalDisbursed.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="card"> — <div class="card-header"><div class="card-title">🧾 RECEIPTS SUBMITTED</div></div> — ${myReceipts.length ? myReceipts.map(r => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${r.type} <span class="tag">${r.jobId}</span></div> — <div class="info-text">${r.driver} · ${r.date} · ${r.notes || ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--amber)">$${(r.amount||0).toFixed(2)}</div> — <div style="margin-top:2px">${r.approved — ? '<span style="color:var(--green);font-size:10px;font-family:var(--font-mono)">✓ APPROVED</span>' — : '<span style="color:var(--amber);font-size:10px;font-family:var(--font-mono)">⏳ PENDING</span>'}</div> — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No receipts submitted yet.</p>`} — ${myReceipts.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Receipts</span> — <span style="font-family:var(--font-mono);color:var(--amber);font-size:14px">$${totalReceipts.toFixed(2)}</span> — </div>` : ''} — </div> — </div>`; — content.innerHTML = html; — } — function openJobExpenseDetail(jobId) { — Switch to jobs tab and open the job detail — closeModal('modal-calday'); — openJobDetail(jobId); — } — function openDisbursementModal(prefillJobId) { — Pre-fill job ID if provided — document.getElementById('disb-jobid').value = prefillJobId || ''; — document.getElementById('disb-driver').value = ''; — document.getElementById('disb-amount').value = ''; — document.getElementById('disb-method').value = getSetting('defaultPay', 'Cash'); — document.getElementById('disb-notes').value = ''; — Populate driver dropdown from users — populateDisbDriverDropdown(); — openModal('modal-disbursement'); — } — async function populateDisbDriverDropdown() { — const sel = document.getElementById('disb-driver'); — sel.innerHTML = '<option value="">-- Select Driver --</option>'; — try { — const snap = await db.collection(COL_USERS) — .where('role', 'in', ['driver','helper']) — .get(); — snap.docs.forEach(d => { — const u = d.data(); — if (u.active) { — const opt = document.createElement('option'); — opt.value = JSON.stringify({ name: u.displayName, username: u.username }); — opt.textContent = u.displayName + ' (' + u.role + ')'; — sel.appendChild(opt); — } — }); — } catch(e) { — Fallback — type name manually — sel.innerHTML = '<option value="">Type driver name in notes</option>'; — } — } — async function saveDisbursementEntry() { — const jobId   = document.getElementById('disb-jobid').value.trim().toUpperCase(); — const driverRaw = document.getElementById('disb-driver').value; — const amount  = parseFloat(document.getElementById('disb-amount').value); — const method  = document.getElementById('disb-method').value; — const notes   = document.getElementById('disb-notes').value.trim(); — const btn     = document.getElementById('disb-submit-btn'); — if (!jobId)   { notify('⚠ Job ID is required', true); return; } — if (!driverRaw) { notify('⚠ Select a driver', true); return; } — if (isNaN(amount) || amount <= 0) { notify('⚠ Enter a valid amount', true); return; } — if (btn.disabled) return; — btn.disabled = true; — btn.textContent = 'SAVING...'; — try { — let driverName = driverRaw, driverUser = ''; — try { — const parsed = JSON.parse(driverRaw); — driverName = parsed.name; — driverUser = parsed.username; — } catch(e) { driverName = driverRaw; } — const disb = { — id:         'DISB-' + Date.now(), — jobId, — driver:     driverName, — driverUser: driverUser, — amount, — method, — notes, — sentBy:     currentUser.name, — date:       getTodayStr(), — createdAt:  new Date().toISOString() — }; — await saveDisbursement(disb); — closeModal('modal-disbursement'); — notify('$' + amount.toFixed(2) + ' disbursed to ' + driverName + ' ✓'); — Notify the driver — try { — if (driverUser) { — await sendNotification(driverUser, driverName, 'funds_sent', — 'Funds Sent 💰', — '$' + amount.toFixed(2) + ' sent via ' + method + ' for ' + jobId + (notes ? ' · ' + notes : '') + ' — from ' + currentUser.name, — { jobId, amount } — ); — } — } catch(e) {} — } catch(e) { — notify('⚠ Error: ' + e.message, true); — } finally { — btn.disabled = false; — btn.textContent = 'SEND FUNDS'; — } — } — async function deleteDisbursementEntry(disbId) { — if (!confirm('Delete this disbursement? This cannot be undone.')) return; — try { — await db.collection(COL_DISBURSEMENTS).doc(disbId).delete(); — notify('Disbursement deleted ✓'); — } catch(e) { notify('⚠ ' + e.message, true); } — }, function renderMyPay() { — const content  = document.getElementById('main-content'); — const role     = currentUser.role; — const isHelper = role === 'helper'; — Collect all pay entries for this person — const myJobs = STATE.jobs.filter(j => { — if (role === 'driver') return nameIncludes(j.driver, currentUser.name); — if (role === 'helper') return (j.payroll?.helpers||[]).some(h => nameIncludes(h.name, currentUser.name)); — return false; — }); — let totalGross      = 0; — let totalCommission = 0; — let totalTax        = 0; — let totalTakeHome   = 0; — const payRows       = []; — myJobs.forEach(j => { — let gross = 0, commission = 0, workerType = '1099', payDetails = ''; — if (role === 'driver') { — gross       = j.payroll?.driver || 0; — commission  = j.payroll?.driverCommission || 0; — workerType  = j.payroll?.driverWorkerType || '1099'; — payDetails  = payLabel({ payType: j.payroll?.driverPayType||'flat', rate: j.payroll?.driverRate||gross, hours: j.payroll?.driverHours||0, days: j.payroll?.driverDays||1 }); — } else { — const helper = (j.payroll?.helpers||[]).find(h => — h.name?.toLowerCase().includes(currentUser.name.toLowerCase()) || — (currentUser.name.toLowerCase().split(' ')[0] && h.name?.toLowerCase().includes(currentUser.name.toLowerCase().split(' ')[0])) — ); — if (helper) { — gross      = calcPay(helper); — commission = helper.commission || 0; — workerType = helper.workerType || '1099'; — payDetails = payLabel(helper); — } — } — if (gross === 0) return; — const tax = calcTax(gross, workerType); — totalGross      += gross; — totalCommission += commission; — totalTax        += tax.totalEstTax; — totalTakeHome   += tax.takeHome; — payRows.push({ job: j, gross, commission, workerType, payDetails, tax }); — }); — let html = ` — <div class="section-header"> — <div> — <div class="section-title">📊 MY PAY SUMMARY</div> — <div class="section-sub">${currentUser.name} · ${role.toUpperCase()} · ${payRows.length} job(s)</div> — </div> — </div> — <!-- YTD SUMMARY STATS --> — <div class="grid-4" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Gross Earned</div> — <div class="stat-value">$${totalGross.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Commission</div> — <div class="stat-value" style="color:var(--amber)">$${totalCommission.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Est. Tax Owed</div> — <div class="stat-value red">$${totalTax.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Est. Take-Home</div> — <div class="stat-value">$${totalTakeHome.toFixed(2)}</div> — </div> — </div> — <!-- TAX ADVISORY BANNER --> — <div class="card" style="border-color:#664400;background:rgba(255,170,0,0.05);margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:11px;color:var(--amber);letter-spacing:2px;margin-bottom:8px">⚠ TAX ADVISORY</div> — <div class="info-text" style="line-height:1.8"> — ${totalGross > 0 ? `Based on your earnings of <strong style="color:var(--white)">$${totalGross.toFixed(2)}</strong>, you should set aside approximately <strong style="color:var(--red)">$${totalTax.toFixed(2)}</strong> (${(totalTax/totalGross*100).toFixed(1)}%) for taxes.` : 'No earnings recorded yet.'} — ${totalGross >= 600 ? ' You will likely receive a 1099 form if classified as a contractor.' : ''} — If you are a <strong style="color:var(--white)">1099 contractor</strong>, you are responsible for paying your own taxes quarterly (April, June, September, January). Consider setting aside <strong style="color:var(--red)">${totalGross > 0 ? (totalTax/totalGross*100).toFixed(0) : '~25'}%</strong> of every paycheck. — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div style="margin-top:4px"> — ${payRows.length ? payRows.map(row => ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:10px"> — <div> — <div style="display:flex;gap:8px;align-items:center;margin-bottom:4px"> — <span class="tag">${row.job.id}</span> — <span class="status-badge status-${row.job.status}">${row.job.status}</span> — <span class="tag">${row.workerType}</span> — </div> — <div style="font-size:14px;font-weight:600">${row.job.client}</div> — <div class="info-text">${row.job.date} · ${row.payDetails}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:20px;color:var(--green)">$${row.gross.toFixed(2)}</div> — ${row.commission > 0 ? `<div style="font-family:var(--font-mono);font-size:12px;color:var(--amber)">+ $${row.commission.toFixed(2)} commission</div>` : ''} — </div> — </div> — <!-- Tax breakdown per job --> — <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;margin-bottom:8px"> — <div class="stat-box" style="padding:10px"> — <div class="stat-label" style="font-size:9px">Gross Pay</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--white)">$${row.gross.toFixed(2)}</div> — </div> — <div class="stat-box" style="padding:10px"> — <div class="stat-label" style="font-size:9px">Est. Tax</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--red)">−$${row.tax.totalEstTax.toFixed(2)}</div> — </div> — <div class="stat-box" style="padding:10px;border-color:var(--green3)"> — <div class="stat-label" style="font-size:9px">Take-Home</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--green)">$${row.tax.takeHome.toFixed(2)}</div> — </div> — </div> — <!-- Detailed tax line items --> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;font-family:var(--font-mono);font-size:10px"> — ${row.tax.selfEmployTax ? `<div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>Self-Employment Tax (15.3%)</span><span style="color:var(--red)">−$${row.tax.selfEmployTax.toFixed(2)}</span></div>` : ''} — ${row.tax.ficaTax ? `<div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>FICA Employee (7.65%)</span><span style="color:var(--red)">−$${row.tax.ficaTax.toFixed(2)}</span></div>` : ''} — <div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>Federal Income (~${(row.tax.federalRate*100).toFixed(0)}%)</span><span style="color:var(--red)">−$${row.tax.federalTax.toFixed(2)}</span></div> — <div style="display:flex;justify-content:space-between;color:var(--gray)"><span>Georgia State (5.5%)</span><span style="color:var(--red)">−$${row.tax.stateTax.toFixed(2)}</span></div> — </div> — </div>`).join('') : ` — <div class="card" style="text-align:center;padding:40px"> — <p class="info-text">No pay records found for ${currentUser.name}.</p> — <p class="info-text" style="margin-top:8px">Your earnings will appear here once jobs are assigned and crew logs are saved.</p> — </div>`} — </div> — <div style="padding:12px;font-family:var(--font-mono);font-size:9px;color:var(--gray);line-height:1.8;border-top:1px solid var(--gray2);margin-top:8px"> — ⚠ DISCLAIMER: Tax estimates are for informational purposes only based on 2024 US federal rates and Georgia state rates. This is not tax advice. Individual circumstances vary. Consult a licensed tax professional or CPA for filing guidance. JPG Systems / JPG Ventures LLC is not responsible for tax obligations of independent contractors. — </div>`; — content.innerHTML = html; — }
| MODAL HELPERS | 19967-20007 | 26 | EDIT JOB  ← NEW FEATURE, JOB DETAIL MODAL | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore, BOL TAB, CLAIMS DEFENSE AI — v51 — Pre-load damage detection using Claude vision — When driver takes a photo at pre-load gate, AI scans for — pre-existing damage, flags it, embeds in BOL as evidence. — Protects moving company from false damage claims., CLIENT MOVE PORTAL — Full transparency experience — This is what the client pays $100-150 for — Real-time gate tracking, item location, messaging, docs, EDIT JOB  ← NEW FEATURE, JOB DETAIL MODAL, JOB DISPLAY ID — broker/carrier ref number support — job.id      = FleetFlow internal ID (never changes) — job.refNum  = Broker/carrier order number (editable by office) — jobDispId() = what to show everywhere in the UI, LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool, NOTIFICATION, Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — }, PAYROLL LEDGER — Mark Paid modal, RECEIPTS, ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard, SIGNATURE MODAL, STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live, SYSTEM HEALTH DASHBOARD — Platform operator view — JPG Ventures sees all licensees, sync status, revenue flow — Accessible only to creator/owner accounts, TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created, TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order, USER MANAGEMENT (Office only), WAREHOUSE WORK LOGGING — Log warehouse work by employee — shows on calendar + payroll, WEEK VIEW — time-based layout like Google Calendar, function openNewJobModal(prefillDate) { — ── HARD RESET — every field to blank/zero ────────────────── — const safe = (id, val='') => { const el = document.getElementById(id); if(el) el.value = val; }; — Core job fields — safe('nj-client'); safe('nj-phone'); safe('nj-email'); — safe('nj-origin'); safe('nj-dest'); safe('nj-pickup-from'); — safe('nj-amount'); safe('nj-notes'); safe('nj-carrier'); — safe('nj-cubic-ft'); safe('nj-cubic-ft-2'); safe('nj-rate-per-cuft'); — safe('nj-weight'); safe('nj-linehaul'); — safe('nj-end-date'); safe('nj-end-time'); — safe('nj-pickup-start'); safe('nj-pickup-end'); — safe('nj-delivery-start'); safe('nj-delivery-end'); — safe('nj-pickup-driver'); safe('nj-delivery-driver'); — safe('nj-job-id'); safe('nj-ref-num'); — safe('nj-priority'); safe('nj-followup'); safe('nj-fadd'); — safe('nj-broker-ref'); safe('nj-carrier'); — ALL estimate fields — zero out completely — safe('nj-est-fuel', ''); — safe('nj-est-fuel-pct', '0'); — safe('nj-est-overload-cuft', ''); — safe('nj-est-stairs-origin', ''); — safe('nj-est-stairs-dest', ''); — safe('nj-est-longcarry-origin', ''); — safe('nj-est-longcarry-dest', ''); — safe('nj-est-shuttle', ''); — safe('nj-est-bulky', ''); — safe('nj-est-split', ''); — safe('nj-est-pgs', ''); — safe('nj-est-packing', ''); — safe('nj-est-storage-days', ''); — safe('nj-est-storage-rate', ''); — safe('nj-est-other', ''); — safe('nj-est-deposit', ''); — safe('nj-est-partial2', ''); — safe('nj-est-tip', ''); — safe('nj-est-discount', ''); — safe('nj-est-discount-label', ''); — safe('nj-est-others1', '');  safe('nj-est-others1-label', ''); — safe('nj-est-others2', '');  safe('nj-est-others2-label', ''); — safe('nj-est-others3', '');  safe('nj-est-others3-label', ''); — safe('nj-est-extra1', '');   safe('nj-est-extra1-label', ''); — safe('nj-est-extra2', '');   safe('nj-est-extra2-label', ''); — safe('nj-est-extra3', '');   safe('nj-est-extra3-label', ''); — safe('nj-est-notes', ''); — safe('nj-est-office-remarks', ''); — safe('nj-est-slip-remarks', ''); — Reset carrier total display — const ct = document.getElementById('nj-carrier-total'); if(ct) ct.textContent = '$0.00'; — Clear helper rows — const hr = document.getElementById('nj-helper-rows'); if(hr) hr.innerHTML = ''; — Reset all selects to index 0 — ['nj-job-for','nj-est-tip-method','nj-est-valuation', — 'nj-est-fuel-mode','nj-est-tip-assign'].forEach(id => { — const el = document.getElementById(id); — if (el) el.selectedIndex = 0; — }); — try { toggleCarrierFields('nj'); } catch(e) {} — Apply defaults — const s2 = loadSettings(); — safe('nj-crew', s2.defaultCrew || '3'); — safe('nj-type', s2.defaultMove || 'local'); — safe('nj-date', prefillDate || getTodayStr()); — safe('nj-time', getSetting('defaultTime', '08:00')); — safe('nj-driver', currentUser.role === 'driver' ? (currentUser.name||'') : ''); — closeModal('modal-calday'); — openModal('modal-newjob'); — setTimeout(() => { try { applyJobDefaults('nj'); } catch(e) {} }, 50); — } — async function createJob() { — const client = document.getElementById('nj-client').value.trim(); — const date   = document.getElementById('nj-date').value; — const origin = document.getElementById('nj-origin').value.trim(); — const dest   = document.getElementById('nj-dest').value.trim(); — if (!client || !date) { notify('⚠ Client name and date required', true); return; } — Custom ID or auto-generate — const customId = (document.getElementById('nj-job-id')?.value||'').trim().toUpperCase(); — const autoId   = 'JOB-' + String((STATE.jobs||[]).length + 1).padStart(3,'0'); — const jobId    = customId || autoId; — if ((STATE.jobs||[]).find(j => j.id === jobId)) { — notify('⚠ Job ID ' + jobId + ' already exists', true); return; — } — Collect helpers — const helpers = []; — document.querySelectorAll('.nj-helper-name').forEach(inp => { — const name = inp.value.trim(); — if (!name) return; — const row = inp.closest('.nj-helper-row'); — helpers.push({ — name, — rate:    parseFloat(row?.querySelector('.nj-helper-rate')?.value) || 0, — payType: row?.querySelector('.nj-helper-type')?.value || 'hourly', — hours: 0, days: 1, workerType: '1099', paid: false — }); — }); — const g = id => (document.getElementById(id)?.value || ''); — const newJob = { — id:            jobId, — refNum:        g('nj-ref-num'), — client, — phone:         g('nj-phone'), — email:         g('nj-email'), — date, — time:          g('nj-time'), — endDate:       g('nj-end-date') || g('nj-delivery-end').slice(0,10) || '', — endTime:       g('nj-end-time'), — origin, — dest, — pickupStart:   g('nj-pickup-start'), — pickupEnd:     g('nj-pickup-end'), — pickupDriver:  g('nj-pickup-driver'), — deliveryStart: g('nj-delivery-start'), — deliveryEnd:   g('nj-delivery-end'), — deliveryDriver:g('nj-delivery-driver'), — driver:        g('nj-driver'), — driverUser:    currentUser.username || '', — crewSize:      parseInt(g('nj-crew')) || 3, — crewNames:     helpers.map(h => h.name), — amount:        parseFloat(g('nj-amount')) || 0, — cubicFt:       parseFloat(g('nj-cubic-ft-2') || g('nj-cubic-ft')) || 0, — weight:        parseFloat(g('nj-weight')) || 0, — linehaul:      parseFloat(g('nj-linehaul')) || 0, — priority:      g('nj-priority'), — followUp:      g('nj-followup'), — fadd:          g('nj-fadd'), — type:          g('nj-type') || 'local', — notes:         g('nj-notes'), — companyId:     COMPANY_ID, — status:        'pending', — gates:         { preload:false, loaded:false, delivered:false, payment:false }, — messages:      { crew:[], client:[] }, — photos:        [], — signatures:    [], — payroll:       { driver:0, helpers } — }; — try { — await db.collection(COL_JOBS).doc(newJob.id).set(newJob); — closeModal('modal-newjob'); — notify('✓ Job ' + newJob.id + ' created — ' + client); — setTimeout(() => renderCalendar(), 200); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('createJob error:', e); — } — }, function renderExpenses() { — const content = document.getElementById('main-content'); — const role = currentUser.role; — const isDriver = role === 'driver'; — Scope to this driver's jobs/disbursements if not office — const myJobs = isDriver — ? STATE.jobs.filter(j => nameIncludes(j.driver, currentUser.name)) — : STATE.jobs; — const myDisbursements = isDriver — ? STATE.disbursements.filter(d => — d.driverUser === currentUser.username || — (d.driver && d.driver.toLowerCase().includes(currentUser.name.toLowerCase())) — ) — : STATE.disbursements; — const myReceipts = isDriver — ? STATE.receipts.filter(r => { — const nm = r.driver && nameIncludes(r.driver, currentUser.name); — const um = r.driverUser && r.driverUser === currentUser.username; — return nm || um; — }) — : STATE.receipts; — Totals — const totalDisbursed = myDisbursements.reduce((s,d) => s + (d.amount||0), 0); — const totalReceipts  = myReceipts.reduce((s,r) => s + (r.amount||0), 0); — const netOwed        = totalReceipts - totalDisbursed; // positive = driver owes, negative = company owes driver — let html = ` — <div class="section-header"> — <div> — <div class="section-title">EXPENSE TRACKER</div> — <div class="section-sub">${isDriver ? 'Your funds & receipts · ' + currentUser.name : 'All driver funds & expense reconciliation'}</div> — </div> — ${isAdmin() ? `<button class="btn-green" onclick="openDisbursementModal()">💰 SEND FUNDS</button>` : ''} — </div> — <!-- SUMMARY STATS --> — <div class="grid-3" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Funds Disbursed</div> — <div class="stat-value">$${totalDisbursed.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Receipts Submitted</div> — <div class="stat-value amber">$${totalReceipts.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">${netOwed > 0 ? 'Driver Owes Back' : netOwed < 0 ? 'Company Owes Driver' : 'Balanced'}</div> — <div class="stat-value ${netOwed > 0 ? 'red' : netOwed < 0 ? 'amber' : ''}"> — ${netOwed === 0 ? '✓ $0.00' : (netOwed > 0 ? '+' : '') + '$' + Math.abs(netOwed).toFixed(2)} — </div> — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div class="card" style="margin-bottom:16px"> — <div class="card-header"><div class="card-title">JOB EXPENSE BREAKDOWN</div></div> — <table> — <thead><tr> — <th>Job ID</th><th>Client</th><th>Driver</th> — <th style="text-align:right">Funds Sent</th> — <th style="text-align:right">Receipts</th> — <th style="text-align:right">Net</th> — <th>Status</th> — </tr></thead> — <tbody> — ${myJobs.length ? myJobs.map(j => { — const jobDisb = myDisbursements.filter(d => d.jobId === j.id); — const jobRec  = myReceipts.filter(r => r.jobId === j.id); — const sent    = jobDisb.reduce((s,d) => s + (d.amount||0), 0); — const spent   = jobRec.reduce((s,r) => s + (r.amount||0), 0); — const net     = spent - sent; — return `<tr onclick="openJobExpenseDetail('${j.id}')" style="cursor:pointer"> — <td><span class="tag">${j.id}</span></td> — <td>${j.client}</td> — <td class="info-text">${j.driver || '—'}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--green)">$${sent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--amber)">$${spent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);font-weight:700;color:${net > 0 ? 'var(--red)' : net < 0 ? 'var(--amber)' : 'var(--green)'}"> — ${net === 0 ? '✓ $0.00' : (net > 0 ? '+' : '') + '$' + Math.abs(net).toFixed(2)} — </td> — <td><span class="status-badge status-${j.status}">${j.status}</span></td> — </tr>`; — }).join('') : `<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--gray);font-family:var(--font-mono)">No jobs found</td></tr>`} — </tbody> — </table> — </div> — <!-- DISBURSEMENTS LOG --> — <div class="grid-2"> — <div class="card"> — <div class="card-header"> — <div class="card-title">💰 FUNDS DISBURSED</div> — ${isAdmin() ? `<button class="btn-green" style="padding:7px 14px;font-size:11px" onclick="openDisbursementModal()">+ SEND FUNDS</button>` : ''} — </div> — ${myDisbursements.length ? myDisbursements.map(d => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${d.driver} <span class="tag">${d.jobId}</span></div> — <div class="info-text">${d.date} · ${d.method || 'Cash'} · ${d.notes || ''}</div> — <div class="info-text" style="color:var(--gray)">${d.sentBy ? 'Sent by: ' + d.sentBy : ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--green)">$${(d.amount||0).toFixed(2)}</div> — ${role === 'office' ? `<button class="btn-sm" style="margin-top:4px;color:var(--red);border-color:#552222;font-size:10px" onclick="event.stopPropagation();deleteDisbursementEntry('${d.id}')">DELETE</button>` : ''} — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No funds disbursed yet.</p>`} — ${myDisbursements.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Disbursed</span> — <span style="font-family:var(--font-mono);color:var(--green);font-size:14px">$${totalDisbursed.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="card"> — <div class="card-header"><div class="card-title">🧾 RECEIPTS SUBMITTED</div></div> — ${myReceipts.length ? myReceipts.map(r => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${r.type} <span class="tag">${r.jobId}</span></div> — <div class="info-text">${r.driver} · ${r.date} · ${r.notes || ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--amber)">$${(r.amount||0).toFixed(2)}</div> — <div style="margin-top:2px">${r.approved — ? '<span style="color:var(--green);font-size:10px;font-family:var(--font-mono)">✓ APPROVED</span>' — : '<span style="color:var(--amber);font-size:10px;font-family:var(--font-mono)">⏳ PENDING</span>'}</div> — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No receipts submitted yet.</p>`} — ${myReceipts.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Receipts</span> — <span style="font-family:var(--font-mono);color:var(--amber);font-size:14px">$${totalReceipts.toFixed(2)}</span> — </div>` : ''} — </div> — </div>`; — content.innerHTML = html; — } — function openJobExpenseDetail(jobId) { — Switch to jobs tab and open the job detail — closeModal('modal-calday'); — openJobDetail(jobId); — } — function openDisbursementModal(prefillJobId) { — Pre-fill job ID if provided — document.getElementById('disb-jobid').value = prefillJobId || ''; — document.getElementById('disb-driver').value = ''; — document.getElementById('disb-amount').value = ''; — document.getElementById('disb-method').value = getSetting('defaultPay', 'Cash'); — document.getElementById('disb-notes').value = ''; — Populate driver dropdown from users — populateDisbDriverDropdown(); — openModal('modal-disbursement'); — } — async function populateDisbDriverDropdown() { — const sel = document.getElementById('disb-driver'); — sel.innerHTML = '<option value="">-- Select Driver --</option>'; — try { — const snap = await db.collection(COL_USERS) — .where('role', 'in', ['driver','helper']) — .get(); — snap.docs.forEach(d => { — const u = d.data(); — if (u.active) { — const opt = document.createElement('option'); — opt.value = JSON.stringify({ name: u.displayName, username: u.username }); — opt.textContent = u.displayName + ' (' + u.role + ')'; — sel.appendChild(opt); — } — }); — } catch(e) { — Fallback — type name manually — sel.innerHTML = '<option value="">Type driver name in notes</option>'; — } — } — async function saveDisbursementEntry() { — const jobId   = document.getElementById('disb-jobid').value.trim().toUpperCase(); — const driverRaw = document.getElementById('disb-driver').value; — const amount  = parseFloat(document.getElementById('disb-amount').value); — const method  = document.getElementById('disb-method').value; — const notes   = document.getElementById('disb-notes').value.trim(); — const btn     = document.getElementById('disb-submit-btn'); — if (!jobId)   { notify('⚠ Job ID is required', true); return; } — if (!driverRaw) { notify('⚠ Select a driver', true); return; } — if (isNaN(amount) || amount <= 0) { notify('⚠ Enter a valid amount', true); return; } — if (btn.disabled) return; — btn.disabled = true; — btn.textContent = 'SAVING...'; — try { — let driverName = driverRaw, driverUser = ''; — try { — const parsed = JSON.parse(driverRaw); — driverName = parsed.name; — driverUser = parsed.username; — } catch(e) { driverName = driverRaw; } — const disb = { — id:         'DISB-' + Date.now(), — jobId, — driver:     driverName, — driverUser: driverUser, — amount, — method, — notes, — sentBy:     currentUser.name, — date:       getTodayStr(), — createdAt:  new Date().toISOString() — }; — await saveDisbursement(disb); — closeModal('modal-disbursement'); — notify('$' + amount.toFixed(2) + ' disbursed to ' + driverName + ' ✓'); — Notify the driver — try { — if (driverUser) { — await sendNotification(driverUser, driverName, 'funds_sent', — 'Funds Sent 💰', — '$' + amount.toFixed(2) + ' sent via ' + method + ' for ' + jobId + (notes ? ' · ' + notes : '') + ' — from ' + currentUser.name, — { jobId, amount } — ); — } — } catch(e) {} — } catch(e) { — notify('⚠ Error: ' + e.message, true); — } finally { — btn.disabled = false; — btn.textContent = 'SEND FUNDS'; — } — } — async function deleteDisbursementEntry(disbId) { — if (!confirm('Delete this disbursement? This cannot be undone.')) return; — try { — await db.collection(COL_DISBURSEMENTS).doc(disbId).delete(); — notify('Disbursement deleted ✓'); — } catch(e) { notify('⚠ ' + e.message, true); } — }, function renderFleet() { — const content = document.getElementById('main-content'); — const role    = currentUser.role; — const canEdit = role === 'office'; — let html = ` — <div class="section-header"> — <div> — <div class="section-title">🚚 FLEET MANAGEMENT</div> — <div class="section-sub">${STATE.fleet.length} vehicle(s) registered · Move Masters OS</div> — </div> — ${canEdit ? `<button class="btn-green" onclick="openFleetModal()">+ ADD VEHICLE</button>` : ''} — </div>`; — if (!STATE.fleet.length) { — html += `<div class="card" style="text-align:center;padding:40px"> — <div style="font-size:48px;margin-bottom:12px">🚚</div> — <p class="info-text">No vehicles registered yet.</p> — ${canEdit ? `<button class="btn-green" style="margin-top:16px" onclick="openFleetModal()">+ ADD FIRST VEHICLE</button>` : ''} — </div>`; — } else { — Summary stats — const active  = STATE.fleet.filter(v => v.status === 'active').length; — const maint   = STATE.fleet.filter(v => v.status === 'maintenance').length; — const offline = STATE.fleet.filter(v => v.status === 'offline').length; — html += `<div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Active</div><div class="stat-value">${active}</div></div> — <div class="stat-box"><div class="stat-label">In Maintenance</div><div class="stat-value amber">${maint}</div></div> — <div class="stat-box"><div class="stat-label">Offline</div><div class="stat-value red">${offline}</div></div> — </div>`; — STATE.fleet.forEach(v => { — const assignedJob = STATE.jobs.find(j => j.vehicleId === v.id && j.status === 'active'); — const statusColor = v.status === 'active' ? 'var(--green)' : v.status === 'maintenance' ? 'var(--amber)' : 'var(--red)'; — const maintDue    = v.nextMaint ? new Date(v.nextMaint) : null; — const maintOverdue = maintDue && maintDue < new Date(); — html += ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:12px"> — <div> — <div style="display:flex;align-items:center;gap:10px;margin-bottom:6px"> — <div style="font-size:28px">${v.type === 'box_truck' ? '🚛' : v.type === 'cargo_van' ? '🚐' : '🚚'}</div> — <div> — <div style="font-family:var(--font-mono);font-size:14px;font-weight:700;color:var(--white)">${v.name || v.id}</div> — <div class="info-text">${v.make||''} ${v.model||''} ${v.year||''} · ${v.plate||'No plate'}</div> — </div> — </div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:11px;color:${statusColor};letter-spacing:2px">${(v.status||'active').toUpperCase()}</div> — ${canEdit ? `<button class="btn-sm" style="margin-top:6px" onclick="openFleetModal('${v.id}')">✎ EDIT</button>` : ''} — </div> — </div> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(110px,1fr));gap:8px;margin-bottom:12px"> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Capacity</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.capacity || '—'}</div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Mileage</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.mileage ? v.mileage.toLocaleString() + ' mi' : '—'}</div> — </div> — <div class="stat-box" style="padding:8px;${maintOverdue ? 'border-color:var(--red)' : ''}"> — <div class="stat-label" style="font-size:9px">Next Service</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${maintOverdue ? 'var(--red)' : 'var(--white)'}"> — ${v.nextMaint || 'Not set'}${maintOverdue ? ' ⚠' : ''} — </div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Assigned Job</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${assignedJob ? 'var(--green)' : 'var(--gray)'}"> — ${assignedJob ? assignedJob.id : 'Available'} — </div> — </div> — </div> — ${v.notes ? `<div class="info-text" style="margin-bottom:10px">📝 ${v.notes}</div>` : ''} — <!-- MAINTENANCE LOG --> — ${v.maintLog && v.maintLog.length ? ` — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:6px">MAINTENANCE LOG</div> — ${v.maintLog.slice(-3).reverse().map(m => ` — <div style="display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px solid var(--gray2);font-size:12px"> — <div><span class="info-text">${m.date}</span> · ${m.work}</div> — <div style="font-family:var(--font-mono);color:var(--amber)">$${(m.cost||0).toFixed(0)}</div> — </div>`).join('')}` : ''} — ${canEdit ? ` — <div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap"> — <button class="btn-sm" onclick="logMaintenance('${v.id}')">🔧 LOG SERVICE</button> — <button class="btn-sm" onclick="updateMileage('${v.id}')">📍 UPDATE MILEAGE</button> — <select style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:5px 8px;font-family:var(--font-mono);font-size:10px;border-radius:3px" — onchange="updateVehicleStatus('${v.id}',this.value)"> — <option value="active"      ${v.status==='active'      ?'selected':''}>● Active</option> — <option value="maintenance" ${v.status==='maintenance' ?'selected':''}>🔧 Maintenance</option> — <option value="offline"     ${v.status==='offline'     ?'selected':''}>○ Offline</option> — </select> — </div>` : ''} — </div>`; — }); — } — content.innerHTML = html; — } — function openFleetModal(vehicleId) { — const v = vehicleId ? STATE.fleet.find(x => x.id === vehicleId) : null; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">${v ? 'EDIT VEHICLE — ' + v.id : 'ADD VEHICLE'}</div> — <div class="grid-2"> — <div class="form-group"><label>Vehicle Name / Unit #</label> — <input type="text" id="flt-name" value="${(v&&v.name)||''}" placeholder="e.g. Truck 1 · Unit 24" /></div> — <div class="form-group"><label>Type</label> — <select id="flt-type"> — <option value="box_truck" ${v?.type==='box_truck'?'selected':''}>🚛 Box Truck</option> — <option value="cargo_van" ${v?.type==='cargo_van'?'selected':''}>🚐 Cargo Van</option> — <option value="flatbed"   ${v?.type==='flatbed'  ?'selected':''}>🚚 Flatbed</option> — <option value="other"     ${v?.type==='other'    ?'selected':''}>📋 Other</option> — </select> — </div> — </div> — <div class="grid-3"> — <div class="form-group"><label>Make</label><input type="text" id="flt-make" value="${(v&&v.make)||''}" placeholder="Ford" /></div> — <div class="form-group"><label>Model</label><input type="text" id="flt-model" value="${(v&&v.model)||''}" placeholder="E-450" /></div> — <div class="form-group"><label>Year</label><input type="number" id="flt-year" value="${(v&&v.year)||''}" placeholder="2019" /></div> — </div> — <div class="grid-2"> — <div class="form-group"><label>License Plate</label><input type="text" id="flt-plate" value="${(v&&v.plate)||''}" placeholder="ABC-1234" style="text-transform:uppercase"/></div> — <div class="form-group"><label>Capacity (text label)</label><input type="text" id="flt-cap" value="${(v&&v.capacity)||''}" placeholder="26 ft · 1500 lbs" /></div> — </div> — <div class="grid-3"> — <div class="form-group"> — <label>Max Cubic Feet <span class="info-text">(for load optimization)</span></label> — <input type="number" id="flt-maxcuft" value="${(v&&v.maxCuFt)||''}" placeholder="e.g. 1200" min="0" step="50" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Fuel Efficiency (MPG) <span class="info-text">(for cost calc)</span></label> — <input type="number" id="flt-mpg" value="${(v&&v.mpg)||''}" placeholder="e.g. 10" min="1" step="0.5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Load Efficiency % <span class="info-text">(usable space)</span></label> — <input type="number" id="flt-loadeff" value="${(v&&v.loadEff)||85}" placeholder="85" min="50" max="100" step="5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — </div> — <div class="grid-2"> — <div class="form-group"><label>Current Mileage</label><input type="number" id="flt-miles" value="${(v&&v.mileage)||''}" placeholder="85000" /></div> — <div class="form-group"><label>Next Service Due</label><input type="date" id="flt-maint" value="${(v&&v.nextMaint)||''}" /></div> — </div> — <div class="form-group"><label>Notes</label> — <input type="text" id="flt-notes" value="${(v&&v.notes)||''}" placeholder="Insurance, registration, special equipment..." /></div> — <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:8px"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" onclick="saveVehicle('${vehicleId||''}')">SAVE VEHICLE</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveVehicle(vehicleId) { — const vehicle = { — id:        vehicleId || 'VEH-' + Date.now(), — name:      document.getElementById('flt-name').value.trim(), — type:      document.getElementById('flt-type').value, — make:      document.getElementById('flt-make').value.trim(), — model:     document.getElementById('flt-model').value.trim(), — year:      document.getElementById('flt-year').value, — plate:     document.getElementById('flt-plate').value.trim().toUpperCase(), — capacity:  document.getElementById('flt-cap').value.trim(), — maxCuFt:   parseFloat(document.getElementById('flt-maxcuft')?.value)||0, — mpg:       parseFloat(document.getElementById('flt-mpg')?.value)||0, — loadEff:   parseFloat(document.getElementById('flt-loadeff')?.value)||85, — mileage:   parseInt(document.getElementById('flt-miles').value)||0, — nextMaint: document.getElementById('flt-maint').value, — notes:     document.getElementById('flt-notes').value.trim(), — status:    vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.status||'active') : 'active', — maintLog:  vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.maintLog||[]) : [], — updatedAt: new Date().toISOString() — }; — if (!vehicle.name) { notify('⚠ Vehicle name is required', true); return; } — try { — await db.collection(COL_FLEET).doc(vehicle.id).set(vehicle); — notify((vehicleId ? 'Vehicle updated' : 'Vehicle added') + ' ✓'); — closeModal('modal-jobdetail'); — } catch(e) { notify('⚠ ' + e.message, true); } — } — async function logMaintenance(vehicleId) { — const work = prompt('Service performed:'); if (!work) return; — const cost = parseFloat(prompt('Cost ($):') || '0'); — const v    = STATE.fleet.find(x => x.id === vehicleId); — if (!v) return; — if (!v.maintLog) v.maintLog = []; — v.maintLog.push({ date: getTodayStr(), work, cost, loggedBy: currentUser.name }); — v.updatedAt = new Date().toISOString(); — await db.collection(COL_FLEET).doc(vehicleId).set(v); — notify('Maintenance logged ✓'); — } — async function updateMileage(vehicleId) { — const miles = prompt('Enter current mileage:'); if (!miles) return; — await db.collection(COL_FLEET).doc(vehicleId).update({ mileage: parseInt(miles)||0 }); — notify('Mileage updated ✓'); — } — async function updateVehicleStatus(vehicleId, status) { — await db.collection(COL_FLEET).doc(vehicleId).update({ status }); — notify('Vehicle status: ' + status.toUpperCase()); — }
| FIREBASE INIT | 1930-1953 | 32 | — | 1099 / W2 YEAR-END TAX EXPORT — Aggregates all payments per contractor/employee for the year — Generates: on-screen summary + CSV export + PDF report, BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore, CLIENT BOOKING PORTAL — URL: fleetflowmovemasters.netlify.app/?book=1&co=COMPANY_ID — Moving company shares this link on their website — Client fills details → instant quote → pays deposit → job created, COMPANY BRANDING ENGINE, DASHBOARD, EDIT JOB  ← NEW FEATURE, EMPLOYEE ID SYSTEM — empId  = auto-assigned badge (EMP-001, EMP-002...) — handle = user-chosen nickname/alias — displayHandle() = handle if set, else empId, FIRST RUN CHECK — runs on page load before login, JOB DETAIL MODAL, LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool, MESSAGING, MIGRATION — Stamp companyId on any legacy docs missing it — Runs silently on every login — safe to run repeatedly, NOTIFICATION ENGINE, OFFLINE MODE — Full PWA with offline queue — Caches app shell on first load — Queues Firestore writes when offline — Syncs queue automatically when back online, Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — }, PAYROLL LEDGER — Mark Paid modal, RECEIPTS, ROLE SWITCHER — instant role switch without logout, SESSION PERSISTENCE — remember me across refreshes/tab closes, SOUND ENGINE — Web Audio API, no external files needed, STATE & STORAGE, STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live, SYSTEM HEALTH DASHBOARD — Platform operator view — JPG Ventures sees all licensees, sync status, revenue flow — Accessible only to creator/owner accounts, TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created, TRIAL + TIER SYSTEM, TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order, USER MANAGEMENT (Office only), WAREHOUSE, WAREHOUSE WORK LOGGING — Log warehouse work by employee — shows on calendar + payroll, function openNewJobModal(prefillDate) { — ── HARD RESET — every field to blank/zero ────────────────── — const safe = (id, val='') => { const el = document.getElementById(id); if(el) el.value = val; }; — Core job fields — safe('nj-client'); safe('nj-phone'); safe('nj-email'); — safe('nj-origin'); safe('nj-dest'); safe('nj-pickup-from'); — safe('nj-amount'); safe('nj-notes'); safe('nj-carrier'); — safe('nj-cubic-ft'); safe('nj-cubic-ft-2'); safe('nj-rate-per-cuft'); — safe('nj-weight'); safe('nj-linehaul'); — safe('nj-end-date'); safe('nj-end-time'); — safe('nj-pickup-start'); safe('nj-pickup-end'); — safe('nj-delivery-start'); safe('nj-delivery-end'); — safe('nj-pickup-driver'); safe('nj-delivery-driver'); — safe('nj-job-id'); safe('nj-ref-num'); — safe('nj-priority'); safe('nj-followup'); safe('nj-fadd'); — safe('nj-broker-ref'); safe('nj-carrier'); — ALL estimate fields — zero out completely — safe('nj-est-fuel', ''); — safe('nj-est-fuel-pct', '0'); — safe('nj-est-overload-cuft', ''); — safe('nj-est-stairs-origin', ''); — safe('nj-est-stairs-dest', ''); — safe('nj-est-longcarry-origin', ''); — safe('nj-est-longcarry-dest', ''); — safe('nj-est-shuttle', ''); — safe('nj-est-bulky', ''); — safe('nj-est-split', ''); — safe('nj-est-pgs', ''); — safe('nj-est-packing', ''); — safe('nj-est-storage-days', ''); — safe('nj-est-storage-rate', ''); — safe('nj-est-other', ''); — safe('nj-est-deposit', ''); — safe('nj-est-partial2', ''); — safe('nj-est-tip', ''); — safe('nj-est-discount', ''); — safe('nj-est-discount-label', ''); — safe('nj-est-others1', '');  safe('nj-est-others1-label', ''); — safe('nj-est-others2', '');  safe('nj-est-others2-label', ''); — safe('nj-est-others3', '');  safe('nj-est-others3-label', ''); — safe('nj-est-extra1', '');   safe('nj-est-extra1-label', ''); — safe('nj-est-extra2', '');   safe('nj-est-extra2-label', ''); — safe('nj-est-extra3', '');   safe('nj-est-extra3-label', ''); — safe('nj-est-notes', ''); — safe('nj-est-office-remarks', ''); — safe('nj-est-slip-remarks', ''); — Reset carrier total display — const ct = document.getElementById('nj-carrier-total'); if(ct) ct.textContent = '$0.00'; — Clear helper rows — const hr = document.getElementById('nj-helper-rows'); if(hr) hr.innerHTML = ''; — Reset all selects to index 0 — ['nj-job-for','nj-est-tip-method','nj-est-valuation', — 'nj-est-fuel-mode','nj-est-tip-assign'].forEach(id => { — const el = document.getElementById(id); — if (el) el.selectedIndex = 0; — }); — try { toggleCarrierFields('nj'); } catch(e) {} — Apply defaults — const s2 = loadSettings(); — safe('nj-crew', s2.defaultCrew || '3'); — safe('nj-type', s2.defaultMove || 'local'); — safe('nj-date', prefillDate || getTodayStr()); — safe('nj-time', getSetting('defaultTime', '08:00')); — safe('nj-driver', currentUser.role === 'driver' ? (currentUser.name||'') : ''); — closeModal('modal-calday'); — openModal('modal-newjob'); — setTimeout(() => { try { applyJobDefaults('nj'); } catch(e) {} }, 50); — } — async function createJob() { — const client = document.getElementById('nj-client').value.trim(); — const date   = document.getElementById('nj-date').value; — const origin = document.getElementById('nj-origin').value.trim(); — const dest   = document.getElementById('nj-dest').value.trim(); — if (!client || !date) { notify('⚠ Client name and date required', true); return; } — Custom ID or auto-generate — const customId = (document.getElementById('nj-job-id')?.value||'').trim().toUpperCase(); — const autoId   = 'JOB-' + String((STATE.jobs||[]).length + 1).padStart(3,'0'); — const jobId    = customId || autoId; — if ((STATE.jobs||[]).find(j => j.id === jobId)) { — notify('⚠ Job ID ' + jobId + ' already exists', true); return; — } — Collect helpers — const helpers = []; — document.querySelectorAll('.nj-helper-name').forEach(inp => { — const name = inp.value.trim(); — if (!name) return; — const row = inp.closest('.nj-helper-row'); — helpers.push({ — name, — rate:    parseFloat(row?.querySelector('.nj-helper-rate')?.value) || 0, — payType: row?.querySelector('.nj-helper-type')?.value || 'hourly', — hours: 0, days: 1, workerType: '1099', paid: false — }); — }); — const g = id => (document.getElementById(id)?.value || ''); — const newJob = { — id:            jobId, — refNum:        g('nj-ref-num'), — client, — phone:         g('nj-phone'), — email:         g('nj-email'), — date, — time:          g('nj-time'), — endDate:       g('nj-end-date') || g('nj-delivery-end').slice(0,10) || '', — endTime:       g('nj-end-time'), — origin, — dest, — pickupStart:   g('nj-pickup-start'), — pickupEnd:     g('nj-pickup-end'), — pickupDriver:  g('nj-pickup-driver'), — deliveryStart: g('nj-delivery-start'), — deliveryEnd:   g('nj-delivery-end'), — deliveryDriver:g('nj-delivery-driver'), — driver:        g('nj-driver'), — driverUser:    currentUser.username || '', — crewSize:      parseInt(g('nj-crew')) || 3, — crewNames:     helpers.map(h => h.name), — amount:        parseFloat(g('nj-amount')) || 0, — cubicFt:       parseFloat(g('nj-cubic-ft-2') || g('nj-cubic-ft')) || 0, — weight:        parseFloat(g('nj-weight')) || 0, — linehaul:      parseFloat(g('nj-linehaul')) || 0, — priority:      g('nj-priority'), — followUp:      g('nj-followup'), — fadd:          g('nj-fadd'), — type:          g('nj-type') || 'local', — notes:         g('nj-notes'), — companyId:     COMPANY_ID, — status:        'pending', — gates:         { preload:false, loaded:false, delivered:false, payment:false }, — messages:      { crew:[], client:[] }, — photos:        [], — signatures:    [], — payroll:       { driver:0, helpers } — }; — try { — await db.collection(COL_JOBS).doc(newJob.id).set(newJob); — closeModal('modal-newjob'); — notify('✓ Job ' + newJob.id + ' created — ' + client); — setTimeout(() => renderCalendar(), 200); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('createJob error:', e); — } — }, function renderExpenses() { — const content = document.getElementById('main-content'); — const role = currentUser.role; — const isDriver = role === 'driver'; — Scope to this driver's jobs/disbursements if not office — const myJobs = isDriver — ? STATE.jobs.filter(j => nameIncludes(j.driver, currentUser.name)) — : STATE.jobs; — const myDisbursements = isDriver — ? STATE.disbursements.filter(d => — d.driverUser === currentUser.username || — (d.driver && d.driver.toLowerCase().includes(currentUser.name.toLowerCase())) — ) — : STATE.disbursements; — const myReceipts = isDriver — ? STATE.receipts.filter(r => { — const nm = r.driver && nameIncludes(r.driver, currentUser.name); — const um = r.driverUser && r.driverUser === currentUser.username; — return nm || um; — }) — : STATE.receipts; — Totals — const totalDisbursed = myDisbursements.reduce((s,d) => s + (d.amount||0), 0); — const totalReceipts  = myReceipts.reduce((s,r) => s + (r.amount||0), 0); — const netOwed        = totalReceipts - totalDisbursed; // positive = driver owes, negative = company owes driver — let html = ` — <div class="section-header"> — <div> — <div class="section-title">EXPENSE TRACKER</div> — <div class="section-sub">${isDriver ? 'Your funds & receipts · ' + currentUser.name : 'All driver funds & expense reconciliation'}</div> — </div> — ${isAdmin() ? `<button class="btn-green" onclick="openDisbursementModal()">💰 SEND FUNDS</button>` : ''} — </div> — <!-- SUMMARY STATS --> — <div class="grid-3" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Funds Disbursed</div> — <div class="stat-value">$${totalDisbursed.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Receipts Submitted</div> — <div class="stat-value amber">$${totalReceipts.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">${netOwed > 0 ? 'Driver Owes Back' : netOwed < 0 ? 'Company Owes Driver' : 'Balanced'}</div> — <div class="stat-value ${netOwed > 0 ? 'red' : netOwed < 0 ? 'amber' : ''}"> — ${netOwed === 0 ? '✓ $0.00' : (netOwed > 0 ? '+' : '') + '$' + Math.abs(netOwed).toFixed(2)} — </div> — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div class="card" style="margin-bottom:16px"> — <div class="card-header"><div class="card-title">JOB EXPENSE BREAKDOWN</div></div> — <table> — <thead><tr> — <th>Job ID</th><th>Client</th><th>Driver</th> — <th style="text-align:right">Funds Sent</th> — <th style="text-align:right">Receipts</th> — <th style="text-align:right">Net</th> — <th>Status</th> — </tr></thead> — <tbody> — ${myJobs.length ? myJobs.map(j => { — const jobDisb = myDisbursements.filter(d => d.jobId === j.id); — const jobRec  = myReceipts.filter(r => r.jobId === j.id); — const sent    = jobDisb.reduce((s,d) => s + (d.amount||0), 0); — const spent   = jobRec.reduce((s,r) => s + (r.amount||0), 0); — const net     = spent - sent; — return `<tr onclick="openJobExpenseDetail('${j.id}')" style="cursor:pointer"> — <td><span class="tag">${j.id}</span></td> — <td>${j.client}</td> — <td class="info-text">${j.driver || '—'}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--green)">$${sent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--amber)">$${spent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);font-weight:700;color:${net > 0 ? 'var(--red)' : net < 0 ? 'var(--amber)' : 'var(--green)'}"> — ${net === 0 ? '✓ $0.00' : (net > 0 ? '+' : '') + '$' + Math.abs(net).toFixed(2)} — </td> — <td><span class="status-badge status-${j.status}">${j.status}</span></td> — </tr>`; — }).join('') : `<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--gray);font-family:var(--font-mono)">No jobs found</td></tr>`} — </tbody> — </table> — </div> — <!-- DISBURSEMENTS LOG --> — <div class="grid-2"> — <div class="card"> — <div class="card-header"> — <div class="card-title">💰 FUNDS DISBURSED</div> — ${isAdmin() ? `<button class="btn-green" style="padding:7px 14px;font-size:11px" onclick="openDisbursementModal()">+ SEND FUNDS</button>` : ''} — </div> — ${myDisbursements.length ? myDisbursements.map(d => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${d.driver} <span class="tag">${d.jobId}</span></div> — <div class="info-text">${d.date} · ${d.method || 'Cash'} · ${d.notes || ''}</div> — <div class="info-text" style="color:var(--gray)">${d.sentBy ? 'Sent by: ' + d.sentBy : ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--green)">$${(d.amount||0).toFixed(2)}</div> — ${role === 'office' ? `<button class="btn-sm" style="margin-top:4px;color:var(--red);border-color:#552222;font-size:10px" onclick="event.stopPropagation();deleteDisbursementEntry('${d.id}')">DELETE</button>` : ''} — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No funds disbursed yet.</p>`} — ${myDisbursements.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Disbursed</span> — <span style="font-family:var(--font-mono);color:var(--green);font-size:14px">$${totalDisbursed.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="card"> — <div class="card-header"><div class="card-title">🧾 RECEIPTS SUBMITTED</div></div> — ${myReceipts.length ? myReceipts.map(r => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${r.type} <span class="tag">${r.jobId}</span></div> — <div class="info-text">${r.driver} · ${r.date} · ${r.notes || ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--amber)">$${(r.amount||0).toFixed(2)}</div> — <div style="margin-top:2px">${r.approved — ? '<span style="color:var(--green);font-size:10px;font-family:var(--font-mono)">✓ APPROVED</span>' — : '<span style="color:var(--amber);font-size:10px;font-family:var(--font-mono)">⏳ PENDING</span>'}</div> — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No receipts submitted yet.</p>`} — ${myReceipts.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Receipts</span> — <span style="font-family:var(--font-mono);color:var(--amber);font-size:14px">$${totalReceipts.toFixed(2)}</span> — </div>` : ''} — </div> — </div>`; — content.innerHTML = html; — } — function openJobExpenseDetail(jobId) { — Switch to jobs tab and open the job detail — closeModal('modal-calday'); — openJobDetail(jobId); — } — function openDisbursementModal(prefillJobId) { — Pre-fill job ID if provided — document.getElementById('disb-jobid').value = prefillJobId || ''; — document.getElementById('disb-driver').value = ''; — document.getElementById('disb-amount').value = ''; — document.getElementById('disb-method').value = getSetting('defaultPay', 'Cash'); — document.getElementById('disb-notes').value = ''; — Populate driver dropdown from users — populateDisbDriverDropdown(); — openModal('modal-disbursement'); — } — async function populateDisbDriverDropdown() { — const sel = document.getElementById('disb-driver'); — sel.innerHTML = '<option value="">-- Select Driver --</option>'; — try { — const snap = await db.collection(COL_USERS) — .where('role', 'in', ['driver','helper']) — .get(); — snap.docs.forEach(d => { — const u = d.data(); — if (u.active) { — const opt = document.createElement('option'); — opt.value = JSON.stringify({ name: u.displayName, username: u.username }); — opt.textContent = u.displayName + ' (' + u.role + ')'; — sel.appendChild(opt); — } — }); — } catch(e) { — Fallback — type name manually — sel.innerHTML = '<option value="">Type driver name in notes</option>'; — } — } — async function saveDisbursementEntry() { — const jobId   = document.getElementById('disb-jobid').value.trim().toUpperCase(); — const driverRaw = document.getElementById('disb-driver').value; — const amount  = parseFloat(document.getElementById('disb-amount').value); — const method  = document.getElementById('disb-method').value; — const notes   = document.getElementById('disb-notes').value.trim(); — const btn     = document.getElementById('disb-submit-btn'); — if (!jobId)   { notify('⚠ Job ID is required', true); return; } — if (!driverRaw) { notify('⚠ Select a driver', true); return; } — if (isNaN(amount) || amount <= 0) { notify('⚠ Enter a valid amount', true); return; } — if (btn.disabled) return; — btn.disabled = true; — btn.textContent = 'SAVING...'; — try { — let driverName = driverRaw, driverUser = ''; — try { — const parsed = JSON.parse(driverRaw); — driverName = parsed.name; — driverUser = parsed.username; — } catch(e) { driverName = driverRaw; } — const disb = { — id:         'DISB-' + Date.now(), — jobId, — driver:     driverName, — driverUser: driverUser, — amount, — method, — notes, — sentBy:     currentUser.name, — date:       getTodayStr(), — createdAt:  new Date().toISOString() — }; — await saveDisbursement(disb); — closeModal('modal-disbursement'); — notify('$' + amount.toFixed(2) + ' disbursed to ' + driverName + ' ✓'); — Notify the driver — try { — if (driverUser) { — await sendNotification(driverUser, driverName, 'funds_sent', — 'Funds Sent 💰', — '$' + amount.toFixed(2) + ' sent via ' + method + ' for ' + jobId + (notes ? ' · ' + notes : '') + ' — from ' + currentUser.name, — { jobId, amount } — ); — } — } catch(e) {} — } catch(e) { — notify('⚠ Error: ' + e.message, true); — } finally { — btn.disabled = false; — btn.textContent = 'SEND FUNDS'; — } — } — async function deleteDisbursementEntry(disbId) { — if (!confirm('Delete this disbursement? This cannot be undone.')) return; — try { — await db.collection(COL_DISBURSEMENTS).doc(disbId).delete(); — notify('Disbursement deleted ✓'); — } catch(e) { notify('⚠ ' + e.message, true); } — }, function renderFleet() { — const content = document.getElementById('main-content'); — const role    = currentUser.role; — const canEdit = role === 'office'; — let html = ` — <div class="section-header"> — <div> — <div class="section-title">🚚 FLEET MANAGEMENT</div> — <div class="section-sub">${STATE.fleet.length} vehicle(s) registered · Move Masters OS</div> — </div> — ${canEdit ? `<button class="btn-green" onclick="openFleetModal()">+ ADD VEHICLE</button>` : ''} — </div>`; — if (!STATE.fleet.length) { — html += `<div class="card" style="text-align:center;padding:40px"> — <div style="font-size:48px;margin-bottom:12px">🚚</div> — <p class="info-text">No vehicles registered yet.</p> — ${canEdit ? `<button class="btn-green" style="margin-top:16px" onclick="openFleetModal()">+ ADD FIRST VEHICLE</button>` : ''} — </div>`; — } else { — Summary stats — const active  = STATE.fleet.filter(v => v.status === 'active').length; — const maint   = STATE.fleet.filter(v => v.status === 'maintenance').length; — const offline = STATE.fleet.filter(v => v.status === 'offline').length; — html += `<div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Active</div><div class="stat-value">${active}</div></div> — <div class="stat-box"><div class="stat-label">In Maintenance</div><div class="stat-value amber">${maint}</div></div> — <div class="stat-box"><div class="stat-label">Offline</div><div class="stat-value red">${offline}</div></div> — </div>`; — STATE.fleet.forEach(v => { — const assignedJob = STATE.jobs.find(j => j.vehicleId === v.id && j.status === 'active'); — const statusColor = v.status === 'active' ? 'var(--green)' : v.status === 'maintenance' ? 'var(--amber)' : 'var(--red)'; — const maintDue    = v.nextMaint ? new Date(v.nextMaint) : null; — const maintOverdue = maintDue && maintDue < new Date(); — html += ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:12px"> — <div> — <div style="display:flex;align-items:center;gap:10px;margin-bottom:6px"> — <div style="font-size:28px">${v.type === 'box_truck' ? '🚛' : v.type === 'cargo_van' ? '🚐' : '🚚'}</div> — <div> — <div style="font-family:var(--font-mono);font-size:14px;font-weight:700;color:var(--white)">${v.name || v.id}</div> — <div class="info-text">${v.make||''} ${v.model||''} ${v.year||''} · ${v.plate||'No plate'}</div> — </div> — </div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:11px;color:${statusColor};letter-spacing:2px">${(v.status||'active').toUpperCase()}</div> — ${canEdit ? `<button class="btn-sm" style="margin-top:6px" onclick="openFleetModal('${v.id}')">✎ EDIT</button>` : ''} — </div> — </div> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(110px,1fr));gap:8px;margin-bottom:12px"> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Capacity</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.capacity || '—'}</div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Mileage</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.mileage ? v.mileage.toLocaleString() + ' mi' : '—'}</div> — </div> — <div class="stat-box" style="padding:8px;${maintOverdue ? 'border-color:var(--red)' : ''}"> — <div class="stat-label" style="font-size:9px">Next Service</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${maintOverdue ? 'var(--red)' : 'var(--white)'}"> — ${v.nextMaint || 'Not set'}${maintOverdue ? ' ⚠' : ''} — </div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Assigned Job</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${assignedJob ? 'var(--green)' : 'var(--gray)'}"> — ${assignedJob ? assignedJob.id : 'Available'} — </div> — </div> — </div> — ${v.notes ? `<div class="info-text" style="margin-bottom:10px">📝 ${v.notes}</div>` : ''} — <!-- MAINTENANCE LOG --> — ${v.maintLog && v.maintLog.length ? ` — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:6px">MAINTENANCE LOG</div> — ${v.maintLog.slice(-3).reverse().map(m => ` — <div style="display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px solid var(--gray2);font-size:12px"> — <div><span class="info-text">${m.date}</span> · ${m.work}</div> — <div style="font-family:var(--font-mono);color:var(--amber)">$${(m.cost||0).toFixed(0)}</div> — </div>`).join('')}` : ''} — ${canEdit ? ` — <div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap"> — <button class="btn-sm" onclick="logMaintenance('${v.id}')">🔧 LOG SERVICE</button> — <button class="btn-sm" onclick="updateMileage('${v.id}')">📍 UPDATE MILEAGE</button> — <select style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:5px 8px;font-family:var(--font-mono);font-size:10px;border-radius:3px" — onchange="updateVehicleStatus('${v.id}',this.value)"> — <option value="active"      ${v.status==='active'      ?'selected':''}>● Active</option> — <option value="maintenance" ${v.status==='maintenance' ?'selected':''}>🔧 Maintenance</option> — <option value="offline"     ${v.status==='offline'     ?'selected':''}>○ Offline</option> — </select> — </div>` : ''} — </div>`; — }); — } — content.innerHTML = html; — } — function openFleetModal(vehicleId) { — const v = vehicleId ? STATE.fleet.find(x => x.id === vehicleId) : null; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">${v ? 'EDIT VEHICLE — ' + v.id : 'ADD VEHICLE'}</div> — <div class="grid-2"> — <div class="form-group"><label>Vehicle Name / Unit #</label> — <input type="text" id="flt-name" value="${(v&&v.name)||''}" placeholder="e.g. Truck 1 · Unit 24" /></div> — <div class="form-group"><label>Type</label> — <select id="flt-type"> — <option value="box_truck" ${v?.type==='box_truck'?'selected':''}>🚛 Box Truck</option> — <option value="cargo_van" ${v?.type==='cargo_van'?'selected':''}>🚐 Cargo Van</option> — <option value="flatbed"   ${v?.type==='flatbed'  ?'selected':''}>🚚 Flatbed</option> — <option value="other"     ${v?.type==='other'    ?'selected':''}>📋 Other</option> — </select> — </div> — </div> — <div class="grid-3"> — <div class="form-group"><label>Make</label><input type="text" id="flt-make" value="${(v&&v.make)||''}" placeholder="Ford" /></div> — <div class="form-group"><label>Model</label><input type="text" id="flt-model" value="${(v&&v.model)||''}" placeholder="E-450" /></div> — <div class="form-group"><label>Year</label><input type="number" id="flt-year" value="${(v&&v.year)||''}" placeholder="2019" /></div> — </div> — <div class="grid-2"> — <div class="form-group"><label>License Plate</label><input type="text" id="flt-plate" value="${(v&&v.plate)||''}" placeholder="ABC-1234" style="text-transform:uppercase"/></div> — <div class="form-group"><label>Capacity (text label)</label><input type="text" id="flt-cap" value="${(v&&v.capacity)||''}" placeholder="26 ft · 1500 lbs" /></div> — </div> — <div class="grid-3"> — <div class="form-group"> — <label>Max Cubic Feet <span class="info-text">(for load optimization)</span></label> — <input type="number" id="flt-maxcuft" value="${(v&&v.maxCuFt)||''}" placeholder="e.g. 1200" min="0" step="50" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Fuel Efficiency (MPG) <span class="info-text">(for cost calc)</span></label> — <input type="number" id="flt-mpg" value="${(v&&v.mpg)||''}" placeholder="e.g. 10" min="1" step="0.5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Load Efficiency % <span class="info-text">(usable space)</span></label> — <input type="number" id="flt-loadeff" value="${(v&&v.loadEff)||85}" placeholder="85" min="50" max="100" step="5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — </div> — <div class="grid-2"> — <div class="form-group"><label>Current Mileage</label><input type="number" id="flt-miles" value="${(v&&v.mileage)||''}" placeholder="85000" /></div> — <div class="form-group"><label>Next Service Due</label><input type="date" id="flt-maint" value="${(v&&v.nextMaint)||''}" /></div> — </div> — <div class="form-group"><label>Notes</label> — <input type="text" id="flt-notes" value="${(v&&v.notes)||''}" placeholder="Insurance, registration, special equipment..." /></div> — <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:8px"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" onclick="saveVehicle('${vehicleId||''}')">SAVE VEHICLE</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveVehicle(vehicleId) { — const vehicle = { — id:        vehicleId || 'VEH-' + Date.now(), — name:      document.getElementById('flt-name').value.trim(), — type:      document.getElementById('flt-type').value, — make:      document.getElementById('flt-make').value.trim(), — model:     document.getElementById('flt-model').value.trim(), — year:      document.getElementById('flt-year').value, — plate:     document.getElementById('flt-plate').value.trim().toUpperCase(), — capacity:  document.getElementById('flt-cap').value.trim(), — maxCuFt:   parseFloat(document.getElementById('flt-maxcuft')?.value)||0, — mpg:       parseFloat(document.getElementById('flt-mpg')?.value)||0, — loadEff:   parseFloat(document.getElementById('flt-loadeff')?.value)||85, — mileage:   parseInt(document.getElementById('flt-miles').value)||0, — nextMaint: document.getElementById('flt-maint').value, — notes:     document.getElementById('flt-notes').value.trim(), — status:    vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.status||'active') : 'active', — maintLog:  vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.maintLog||[]) : [], — updatedAt: new Date().toISOString() — }; — if (!vehicle.name) { notify('⚠ Vehicle name is required', true); return; } — try { — await db.collection(COL_FLEET).doc(vehicle.id).set(vehicle); — notify((vehicleId ? 'Vehicle updated' : 'Vehicle added') + ' ✓'); — closeModal('modal-jobdetail'); — } catch(e) { notify('⚠ ' + e.message, true); } — } — async function logMaintenance(vehicleId) { — const work = prompt('Service performed:'); if (!work) return; — const cost = parseFloat(prompt('Cost ($):') || '0'); — const v    = STATE.fleet.find(x => x.id === vehicleId); — if (!v) return; — if (!v.maintLog) v.maintLog = []; — v.maintLog.push({ date: getTodayStr(), work, cost, loggedBy: currentUser.name }); — v.updatedAt = new Date().toISOString(); — await db.collection(COL_FLEET).doc(vehicleId).set(v); — notify('Maintenance logged ✓'); — } — async function updateMileage(vehicleId) { — const miles = prompt('Enter current mileage:'); if (!miles) return; — await db.collection(COL_FLEET).doc(vehicleId).update({ mileage: parseInt(miles)||0 }); — notify('Mileage updated ✓'); — } — async function updateVehicleStatus(vehicleId, status) { — await db.collection(COL_FLEET).doc(vehicleId).update({ status }); — notify('Vehicle status: ' + status.toUpperCase()); — }
| ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 1958-2001 | 36 | STATE & STORAGE | 1099 / W2 YEAR-END TAX EXPORT — Aggregates all payments per contractor/employee for the year — Generates: on-screen summary + CSV export + PDF report, BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore, CALENDAR, COMPANY BRANDING ENGINE, CREW LOG HELPERS — extracted to avoid nested template literal — Chrome syntax errors from 4+ levels of backtick nesting, DASHBOARD, EDIT JOB  ← NEW FEATURE, EMPLOYEE ID SYSTEM — empId  = auto-assigned badge (EMP-001, EMP-002...) — handle = user-chosen nickname/alias — displayHandle() = handle if set, else empId, FIRST RUN CHECK — runs on page load before login, JOB DETAIL MODAL, LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool, MESSAGING, MIGRATION — Stamp companyId on any legacy docs missing it — Runs silently on every login — safe to run repeatedly, MONTH VIEW — Google Calendar style with multi-day span bars, NOTIFICATION ENGINE, Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — }, PAY SCHEDULE & UNPAID CREW CHECKER, PAYROLL LEDGER — Mark Paid modal, RECEIPTS, ROLE SWITCHER — instant role switch without logout, SESSION PERSISTENCE — remember me across refreshes/tab closes, SETTINGS TAB, SOUND ENGINE — Web Audio API, no external files needed, STATE & STORAGE, STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live, TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created, TRIAL + TIER SYSTEM, TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order, USER MANAGEMENT (Office only), WAREHOUSE, WAREHOUSE WORK LOGGING — Log warehouse work by employee — shows on calendar + payroll, WEEK VIEW — time-based layout like Google Calendar, function openNewJobModal(prefillDate) { — ── HARD RESET — every field to blank/zero ────────────────── — const safe = (id, val='') => { const el = document.getElementById(id); if(el) el.value = val; }; — Core job fields — safe('nj-client'); safe('nj-phone'); safe('nj-email'); — safe('nj-origin'); safe('nj-dest'); safe('nj-pickup-from'); — safe('nj-amount'); safe('nj-notes'); safe('nj-carrier'); — safe('nj-cubic-ft'); safe('nj-cubic-ft-2'); safe('nj-rate-per-cuft'); — safe('nj-weight'); safe('nj-linehaul'); — safe('nj-end-date'); safe('nj-end-time'); — safe('nj-pickup-start'); safe('nj-pickup-end'); — safe('nj-delivery-start'); safe('nj-delivery-end'); — safe('nj-pickup-driver'); safe('nj-delivery-driver'); — safe('nj-job-id'); safe('nj-ref-num'); — safe('nj-priority'); safe('nj-followup'); safe('nj-fadd'); — safe('nj-broker-ref'); safe('nj-carrier'); — ALL estimate fields — zero out completely — safe('nj-est-fuel', ''); — safe('nj-est-fuel-pct', '0'); — safe('nj-est-overload-cuft', ''); — safe('nj-est-stairs-origin', ''); — safe('nj-est-stairs-dest', ''); — safe('nj-est-longcarry-origin', ''); — safe('nj-est-longcarry-dest', ''); — safe('nj-est-shuttle', ''); — safe('nj-est-bulky', ''); — safe('nj-est-split', ''); — safe('nj-est-pgs', ''); — safe('nj-est-packing', ''); — safe('nj-est-storage-days', ''); — safe('nj-est-storage-rate', ''); — safe('nj-est-other', ''); — safe('nj-est-deposit', ''); — safe('nj-est-partial2', ''); — safe('nj-est-tip', ''); — safe('nj-est-discount', ''); — safe('nj-est-discount-label', ''); — safe('nj-est-others1', '');  safe('nj-est-others1-label', ''); — safe('nj-est-others2', '');  safe('nj-est-others2-label', ''); — safe('nj-est-others3', '');  safe('nj-est-others3-label', ''); — safe('nj-est-extra1', '');   safe('nj-est-extra1-label', ''); — safe('nj-est-extra2', '');   safe('nj-est-extra2-label', ''); — safe('nj-est-extra3', '');   safe('nj-est-extra3-label', ''); — safe('nj-est-notes', ''); — safe('nj-est-office-remarks', ''); — safe('nj-est-slip-remarks', ''); — Reset carrier total display — const ct = document.getElementById('nj-carrier-total'); if(ct) ct.textContent = '$0.00'; — Clear helper rows — const hr = document.getElementById('nj-helper-rows'); if(hr) hr.innerHTML = ''; — Reset all selects to index 0 — ['nj-job-for','nj-est-tip-method','nj-est-valuation', — 'nj-est-fuel-mode','nj-est-tip-assign'].forEach(id => { — const el = document.getElementById(id); — if (el) el.selectedIndex = 0; — }); — try { toggleCarrierFields('nj'); } catch(e) {} — Apply defaults — const s2 = loadSettings(); — safe('nj-crew', s2.defaultCrew || '3'); — safe('nj-type', s2.defaultMove || 'local'); — safe('nj-date', prefillDate || getTodayStr()); — safe('nj-time', getSetting('defaultTime', '08:00')); — safe('nj-driver', currentUser.role === 'driver' ? (currentUser.name||'') : ''); — closeModal('modal-calday'); — openModal('modal-newjob'); — setTimeout(() => { try { applyJobDefaults('nj'); } catch(e) {} }, 50); — } — async function createJob() { — const client = document.getElementById('nj-client').value.trim(); — const date   = document.getElementById('nj-date').value; — const origin = document.getElementById('nj-origin').value.trim(); — const dest   = document.getElementById('nj-dest').value.trim(); — if (!client || !date) { notify('⚠ Client name and date required', true); return; } — Custom ID or auto-generate — const customId = (document.getElementById('nj-job-id')?.value||'').trim().toUpperCase(); — const autoId   = 'JOB-' + String((STATE.jobs||[]).length + 1).padStart(3,'0'); — const jobId    = customId || autoId; — if ((STATE.jobs||[]).find(j => j.id === jobId)) { — notify('⚠ Job ID ' + jobId + ' already exists', true); return; — } — Collect helpers — const helpers = []; — document.querySelectorAll('.nj-helper-name').forEach(inp => { — const name = inp.value.trim(); — if (!name) return; — const row = inp.closest('.nj-helper-row'); — helpers.push({ — name, — rate:    parseFloat(row?.querySelector('.nj-helper-rate')?.value) || 0, — payType: row?.querySelector('.nj-helper-type')?.value || 'hourly', — hours: 0, days: 1, workerType: '1099', paid: false — }); — }); — const g = id => (document.getElementById(id)?.value || ''); — const newJob = { — id:            jobId, — refNum:        g('nj-ref-num'), — client, — phone:         g('nj-phone'), — email:         g('nj-email'), — date, — time:          g('nj-time'), — endDate:       g('nj-end-date') || g('nj-delivery-end').slice(0,10) || '', — endTime:       g('nj-end-time'), — origin, — dest, — pickupStart:   g('nj-pickup-start'), — pickupEnd:     g('nj-pickup-end'), — pickupDriver:  g('nj-pickup-driver'), — deliveryStart: g('nj-delivery-start'), — deliveryEnd:   g('nj-delivery-end'), — deliveryDriver:g('nj-delivery-driver'), — driver:        g('nj-driver'), — driverUser:    currentUser.username || '', — crewSize:      parseInt(g('nj-crew')) || 3, — crewNames:     helpers.map(h => h.name), — amount:        parseFloat(g('nj-amount')) || 0, — cubicFt:       parseFloat(g('nj-cubic-ft-2') || g('nj-cubic-ft')) || 0, — weight:        parseFloat(g('nj-weight')) || 0, — linehaul:      parseFloat(g('nj-linehaul')) || 0, — priority:      g('nj-priority'), — followUp:      g('nj-followup'), — fadd:          g('nj-fadd'), — type:          g('nj-type') || 'local', — notes:         g('nj-notes'), — companyId:     COMPANY_ID, — status:        'pending', — gates:         { preload:false, loaded:false, delivered:false, payment:false }, — messages:      { crew:[], client:[] }, — photos:        [], — signatures:    [], — payroll:       { driver:0, helpers } — }; — try { — await db.collection(COL_JOBS).doc(newJob.id).set(newJob); — closeModal('modal-newjob'); — notify('✓ Job ' + newJob.id + ' created — ' + client); — setTimeout(() => renderCalendar(), 200); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('createJob error:', e); — } — }, function renderExpenses() { — const content = document.getElementById('main-content'); — const role = currentUser.role; — const isDriver = role === 'driver'; — Scope to this driver's jobs/disbursements if not office — const myJobs = isDriver — ? STATE.jobs.filter(j => nameIncludes(j.driver, currentUser.name)) — : STATE.jobs; — const myDisbursements = isDriver — ? STATE.disbursements.filter(d => — d.driverUser === currentUser.username || — (d.driver && d.driver.toLowerCase().includes(currentUser.name.toLowerCase())) — ) — : STATE.disbursements; — const myReceipts = isDriver — ? STATE.receipts.filter(r => { — const nm = r.driver && nameIncludes(r.driver, currentUser.name); — const um = r.driverUser && r.driverUser === currentUser.username; — return nm || um; — }) — : STATE.receipts; — Totals — const totalDisbursed = myDisbursements.reduce((s,d) => s + (d.amount||0), 0); — const totalReceipts  = myReceipts.reduce((s,r) => s + (r.amount||0), 0); — const netOwed        = totalReceipts - totalDisbursed; // positive = driver owes, negative = company owes driver — let html = ` — <div class="section-header"> — <div> — <div class="section-title">EXPENSE TRACKER</div> — <div class="section-sub">${isDriver ? 'Your funds & receipts · ' + currentUser.name : 'All driver funds & expense reconciliation'}</div> — </div> — ${isAdmin() ? `<button class="btn-green" onclick="openDisbursementModal()">💰 SEND FUNDS</button>` : ''} — </div> — <!-- SUMMARY STATS --> — <div class="grid-3" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Funds Disbursed</div> — <div class="stat-value">$${totalDisbursed.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Receipts Submitted</div> — <div class="stat-value amber">$${totalReceipts.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">${netOwed > 0 ? 'Driver Owes Back' : netOwed < 0 ? 'Company Owes Driver' : 'Balanced'}</div> — <div class="stat-value ${netOwed > 0 ? 'red' : netOwed < 0 ? 'amber' : ''}"> — ${netOwed === 0 ? '✓ $0.00' : (netOwed > 0 ? '+' : '') + '$' + Math.abs(netOwed).toFixed(2)} — </div> — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div class="card" style="margin-bottom:16px"> — <div class="card-header"><div class="card-title">JOB EXPENSE BREAKDOWN</div></div> — <table> — <thead><tr> — <th>Job ID</th><th>Client</th><th>Driver</th> — <th style="text-align:right">Funds Sent</th> — <th style="text-align:right">Receipts</th> — <th style="text-align:right">Net</th> — <th>Status</th> — </tr></thead> — <tbody> — ${myJobs.length ? myJobs.map(j => { — const jobDisb = myDisbursements.filter(d => d.jobId === j.id); — const jobRec  = myReceipts.filter(r => r.jobId === j.id); — const sent    = jobDisb.reduce((s,d) => s + (d.amount||0), 0); — const spent   = jobRec.reduce((s,r) => s + (r.amount||0), 0); — const net     = spent - sent; — return `<tr onclick="openJobExpenseDetail('${j.id}')" style="cursor:pointer"> — <td><span class="tag">${j.id}</span></td> — <td>${j.client}</td> — <td class="info-text">${j.driver || '—'}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--green)">$${sent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--amber)">$${spent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);font-weight:700;color:${net > 0 ? 'var(--red)' : net < 0 ? 'var(--amber)' : 'var(--green)'}"> — ${net === 0 ? '✓ $0.00' : (net > 0 ? '+' : '') + '$' + Math.abs(net).toFixed(2)} — </td> — <td><span class="status-badge status-${j.status}">${j.status}</span></td> — </tr>`; — }).join('') : `<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--gray);font-family:var(--font-mono)">No jobs found</td></tr>`} — </tbody> — </table> — </div> — <!-- DISBURSEMENTS LOG --> — <div class="grid-2"> — <div class="card"> — <div class="card-header"> — <div class="card-title">💰 FUNDS DISBURSED</div> — ${isAdmin() ? `<button class="btn-green" style="padding:7px 14px;font-size:11px" onclick="openDisbursementModal()">+ SEND FUNDS</button>` : ''} — </div> — ${myDisbursements.length ? myDisbursements.map(d => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${d.driver} <span class="tag">${d.jobId}</span></div> — <div class="info-text">${d.date} · ${d.method || 'Cash'} · ${d.notes || ''}</div> — <div class="info-text" style="color:var(--gray)">${d.sentBy ? 'Sent by: ' + d.sentBy : ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--green)">$${(d.amount||0).toFixed(2)}</div> — ${role === 'office' ? `<button class="btn-sm" style="margin-top:4px;color:var(--red);border-color:#552222;font-size:10px" onclick="event.stopPropagation();deleteDisbursementEntry('${d.id}')">DELETE</button>` : ''} — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No funds disbursed yet.</p>`} — ${myDisbursements.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Disbursed</span> — <span style="font-family:var(--font-mono);color:var(--green);font-size:14px">$${totalDisbursed.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="card"> — <div class="card-header"><div class="card-title">🧾 RECEIPTS SUBMITTED</div></div> — ${myReceipts.length ? myReceipts.map(r => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${r.type} <span class="tag">${r.jobId}</span></div> — <div class="info-text">${r.driver} · ${r.date} · ${r.notes || ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--amber)">$${(r.amount||0).toFixed(2)}</div> — <div style="margin-top:2px">${r.approved — ? '<span style="color:var(--green);font-size:10px;font-family:var(--font-mono)">✓ APPROVED</span>' — : '<span style="color:var(--amber);font-size:10px;font-family:var(--font-mono)">⏳ PENDING</span>'}</div> — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No receipts submitted yet.</p>`} — ${myReceipts.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Receipts</span> — <span style="font-family:var(--font-mono);color:var(--amber);font-size:14px">$${totalReceipts.toFixed(2)}</span> — </div>` : ''} — </div> — </div>`; — content.innerHTML = html; — } — function openJobExpenseDetail(jobId) { — Switch to jobs tab and open the job detail — closeModal('modal-calday'); — openJobDetail(jobId); — } — function openDisbursementModal(prefillJobId) { — Pre-fill job ID if provided — document.getElementById('disb-jobid').value = prefillJobId || ''; — document.getElementById('disb-driver').value = ''; — document.getElementById('disb-amount').value = ''; — document.getElementById('disb-method').value = getSetting('defaultPay', 'Cash'); — document.getElementById('disb-notes').value = ''; — Populate driver dropdown from users — populateDisbDriverDropdown(); — openModal('modal-disbursement'); — } — async function populateDisbDriverDropdown() { — const sel = document.getElementById('disb-driver'); — sel.innerHTML = '<option value="">-- Select Driver --</option>'; — try { — const snap = await db.collection(COL_USERS) — .where('role', 'in', ['driver','helper']) — .get(); — snap.docs.forEach(d => { — const u = d.data(); — if (u.active) { — const opt = document.createElement('option'); — opt.value = JSON.stringify({ name: u.displayName, username: u.username }); — opt.textContent = u.displayName + ' (' + u.role + ')'; — sel.appendChild(opt); — } — }); — } catch(e) { — Fallback — type name manually — sel.innerHTML = '<option value="">Type driver name in notes</option>'; — } — } — async function saveDisbursementEntry() { — const jobId   = document.getElementById('disb-jobid').value.trim().toUpperCase(); — const driverRaw = document.getElementById('disb-driver').value; — const amount  = parseFloat(document.getElementById('disb-amount').value); — const method  = document.getElementById('disb-method').value; — const notes   = document.getElementById('disb-notes').value.trim(); — const btn     = document.getElementById('disb-submit-btn'); — if (!jobId)   { notify('⚠ Job ID is required', true); return; } — if (!driverRaw) { notify('⚠ Select a driver', true); return; } — if (isNaN(amount) || amount <= 0) { notify('⚠ Enter a valid amount', true); return; } — if (btn.disabled) return; — btn.disabled = true; — btn.textContent = 'SAVING...'; — try { — let driverName = driverRaw, driverUser = ''; — try { — const parsed = JSON.parse(driverRaw); — driverName = parsed.name; — driverUser = parsed.username; — } catch(e) { driverName = driverRaw; } — const disb = { — id:         'DISB-' + Date.now(), — jobId, — driver:     driverName, — driverUser: driverUser, — amount, — method, — notes, — sentBy:     currentUser.name, — date:       getTodayStr(), — createdAt:  new Date().toISOString() — }; — await saveDisbursement(disb); — closeModal('modal-disbursement'); — notify('$' + amount.toFixed(2) + ' disbursed to ' + driverName + ' ✓'); — Notify the driver — try { — if (driverUser) { — await sendNotification(driverUser, driverName, 'funds_sent', — 'Funds Sent 💰', — '$' + amount.toFixed(2) + ' sent via ' + method + ' for ' + jobId + (notes ? ' · ' + notes : '') + ' — from ' + currentUser.name, — { jobId, amount } — ); — } — } catch(e) {} — } catch(e) { — notify('⚠ Error: ' + e.message, true); — } finally { — btn.disabled = false; — btn.textContent = 'SEND FUNDS'; — } — } — async function deleteDisbursementEntry(disbId) { — if (!confirm('Delete this disbursement? This cannot be undone.')) return; — try { — await db.collection(COL_DISBURSEMENTS).doc(disbId).delete(); — notify('Disbursement deleted ✓'); — } catch(e) { notify('⚠ ' + e.message, true); } — }, function renderFleet() { — const content = document.getElementById('main-content'); — const role    = currentUser.role; — const canEdit = role === 'office'; — let html = ` — <div class="section-header"> — <div> — <div class="section-title">🚚 FLEET MANAGEMENT</div> — <div class="section-sub">${STATE.fleet.length} vehicle(s) registered · Move Masters OS</div> — </div> — ${canEdit ? `<button class="btn-green" onclick="openFleetModal()">+ ADD VEHICLE</button>` : ''} — </div>`; — if (!STATE.fleet.length) { — html += `<div class="card" style="text-align:center;padding:40px"> — <div style="font-size:48px;margin-bottom:12px">🚚</div> — <p class="info-text">No vehicles registered yet.</p> — ${canEdit ? `<button class="btn-green" style="margin-top:16px" onclick="openFleetModal()">+ ADD FIRST VEHICLE</button>` : ''} — </div>`; — } else { — Summary stats — const active  = STATE.fleet.filter(v => v.status === 'active').length; — const maint   = STATE.fleet.filter(v => v.status === 'maintenance').length; — const offline = STATE.fleet.filter(v => v.status === 'offline').length; — html += `<div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Active</div><div class="stat-value">${active}</div></div> — <div class="stat-box"><div class="stat-label">In Maintenance</div><div class="stat-value amber">${maint}</div></div> — <div class="stat-box"><div class="stat-label">Offline</div><div class="stat-value red">${offline}</div></div> — </div>`; — STATE.fleet.forEach(v => { — const assignedJob = STATE.jobs.find(j => j.vehicleId === v.id && j.status === 'active'); — const statusColor = v.status === 'active' ? 'var(--green)' : v.status === 'maintenance' ? 'var(--amber)' : 'var(--red)'; — const maintDue    = v.nextMaint ? new Date(v.nextMaint) : null; — const maintOverdue = maintDue && maintDue < new Date(); — html += ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:12px"> — <div> — <div style="display:flex;align-items:center;gap:10px;margin-bottom:6px"> — <div style="font-size:28px">${v.type === 'box_truck' ? '🚛' : v.type === 'cargo_van' ? '🚐' : '🚚'}</div> — <div> — <div style="font-family:var(--font-mono);font-size:14px;font-weight:700;color:var(--white)">${v.name || v.id}</div> — <div class="info-text">${v.make||''} ${v.model||''} ${v.year||''} · ${v.plate||'No plate'}</div> — </div> — </div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:11px;color:${statusColor};letter-spacing:2px">${(v.status||'active').toUpperCase()}</div> — ${canEdit ? `<button class="btn-sm" style="margin-top:6px" onclick="openFleetModal('${v.id}')">✎ EDIT</button>` : ''} — </div> — </div> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(110px,1fr));gap:8px;margin-bottom:12px"> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Capacity</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.capacity || '—'}</div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Mileage</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.mileage ? v.mileage.toLocaleString() + ' mi' : '—'}</div> — </div> — <div class="stat-box" style="padding:8px;${maintOverdue ? 'border-color:var(--red)' : ''}"> — <div class="stat-label" style="font-size:9px">Next Service</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${maintOverdue ? 'var(--red)' : 'var(--white)'}"> — ${v.nextMaint || 'Not set'}${maintOverdue ? ' ⚠' : ''} — </div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Assigned Job</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${assignedJob ? 'var(--green)' : 'var(--gray)'}"> — ${assignedJob ? assignedJob.id : 'Available'} — </div> — </div> — </div> — ${v.notes ? `<div class="info-text" style="margin-bottom:10px">📝 ${v.notes}</div>` : ''} — <!-- MAINTENANCE LOG --> — ${v.maintLog && v.maintLog.length ? ` — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:6px">MAINTENANCE LOG</div> — ${v.maintLog.slice(-3).reverse().map(m => ` — <div style="display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px solid var(--gray2);font-size:12px"> — <div><span class="info-text">${m.date}</span> · ${m.work}</div> — <div style="font-family:var(--font-mono);color:var(--amber)">$${(m.cost||0).toFixed(0)}</div> — </div>`).join('')}` : ''} — ${canEdit ? ` — <div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap"> — <button class="btn-sm" onclick="logMaintenance('${v.id}')">🔧 LOG SERVICE</button> — <button class="btn-sm" onclick="updateMileage('${v.id}')">📍 UPDATE MILEAGE</button> — <select style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:5px 8px;font-family:var(--font-mono);font-size:10px;border-radius:3px" — onchange="updateVehicleStatus('${v.id}',this.value)"> — <option value="active"      ${v.status==='active'      ?'selected':''}>● Active</option> — <option value="maintenance" ${v.status==='maintenance' ?'selected':''}>🔧 Maintenance</option> — <option value="offline"     ${v.status==='offline'     ?'selected':''}>○ Offline</option> — </select> — </div>` : ''} — </div>`; — }); — } — content.innerHTML = html; — } — function openFleetModal(vehicleId) { — const v = vehicleId ? STATE.fleet.find(x => x.id === vehicleId) : null; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">${v ? 'EDIT VEHICLE — ' + v.id : 'ADD VEHICLE'}</div> — <div class="grid-2"> — <div class="form-group"><label>Vehicle Name / Unit #</label> — <input type="text" id="flt-name" value="${(v&&v.name)||''}" placeholder="e.g. Truck 1 · Unit 24" /></div> — <div class="form-group"><label>Type</label> — <select id="flt-type"> — <option value="box_truck" ${v?.type==='box_truck'?'selected':''}>🚛 Box Truck</option> — <option value="cargo_van" ${v?.type==='cargo_van'?'selected':''}>🚐 Cargo Van</option> — <option value="flatbed"   ${v?.type==='flatbed'  ?'selected':''}>🚚 Flatbed</option> — <option value="other"     ${v?.type==='other'    ?'selected':''}>📋 Other</option> — </select> — </div> — </div> — <div class="grid-3"> — <div class="form-group"><label>Make</label><input type="text" id="flt-make" value="${(v&&v.make)||''}" placeholder="Ford" /></div> — <div class="form-group"><label>Model</label><input type="text" id="flt-model" value="${(v&&v.model)||''}" placeholder="E-450" /></div> — <div class="form-group"><label>Year</label><input type="number" id="flt-year" value="${(v&&v.year)||''}" placeholder="2019" /></div> — </div> — <div class="grid-2"> — <div class="form-group"><label>License Plate</label><input type="text" id="flt-plate" value="${(v&&v.plate)||''}" placeholder="ABC-1234" style="text-transform:uppercase"/></div> — <div class="form-group"><label>Capacity (text label)</label><input type="text" id="flt-cap" value="${(v&&v.capacity)||''}" placeholder="26 ft · 1500 lbs" /></div> — </div> — <div class="grid-3"> — <div class="form-group"> — <label>Max Cubic Feet <span class="info-text">(for load optimization)</span></label> — <input type="number" id="flt-maxcuft" value="${(v&&v.maxCuFt)||''}" placeholder="e.g. 1200" min="0" step="50" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Fuel Efficiency (MPG) <span class="info-text">(for cost calc)</span></label> — <input type="number" id="flt-mpg" value="${(v&&v.mpg)||''}" placeholder="e.g. 10" min="1" step="0.5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Load Efficiency % <span class="info-text">(usable space)</span></label> — <input type="number" id="flt-loadeff" value="${(v&&v.loadEff)||85}" placeholder="85" min="50" max="100" step="5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — </div> — <div class="grid-2"> — <div class="form-group"><label>Current Mileage</label><input type="number" id="flt-miles" value="${(v&&v.mileage)||''}" placeholder="85000" /></div> — <div class="form-group"><label>Next Service Due</label><input type="date" id="flt-maint" value="${(v&&v.nextMaint)||''}" /></div> — </div> — <div class="form-group"><label>Notes</label> — <input type="text" id="flt-notes" value="${(v&&v.notes)||''}" placeholder="Insurance, registration, special equipment..." /></div> — <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:8px"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" onclick="saveVehicle('${vehicleId||''}')">SAVE VEHICLE</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveVehicle(vehicleId) { — const vehicle = { — id:        vehicleId || 'VEH-' + Date.now(), — name:      document.getElementById('flt-name').value.trim(), — type:      document.getElementById('flt-type').value, — make:      document.getElementById('flt-make').value.trim(), — model:     document.getElementById('flt-model').value.trim(), — year:      document.getElementById('flt-year').value, — plate:     document.getElementById('flt-plate').value.trim().toUpperCase(), — capacity:  document.getElementById('flt-cap').value.trim(), — maxCuFt:   parseFloat(document.getElementById('flt-maxcuft')?.value)||0, — mpg:       parseFloat(document.getElementById('flt-mpg')?.value)||0, — loadEff:   parseFloat(document.getElementById('flt-loadeff')?.value)||85, — mileage:   parseInt(document.getElementById('flt-miles').value)||0, — nextMaint: document.getElementById('flt-maint').value, — notes:     document.getElementById('flt-notes').value.trim(), — status:    vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.status||'active') : 'active', — maintLog:  vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.maintLog||[]) : [], — updatedAt: new Date().toISOString() — }; — if (!vehicle.name) { notify('⚠ Vehicle name is required', true); return; } — try { — await db.collection(COL_FLEET).doc(vehicle.id).set(vehicle); — notify((vehicleId ? 'Vehicle updated' : 'Vehicle added') + ' ✓'); — closeModal('modal-jobdetail'); — } catch(e) { notify('⚠ ' + e.message, true); } — } — async function logMaintenance(vehicleId) { — const work = prompt('Service performed:'); if (!work) return; — const cost = parseFloat(prompt('Cost ($):') || '0'); — const v    = STATE.fleet.find(x => x.id === vehicleId); — if (!v) return; — if (!v.maintLog) v.maintLog = []; — v.maintLog.push({ date: getTodayStr(), work, cost, loggedBy: currentUser.name }); — v.updatedAt = new Date().toISOString(); — await db.collection(COL_FLEET).doc(vehicleId).set(v); — notify('Maintenance logged ✓'); — } — async function updateMileage(vehicleId) { — const miles = prompt('Enter current mileage:'); if (!miles) return; — await db.collection(COL_FLEET).doc(vehicleId).update({ mileage: parseInt(miles)||0 }); — notify('Mileage updated ✓'); — } — async function updateVehicleStatus(vehicleId, status) { — await db.collection(COL_FLEET).doc(vehicleId).update({ status }); — notify('Vehicle status: ' + status.toUpperCase()); — }
| TABS | 6585-6705 | 40 | 1099 / W2 YEAR-END TAX EXPORT — Aggregates all payments per contractor/employee for the year — Generates: on-screen summary + CSV export + PDF report, BOL TAB, CALENDAR, CREW LOG HELPERS — extracted to avoid nested template literal — Chrome syntax errors from 4+ levels of backtick nesting, DASHBOARD, ESTIMATE TAB — Per-job BOL line item editor — This is the source of truth for the BOL PDF, FINANCIALS, HISTORY, LABOR PRODUCTIVITY SCORES — v52 — Uses gate timestamps to calculate efficiency per crew member — Crew A moves 50 cu.ft/hr vs Crew B at 35 cu.ft/hr — Visible to owner/office — used for rewards and training, LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool, MESSAGING, P&L ACCOUNTING MODULE — Kills QuickBooks — Chart of accounts, monthly P&L, period reports, export, PAYROLL LEDGER — Running balance per crew member — Balance = earned - paid. When paid → resets to 0 → new cycle, RECEIPTS, ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard, SCHEDULE, SETTINGS TAB, STATE & STORAGE, SYSTEM HEALTH DASHBOARD — Platform operator view — JPG Ventures sees all licensees, sync status, revenue flow — Accessible only to creator/owner accounts, TRIAL + TIER SYSTEM, TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order, USER MANAGEMENT (Office only), WAREHOUSE, function renderExpenses() { — const content = document.getElementById('main-content'); — const role = currentUser.role; — const isDriver = role === 'driver'; — Scope to this driver's jobs/disbursements if not office — const myJobs = isDriver — ? STATE.jobs.filter(j => nameIncludes(j.driver, currentUser.name)) — : STATE.jobs; — const myDisbursements = isDriver — ? STATE.disbursements.filter(d => — d.driverUser === currentUser.username || — (d.driver && d.driver.toLowerCase().includes(currentUser.name.toLowerCase())) — ) — : STATE.disbursements; — const myReceipts = isDriver — ? STATE.receipts.filter(r => { — const nm = r.driver && nameIncludes(r.driver, currentUser.name); — const um = r.driverUser && r.driverUser === currentUser.username; — return nm || um; — }) — : STATE.receipts; — Totals — const totalDisbursed = myDisbursements.reduce((s,d) => s + (d.amount||0), 0); — const totalReceipts  = myReceipts.reduce((s,r) => s + (r.amount||0), 0); — const netOwed        = totalReceipts - totalDisbursed; // positive = driver owes, negative = company owes driver — let html = ` — <div class="section-header"> — <div> — <div class="section-title">EXPENSE TRACKER</div> — <div class="section-sub">${isDriver ? 'Your funds & receipts · ' + currentUser.name : 'All driver funds & expense reconciliation'}</div> — </div> — ${isAdmin() ? `<button class="btn-green" onclick="openDisbursementModal()">💰 SEND FUNDS</button>` : ''} — </div> — <!-- SUMMARY STATS --> — <div class="grid-3" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Funds Disbursed</div> — <div class="stat-value">$${totalDisbursed.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Receipts Submitted</div> — <div class="stat-value amber">$${totalReceipts.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">${netOwed > 0 ? 'Driver Owes Back' : netOwed < 0 ? 'Company Owes Driver' : 'Balanced'}</div> — <div class="stat-value ${netOwed > 0 ? 'red' : netOwed < 0 ? 'amber' : ''}"> — ${netOwed === 0 ? '✓ $0.00' : (netOwed > 0 ? '+' : '') + '$' + Math.abs(netOwed).toFixed(2)} — </div> — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div class="card" style="margin-bottom:16px"> — <div class="card-header"><div class="card-title">JOB EXPENSE BREAKDOWN</div></div> — <table> — <thead><tr> — <th>Job ID</th><th>Client</th><th>Driver</th> — <th style="text-align:right">Funds Sent</th> — <th style="text-align:right">Receipts</th> — <th style="text-align:right">Net</th> — <th>Status</th> — </tr></thead> — <tbody> — ${myJobs.length ? myJobs.map(j => { — const jobDisb = myDisbursements.filter(d => d.jobId === j.id); — const jobRec  = myReceipts.filter(r => r.jobId === j.id); — const sent    = jobDisb.reduce((s,d) => s + (d.amount||0), 0); — const spent   = jobRec.reduce((s,r) => s + (r.amount||0), 0); — const net     = spent - sent; — return `<tr onclick="openJobExpenseDetail('${j.id}')" style="cursor:pointer"> — <td><span class="tag">${j.id}</span></td> — <td>${j.client}</td> — <td class="info-text">${j.driver || '—'}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--green)">$${sent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--amber)">$${spent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);font-weight:700;color:${net > 0 ? 'var(--red)' : net < 0 ? 'var(--amber)' : 'var(--green)'}"> — ${net === 0 ? '✓ $0.00' : (net > 0 ? '+' : '') + '$' + Math.abs(net).toFixed(2)} — </td> — <td><span class="status-badge status-${j.status}">${j.status}</span></td> — </tr>`; — }).join('') : `<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--gray);font-family:var(--font-mono)">No jobs found</td></tr>`} — </tbody> — </table> — </div> — <!-- DISBURSEMENTS LOG --> — <div class="grid-2"> — <div class="card"> — <div class="card-header"> — <div class="card-title">💰 FUNDS DISBURSED</div> — ${isAdmin() ? `<button class="btn-green" style="padding:7px 14px;font-size:11px" onclick="openDisbursementModal()">+ SEND FUNDS</button>` : ''} — </div> — ${myDisbursements.length ? myDisbursements.map(d => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${d.driver} <span class="tag">${d.jobId}</span></div> — <div class="info-text">${d.date} · ${d.method || 'Cash'} · ${d.notes || ''}</div> — <div class="info-text" style="color:var(--gray)">${d.sentBy ? 'Sent by: ' + d.sentBy : ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--green)">$${(d.amount||0).toFixed(2)}</div> — ${role === 'office' ? `<button class="btn-sm" style="margin-top:4px;color:var(--red);border-color:#552222;font-size:10px" onclick="event.stopPropagation();deleteDisbursementEntry('${d.id}')">DELETE</button>` : ''} — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No funds disbursed yet.</p>`} — ${myDisbursements.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Disbursed</span> — <span style="font-family:var(--font-mono);color:var(--green);font-size:14px">$${totalDisbursed.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="card"> — <div class="card-header"><div class="card-title">🧾 RECEIPTS SUBMITTED</div></div> — ${myReceipts.length ? myReceipts.map(r => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${r.type} <span class="tag">${r.jobId}</span></div> — <div class="info-text">${r.driver} · ${r.date} · ${r.notes || ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--amber)">$${(r.amount||0).toFixed(2)}</div> — <div style="margin-top:2px">${r.approved — ? '<span style="color:var(--green);font-size:10px;font-family:var(--font-mono)">✓ APPROVED</span>' — : '<span style="color:var(--amber);font-size:10px;font-family:var(--font-mono)">⏳ PENDING</span>'}</div> — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No receipts submitted yet.</p>`} — ${myReceipts.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Receipts</span> — <span style="font-family:var(--font-mono);color:var(--amber);font-size:14px">$${totalReceipts.toFixed(2)}</span> — </div>` : ''} — </div> — </div>`; — content.innerHTML = html; — } — function openJobExpenseDetail(jobId) { — Switch to jobs tab and open the job detail — closeModal('modal-calday'); — openJobDetail(jobId); — } — function openDisbursementModal(prefillJobId) { — Pre-fill job ID if provided — document.getElementById('disb-jobid').value = prefillJobId || ''; — document.getElementById('disb-driver').value = ''; — document.getElementById('disb-amount').value = ''; — document.getElementById('disb-method').value = getSetting('defaultPay', 'Cash'); — document.getElementById('disb-notes').value = ''; — Populate driver dropdown from users — populateDisbDriverDropdown(); — openModal('modal-disbursement'); — } — async function populateDisbDriverDropdown() { — const sel = document.getElementById('disb-driver'); — sel.innerHTML = '<option value="">-- Select Driver --</option>'; — try { — const snap = await db.collection(COL_USERS) — .where('role', 'in', ['driver','helper']) — .get(); — snap.docs.forEach(d => { — const u = d.data(); — if (u.active) { — const opt = document.createElement('option'); — opt.value = JSON.stringify({ name: u.displayName, username: u.username }); — opt.textContent = u.displayName + ' (' + u.role + ')'; — sel.appendChild(opt); — } — }); — } catch(e) { — Fallback — type name manually — sel.innerHTML = '<option value="">Type driver name in notes</option>'; — } — } — async function saveDisbursementEntry() { — const jobId   = document.getElementById('disb-jobid').value.trim().toUpperCase(); — const driverRaw = document.getElementById('disb-driver').value; — const amount  = parseFloat(document.getElementById('disb-amount').value); — const method  = document.getElementById('disb-method').value; — const notes   = document.getElementById('disb-notes').value.trim(); — const btn     = document.getElementById('disb-submit-btn'); — if (!jobId)   { notify('⚠ Job ID is required', true); return; } — if (!driverRaw) { notify('⚠ Select a driver', true); return; } — if (isNaN(amount) || amount <= 0) { notify('⚠ Enter a valid amount', true); return; } — if (btn.disabled) return; — btn.disabled = true; — btn.textContent = 'SAVING...'; — try { — let driverName = driverRaw, driverUser = ''; — try { — const parsed = JSON.parse(driverRaw); — driverName = parsed.name; — driverUser = parsed.username; — } catch(e) { driverName = driverRaw; } — const disb = { — id:         'DISB-' + Date.now(), — jobId, — driver:     driverName, — driverUser: driverUser, — amount, — method, — notes, — sentBy:     currentUser.name, — date:       getTodayStr(), — createdAt:  new Date().toISOString() — }; — await saveDisbursement(disb); — closeModal('modal-disbursement'); — notify('$' + amount.toFixed(2) + ' disbursed to ' + driverName + ' ✓'); — Notify the driver — try { — if (driverUser) { — await sendNotification(driverUser, driverName, 'funds_sent', — 'Funds Sent 💰', — '$' + amount.toFixed(2) + ' sent via ' + method + ' for ' + jobId + (notes ? ' · ' + notes : '') + ' — from ' + currentUser.name, — { jobId, amount } — ); — } — } catch(e) {} — } catch(e) { — notify('⚠ Error: ' + e.message, true); — } finally { — btn.disabled = false; — btn.textContent = 'SEND FUNDS'; — } — } — async function deleteDisbursementEntry(disbId) { — if (!confirm('Delete this disbursement? This cannot be undone.')) return; — try { — await db.collection(COL_DISBURSEMENTS).doc(disbId).delete(); — notify('Disbursement deleted ✓'); — } catch(e) { notify('⚠ ' + e.message, true); } — }, function renderFleet() { — const content = document.getElementById('main-content'); — const role    = currentUser.role; — const canEdit = role === 'office'; — let html = ` — <div class="section-header"> — <div> — <div class="section-title">🚚 FLEET MANAGEMENT</div> — <div class="section-sub">${STATE.fleet.length} vehicle(s) registered · Move Masters OS</div> — </div> — ${canEdit ? `<button class="btn-green" onclick="openFleetModal()">+ ADD VEHICLE</button>` : ''} — </div>`; — if (!STATE.fleet.length) { — html += `<div class="card" style="text-align:center;padding:40px"> — <div style="font-size:48px;margin-bottom:12px">🚚</div> — <p class="info-text">No vehicles registered yet.</p> — ${canEdit ? `<button class="btn-green" style="margin-top:16px" onclick="openFleetModal()">+ ADD FIRST VEHICLE</button>` : ''} — </div>`; — } else { — Summary stats — const active  = STATE.fleet.filter(v => v.status === 'active').length; — const maint   = STATE.fleet.filter(v => v.status === 'maintenance').length; — const offline = STATE.fleet.filter(v => v.status === 'offline').length; — html += `<div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Active</div><div class="stat-value">${active}</div></div> — <div class="stat-box"><div class="stat-label">In Maintenance</div><div class="stat-value amber">${maint}</div></div> — <div class="stat-box"><div class="stat-label">Offline</div><div class="stat-value red">${offline}</div></div> — </div>`; — STATE.fleet.forEach(v => { — const assignedJob = STATE.jobs.find(j => j.vehicleId === v.id && j.status === 'active'); — const statusColor = v.status === 'active' ? 'var(--green)' : v.status === 'maintenance' ? 'var(--amber)' : 'var(--red)'; — const maintDue    = v.nextMaint ? new Date(v.nextMaint) : null; — const maintOverdue = maintDue && maintDue < new Date(); — html += ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:12px"> — <div> — <div style="display:flex;align-items:center;gap:10px;margin-bottom:6px"> — <div style="font-size:28px">${v.type === 'box_truck' ? '🚛' : v.type === 'cargo_van' ? '🚐' : '🚚'}</div> — <div> — <div style="font-family:var(--font-mono);font-size:14px;font-weight:700;color:var(--white)">${v.name || v.id}</div> — <div class="info-text">${v.make||''} ${v.model||''} ${v.year||''} · ${v.plate||'No plate'}</div> — </div> — </div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:11px;color:${statusColor};letter-spacing:2px">${(v.status||'active').toUpperCase()}</div> — ${canEdit ? `<button class="btn-sm" style="margin-top:6px" onclick="openFleetModal('${v.id}')">✎ EDIT</button>` : ''} — </div> — </div> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(110px,1fr));gap:8px;margin-bottom:12px"> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Capacity</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.capacity || '—'}</div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Mileage</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.mileage ? v.mileage.toLocaleString() + ' mi' : '—'}</div> — </div> — <div class="stat-box" style="padding:8px;${maintOverdue ? 'border-color:var(--red)' : ''}"> — <div class="stat-label" style="font-size:9px">Next Service</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${maintOverdue ? 'var(--red)' : 'var(--white)'}"> — ${v.nextMaint || 'Not set'}${maintOverdue ? ' ⚠' : ''} — </div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Assigned Job</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${assignedJob ? 'var(--green)' : 'var(--gray)'}"> — ${assignedJob ? assignedJob.id : 'Available'} — </div> — </div> — </div> — ${v.notes ? `<div class="info-text" style="margin-bottom:10px">📝 ${v.notes}</div>` : ''} — <!-- MAINTENANCE LOG --> — ${v.maintLog && v.maintLog.length ? ` — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:6px">MAINTENANCE LOG</div> — ${v.maintLog.slice(-3).reverse().map(m => ` — <div style="display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px solid var(--gray2);font-size:12px"> — <div><span class="info-text">${m.date}</span> · ${m.work}</div> — <div style="font-family:var(--font-mono);color:var(--amber)">$${(m.cost||0).toFixed(0)}</div> — </div>`).join('')}` : ''} — ${canEdit ? ` — <div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap"> — <button class="btn-sm" onclick="logMaintenance('${v.id}')">🔧 LOG SERVICE</button> — <button class="btn-sm" onclick="updateMileage('${v.id}')">📍 UPDATE MILEAGE</button> — <select style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:5px 8px;font-family:var(--font-mono);font-size:10px;border-radius:3px" — onchange="updateVehicleStatus('${v.id}',this.value)"> — <option value="active"      ${v.status==='active'      ?'selected':''}>● Active</option> — <option value="maintenance" ${v.status==='maintenance' ?'selected':''}>🔧 Maintenance</option> — <option value="offline"     ${v.status==='offline'     ?'selected':''}>○ Offline</option> — </select> — </div>` : ''} — </div>`; — }); — } — content.innerHTML = html; — } — function openFleetModal(vehicleId) { — const v = vehicleId ? STATE.fleet.find(x => x.id === vehicleId) : null; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">${v ? 'EDIT VEHICLE — ' + v.id : 'ADD VEHICLE'}</div> — <div class="grid-2"> — <div class="form-group"><label>Vehicle Name / Unit #</label> — <input type="text" id="flt-name" value="${(v&&v.name)||''}" placeholder="e.g. Truck 1 · Unit 24" /></div> — <div class="form-group"><label>Type</label> — <select id="flt-type"> — <option value="box_truck" ${v?.type==='box_truck'?'selected':''}>🚛 Box Truck</option> — <option value="cargo_van" ${v?.type==='cargo_van'?'selected':''}>🚐 Cargo Van</option> — <option value="flatbed"   ${v?.type==='flatbed'  ?'selected':''}>🚚 Flatbed</option> — <option value="other"     ${v?.type==='other'    ?'selected':''}>📋 Other</option> — </select> — </div> — </div> — <div class="grid-3"> — <div class="form-group"><label>Make</label><input type="text" id="flt-make" value="${(v&&v.make)||''}" placeholder="Ford" /></div> — <div class="form-group"><label>Model</label><input type="text" id="flt-model" value="${(v&&v.model)||''}" placeholder="E-450" /></div> — <div class="form-group"><label>Year</label><input type="number" id="flt-year" value="${(v&&v.year)||''}" placeholder="2019" /></div> — </div> — <div class="grid-2"> — <div class="form-group"><label>License Plate</label><input type="text" id="flt-plate" value="${(v&&v.plate)||''}" placeholder="ABC-1234" style="text-transform:uppercase"/></div> — <div class="form-group"><label>Capacity (text label)</label><input type="text" id="flt-cap" value="${(v&&v.capacity)||''}" placeholder="26 ft · 1500 lbs" /></div> — </div> — <div class="grid-3"> — <div class="form-group"> — <label>Max Cubic Feet <span class="info-text">(for load optimization)</span></label> — <input type="number" id="flt-maxcuft" value="${(v&&v.maxCuFt)||''}" placeholder="e.g. 1200" min="0" step="50" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Fuel Efficiency (MPG) <span class="info-text">(for cost calc)</span></label> — <input type="number" id="flt-mpg" value="${(v&&v.mpg)||''}" placeholder="e.g. 10" min="1" step="0.5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Load Efficiency % <span class="info-text">(usable space)</span></label> — <input type="number" id="flt-loadeff" value="${(v&&v.loadEff)||85}" placeholder="85" min="50" max="100" step="5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — </div> — <div class="grid-2"> — <div class="form-group"><label>Current Mileage</label><input type="number" id="flt-miles" value="${(v&&v.mileage)||''}" placeholder="85000" /></div> — <div class="form-group"><label>Next Service Due</label><input type="date" id="flt-maint" value="${(v&&v.nextMaint)||''}" /></div> — </div> — <div class="form-group"><label>Notes</label> — <input type="text" id="flt-notes" value="${(v&&v.notes)||''}" placeholder="Insurance, registration, special equipment..." /></div> — <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:8px"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" onclick="saveVehicle('${vehicleId||''}')">SAVE VEHICLE</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveVehicle(vehicleId) { — const vehicle = { — id:        vehicleId || 'VEH-' + Date.now(), — name:      document.getElementById('flt-name').value.trim(), — type:      document.getElementById('flt-type').value, — make:      document.getElementById('flt-make').value.trim(), — model:     document.getElementById('flt-model').value.trim(), — year:      document.getElementById('flt-year').value, — plate:     document.getElementById('flt-plate').value.trim().toUpperCase(), — capacity:  document.getElementById('flt-cap').value.trim(), — maxCuFt:   parseFloat(document.getElementById('flt-maxcuft')?.value)||0, — mpg:       parseFloat(document.getElementById('flt-mpg')?.value)||0, — loadEff:   parseFloat(document.getElementById('flt-loadeff')?.value)||85, — mileage:   parseInt(document.getElementById('flt-miles').value)||0, — nextMaint: document.getElementById('flt-maint').value, — notes:     document.getElementById('flt-notes').value.trim(), — status:    vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.status||'active') : 'active', — maintLog:  vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.maintLog||[]) : [], — updatedAt: new Date().toISOString() — }; — if (!vehicle.name) { notify('⚠ Vehicle name is required', true); return; } — try { — await db.collection(COL_FLEET).doc(vehicle.id).set(vehicle); — notify((vehicleId ? 'Vehicle updated' : 'Vehicle added') + ' ✓'); — closeModal('modal-jobdetail'); — } catch(e) { notify('⚠ ' + e.message, true); } — } — async function logMaintenance(vehicleId) { — const work = prompt('Service performed:'); if (!work) return; — const cost = parseFloat(prompt('Cost ($):') || '0'); — const v    = STATE.fleet.find(x => x.id === vehicleId); — if (!v) return; — if (!v.maintLog) v.maintLog = []; — v.maintLog.push({ date: getTodayStr(), work, cost, loggedBy: currentUser.name }); — v.updatedAt = new Date().toISOString(); — await db.collection(COL_FLEET).doc(vehicleId).set(v); — notify('Maintenance logged ✓'); — } — async function updateMileage(vehicleId) { — const miles = prompt('Enter current mileage:'); if (!miles) return; — await db.collection(COL_FLEET).doc(vehicleId).update({ mileage: parseInt(miles)||0 }); — notify('Mileage updated ✓'); — } — async function updateVehicleStatus(vehicleId, status) { — await db.collection(COL_FLEET).doc(vehicleId).update({ status }); — notify('Vehicle status: ' + status.toUpperCase()); — }, function renderMargins() { — const content = document.getElementById('main-content'); — Build margin data per job — const jobMargins = STATE.jobs — .filter(j => j.amount > 0 || j.payroll) — .map(j => { — const revenue   = j.amount || 0; — const driverAccBonus = parseFloat(j.payroll?.accessorialBonus) || 0; — const crewCost  = (j.payroll?.driver || 0) + — driverAccBonus + — (j.payroll?.helpers || []).reduce((s,h) => s + calcPay(h), 0) + — (j.payroll?.driverCommission || 0); — Company revenue excludes driver accessorial bonuses (passed through to driver) — const expenses  = STATE.receipts — .filter(r => r.jobId === j.id && r.approved !== false) — .reduce((s,r) => s + (r.amount||0), 0); — const net       = revenue - crewCost - expenses; — const margin    = revenue > 0 ? (net / revenue * 100) : 0; — return { job: j, revenue, crewCost, expenses, net, margin }; — }) — .sort((a,b) => { — Default: worst margin first (ascending) — const mA = a.revenue > 0 ? a.profit/a.revenue : 0; — const mB = b.revenue > 0 ? b.profit/b.revenue : 0; — return mA - mB; — }); — Sort control — if (window._marginSort === 'date') jobMargins.sort((a,b) => b.job.date > a.job.date ? 1 : -1); — else if (window._marginSort === 'revenue') jobMargins.sort((a,b) => b.revenue - a.revenue); — ── Margin intelligence ── — const overpayJobs   = jobMargins.filter(m => m.revenue > 0 && (m.crewCost/m.revenue) > 0.40); — const lowProfitJobs = jobMargins.filter(m => m.revenue > 0 && (m.profit/m.revenue) < 0.20); — const bestJob       = [...jobMargins].sort((a,b) => (b.profit/b.revenue||0)-(a.profit/a.revenue||0))[0]; — const worstJob      = jobMargins[0]; // already sorted worst first — Overall stats — const totalRevenue  = jobMargins.reduce((s,m) => s + m.revenue, 0); — const totalCrew     = jobMargins.reduce((s,m) => s + m.crewCost, 0); — const totalExpenses = jobMargins.reduce((s,m) => s + m.expenses, 0); — const totalNet      = jobMargins.reduce((s,m) => s + m.net, 0); — const avgMargin     = totalRevenue > 0 ? (totalNet / totalRevenue * 100) : 0; — const profitable    = jobMargins.filter(m => m.net > 0).length; — const losing        = jobMargins.filter(m => m.net < 0).length; — Best and worst job — const best  = jobMargins.reduce((a,b) => b.margin > a.margin ? b : a, jobMargins[0]); — const worst = jobMargins.reduce((a,b) => b.margin < a.margin ? b : a, jobMargins[0]); — const marginColor = (n) => n >= 40 ? 'var(--green)' : n >= 20 ? 'var(--amber)' : n >= 0 ? '#ff8c00' : 'var(--red)'; — const marginBg    = (n) => n >= 40 ? 'rgba(0,255,65,0.08)' : n >= 20 ? 'rgba(255,170,0,0.08)' : n >= 0 ? 'rgba(255,140,0,0.08)' : 'rgba(255,51,51,0.08)'; — let html = ` — ${overpayJobs.length ? `<div style="background:rgba(255,30,30,0.06);border:1px solid rgba(255,30,30,0.3);border-radius:6px;padding:10px 14px;margin-bottom:12px;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px"> — <div> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;font-weight:700">⚠ LABOR OVERPAY ALERT</div> — <div style="font-size:12px;color:var(--white);margin-top:3px">${overpayJobs.length} job${overpayJobs.length>1?'s':''} with crew cost >40% of revenue</div> — <div style="font-size:11px;color:var(--gray);margin-top:2px">${overpayJobs.slice(0,3).map(m=>m.job.client+' ('+Math.round(m.crewCost/m.revenue*100)+'%)').join(' · ')}</div> — </div> — </div>` : ''} — <div style="display:flex;gap:8px;margin-bottom:12px;flex-wrap:wrap"> — <span style="font-family:var(--font-mono);font-size:10px;color:var(--gray);align-self:center">Sort:</span> — ${['worst:⬇ Worst Margin','revenue:$ Revenue','date:📅 Date'].map(o=>{const[v,l]=o.split(':');return `<button onclick="window._marginSort='${v}';renderMargins()" style="padding:5px 12px;font-family:var(--font-mono);font-size:10px;border-radius:3px;cursor:pointer;background:${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--green)':'transparent'};color:${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--navy)':'var(--gray)'};border:1px solid ${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--green)':'var(--gray2)'}">${l}</button>`;}).join('')} — </div> — <div class="section-header"> — <div> — <div class="section-title">📈 JOB MARGIN DASHBOARD</div> — <div class="section-sub">Revenue · Crew Cost · Expenses · Net per Job</div> — </div> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportJobsCSV()">⬇ EXPORT</button> — </div> — <!-- OVERALL STATS --> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(130px,1fr));gap:8px;margin-bottom:16px"> — <div class="stat-box"> — <div class="stat-label">Total Revenue</div> — <div class="stat-value">$${totalRevenue.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${jobMargins.length} jobs</div> — </div> — <div class="stat-box"> — <div class="stat-label">Crew Cost</div> — <div class="stat-value red">$${totalCrew.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${totalRevenue>0?(totalCrew/totalRevenue*100).toFixed(1):'0'}% of revenue</div> — </div> — <div class="stat-box"> — <div class="stat-label">Expenses</div> — <div class="stat-value red">$${totalExpenses.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${totalRevenue>0?(totalExpenses/totalRevenue*100).toFixed(1):'0'}% of revenue</div> — </div> — <div class="stat-box" style="border-color:${marginColor(avgMargin)}"> — <div class="stat-label">Net Profit</div> — <div class="stat-value" style="color:${marginColor(avgMargin)}">$${totalNet.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px;color:${marginColor(avgMargin)}">${avgMargin.toFixed(1)}% avg margin</div> — </div> — <div class="stat-box"> — <div class="stat-label">Profitable Jobs</div> — <div class="stat-value" style="color:var(--green)">${profitable}</div> — <div class="info-text" style="margin-top:4px;color:var(--red)">${losing} losing</div> — </div> — </div> — <!-- MARGIN BREAKDOWN BAR --> — ${totalRevenue > 0 ? ` — <div class="card" style="margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;margin-bottom:10px">REVENUE BREAKDOWN</div> — <div style="height:32px;border-radius:4px;overflow:hidden;display:flex;margin-bottom:8px"> — <div style="width:${(totalCrew/totalRevenue*100).toFixed(1)}%;background:rgba(255,51,51,0.7);transition:width 0.5s" title="Crew Cost"></div> — <div style="width:${(totalExpenses/totalRevenue*100).toFixed(1)}%;background:rgba(255,140,0,0.7);transition:width 0.5s" title="Expenses"></div> — <div style="flex:1;background:rgba(0,255,65,0.4);transition:width 0.5s" title="Net Profit"></div> — </div> — <div style="display:flex;gap:16px;flex-wrap:wrap"> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(255,51,51,0.7);border-radius:2px"></div><span class="info-text">Crew ${(totalCrew/totalRevenue*100).toFixed(1)}%</span></div> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(255,140,0,0.7);border-radius:2px"></div><span class="info-text">Expenses ${(totalExpenses/totalRevenue*100).toFixed(1)}%</span></div> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(0,255,65,0.4);border-radius:2px"></div><span class="info-text">Net ${avgMargin.toFixed(1)}%</span></div> — </div> — </div>` : ''} — <!-- BEST / WORST --> — ${jobMargins.length >= 2 ? ` — <div class="grid-2" style="margin-bottom:16px"> — ${best ? `<div class="card" style="border-color:var(--green3)"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--green);letter-spacing:2px;margin-bottom:6px">🏆 BEST MARGIN JOB</div> — <div style="font-size:15px;font-weight:700">${best.job.client} <span class="tag">${best.job.id}</span></div> — <div class="info-text">${best.job.date}</div> — <div style="font-family:var(--font-mono);font-size:22px;color:var(--green);margin-top:6px">${best.margin.toFixed(1)}%</div> — <div class="info-text">$${best.net.toFixed(2)} net on $${best.revenue.toLocaleString()}</div> — </div>` : ''} — ${worst ? `<div class="card" style="border-color:${marginColor(worst.margin)}"> — <div style="font-family:var(--font-mono);font-size:9px;color:${marginColor(worst.margin)};letter-spacing:2px;margin-bottom:6px">${worst.net < 0 ? '⚠ LOSING JOB' : '📉 LOWEST MARGIN'}</div> — <div style="font-size:15px;font-weight:700">${worst.job.client} <span class="tag">${worst.job.id}</span></div> — <div class="info-text">${worst.job.date}</div> — <div style="font-family:var(--font-mono);font-size:22px;color:${marginColor(worst.margin)};margin-top:6px">${worst.margin.toFixed(1)}%</div> — <div class="info-text">$${worst.net.toFixed(2)} net on $${worst.revenue.toLocaleString()}</div> — </div>` : ''} — </div>` : ''} — <!-- PER-JOB TABLE --> — <div class="card" style="padding:0;overflow:hidden"> — <div style="padding:12px 16px;border-bottom:1px solid var(--gray2)"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--green);letter-spacing:2px">JOB BY JOB BREAKDOWN</div> — </div> — ${jobMargins.length ? ` — <div style="overflow-x:auto"> — <table style="min-width:700px"> — <thead><tr> — <th>Job</th> — <th>Client</th> — <th>Date</th> — <th style="text-align:right">Revenue</th> — <th style="text-align:right">Crew Cost</th> — <th style="text-align:right">Expenses</th> — <th style="text-align:right">Net</th> — <th style="text-align:center">Margin</th> — <th>Status</th> — </tr></thead> — <tbody> — ${jobMargins.map(m => ` — <tr style="cursor:pointer;background:${marginBg(m.margin)}" onclick="switchTab('jobs');setTimeout(()=>openJobDetail('${m.job.id}'),300)"> — <td><span class="tag">${m.job.id}</span></td> — <td style="font-weight:600">${m.job.client}</td> — <td class="info-text">${m.job.date}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${m.revenue.toLocaleString()}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${m.crewCost.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:#ff8c00">$${m.expenses.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;font-weight:700;color:${marginColor(m.margin)}"> — ${m.net >= 0 ? '+' : ''}$${m.net.toFixed(2)} — </td> — <td style="text-align:center"> — <div style="display:inline-flex;align-items:center;justify-content:center;width:52px;height:24px;border-radius:12px;background:${marginBg(m.margin)};border:1px solid ${marginColor(m.margin)};font-family:var(--font-mono);font-size:10px;font-weight:700;color:${marginColor(m.margin)}"> — ${m.margin.toFixed(0)}% — </div> — </td> — <td><span class="status-badge status-${m.job.status}">${m.job.status}</span></td> — </tr>`).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="3" style="font-family:var(--font-mono);color:var(--green);font-size:11px">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${totalRevenue.toLocaleString()}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${totalCrew.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:#ff8c00">$${totalExpenses.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;font-weight:700;color:${marginColor(avgMargin)}"> — ${totalNet >= 0 ? '+' : ''}$${totalNet.toFixed(2)} — </td> — <td style="text-align:center"> — <div style="display:inline-flex;align-items:center;justify-content:center;width:52px;height:24px;border-radius:12px;background:${marginBg(avgMargin)};border:1px solid ${marginColor(avgMargin)};font-family:var(--font-mono);font-size:10px;font-weight:700;color:${marginColor(avgMargin)}"> — ${avgMargin.toFixed(0)}% — </div> — </td> — <td></td> — </tr> — </tbody> — </table> — </div>` : ` — <div style="text-align:center;padding:48px"> — <div style="font-size:40px;margin-bottom:12px">📈</div> — <p class="info-text">No job data yet. Create jobs with quoted amounts and crew logs to see margins.</p> — </div>`} — </div> — <!-- MARGIN LEGEND --> — <div style="display:flex;gap:16px;padding:12px 4px;flex-wrap:wrap;align-items:center"> — <span class="info-text">Margin health:</span> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(0,255,65,0.2);border:1px solid var(--green);border-radius:2px"></div><span class="info-text" style="color:var(--green)">40%+ Excellent</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,170,0,0.2);border:1px solid var(--amber);border-radius:2px"></div><span class="info-text" style="color:var(--amber)">20–39% Good</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,140,0,0.2);border:1px solid #ff8c00;border-radius:2px"></div><span class="info-text" style="color:#ff8c00">0–19% Thin</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,51,51,0.15);border:1px solid var(--red);border-radius:2px"></div><span class="info-text" style="color:var(--red)">Negative — Losing money</span></div> — </div>`; — content.innerHTML = html; — }, function renderMyPay() { — const content  = document.getElementById('main-content'); — const role     = currentUser.role; — const isHelper = role === 'helper'; — Collect all pay entries for this person — const myJobs = STATE.jobs.filter(j => { — if (role === 'driver') return nameIncludes(j.driver, currentUser.name); — if (role === 'helper') return (j.payroll?.helpers||[]).some(h => nameIncludes(h.name, currentUser.name)); — return false; — }); — let totalGross      = 0; — let totalCommission = 0; — let totalTax        = 0; — let totalTakeHome   = 0; — const payRows       = []; — myJobs.forEach(j => { — let gross = 0, commission = 0, workerType = '1099', payDetails = ''; — if (role === 'driver') { — gross       = j.payroll?.driver || 0; — commission  = j.payroll?.driverCommission || 0; — workerType  = j.payroll?.driverWorkerType || '1099'; — payDetails  = payLabel({ payType: j.payroll?.driverPayType||'flat', rate: j.payroll?.driverRate||gross, hours: j.payroll?.driverHours||0, days: j.payroll?.driverDays||1 }); — } else { — const helper = (j.payroll?.helpers||[]).find(h => — h.name?.toLowerCase().includes(currentUser.name.toLowerCase()) || — (currentUser.name.toLowerCase().split(' ')[0] && h.name?.toLowerCase().includes(currentUser.name.toLowerCase().split(' ')[0])) — ); — if (helper) { — gross      = calcPay(helper); — commission = helper.commission || 0; — workerType = helper.workerType || '1099'; — payDetails = payLabel(helper); — } — } — if (gross === 0) return; — const tax = calcTax(gross, workerType); — totalGross      += gross; — totalCommission += commission; — totalTax        += tax.totalEstTax; — totalTakeHome   += tax.takeHome; — payRows.push({ job: j, gross, commission, workerType, payDetails, tax }); — }); — let html = ` — <div class="section-header"> — <div> — <div class="section-title">📊 MY PAY SUMMARY</div> — <div class="section-sub">${currentUser.name} · ${role.toUpperCase()} · ${payRows.length} job(s)</div> — </div> — </div> — <!-- YTD SUMMARY STATS --> — <div class="grid-4" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Gross Earned</div> — <div class="stat-value">$${totalGross.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Commission</div> — <div class="stat-value" style="color:var(--amber)">$${totalCommission.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Est. Tax Owed</div> — <div class="stat-value red">$${totalTax.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Est. Take-Home</div> — <div class="stat-value">$${totalTakeHome.toFixed(2)}</div> — </div> — </div> — <!-- TAX ADVISORY BANNER --> — <div class="card" style="border-color:#664400;background:rgba(255,170,0,0.05);margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:11px;color:var(--amber);letter-spacing:2px;margin-bottom:8px">⚠ TAX ADVISORY</div> — <div class="info-text" style="line-height:1.8"> — ${totalGross > 0 ? `Based on your earnings of <strong style="color:var(--white)">$${totalGross.toFixed(2)}</strong>, you should set aside approximately <strong style="color:var(--red)">$${totalTax.toFixed(2)}</strong> (${(totalTax/totalGross*100).toFixed(1)}%) for taxes.` : 'No earnings recorded yet.'} — ${totalGross >= 600 ? ' You will likely receive a 1099 form if classified as a contractor.' : ''} — If you are a <strong style="color:var(--white)">1099 contractor</strong>, you are responsible for paying your own taxes quarterly (April, June, September, January). Consider setting aside <strong style="color:var(--red)">${totalGross > 0 ? (totalTax/totalGross*100).toFixed(0) : '~25'}%</strong> of every paycheck. — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div style="margin-top:4px"> — ${payRows.length ? payRows.map(row => ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:10px"> — <div> — <div style="display:flex;gap:8px;align-items:center;margin-bottom:4px"> — <span class="tag">${row.job.id}</span> — <span class="status-badge status-${row.job.status}">${row.job.status}</span> — <span class="tag">${row.workerType}</span> — </div> — <div style="font-size:14px;font-weight:600">${row.job.client}</div> — <div class="info-text">${row.job.date} · ${row.payDetails}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:20px;color:var(--green)">$${row.gross.toFixed(2)}</div> — ${row.commission > 0 ? `<div style="font-family:var(--font-mono);font-size:12px;color:var(--amber)">+ $${row.commission.toFixed(2)} commission</div>` : ''} — </div> — </div> — <!-- Tax breakdown per job --> — <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;margin-bottom:8px"> — <div class="stat-box" style="padding:10px"> — <div class="stat-label" style="font-size:9px">Gross Pay</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--white)">$${row.gross.toFixed(2)}</div> — </div> — <div class="stat-box" style="padding:10px"> — <div class="stat-label" style="font-size:9px">Est. Tax</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--red)">−$${row.tax.totalEstTax.toFixed(2)}</div> — </div> — <div class="stat-box" style="padding:10px;border-color:var(--green3)"> — <div class="stat-label" style="font-size:9px">Take-Home</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--green)">$${row.tax.takeHome.toFixed(2)}</div> — </div> — </div> — <!-- Detailed tax line items --> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;font-family:var(--font-mono);font-size:10px"> — ${row.tax.selfEmployTax ? `<div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>Self-Employment Tax (15.3%)</span><span style="color:var(--red)">−$${row.tax.selfEmployTax.toFixed(2)}</span></div>` : ''} — ${row.tax.ficaTax ? `<div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>FICA Employee (7.65%)</span><span style="color:var(--red)">−$${row.tax.ficaTax.toFixed(2)}</span></div>` : ''} — <div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>Federal Income (~${(row.tax.federalRate*100).toFixed(0)}%)</span><span style="color:var(--red)">−$${row.tax.federalTax.toFixed(2)}</span></div> — <div style="display:flex;justify-content:space-between;color:var(--gray)"><span>Georgia State (5.5%)</span><span style="color:var(--red)">−$${row.tax.stateTax.toFixed(2)}</span></div> — </div> — </div>`).join('') : ` — <div class="card" style="text-align:center;padding:40px"> — <p class="info-text">No pay records found for ${currentUser.name}.</p> — <p class="info-text" style="margin-top:8px">Your earnings will appear here once jobs are assigned and crew logs are saved.</p> — </div>`} — </div> — <div style="padding:12px;font-family:var(--font-mono);font-size:9px;color:var(--gray);line-height:1.8;border-top:1px solid var(--gray2);margin-top:8px"> — ⚠ DISCLAIMER: Tax estimates are for informational purposes only based on 2024 US federal rates and Georgia state rates. This is not tax advice. Individual circumstances vary. Consult a licensed tax professional or CPA for filing guidance. JPG Systems / JPG Ventures LLC is not responsible for tax obligations of independent contractors. — </div>`; — content.innerHTML = html; — } | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore, CLIENT MOVE PORTAL — Full transparency experience — This is what the client pays $100-150 for — Real-time gate tracking, item location, messaging, docs, DASHBOARD, FIRST RUN CHECK — runs on page load before login, MIGRATION — Stamp companyId on any legacy docs missing it — Runs silently on every login — safe to run repeatedly, ROLE SWITCHER — instant role switch without logout, ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard, SESSION PERSISTENCE — remember me across refreshes/tab closes, SOUND ENGINE — Web Audio API, no external files needed, STATE & STORAGE, TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order, function renderMargins() { — const content = document.getElementById('main-content'); — Build margin data per job — const jobMargins = STATE.jobs — .filter(j => j.amount > 0 || j.payroll) — .map(j => { — const revenue   = j.amount || 0; — const driverAccBonus = parseFloat(j.payroll?.accessorialBonus) || 0; — const crewCost  = (j.payroll?.driver || 0) + — driverAccBonus + — (j.payroll?.helpers || []).reduce((s,h) => s + calcPay(h), 0) + — (j.payroll?.driverCommission || 0); — Company revenue excludes driver accessorial bonuses (passed through to driver) — const expenses  = STATE.receipts — .filter(r => r.jobId === j.id && r.approved !== false) — .reduce((s,r) => s + (r.amount||0), 0); — const net       = revenue - crewCost - expenses; — const margin    = revenue > 0 ? (net / revenue * 100) : 0; — return { job: j, revenue, crewCost, expenses, net, margin }; — }) — .sort((a,b) => { — Default: worst margin first (ascending) — const mA = a.revenue > 0 ? a.profit/a.revenue : 0; — const mB = b.revenue > 0 ? b.profit/b.revenue : 0; — return mA - mB; — }); — Sort control — if (window._marginSort === 'date') jobMargins.sort((a,b) => b.job.date > a.job.date ? 1 : -1); — else if (window._marginSort === 'revenue') jobMargins.sort((a,b) => b.revenue - a.revenue); — ── Margin intelligence ── — const overpayJobs   = jobMargins.filter(m => m.revenue > 0 && (m.crewCost/m.revenue) > 0.40); — const lowProfitJobs = jobMargins.filter(m => m.revenue > 0 && (m.profit/m.revenue) < 0.20); — const bestJob       = [...jobMargins].sort((a,b) => (b.profit/b.revenue||0)-(a.profit/a.revenue||0))[0]; — const worstJob      = jobMargins[0]; // already sorted worst first — Overall stats — const totalRevenue  = jobMargins.reduce((s,m) => s + m.revenue, 0); — const totalCrew     = jobMargins.reduce((s,m) => s + m.crewCost, 0); — const totalExpenses = jobMargins.reduce((s,m) => s + m.expenses, 0); — const totalNet      = jobMargins.reduce((s,m) => s + m.net, 0); — const avgMargin     = totalRevenue > 0 ? (totalNet / totalRevenue * 100) : 0; — const profitable    = jobMargins.filter(m => m.net > 0).length; — const losing        = jobMargins.filter(m => m.net < 0).length; — Best and worst job — const best  = jobMargins.reduce((a,b) => b.margin > a.margin ? b : a, jobMargins[0]); — const worst = jobMargins.reduce((a,b) => b.margin < a.margin ? b : a, jobMargins[0]); — const marginColor = (n) => n >= 40 ? 'var(--green)' : n >= 20 ? 'var(--amber)' : n >= 0 ? '#ff8c00' : 'var(--red)'; — const marginBg    = (n) => n >= 40 ? 'rgba(0,255,65,0.08)' : n >= 20 ? 'rgba(255,170,0,0.08)' : n >= 0 ? 'rgba(255,140,0,0.08)' : 'rgba(255,51,51,0.08)'; — let html = ` — ${overpayJobs.length ? `<div style="background:rgba(255,30,30,0.06);border:1px solid rgba(255,30,30,0.3);border-radius:6px;padding:10px 14px;margin-bottom:12px;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px"> — <div> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;font-weight:700">⚠ LABOR OVERPAY ALERT</div> — <div style="font-size:12px;color:var(--white);margin-top:3px">${overpayJobs.length} job${overpayJobs.length>1?'s':''} with crew cost >40% of revenue</div> — <div style="font-size:11px;color:var(--gray);margin-top:2px">${overpayJobs.slice(0,3).map(m=>m.job.client+' ('+Math.round(m.crewCost/m.revenue*100)+'%)').join(' · ')}</div> — </div> — </div>` : ''} — <div style="display:flex;gap:8px;margin-bottom:12px;flex-wrap:wrap"> — <span style="font-family:var(--font-mono);font-size:10px;color:var(--gray);align-self:center">Sort:</span> — ${['worst:⬇ Worst Margin','revenue:$ Revenue','date:📅 Date'].map(o=>{const[v,l]=o.split(':');return `<button onclick="window._marginSort='${v}';renderMargins()" style="padding:5px 12px;font-family:var(--font-mono);font-size:10px;border-radius:3px;cursor:pointer;background:${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--green)':'transparent'};color:${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--navy)':'var(--gray)'};border:1px solid ${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--green)':'var(--gray2)'}">${l}</button>`;}).join('')} — </div> — <div class="section-header"> — <div> — <div class="section-title">📈 JOB MARGIN DASHBOARD</div> — <div class="section-sub">Revenue · Crew Cost · Expenses · Net per Job</div> — </div> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportJobsCSV()">⬇ EXPORT</button> — </div> — <!-- OVERALL STATS --> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(130px,1fr));gap:8px;margin-bottom:16px"> — <div class="stat-box"> — <div class="stat-label">Total Revenue</div> — <div class="stat-value">$${totalRevenue.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${jobMargins.length} jobs</div> — </div> — <div class="stat-box"> — <div class="stat-label">Crew Cost</div> — <div class="stat-value red">$${totalCrew.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${totalRevenue>0?(totalCrew/totalRevenue*100).toFixed(1):'0'}% of revenue</div> — </div> — <div class="stat-box"> — <div class="stat-label">Expenses</div> — <div class="stat-value red">$${totalExpenses.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${totalRevenue>0?(totalExpenses/totalRevenue*100).toFixed(1):'0'}% of revenue</div> — </div> — <div class="stat-box" style="border-color:${marginColor(avgMargin)}"> — <div class="stat-label">Net Profit</div> — <div class="stat-value" style="color:${marginColor(avgMargin)}">$${totalNet.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px;color:${marginColor(avgMargin)}">${avgMargin.toFixed(1)}% avg margin</div> — </div> — <div class="stat-box"> — <div class="stat-label">Profitable Jobs</div> — <div class="stat-value" style="color:var(--green)">${profitable}</div> — <div class="info-text" style="margin-top:4px;color:var(--red)">${losing} losing</div> — </div> — </div> — <!-- MARGIN BREAKDOWN BAR --> — ${totalRevenue > 0 ? ` — <div class="card" style="margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;margin-bottom:10px">REVENUE BREAKDOWN</div> — <div style="height:32px;border-radius:4px;overflow:hidden;display:flex;margin-bottom:8px"> — <div style="width:${(totalCrew/totalRevenue*100).toFixed(1)}%;background:rgba(255,51,51,0.7);transition:width 0.5s" title="Crew Cost"></div> — <div style="width:${(totalExpenses/totalRevenue*100).toFixed(1)}%;background:rgba(255,140,0,0.7);transition:width 0.5s" title="Expenses"></div> — <div style="flex:1;background:rgba(0,255,65,0.4);transition:width 0.5s" title="Net Profit"></div> — </div> — <div style="display:flex;gap:16px;flex-wrap:wrap"> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(255,51,51,0.7);border-radius:2px"></div><span class="info-text">Crew ${(totalCrew/totalRevenue*100).toFixed(1)}%</span></div> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(255,140,0,0.7);border-radius:2px"></div><span class="info-text">Expenses ${(totalExpenses/totalRevenue*100).toFixed(1)}%</span></div> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(0,255,65,0.4);border-radius:2px"></div><span class="info-text">Net ${avgMargin.toFixed(1)}%</span></div> — </div> — </div>` : ''} — <!-- BEST / WORST --> — ${jobMargins.length >= 2 ? ` — <div class="grid-2" style="margin-bottom:16px"> — ${best ? `<div class="card" style="border-color:var(--green3)"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--green);letter-spacing:2px;margin-bottom:6px">🏆 BEST MARGIN JOB</div> — <div style="font-size:15px;font-weight:700">${best.job.client} <span class="tag">${best.job.id}</span></div> — <div class="info-text">${best.job.date}</div> — <div style="font-family:var(--font-mono);font-size:22px;color:var(--green);margin-top:6px">${best.margin.toFixed(1)}%</div> — <div class="info-text">$${best.net.toFixed(2)} net on $${best.revenue.toLocaleString()}</div> — </div>` : ''} — ${worst ? `<div class="card" style="border-color:${marginColor(worst.margin)}"> — <div style="font-family:var(--font-mono);font-size:9px;color:${marginColor(worst.margin)};letter-spacing:2px;margin-bottom:6px">${worst.net < 0 ? '⚠ LOSING JOB' : '📉 LOWEST MARGIN'}</div> — <div style="font-size:15px;font-weight:700">${worst.job.client} <span class="tag">${worst.job.id}</span></div> — <div class="info-text">${worst.job.date}</div> — <div style="font-family:var(--font-mono);font-size:22px;color:${marginColor(worst.margin)};margin-top:6px">${worst.margin.toFixed(1)}%</div> — <div class="info-text">$${worst.net.toFixed(2)} net on $${worst.revenue.toLocaleString()}</div> — </div>` : ''} — </div>` : ''} — <!-- PER-JOB TABLE --> — <div class="card" style="padding:0;overflow:hidden"> — <div style="padding:12px 16px;border-bottom:1px solid var(--gray2)"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--green);letter-spacing:2px">JOB BY JOB BREAKDOWN</div> — </div> — ${jobMargins.length ? ` — <div style="overflow-x:auto"> — <table style="min-width:700px"> — <thead><tr> — <th>Job</th> — <th>Client</th> — <th>Date</th> — <th style="text-align:right">Revenue</th> — <th style="text-align:right">Crew Cost</th> — <th style="text-align:right">Expenses</th> — <th style="text-align:right">Net</th> — <th style="text-align:center">Margin</th> — <th>Status</th> — </tr></thead> — <tbody> — ${jobMargins.map(m => ` — <tr style="cursor:pointer;background:${marginBg(m.margin)}" onclick="switchTab('jobs');setTimeout(()=>openJobDetail('${m.job.id}'),300)"> — <td><span class="tag">${m.job.id}</span></td> — <td style="font-weight:600">${m.job.client}</td> — <td class="info-text">${m.job.date}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${m.revenue.toLocaleString()}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${m.crewCost.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:#ff8c00">$${m.expenses.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;font-weight:700;color:${marginColor(m.margin)}"> — ${m.net >= 0 ? '+' : ''}$${m.net.toFixed(2)} — </td> — <td style="text-align:center"> — <div style="display:inline-flex;align-items:center;justify-content:center;width:52px;height:24px;border-radius:12px;background:${marginBg(m.margin)};border:1px solid ${marginColor(m.margin)};font-family:var(--font-mono);font-size:10px;font-weight:700;color:${marginColor(m.margin)}"> — ${m.margin.toFixed(0)}% — </div> — </td> — <td><span class="status-badge status-${m.job.status}">${m.job.status}</span></td> — </tr>`).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="3" style="font-family:var(--font-mono);color:var(--green);font-size:11px">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${totalRevenue.toLocaleString()}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${totalCrew.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:#ff8c00">$${totalExpenses.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;font-weight:700;color:${marginColor(avgMargin)}"> — ${totalNet >= 0 ? '+' : ''}$${totalNet.toFixed(2)} — </td> — <td style="text-align:center"> — <div style="display:inline-flex;align-items:center;justify-content:center;width:52px;height:24px;border-radius:12px;background:${marginBg(avgMargin)};border:1px solid ${marginColor(avgMargin)};font-family:var(--font-mono);font-size:10px;font-weight:700;color:${marginColor(avgMargin)}"> — ${avgMargin.toFixed(0)}% — </div> — </td> — <td></td> — </tr> — </tbody> — </table> — </div>` : ` — <div style="text-align:center;padding:48px"> — <div style="font-size:40px;margin-bottom:12px">📈</div> — <p class="info-text">No job data yet. Create jobs with quoted amounts and crew logs to see margins.</p> — </div>`} — </div> — <!-- MARGIN LEGEND --> — <div style="display:flex;gap:16px;padding:12px 4px;flex-wrap:wrap;align-items:center"> — <span class="info-text">Margin health:</span> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(0,255,65,0.2);border:1px solid var(--green);border-radius:2px"></div><span class="info-text" style="color:var(--green)">40%+ Excellent</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,170,0,0.2);border:1px solid var(--amber);border-radius:2px"></div><span class="info-text" style="color:var(--amber)">20–39% Good</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,140,0,0.2);border:1px solid #ff8c00;border-radius:2px"></div><span class="info-text" style="color:#ff8c00">0–19% Thin</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,51,51,0.15);border:1px solid var(--red);border-radius:2px"></div><span class="info-text" style="color:var(--red)">Negative — Losing money</span></div> — </div>`; — content.innerHTML = html; — }, function renderPayrollSummary() { — This is the office-facing payroll output — all crew, all jobs — const content = document.getElementById('main-content'); — const role    = currentUser.role; — if (role !== 'office') { switchTab('mypay'); return; } — Aggregate per person — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — Driver — const dKey = j.driver; — if (!payMap[dKey]) payMap[dKey] = { name: dKey, role: 'Driver', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[dKey].jobs++; — payMap[dKey].gross      += j.payroll.driver || 0; — payMap[dKey].commission += j.payroll.driverCommission || 0; — payMap[dKey].workerType  = j.payroll.driverWorkerType || '1099'; — Helpers — (j.payroll.helpers||[]).forEach(h => { — if (!h.name) return; — const hKey = h.name; — if (!payMap[hKey]) payMap[hKey] = { name: hKey, role: 'Helper', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[hKey].jobs++; — payMap[hKey].gross      += calcPay(h); — payMap[hKey].commission += h.commission || 0; — payMap[hKey].workerType  = h.workerType || '1099'; — }); — }); — const people    = Object.values(payMap).sort((a,b) => b.gross - a.gross); — const totalGross = people.reduce((s,p) => s + p.gross + p.commission, 0); — let html = ` — <div class="section-header"> — <div> — <div class="section-title">💵 PAYROLL SUMMARY</div> — <div class="section-sub">All crew · All jobs · JPG Systems</div> — </div> — <div style="display:flex;gap:8px"> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportPayrollCSV()">⬇ EXPORT CSV</button> — </div> — </div> — <div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Total Crew</div><div class="stat-value">${people.length}</div></div> — <div class="stat-box"><div class="stat-label">Total Payroll</div><div class="stat-value amber">$${totalGross.toFixed(2)}</div></div> — <div class="stat-box"><div class="stat-label">Jobs w/ Payroll</div><div class="stat-value">${STATE.jobs.filter(j=>j.payroll).length}</div></div> — </div> — <div class="card"> — <table> — <thead><tr> — <th>Name</th><th>Role</th><th>Type</th><th>Jobs</th> — <th style="text-align:right">Base Pay</th> — <th style="text-align:right">Commission</th> — <th style="text-align:right">Total Gross</th> — <th style="text-align:right">Est. Tax</th> — <th style="text-align:right">Take-Home</th> — </tr></thead> — <tbody> — ${people.map(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — return `<tr> — <td style="font-weight:600">${p.name}</td> — <td><span class="tag">${p.role}</span></td> — <td><span class="tag">${p.workerType}</span></td> — <td style="font-family:var(--font-mono);text-align:center">${p.jobs}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${p.gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">${p.commission>0?'$'+p.commission.toFixed(2):'—'}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green);font-weight:700">$${gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${tax.totalEstTax.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${tax.takeHome.toFixed(2)}</td> — </tr>`; — }).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="4" style="font-family:var(--font-mono);color:var(--green)">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${people.reduce((s,p)=>s+p.gross,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">$${people.reduce((s,p)=>s+p.commission,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${totalGross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).totalEstTax,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).takeHome,0).toFixed(2)}</td> — </tr> — </tbody> — </table> — </div>`; — content.innerHTML = html; — }
| NOTIFICATION | 20008-20032 | 44 | MODAL HELPERS | 1099 / W2 YEAR-END TAX EXPORT — Aggregates all payments per contractor/employee for the year — Generates: on-screen summary + CSV export + PDF report, BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore, BOL TAB, CLAIMS DEFENSE AI — v51 — Pre-load damage detection using Claude vision — When driver takes a photo at pre-load gate, AI scans for — pre-existing damage, flags it, embeds in BOL as evidence. — Protects moving company from false damage claims., CLIENT BOOKING PORTAL — URL: fleetflowmovemasters.netlify.app/?book=1&co=COMPANY_ID — Moving company shares this link on their website — Client fills details → instant quote → pays deposit → job created, COMPANY BRANDING ENGINE, DASHBOARD, EDIT JOB  ← NEW FEATURE, FIELD NOTES, FIRST RUN CHECK — runs on page load before login, JOB DETAIL MODAL, JOB DISPLAY ID — broker/carrier ref number support — job.id      = FleetFlow internal ID (never changes) — job.refNum  = Broker/carrier order number (editable by office) — jobDispId() = what to show everywhere in the UI, LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool, MESSAGING, OFFLINE MODE — Full PWA with offline queue — Caches app shell on first load — Queues Firestore writes when offline — Syncs queue automatically when back online, Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — }, P&L ACCOUNTING MODULE — Kills QuickBooks — Chart of accounts, monthly P&L, period reports, export, PAY SCHEDULE & UNPAID CREW CHECKER, PAYROLL LEDGER — Mark Paid modal, PHOTO VIEWER, PORTAL ROUTE CHECK — runs before login screen — Matches Good Friends Movers LLC document package exactly — US DOT# 3921653 / MC# 1450729 — Structure: Interstate BOL | Legal Terms | Inventory x2 | Packing | Post Contract | Valuation, PWA — SPLASH, INSTALL PROMPT, SERVICE WORKER, RECEIPTS, ROLE SWITCHER — instant role switch without logout, ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard, SETTINGS ACTION FUNCTIONS, SETTINGS TAB, SIGNATURE MODAL, SOUND ENGINE — Web Audio API, no external files needed, STATE & STORAGE, STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live, SYSTEM HEALTH DASHBOARD — Platform operator view — JPG Ventures sees all licensees, sync status, revenue flow — Accessible only to creator/owner accounts, TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate, TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created, TRIAL + TIER SYSTEM, TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order, USER MANAGEMENT (Office only), WAREHOUSE, WAREHOUSE WORK LOGGING — Log warehouse work by employee — shows on calendar + payroll, function exportPayrollCSV() { — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — const addPerson = (name, role, gross, commission, workerType) => { — if (!name) return; — if (!payMap[name]) payMap[name] = { name, role, jobs: 0, gross: 0, commission: 0, workerType }; — payMap[name].jobs++; — payMap[name].gross      += gross; — payMap[name].commission += commission; — }; — addPerson(j.driver, 'Driver', j.payroll.driver||0, j.payroll.driverCommission||0, j.payroll.driverWorkerType||'1099'); — (j.payroll.helpers||[]).forEach(h => addPerson(h.name, 'Helper', calcPay(h), h.commission||0, h.workerType||'1099')); — }); — const rows = [['Name','Role','Worker Type','Jobs','Base Pay','Commission','Total Gross','Est Tax','Est Take-Home']]; — Object.values(payMap).forEach(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — rows.push([p.name, p.role, p.workerType, p.jobs, p.gross.toFixed(2), p.commission.toFixed(2), — gross.toFixed(2), tax.totalEstTax.toFixed(2), tax.takeHome.toFixed(2)]); — }); — downloadCSV(rows, 'jpgsystems-payroll-' + getTodayStr() + '.csv'); — notify('Payroll CSV exported ✓'); — } — function exportJobsCSV() { — const rows = [['Job ID','Client','Date','Time','Driver','Type','Status','Amount','Gates Passed','Origin','Destination']]; — STATE.jobs.forEach(j => { — const gates = Object.values(j.gates||{}).filter(Boolean).length; — rows.push([j.id, j.client, j.date, j.time||'', j.driver||'', j.type||'', — j.status, (j.amount||0).toFixed(2), gates + '/' + Object.keys(j.gates||{}).length, — j.origin||'', j.dest||'']); — }); — downloadCSV(rows, 'jpgsystems-jobs-' + getTodayStr() + '.csv'); — notify('Jobs CSV exported ✓'); — } — function exportReceiptsCSV() { — const rows = [['Receipt ID','Job ID','Driver','Date','Type','Amount','Status','Notes']]; — STATE.receipts.forEach(r => { — rows.push([r.id, r.jobId, r.driver||'', r.date||'', r.type||'', — (r.amount||0).toFixed(2), r.approved ? 'APPROVED' : r.rejected ? 'REJECTED' : 'PENDING', r.notes||'']); — }); — downloadCSV(rows, 'jpgsystems-receipts-' + getTodayStr() + '.csv'); — notify('Receipts CSV exported ✓'); — } — function downloadCSV(rows, filename) { — const csv  = rows.map(r => r.map(c => '"' + String(c).replace(/"/g,'""') + '"').join(',')).join('\n'); — const blob = new Blob([csv], { type: 'text/csv' }); — const url  = URL.createObjectURL(blob); — const a    = document.createElement('a'); — a.href     = url; — a.download = filename; — a.click(); — URL.revokeObjectURL(url); — } — Add export buttons to financials tab — function renderExportBar() { — return ` — <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;align-self:center">EXPORT:</div> — <button class="btn-sm" onclick="exportJobsCSV()">⬇ Jobs CSV</button> — <button class="btn-sm" onclick="exportReceiptsCSV()">⬇ Receipts CSV</button> — <button class="btn-sm" onclick="exportPayrollCSV()">⬇ Payroll CSV</button> — <button class="btn-sm" onclick="renderPayrollSummary()">💵 Payroll Report</button> — </div>`; — }, function openNewJobModal(prefillDate) { — ── HARD RESET — every field to blank/zero ────────────────── — const safe = (id, val='') => { const el = document.getElementById(id); if(el) el.value = val; }; — Core job fields — safe('nj-client'); safe('nj-phone'); safe('nj-email'); — safe('nj-origin'); safe('nj-dest'); safe('nj-pickup-from'); — safe('nj-amount'); safe('nj-notes'); safe('nj-carrier'); — safe('nj-cubic-ft'); safe('nj-cubic-ft-2'); safe('nj-rate-per-cuft'); — safe('nj-weight'); safe('nj-linehaul'); — safe('nj-end-date'); safe('nj-end-time'); — safe('nj-pickup-start'); safe('nj-pickup-end'); — safe('nj-delivery-start'); safe('nj-delivery-end'); — safe('nj-pickup-driver'); safe('nj-delivery-driver'); — safe('nj-job-id'); safe('nj-ref-num'); — safe('nj-priority'); safe('nj-followup'); safe('nj-fadd'); — safe('nj-broker-ref'); safe('nj-carrier'); — ALL estimate fields — zero out completely — safe('nj-est-fuel', ''); — safe('nj-est-fuel-pct', '0'); — safe('nj-est-overload-cuft', ''); — safe('nj-est-stairs-origin', ''); — safe('nj-est-stairs-dest', ''); — safe('nj-est-longcarry-origin', ''); — safe('nj-est-longcarry-dest', ''); — safe('nj-est-shuttle', ''); — safe('nj-est-bulky', ''); — safe('nj-est-split', ''); — safe('nj-est-pgs', ''); — safe('nj-est-packing', ''); — safe('nj-est-storage-days', ''); — safe('nj-est-storage-rate', ''); — safe('nj-est-other', ''); — safe('nj-est-deposit', ''); — safe('nj-est-partial2', ''); — safe('nj-est-tip', ''); — safe('nj-est-discount', ''); — safe('nj-est-discount-label', ''); — safe('nj-est-others1', '');  safe('nj-est-others1-label', ''); — safe('nj-est-others2', '');  safe('nj-est-others2-label', ''); — safe('nj-est-others3', '');  safe('nj-est-others3-label', ''); — safe('nj-est-extra1', '');   safe('nj-est-extra1-label', ''); — safe('nj-est-extra2', '');   safe('nj-est-extra2-label', ''); — safe('nj-est-extra3', '');   safe('nj-est-extra3-label', ''); — safe('nj-est-notes', ''); — safe('nj-est-office-remarks', ''); — safe('nj-est-slip-remarks', ''); — Reset carrier total display — const ct = document.getElementById('nj-carrier-total'); if(ct) ct.textContent = '$0.00'; — Clear helper rows — const hr = document.getElementById('nj-helper-rows'); if(hr) hr.innerHTML = ''; — Reset all selects to index 0 — ['nj-job-for','nj-est-tip-method','nj-est-valuation', — 'nj-est-fuel-mode','nj-est-tip-assign'].forEach(id => { — const el = document.getElementById(id); — if (el) el.selectedIndex = 0; — }); — try { toggleCarrierFields('nj'); } catch(e) {} — Apply defaults — const s2 = loadSettings(); — safe('nj-crew', s2.defaultCrew || '3'); — safe('nj-type', s2.defaultMove || 'local'); — safe('nj-date', prefillDate || getTodayStr()); — safe('nj-time', getSetting('defaultTime', '08:00')); — safe('nj-driver', currentUser.role === 'driver' ? (currentUser.name||'') : ''); — closeModal('modal-calday'); — openModal('modal-newjob'); — setTimeout(() => { try { applyJobDefaults('nj'); } catch(e) {} }, 50); — } — async function createJob() { — const client = document.getElementById('nj-client').value.trim(); — const date   = document.getElementById('nj-date').value; — const origin = document.getElementById('nj-origin').value.trim(); — const dest   = document.getElementById('nj-dest').value.trim(); — if (!client || !date) { notify('⚠ Client name and date required', true); return; } — Custom ID or auto-generate — const customId = (document.getElementById('nj-job-id')?.value||'').trim().toUpperCase(); — const autoId   = 'JOB-' + String((STATE.jobs||[]).length + 1).padStart(3,'0'); — const jobId    = customId || autoId; — if ((STATE.jobs||[]).find(j => j.id === jobId)) { — notify('⚠ Job ID ' + jobId + ' already exists', true); return; — } — Collect helpers — const helpers = []; — document.querySelectorAll('.nj-helper-name').forEach(inp => { — const name = inp.value.trim(); — if (!name) return; — const row = inp.closest('.nj-helper-row'); — helpers.push({ — name, — rate:    parseFloat(row?.querySelector('.nj-helper-rate')?.value) || 0, — payType: row?.querySelector('.nj-helper-type')?.value || 'hourly', — hours: 0, days: 1, workerType: '1099', paid: false — }); — }); — const g = id => (document.getElementById(id)?.value || ''); — const newJob = { — id:            jobId, — refNum:        g('nj-ref-num'), — client, — phone:         g('nj-phone'), — email:         g('nj-email'), — date, — time:          g('nj-time'), — endDate:       g('nj-end-date') || g('nj-delivery-end').slice(0,10) || '', — endTime:       g('nj-end-time'), — origin, — dest, — pickupStart:   g('nj-pickup-start'), — pickupEnd:     g('nj-pickup-end'), — pickupDriver:  g('nj-pickup-driver'), — deliveryStart: g('nj-delivery-start'), — deliveryEnd:   g('nj-delivery-end'), — deliveryDriver:g('nj-delivery-driver'), — driver:        g('nj-driver'), — driverUser:    currentUser.username || '', — crewSize:      parseInt(g('nj-crew')) || 3, — crewNames:     helpers.map(h => h.name), — amount:        parseFloat(g('nj-amount')) || 0, — cubicFt:       parseFloat(g('nj-cubic-ft-2') || g('nj-cubic-ft')) || 0, — weight:        parseFloat(g('nj-weight')) || 0, — linehaul:      parseFloat(g('nj-linehaul')) || 0, — priority:      g('nj-priority'), — followUp:      g('nj-followup'), — fadd:          g('nj-fadd'), — type:          g('nj-type') || 'local', — notes:         g('nj-notes'), — companyId:     COMPANY_ID, — status:        'pending', — gates:         { preload:false, loaded:false, delivered:false, payment:false }, — messages:      { crew:[], client:[] }, — photos:        [], — signatures:    [], — payroll:       { driver:0, helpers } — }; — try { — await db.collection(COL_JOBS).doc(newJob.id).set(newJob); — closeModal('modal-newjob'); — notify('✓ Job ' + newJob.id + ' created — ' + client); — setTimeout(() => renderCalendar(), 200); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('createJob error:', e); — } — }, function renderExpenses() { — const content = document.getElementById('main-content'); — const role = currentUser.role; — const isDriver = role === 'driver'; — Scope to this driver's jobs/disbursements if not office — const myJobs = isDriver — ? STATE.jobs.filter(j => nameIncludes(j.driver, currentUser.name)) — : STATE.jobs; — const myDisbursements = isDriver — ? STATE.disbursements.filter(d => — d.driverUser === currentUser.username || — (d.driver && d.driver.toLowerCase().includes(currentUser.name.toLowerCase())) — ) — : STATE.disbursements; — const myReceipts = isDriver — ? STATE.receipts.filter(r => { — const nm = r.driver && nameIncludes(r.driver, currentUser.name); — const um = r.driverUser && r.driverUser === currentUser.username; — return nm || um; — }) — : STATE.receipts; — Totals — const totalDisbursed = myDisbursements.reduce((s,d) => s + (d.amount||0), 0); — const totalReceipts  = myReceipts.reduce((s,r) => s + (r.amount||0), 0); — const netOwed        = totalReceipts - totalDisbursed; // positive = driver owes, negative = company owes driver — let html = ` — <div class="section-header"> — <div> — <div class="section-title">EXPENSE TRACKER</div> — <div class="section-sub">${isDriver ? 'Your funds & receipts · ' + currentUser.name : 'All driver funds & expense reconciliation'}</div> — </div> — ${isAdmin() ? `<button class="btn-green" onclick="openDisbursementModal()">💰 SEND FUNDS</button>` : ''} — </div> — <!-- SUMMARY STATS --> — <div class="grid-3" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Funds Disbursed</div> — <div class="stat-value">$${totalDisbursed.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Receipts Submitted</div> — <div class="stat-value amber">$${totalReceipts.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">${netOwed > 0 ? 'Driver Owes Back' : netOwed < 0 ? 'Company Owes Driver' : 'Balanced'}</div> — <div class="stat-value ${netOwed > 0 ? 'red' : netOwed < 0 ? 'amber' : ''}"> — ${netOwed === 0 ? '✓ $0.00' : (netOwed > 0 ? '+' : '') + '$' + Math.abs(netOwed).toFixed(2)} — </div> — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div class="card" style="margin-bottom:16px"> — <div class="card-header"><div class="card-title">JOB EXPENSE BREAKDOWN</div></div> — <table> — <thead><tr> — <th>Job ID</th><th>Client</th><th>Driver</th> — <th style="text-align:right">Funds Sent</th> — <th style="text-align:right">Receipts</th> — <th style="text-align:right">Net</th> — <th>Status</th> — </tr></thead> — <tbody> — ${myJobs.length ? myJobs.map(j => { — const jobDisb = myDisbursements.filter(d => d.jobId === j.id); — const jobRec  = myReceipts.filter(r => r.jobId === j.id); — const sent    = jobDisb.reduce((s,d) => s + (d.amount||0), 0); — const spent   = jobRec.reduce((s,r) => s + (r.amount||0), 0); — const net     = spent - sent; — return `<tr onclick="openJobExpenseDetail('${j.id}')" style="cursor:pointer"> — <td><span class="tag">${j.id}</span></td> — <td>${j.client}</td> — <td class="info-text">${j.driver || '—'}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--green)">$${sent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--amber)">$${spent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);font-weight:700;color:${net > 0 ? 'var(--red)' : net < 0 ? 'var(--amber)' : 'var(--green)'}"> — ${net === 0 ? '✓ $0.00' : (net > 0 ? '+' : '') + '$' + Math.abs(net).toFixed(2)} — </td> — <td><span class="status-badge status-${j.status}">${j.status}</span></td> — </tr>`; — }).join('') : `<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--gray);font-family:var(--font-mono)">No jobs found</td></tr>`} — </tbody> — </table> — </div> — <!-- DISBURSEMENTS LOG --> — <div class="grid-2"> — <div class="card"> — <div class="card-header"> — <div class="card-title">💰 FUNDS DISBURSED</div> — ${isAdmin() ? `<button class="btn-green" style="padding:7px 14px;font-size:11px" onclick="openDisbursementModal()">+ SEND FUNDS</button>` : ''} — </div> — ${myDisbursements.length ? myDisbursements.map(d => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${d.driver} <span class="tag">${d.jobId}</span></div> — <div class="info-text">${d.date} · ${d.method || 'Cash'} · ${d.notes || ''}</div> — <div class="info-text" style="color:var(--gray)">${d.sentBy ? 'Sent by: ' + d.sentBy : ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--green)">$${(d.amount||0).toFixed(2)}</div> — ${role === 'office' ? `<button class="btn-sm" style="margin-top:4px;color:var(--red);border-color:#552222;font-size:10px" onclick="event.stopPropagation();deleteDisbursementEntry('${d.id}')">DELETE</button>` : ''} — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No funds disbursed yet.</p>`} — ${myDisbursements.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Disbursed</span> — <span style="font-family:var(--font-mono);color:var(--green);font-size:14px">$${totalDisbursed.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="card"> — <div class="card-header"><div class="card-title">🧾 RECEIPTS SUBMITTED</div></div> — ${myReceipts.length ? myReceipts.map(r => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${r.type} <span class="tag">${r.jobId}</span></div> — <div class="info-text">${r.driver} · ${r.date} · ${r.notes || ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--amber)">$${(r.amount||0).toFixed(2)}</div> — <div style="margin-top:2px">${r.approved — ? '<span style="color:var(--green);font-size:10px;font-family:var(--font-mono)">✓ APPROVED</span>' — : '<span style="color:var(--amber);font-size:10px;font-family:var(--font-mono)">⏳ PENDING</span>'}</div> — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No receipts submitted yet.</p>`} — ${myReceipts.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Receipts</span> — <span style="font-family:var(--font-mono);color:var(--amber);font-size:14px">$${totalReceipts.toFixed(2)}</span> — </div>` : ''} — </div> — </div>`; — content.innerHTML = html; — } — function openJobExpenseDetail(jobId) { — Switch to jobs tab and open the job detail — closeModal('modal-calday'); — openJobDetail(jobId); — } — function openDisbursementModal(prefillJobId) { — Pre-fill job ID if provided — document.getElementById('disb-jobid').value = prefillJobId || ''; — document.getElementById('disb-driver').value = ''; — document.getElementById('disb-amount').value = ''; — document.getElementById('disb-method').value = getSetting('defaultPay', 'Cash'); — document.getElementById('disb-notes').value = ''; — Populate driver dropdown from users — populateDisbDriverDropdown(); — openModal('modal-disbursement'); — } — async function populateDisbDriverDropdown() { — const sel = document.getElementById('disb-driver'); — sel.innerHTML = '<option value="">-- Select Driver --</option>'; — try { — const snap = await db.collection(COL_USERS) — .where('role', 'in', ['driver','helper']) — .get(); — snap.docs.forEach(d => { — const u = d.data(); — if (u.active) { — const opt = document.createElement('option'); — opt.value = JSON.stringify({ name: u.displayName, username: u.username }); — opt.textContent = u.displayName + ' (' + u.role + ')'; — sel.appendChild(opt); — } — }); — } catch(e) { — Fallback — type name manually — sel.innerHTML = '<option value="">Type driver name in notes</option>'; — } — } — async function saveDisbursementEntry() { — const jobId   = document.getElementById('disb-jobid').value.trim().toUpperCase(); — const driverRaw = document.getElementById('disb-driver').value; — const amount  = parseFloat(document.getElementById('disb-amount').value); — const method  = document.getElementById('disb-method').value; — const notes   = document.getElementById('disb-notes').value.trim(); — const btn     = document.getElementById('disb-submit-btn'); — if (!jobId)   { notify('⚠ Job ID is required', true); return; } — if (!driverRaw) { notify('⚠ Select a driver', true); return; } — if (isNaN(amount) || amount <= 0) { notify('⚠ Enter a valid amount', true); return; } — if (btn.disabled) return; — btn.disabled = true; — btn.textContent = 'SAVING...'; — try { — let driverName = driverRaw, driverUser = ''; — try { — const parsed = JSON.parse(driverRaw); — driverName = parsed.name; — driverUser = parsed.username; — } catch(e) { driverName = driverRaw; } — const disb = { — id:         'DISB-' + Date.now(), — jobId, — driver:     driverName, — driverUser: driverUser, — amount, — method, — notes, — sentBy:     currentUser.name, — date:       getTodayStr(), — createdAt:  new Date().toISOString() — }; — await saveDisbursement(disb); — closeModal('modal-disbursement'); — notify('$' + amount.toFixed(2) + ' disbursed to ' + driverName + ' ✓'); — Notify the driver — try { — if (driverUser) { — await sendNotification(driverUser, driverName, 'funds_sent', — 'Funds Sent 💰', — '$' + amount.toFixed(2) + ' sent via ' + method + ' for ' + jobId + (notes ? ' · ' + notes : '') + ' — from ' + currentUser.name, — { jobId, amount } — ); — } — } catch(e) {} — } catch(e) { — notify('⚠ Error: ' + e.message, true); — } finally { — btn.disabled = false; — btn.textContent = 'SEND FUNDS'; — } — } — async function deleteDisbursementEntry(disbId) { — if (!confirm('Delete this disbursement? This cannot be undone.')) return; — try { — await db.collection(COL_DISBURSEMENTS).doc(disbId).delete(); — notify('Disbursement deleted ✓'); — } catch(e) { notify('⚠ ' + e.message, true); } — }, function renderFleet() { — const content = document.getElementById('main-content'); — const role    = currentUser.role; — const canEdit = role === 'office'; — let html = ` — <div class="section-header"> — <div> — <div class="section-title">🚚 FLEET MANAGEMENT</div> — <div class="section-sub">${STATE.fleet.length} vehicle(s) registered · Move Masters OS</div> — </div> — ${canEdit ? `<button class="btn-green" onclick="openFleetModal()">+ ADD VEHICLE</button>` : ''} — </div>`; — if (!STATE.fleet.length) { — html += `<div class="card" style="text-align:center;padding:40px"> — <div style="font-size:48px;margin-bottom:12px">🚚</div> — <p class="info-text">No vehicles registered yet.</p> — ${canEdit ? `<button class="btn-green" style="margin-top:16px" onclick="openFleetModal()">+ ADD FIRST VEHICLE</button>` : ''} — </div>`; — } else { — Summary stats — const active  = STATE.fleet.filter(v => v.status === 'active').length; — const maint   = STATE.fleet.filter(v => v.status === 'maintenance').length; — const offline = STATE.fleet.filter(v => v.status === 'offline').length; — html += `<div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Active</div><div class="stat-value">${active}</div></div> — <div class="stat-box"><div class="stat-label">In Maintenance</div><div class="stat-value amber">${maint}</div></div> — <div class="stat-box"><div class="stat-label">Offline</div><div class="stat-value red">${offline}</div></div> — </div>`; — STATE.fleet.forEach(v => { — const assignedJob = STATE.jobs.find(j => j.vehicleId === v.id && j.status === 'active'); — const statusColor = v.status === 'active' ? 'var(--green)' : v.status === 'maintenance' ? 'var(--amber)' : 'var(--red)'; — const maintDue    = v.nextMaint ? new Date(v.nextMaint) : null; — const maintOverdue = maintDue && maintDue < new Date(); — html += ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:12px"> — <div> — <div style="display:flex;align-items:center;gap:10px;margin-bottom:6px"> — <div style="font-size:28px">${v.type === 'box_truck' ? '🚛' : v.type === 'cargo_van' ? '🚐' : '🚚'}</div> — <div> — <div style="font-family:var(--font-mono);font-size:14px;font-weight:700;color:var(--white)">${v.name || v.id}</div> — <div class="info-text">${v.make||''} ${v.model||''} ${v.year||''} · ${v.plate||'No plate'}</div> — </div> — </div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:11px;color:${statusColor};letter-spacing:2px">${(v.status||'active').toUpperCase()}</div> — ${canEdit ? `<button class="btn-sm" style="margin-top:6px" onclick="openFleetModal('${v.id}')">✎ EDIT</button>` : ''} — </div> — </div> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(110px,1fr));gap:8px;margin-bottom:12px"> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Capacity</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.capacity || '—'}</div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Mileage</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.mileage ? v.mileage.toLocaleString() + ' mi' : '—'}</div> — </div> — <div class="stat-box" style="padding:8px;${maintOverdue ? 'border-color:var(--red)' : ''}"> — <div class="stat-label" style="font-size:9px">Next Service</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${maintOverdue ? 'var(--red)' : 'var(--white)'}"> — ${v.nextMaint || 'Not set'}${maintOverdue ? ' ⚠' : ''} — </div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Assigned Job</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${assignedJob ? 'var(--green)' : 'var(--gray)'}"> — ${assignedJob ? assignedJob.id : 'Available'} — </div> — </div> — </div> — ${v.notes ? `<div class="info-text" style="margin-bottom:10px">📝 ${v.notes}</div>` : ''} — <!-- MAINTENANCE LOG --> — ${v.maintLog && v.maintLog.length ? ` — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:6px">MAINTENANCE LOG</div> — ${v.maintLog.slice(-3).reverse().map(m => ` — <div style="display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px solid var(--gray2);font-size:12px"> — <div><span class="info-text">${m.date}</span> · ${m.work}</div> — <div style="font-family:var(--font-mono);color:var(--amber)">$${(m.cost||0).toFixed(0)}</div> — </div>`).join('')}` : ''} — ${canEdit ? ` — <div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap"> — <button class="btn-sm" onclick="logMaintenance('${v.id}')">🔧 LOG SERVICE</button> — <button class="btn-sm" onclick="updateMileage('${v.id}')">📍 UPDATE MILEAGE</button> — <select style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:5px 8px;font-family:var(--font-mono);font-size:10px;border-radius:3px" — onchange="updateVehicleStatus('${v.id}',this.value)"> — <option value="active"      ${v.status==='active'      ?'selected':''}>● Active</option> — <option value="maintenance" ${v.status==='maintenance' ?'selected':''}>🔧 Maintenance</option> — <option value="offline"     ${v.status==='offline'     ?'selected':''}>○ Offline</option> — </select> — </div>` : ''} — </div>`; — }); — } — content.innerHTML = html; — } — function openFleetModal(vehicleId) { — const v = vehicleId ? STATE.fleet.find(x => x.id === vehicleId) : null; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">${v ? 'EDIT VEHICLE — ' + v.id : 'ADD VEHICLE'}</div> — <div class="grid-2"> — <div class="form-group"><label>Vehicle Name / Unit #</label> — <input type="text" id="flt-name" value="${(v&&v.name)||''}" placeholder="e.g. Truck 1 · Unit 24" /></div> — <div class="form-group"><label>Type</label> — <select id="flt-type"> — <option value="box_truck" ${v?.type==='box_truck'?'selected':''}>🚛 Box Truck</option> — <option value="cargo_van" ${v?.type==='cargo_van'?'selected':''}>🚐 Cargo Van</option> — <option value="flatbed"   ${v?.type==='flatbed'  ?'selected':''}>🚚 Flatbed</option> — <option value="other"     ${v?.type==='other'    ?'selected':''}>📋 Other</option> — </select> — </div> — </div> — <div class="grid-3"> — <div class="form-group"><label>Make</label><input type="text" id="flt-make" value="${(v&&v.make)||''}" placeholder="Ford" /></div> — <div class="form-group"><label>Model</label><input type="text" id="flt-model" value="${(v&&v.model)||''}" placeholder="E-450" /></div> — <div class="form-group"><label>Year</label><input type="number" id="flt-year" value="${(v&&v.year)||''}" placeholder="2019" /></div> — </div> — <div class="grid-2"> — <div class="form-group"><label>License Plate</label><input type="text" id="flt-plate" value="${(v&&v.plate)||''}" placeholder="ABC-1234" style="text-transform:uppercase"/></div> — <div class="form-group"><label>Capacity (text label)</label><input type="text" id="flt-cap" value="${(v&&v.capacity)||''}" placeholder="26 ft · 1500 lbs" /></div> — </div> — <div class="grid-3"> — <div class="form-group"> — <label>Max Cubic Feet <span class="info-text">(for load optimization)</span></label> — <input type="number" id="flt-maxcuft" value="${(v&&v.maxCuFt)||''}" placeholder="e.g. 1200" min="0" step="50" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Fuel Efficiency (MPG) <span class="info-text">(for cost calc)</span></label> — <input type="number" id="flt-mpg" value="${(v&&v.mpg)||''}" placeholder="e.g. 10" min="1" step="0.5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Load Efficiency % <span class="info-text">(usable space)</span></label> — <input type="number" id="flt-loadeff" value="${(v&&v.loadEff)||85}" placeholder="85" min="50" max="100" step="5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — </div> — <div class="grid-2"> — <div class="form-group"><label>Current Mileage</label><input type="number" id="flt-miles" value="${(v&&v.mileage)||''}" placeholder="85000" /></div> — <div class="form-group"><label>Next Service Due</label><input type="date" id="flt-maint" value="${(v&&v.nextMaint)||''}" /></div> — </div> — <div class="form-group"><label>Notes</label> — <input type="text" id="flt-notes" value="${(v&&v.notes)||''}" placeholder="Insurance, registration, special equipment..." /></div> — <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:8px"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" onclick="saveVehicle('${vehicleId||''}')">SAVE VEHICLE</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveVehicle(vehicleId) { — const vehicle = { — id:        vehicleId || 'VEH-' + Date.now(), — name:      document.getElementById('flt-name').value.trim(), — type:      document.getElementById('flt-type').value, — make:      document.getElementById('flt-make').value.trim(), — model:     document.getElementById('flt-model').value.trim(), — year:      document.getElementById('flt-year').value, — plate:     document.getElementById('flt-plate').value.trim().toUpperCase(), — capacity:  document.getElementById('flt-cap').value.trim(), — maxCuFt:   parseFloat(document.getElementById('flt-maxcuft')?.value)||0, — mpg:       parseFloat(document.getElementById('flt-mpg')?.value)||0, — loadEff:   parseFloat(document.getElementById('flt-loadeff')?.value)||85, — mileage:   parseInt(document.getElementById('flt-miles').value)||0, — nextMaint: document.getElementById('flt-maint').value, — notes:     document.getElementById('flt-notes').value.trim(), — status:    vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.status||'active') : 'active', — maintLog:  vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.maintLog||[]) : [], — updatedAt: new Date().toISOString() — }; — if (!vehicle.name) { notify('⚠ Vehicle name is required', true); return; } — try { — await db.collection(COL_FLEET).doc(vehicle.id).set(vehicle); — notify((vehicleId ? 'Vehicle updated' : 'Vehicle added') + ' ✓'); — closeModal('modal-jobdetail'); — } catch(e) { notify('⚠ ' + e.message, true); } — } — async function logMaintenance(vehicleId) { — const work = prompt('Service performed:'); if (!work) return; — const cost = parseFloat(prompt('Cost ($):') || '0'); — const v    = STATE.fleet.find(x => x.id === vehicleId); — if (!v) return; — if (!v.maintLog) v.maintLog = []; — v.maintLog.push({ date: getTodayStr(), work, cost, loggedBy: currentUser.name }); — v.updatedAt = new Date().toISOString(); — await db.collection(COL_FLEET).doc(vehicleId).set(v); — notify('Maintenance logged ✓'); — } — async function updateMileage(vehicleId) { — const miles = prompt('Enter current mileage:'); if (!miles) return; — await db.collection(COL_FLEET).doc(vehicleId).update({ mileage: parseInt(miles)||0 }); — notify('Mileage updated ✓'); — } — async function updateVehicleStatus(vehicleId, status) { — await db.collection(COL_FLEET).doc(vehicleId).update({ status }); — notify('Vehicle status: ' + status.toUpperCase()); — }
| STATE & STORAGE | 5313-5840 | 72 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore, COMPANY BRANDING ENGINE, FIREBASE INIT, FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill, LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool, MIGRATION — Stamp companyId on any legacy docs missing it — Runs silently on every login — safe to run repeatedly, NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive, NOTIFICATION, OFFLINE MODE — Full PWA with offline queue — Caches app shell on first load — Queues Firestore writes when offline — Syncs queue automatically when back online, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, SOUND ENGINE — Web Audio API, no external files needed, TABS, TRIAL + TIER SYSTEM | 1099 / W2 YEAR-END TAX EXPORT — Aggregates all payments per contractor/employee for the year — Generates: on-screen summary + CSV export + PDF report, BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore, BOL TAB, CALENDAR, CLAIMS DEFENSE AI — v51 — Pre-load damage detection using Claude vision — When driver takes a photo at pre-load gate, AI scans for — pre-existing damage, flags it, embeds in BOL as evidence. — Protects moving company from false damage claims., CLIENT MOVE PORTAL — Full transparency experience — This is what the client pays $100-150 for — Real-time gate tracking, item location, messaging, docs, COMPANY BRANDING ENGINE, CREW LOG HELPERS — extracted to avoid nested template literal — Chrome syntax errors from 4+ levels of backtick nesting, DASHBOARD, EDIT JOB  ← NEW FEATURE, ESTIMATE TAB — Per-job BOL line item editor — This is the source of truth for the BOL PDF, FIELD NOTES, FINANCIALS, FIRST RUN CHECK — runs on page load before login, HISTORY, JOB DETAIL MODAL, JOB DISPLAY ID — broker/carrier ref number support — job.id      = FleetFlow internal ID (never changes) — job.refNum  = Broker/carrier order number (editable by office) — jobDispId() = what to show everywhere in the UI, LABOR PRODUCTIVITY SCORES — v52 — Uses gate timestamps to calculate efficiency per crew member — Crew A moves 50 cu.ft/hr vs Crew B at 35 cu.ft/hr — Visible to owner/office — used for rewards and training, LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool, MESSAGING, MIGRATION — Stamp companyId on any legacy docs missing it — Runs silently on every login — safe to run repeatedly, MONTH VIEW — Google Calendar style with multi-day span bars, NOTIFICATION ENGINE, Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — }, P&L ACCOUNTING MODULE — Kills QuickBooks — Chart of accounts, monthly P&L, period reports, export, PAY SCHEDULE & UNPAID CREW CHECKER, PAYROLL LEDGER — Build running balance from jobs + payments, PAYROLL LEDGER — Mark Paid modal, PAYROLL LEDGER — Running balance per crew member — Balance = earned - paid. When paid → resets to 0 → new cycle, PHOTO VIEWER, PORTAL ROUTE CHECK — runs before login screen — Matches Good Friends Movers LLC document package exactly — US DOT# 3921653 / MC# 1450729 — Structure: Interstate BOL | Legal Terms | Inventory x2 | Packing | Post Contract | Valuation, RECEIPTS, ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client, ROLE SWITCHER — instant role switch without logout, ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard, SCHEDULE, SESSION PERSISTENCE — remember me across refreshes/tab closes, SETTINGS TAB, SIGNATURE MODAL, SOUND ENGINE — Web Audio API, no external files needed, STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live, TABS, TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate, TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created, TRIAL + TIER SYSTEM, TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order, TWILIO SMS — Real text notifications — Configure in Settings → Integrations, USER MANAGEMENT (Office only), USER SETTINGS — stored in localStorage per user, WAREHOUSE, WAREHOUSE WORK LOGGING — Log warehouse work by employee — shows on calendar + payroll, WEEK VIEW — time-based layout like Google Calendar, function exportPayrollCSV() { — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — const addPerson = (name, role, gross, commission, workerType) => { — if (!name) return; — if (!payMap[name]) payMap[name] = { name, role, jobs: 0, gross: 0, commission: 0, workerType }; — payMap[name].jobs++; — payMap[name].gross      += gross; — payMap[name].commission += commission; — }; — addPerson(j.driver, 'Driver', j.payroll.driver||0, j.payroll.driverCommission||0, j.payroll.driverWorkerType||'1099'); — (j.payroll.helpers||[]).forEach(h => addPerson(h.name, 'Helper', calcPay(h), h.commission||0, h.workerType||'1099')); — }); — const rows = [['Name','Role','Worker Type','Jobs','Base Pay','Commission','Total Gross','Est Tax','Est Take-Home']]; — Object.values(payMap).forEach(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — rows.push([p.name, p.role, p.workerType, p.jobs, p.gross.toFixed(2), p.commission.toFixed(2), — gross.toFixed(2), tax.totalEstTax.toFixed(2), tax.takeHome.toFixed(2)]); — }); — downloadCSV(rows, 'jpgsystems-payroll-' + getTodayStr() + '.csv'); — notify('Payroll CSV exported ✓'); — } — function exportJobsCSV() { — const rows = [['Job ID','Client','Date','Time','Driver','Type','Status','Amount','Gates Passed','Origin','Destination']]; — STATE.jobs.forEach(j => { — const gates = Object.values(j.gates||{}).filter(Boolean).length; — rows.push([j.id, j.client, j.date, j.time||'', j.driver||'', j.type||'', — j.status, (j.amount||0).toFixed(2), gates + '/' + Object.keys(j.gates||{}).length, — j.origin||'', j.dest||'']); — }); — downloadCSV(rows, 'jpgsystems-jobs-' + getTodayStr() + '.csv'); — notify('Jobs CSV exported ✓'); — } — function exportReceiptsCSV() { — const rows = [['Receipt ID','Job ID','Driver','Date','Type','Amount','Status','Notes']]; — STATE.receipts.forEach(r => { — rows.push([r.id, r.jobId, r.driver||'', r.date||'', r.type||'', — (r.amount||0).toFixed(2), r.approved ? 'APPROVED' : r.rejected ? 'REJECTED' : 'PENDING', r.notes||'']); — }); — downloadCSV(rows, 'jpgsystems-receipts-' + getTodayStr() + '.csv'); — notify('Receipts CSV exported ✓'); — } — function downloadCSV(rows, filename) { — const csv  = rows.map(r => r.map(c => '"' + String(c).replace(/"/g,'""') + '"').join(',')).join('\n'); — const blob = new Blob([csv], { type: 'text/csv' }); — const url  = URL.createObjectURL(blob); — const a    = document.createElement('a'); — a.href     = url; — a.download = filename; — a.click(); — URL.revokeObjectURL(url); — } — Add export buttons to financials tab — function renderExportBar() { — return ` — <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;align-self:center">EXPORT:</div> — <button class="btn-sm" onclick="exportJobsCSV()">⬇ Jobs CSV</button> — <button class="btn-sm" onclick="exportReceiptsCSV()">⬇ Receipts CSV</button> — <button class="btn-sm" onclick="exportPayrollCSV()">⬇ Payroll CSV</button> — <button class="btn-sm" onclick="renderPayrollSummary()">💵 Payroll Report</button> — </div>`; — }, function openNewJobModal(prefillDate) { — ── HARD RESET — every field to blank/zero ────────────────── — const safe = (id, val='') => { const el = document.getElementById(id); if(el) el.value = val; }; — Core job fields — safe('nj-client'); safe('nj-phone'); safe('nj-email'); — safe('nj-origin'); safe('nj-dest'); safe('nj-pickup-from'); — safe('nj-amount'); safe('nj-notes'); safe('nj-carrier'); — safe('nj-cubic-ft'); safe('nj-cubic-ft-2'); safe('nj-rate-per-cuft'); — safe('nj-weight'); safe('nj-linehaul'); — safe('nj-end-date'); safe('nj-end-time'); — safe('nj-pickup-start'); safe('nj-pickup-end'); — safe('nj-delivery-start'); safe('nj-delivery-end'); — safe('nj-pickup-driver'); safe('nj-delivery-driver'); — safe('nj-job-id'); safe('nj-ref-num'); — safe('nj-priority'); safe('nj-followup'); safe('nj-fadd'); — safe('nj-broker-ref'); safe('nj-carrier'); — ALL estimate fields — zero out completely — safe('nj-est-fuel', ''); — safe('nj-est-fuel-pct', '0'); — safe('nj-est-overload-cuft', ''); — safe('nj-est-stairs-origin', ''); — safe('nj-est-stairs-dest', ''); — safe('nj-est-longcarry-origin', ''); — safe('nj-est-longcarry-dest', ''); — safe('nj-est-shuttle', ''); — safe('nj-est-bulky', ''); — safe('nj-est-split', ''); — safe('nj-est-pgs', ''); — safe('nj-est-packing', ''); — safe('nj-est-storage-days', ''); — safe('nj-est-storage-rate', ''); — safe('nj-est-other', ''); — safe('nj-est-deposit', ''); — safe('nj-est-partial2', ''); — safe('nj-est-tip', ''); — safe('nj-est-discount', ''); — safe('nj-est-discount-label', ''); — safe('nj-est-others1', '');  safe('nj-est-others1-label', ''); — safe('nj-est-others2', '');  safe('nj-est-others2-label', ''); — safe('nj-est-others3', '');  safe('nj-est-others3-label', ''); — safe('nj-est-extra1', '');   safe('nj-est-extra1-label', ''); — safe('nj-est-extra2', '');   safe('nj-est-extra2-label', ''); — safe('nj-est-extra3', '');   safe('nj-est-extra3-label', ''); — safe('nj-est-notes', ''); — safe('nj-est-office-remarks', ''); — safe('nj-est-slip-remarks', ''); — Reset carrier total display — const ct = document.getElementById('nj-carrier-total'); if(ct) ct.textContent = '$0.00'; — Clear helper rows — const hr = document.getElementById('nj-helper-rows'); if(hr) hr.innerHTML = ''; — Reset all selects to index 0 — ['nj-job-for','nj-est-tip-method','nj-est-valuation', — 'nj-est-fuel-mode','nj-est-tip-assign'].forEach(id => { — const el = document.getElementById(id); — if (el) el.selectedIndex = 0; — }); — try { toggleCarrierFields('nj'); } catch(e) {} — Apply defaults — const s2 = loadSettings(); — safe('nj-crew', s2.defaultCrew || '3'); — safe('nj-type', s2.defaultMove || 'local'); — safe('nj-date', prefillDate || getTodayStr()); — safe('nj-time', getSetting('defaultTime', '08:00')); — safe('nj-driver', currentUser.role === 'driver' ? (currentUser.name||'') : ''); — closeModal('modal-calday'); — openModal('modal-newjob'); — setTimeout(() => { try { applyJobDefaults('nj'); } catch(e) {} }, 50); — } — async function createJob() { — const client = document.getElementById('nj-client').value.trim(); — const date   = document.getElementById('nj-date').value; — const origin = document.getElementById('nj-origin').value.trim(); — const dest   = document.getElementById('nj-dest').value.trim(); — if (!client || !date) { notify('⚠ Client name and date required', true); return; } — Custom ID or auto-generate — const customId = (document.getElementById('nj-job-id')?.value||'').trim().toUpperCase(); — const autoId   = 'JOB-' + String((STATE.jobs||[]).length + 1).padStart(3,'0'); — const jobId    = customId || autoId; — if ((STATE.jobs||[]).find(j => j.id === jobId)) { — notify('⚠ Job ID ' + jobId + ' already exists', true); return; — } — Collect helpers — const helpers = []; — document.querySelectorAll('.nj-helper-name').forEach(inp => { — const name = inp.value.trim(); — if (!name) return; — const row = inp.closest('.nj-helper-row'); — helpers.push({ — name, — rate:    parseFloat(row?.querySelector('.nj-helper-rate')?.value) || 0, — payType: row?.querySelector('.nj-helper-type')?.value || 'hourly', — hours: 0, days: 1, workerType: '1099', paid: false — }); — }); — const g = id => (document.getElementById(id)?.value || ''); — const newJob = { — id:            jobId, — refNum:        g('nj-ref-num'), — client, — phone:         g('nj-phone'), — email:         g('nj-email'), — date, — time:          g('nj-time'), — endDate:       g('nj-end-date') || g('nj-delivery-end').slice(0,10) || '', — endTime:       g('nj-end-time'), — origin, — dest, — pickupStart:   g('nj-pickup-start'), — pickupEnd:     g('nj-pickup-end'), — pickupDriver:  g('nj-pickup-driver'), — deliveryStart: g('nj-delivery-start'), — deliveryEnd:   g('nj-delivery-end'), — deliveryDriver:g('nj-delivery-driver'), — driver:        g('nj-driver'), — driverUser:    currentUser.username || '', — crewSize:      parseInt(g('nj-crew')) || 3, — crewNames:     helpers.map(h => h.name), — amount:        parseFloat(g('nj-amount')) || 0, — cubicFt:       parseFloat(g('nj-cubic-ft-2') || g('nj-cubic-ft')) || 0, — weight:        parseFloat(g('nj-weight')) || 0, — linehaul:      parseFloat(g('nj-linehaul')) || 0, — priority:      g('nj-priority'), — followUp:      g('nj-followup'), — fadd:          g('nj-fadd'), — type:          g('nj-type') || 'local', — notes:         g('nj-notes'), — companyId:     COMPANY_ID, — status:        'pending', — gates:         { preload:false, loaded:false, delivered:false, payment:false }, — messages:      { crew:[], client:[] }, — photos:        [], — signatures:    [], — payroll:       { driver:0, helpers } — }; — try { — await db.collection(COL_JOBS).doc(newJob.id).set(newJob); — closeModal('modal-newjob'); — notify('✓ Job ' + newJob.id + ' created — ' + client); — setTimeout(() => renderCalendar(), 200); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('createJob error:', e); — } — }, function renderExpenses() { — const content = document.getElementById('main-content'); — const role = currentUser.role; — const isDriver = role === 'driver'; — Scope to this driver's jobs/disbursements if not office — const myJobs = isDriver — ? STATE.jobs.filter(j => nameIncludes(j.driver, currentUser.name)) — : STATE.jobs; — const myDisbursements = isDriver — ? STATE.disbursements.filter(d => — d.driverUser === currentUser.username || — (d.driver && d.driver.toLowerCase().includes(currentUser.name.toLowerCase())) — ) — : STATE.disbursements; — const myReceipts = isDriver — ? STATE.receipts.filter(r => { — const nm = r.driver && nameIncludes(r.driver, currentUser.name); — const um = r.driverUser && r.driverUser === currentUser.username; — return nm || um; — }) — : STATE.receipts; — Totals — const totalDisbursed = myDisbursements.reduce((s,d) => s + (d.amount||0), 0); — const totalReceipts  = myReceipts.reduce((s,r) => s + (r.amount||0), 0); — const netOwed        = totalReceipts - totalDisbursed; // positive = driver owes, negative = company owes driver — let html = ` — <div class="section-header"> — <div> — <div class="section-title">EXPENSE TRACKER</div> — <div class="section-sub">${isDriver ? 'Your funds & receipts · ' + currentUser.name : 'All driver funds & expense reconciliation'}</div> — </div> — ${isAdmin() ? `<button class="btn-green" onclick="openDisbursementModal()">💰 SEND FUNDS</button>` : ''} — </div> — <!-- SUMMARY STATS --> — <div class="grid-3" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Funds Disbursed</div> — <div class="stat-value">$${totalDisbursed.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Receipts Submitted</div> — <div class="stat-value amber">$${totalReceipts.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">${netOwed > 0 ? 'Driver Owes Back' : netOwed < 0 ? 'Company Owes Driver' : 'Balanced'}</div> — <div class="stat-value ${netOwed > 0 ? 'red' : netOwed < 0 ? 'amber' : ''}"> — ${netOwed === 0 ? '✓ $0.00' : (netOwed > 0 ? '+' : '') + '$' + Math.abs(netOwed).toFixed(2)} — </div> — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div class="card" style="margin-bottom:16px"> — <div class="card-header"><div class="card-title">JOB EXPENSE BREAKDOWN</div></div> — <table> — <thead><tr> — <th>Job ID</th><th>Client</th><th>Driver</th> — <th style="text-align:right">Funds Sent</th> — <th style="text-align:right">Receipts</th> — <th style="text-align:right">Net</th> — <th>Status</th> — </tr></thead> — <tbody> — ${myJobs.length ? myJobs.map(j => { — const jobDisb = myDisbursements.filter(d => d.jobId === j.id); — const jobRec  = myReceipts.filter(r => r.jobId === j.id); — const sent    = jobDisb.reduce((s,d) => s + (d.amount||0), 0); — const spent   = jobRec.reduce((s,r) => s + (r.amount||0), 0); — const net     = spent - sent; — return `<tr onclick="openJobExpenseDetail('${j.id}')" style="cursor:pointer"> — <td><span class="tag">${j.id}</span></td> — <td>${j.client}</td> — <td class="info-text">${j.driver || '—'}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--green)">$${sent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--amber)">$${spent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);font-weight:700;color:${net > 0 ? 'var(--red)' : net < 0 ? 'var(--amber)' : 'var(--green)'}"> — ${net === 0 ? '✓ $0.00' : (net > 0 ? '+' : '') + '$' + Math.abs(net).toFixed(2)} — </td> — <td><span class="status-badge status-${j.status}">${j.status}</span></td> — </tr>`; — }).join('') : `<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--gray);font-family:var(--font-mono)">No jobs found</td></tr>`} — </tbody> — </table> — </div> — <!-- DISBURSEMENTS LOG --> — <div class="grid-2"> — <div class="card"> — <div class="card-header"> — <div class="card-title">💰 FUNDS DISBURSED</div> — ${isAdmin() ? `<button class="btn-green" style="padding:7px 14px;font-size:11px" onclick="openDisbursementModal()">+ SEND FUNDS</button>` : ''} — </div> — ${myDisbursements.length ? myDisbursements.map(d => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${d.driver} <span class="tag">${d.jobId}</span></div> — <div class="info-text">${d.date} · ${d.method || 'Cash'} · ${d.notes || ''}</div> — <div class="info-text" style="color:var(--gray)">${d.sentBy ? 'Sent by: ' + d.sentBy : ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--green)">$${(d.amount||0).toFixed(2)}</div> — ${role === 'office' ? `<button class="btn-sm" style="margin-top:4px;color:var(--red);border-color:#552222;font-size:10px" onclick="event.stopPropagation();deleteDisbursementEntry('${d.id}')">DELETE</button>` : ''} — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No funds disbursed yet.</p>`} — ${myDisbursements.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Disbursed</span> — <span style="font-family:var(--font-mono);color:var(--green);font-size:14px">$${totalDisbursed.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="card"> — <div class="card-header"><div class="card-title">🧾 RECEIPTS SUBMITTED</div></div> — ${myReceipts.length ? myReceipts.map(r => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${r.type} <span class="tag">${r.jobId}</span></div> — <div class="info-text">${r.driver} · ${r.date} · ${r.notes || ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--amber)">$${(r.amount||0).toFixed(2)}</div> — <div style="margin-top:2px">${r.approved — ? '<span style="color:var(--green);font-size:10px;font-family:var(--font-mono)">✓ APPROVED</span>' — : '<span style="color:var(--amber);font-size:10px;font-family:var(--font-mono)">⏳ PENDING</span>'}</div> — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No receipts submitted yet.</p>`} — ${myReceipts.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Receipts</span> — <span style="font-family:var(--font-mono);color:var(--amber);font-size:14px">$${totalReceipts.toFixed(2)}</span> — </div>` : ''} — </div> — </div>`; — content.innerHTML = html; — } — function openJobExpenseDetail(jobId) { — Switch to jobs tab and open the job detail — closeModal('modal-calday'); — openJobDetail(jobId); — } — function openDisbursementModal(prefillJobId) { — Pre-fill job ID if provided — document.getElementById('disb-jobid').value = prefillJobId || ''; — document.getElementById('disb-driver').value = ''; — document.getElementById('disb-amount').value = ''; — document.getElementById('disb-method').value = getSetting('defaultPay', 'Cash'); — document.getElementById('disb-notes').value = ''; — Populate driver dropdown from users — populateDisbDriverDropdown(); — openModal('modal-disbursement'); — } — async function populateDisbDriverDropdown() { — const sel = document.getElementById('disb-driver'); — sel.innerHTML = '<option value="">-- Select Driver --</option>'; — try { — const snap = await db.collection(COL_USERS) — .where('role', 'in', ['driver','helper']) — .get(); — snap.docs.forEach(d => { — const u = d.data(); — if (u.active) { — const opt = document.createElement('option'); — opt.value = JSON.stringify({ name: u.displayName, username: u.username }); — opt.textContent = u.displayName + ' (' + u.role + ')'; — sel.appendChild(opt); — } — }); — } catch(e) { — Fallback — type name manually — sel.innerHTML = '<option value="">Type driver name in notes</option>'; — } — } — async function saveDisbursementEntry() { — const jobId   = document.getElementById('disb-jobid').value.trim().toUpperCase(); — const driverRaw = document.getElementById('disb-driver').value; — const amount  = parseFloat(document.getElementById('disb-amount').value); — const method  = document.getElementById('disb-method').value; — const notes   = document.getElementById('disb-notes').value.trim(); — const btn     = document.getElementById('disb-submit-btn'); — if (!jobId)   { notify('⚠ Job ID is required', true); return; } — if (!driverRaw) { notify('⚠ Select a driver', true); return; } — if (isNaN(amount) || amount <= 0) { notify('⚠ Enter a valid amount', true); return; } — if (btn.disabled) return; — btn.disabled = true; — btn.textContent = 'SAVING...'; — try { — let driverName = driverRaw, driverUser = ''; — try { — const parsed = JSON.parse(driverRaw); — driverName = parsed.name; — driverUser = parsed.username; — } catch(e) { driverName = driverRaw; } — const disb = { — id:         'DISB-' + Date.now(), — jobId, — driver:     driverName, — driverUser: driverUser, — amount, — method, — notes, — sentBy:     currentUser.name, — date:       getTodayStr(), — createdAt:  new Date().toISOString() — }; — await saveDisbursement(disb); — closeModal('modal-disbursement'); — notify('$' + amount.toFixed(2) + ' disbursed to ' + driverName + ' ✓'); — Notify the driver — try { — if (driverUser) { — await sendNotification(driverUser, driverName, 'funds_sent', — 'Funds Sent 💰', — '$' + amount.toFixed(2) + ' sent via ' + method + ' for ' + jobId + (notes ? ' · ' + notes : '') + ' — from ' + currentUser.name, — { jobId, amount } — ); — } — } catch(e) {} — } catch(e) { — notify('⚠ Error: ' + e.message, true); — } finally { — btn.disabled = false; — btn.textContent = 'SEND FUNDS'; — } — } — async function deleteDisbursementEntry(disbId) { — if (!confirm('Delete this disbursement? This cannot be undone.')) return; — try { — await db.collection(COL_DISBURSEMENTS).doc(disbId).delete(); — notify('Disbursement deleted ✓'); — } catch(e) { notify('⚠ ' + e.message, true); } — }, function renderFleet() { — const content = document.getElementById('main-content'); — const role    = currentUser.role; — const canEdit = role === 'office'; — let html = ` — <div class="section-header"> — <div> — <div class="section-title">🚚 FLEET MANAGEMENT</div> — <div class="section-sub">${STATE.fleet.length} vehicle(s) registered · Move Masters OS</div> — </div> — ${canEdit ? `<button class="btn-green" onclick="openFleetModal()">+ ADD VEHICLE</button>` : ''} — </div>`; — if (!STATE.fleet.length) { — html += `<div class="card" style="text-align:center;padding:40px"> — <div style="font-size:48px;margin-bottom:12px">🚚</div> — <p class="info-text">No vehicles registered yet.</p> — ${canEdit ? `<button class="btn-green" style="margin-top:16px" onclick="openFleetModal()">+ ADD FIRST VEHICLE</button>` : ''} — </div>`; — } else { — Summary stats — const active  = STATE.fleet.filter(v => v.status === 'active').length; — const maint   = STATE.fleet.filter(v => v.status === 'maintenance').length; — const offline = STATE.fleet.filter(v => v.status === 'offline').length; — html += `<div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Active</div><div class="stat-value">${active}</div></div> — <div class="stat-box"><div class="stat-label">In Maintenance</div><div class="stat-value amber">${maint}</div></div> — <div class="stat-box"><div class="stat-label">Offline</div><div class="stat-value red">${offline}</div></div> — </div>`; — STATE.fleet.forEach(v => { — const assignedJob = STATE.jobs.find(j => j.vehicleId === v.id && j.status === 'active'); — const statusColor = v.status === 'active' ? 'var(--green)' : v.status === 'maintenance' ? 'var(--amber)' : 'var(--red)'; — const maintDue    = v.nextMaint ? new Date(v.nextMaint) : null; — const maintOverdue = maintDue && maintDue < new Date(); — html += ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:12px"> — <div> — <div style="display:flex;align-items:center;gap:10px;margin-bottom:6px"> — <div style="font-size:28px">${v.type === 'box_truck' ? '🚛' : v.type === 'cargo_van' ? '🚐' : '🚚'}</div> — <div> — <div style="font-family:var(--font-mono);font-size:14px;font-weight:700;color:var(--white)">${v.name || v.id}</div> — <div class="info-text">${v.make||''} ${v.model||''} ${v.year||''} · ${v.plate||'No plate'}</div> — </div> — </div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:11px;color:${statusColor};letter-spacing:2px">${(v.status||'active').toUpperCase()}</div> — ${canEdit ? `<button class="btn-sm" style="margin-top:6px" onclick="openFleetModal('${v.id}')">✎ EDIT</button>` : ''} — </div> — </div> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(110px,1fr));gap:8px;margin-bottom:12px"> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Capacity</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.capacity || '—'}</div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Mileage</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.mileage ? v.mileage.toLocaleString() + ' mi' : '—'}</div> — </div> — <div class="stat-box" style="padding:8px;${maintOverdue ? 'border-color:var(--red)' : ''}"> — <div class="stat-label" style="font-size:9px">Next Service</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${maintOverdue ? 'var(--red)' : 'var(--white)'}"> — ${v.nextMaint || 'Not set'}${maintOverdue ? ' ⚠' : ''} — </div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Assigned Job</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${assignedJob ? 'var(--green)' : 'var(--gray)'}"> — ${assignedJob ? assignedJob.id : 'Available'} — </div> — </div> — </div> — ${v.notes ? `<div class="info-text" style="margin-bottom:10px">📝 ${v.notes}</div>` : ''} — <!-- MAINTENANCE LOG --> — ${v.maintLog && v.maintLog.length ? ` — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:6px">MAINTENANCE LOG</div> — ${v.maintLog.slice(-3).reverse().map(m => ` — <div style="display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px solid var(--gray2);font-size:12px"> — <div><span class="info-text">${m.date}</span> · ${m.work}</div> — <div style="font-family:var(--font-mono);color:var(--amber)">$${(m.cost||0).toFixed(0)}</div> — </div>`).join('')}` : ''} — ${canEdit ? ` — <div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap"> — <button class="btn-sm" onclick="logMaintenance('${v.id}')">🔧 LOG SERVICE</button> — <button class="btn-sm" onclick="updateMileage('${v.id}')">📍 UPDATE MILEAGE</button> — <select style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:5px 8px;font-family:var(--font-mono);font-size:10px;border-radius:3px" — onchange="updateVehicleStatus('${v.id}',this.value)"> — <option value="active"      ${v.status==='active'      ?'selected':''}>● Active</option> — <option value="maintenance" ${v.status==='maintenance' ?'selected':''}>🔧 Maintenance</option> — <option value="offline"     ${v.status==='offline'     ?'selected':''}>○ Offline</option> — </select> — </div>` : ''} — </div>`; — }); — } — content.innerHTML = html; — } — function openFleetModal(vehicleId) { — const v = vehicleId ? STATE.fleet.find(x => x.id === vehicleId) : null; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">${v ? 'EDIT VEHICLE — ' + v.id : 'ADD VEHICLE'}</div> — <div class="grid-2"> — <div class="form-group"><label>Vehicle Name / Unit #</label> — <input type="text" id="flt-name" value="${(v&&v.name)||''}" placeholder="e.g. Truck 1 · Unit 24" /></div> — <div class="form-group"><label>Type</label> — <select id="flt-type"> — <option value="box_truck" ${v?.type==='box_truck'?'selected':''}>🚛 Box Truck</option> — <option value="cargo_van" ${v?.type==='cargo_van'?'selected':''}>🚐 Cargo Van</option> — <option value="flatbed"   ${v?.type==='flatbed'  ?'selected':''}>🚚 Flatbed</option> — <option value="other"     ${v?.type==='other'    ?'selected':''}>📋 Other</option> — </select> — </div> — </div> — <div class="grid-3"> — <div class="form-group"><label>Make</label><input type="text" id="flt-make" value="${(v&&v.make)||''}" placeholder="Ford" /></div> — <div class="form-group"><label>Model</label><input type="text" id="flt-model" value="${(v&&v.model)||''}" placeholder="E-450" /></div> — <div class="form-group"><label>Year</label><input type="number" id="flt-year" value="${(v&&v.year)||''}" placeholder="2019" /></div> — </div> — <div class="grid-2"> — <div class="form-group"><label>License Plate</label><input type="text" id="flt-plate" value="${(v&&v.plate)||''}" placeholder="ABC-1234" style="text-transform:uppercase"/></div> — <div class="form-group"><label>Capacity (text label)</label><input type="text" id="flt-cap" value="${(v&&v.capacity)||''}" placeholder="26 ft · 1500 lbs" /></div> — </div> — <div class="grid-3"> — <div class="form-group"> — <label>Max Cubic Feet <span class="info-text">(for load optimization)</span></label> — <input type="number" id="flt-maxcuft" value="${(v&&v.maxCuFt)||''}" placeholder="e.g. 1200" min="0" step="50" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Fuel Efficiency (MPG) <span class="info-text">(for cost calc)</span></label> — <input type="number" id="flt-mpg" value="${(v&&v.mpg)||''}" placeholder="e.g. 10" min="1" step="0.5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Load Efficiency % <span class="info-text">(usable space)</span></label> — <input type="number" id="flt-loadeff" value="${(v&&v.loadEff)||85}" placeholder="85" min="50" max="100" step="5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — </div> — <div class="grid-2"> — <div class="form-group"><label>Current Mileage</label><input type="number" id="flt-miles" value="${(v&&v.mileage)||''}" placeholder="85000" /></div> — <div class="form-group"><label>Next Service Due</label><input type="date" id="flt-maint" value="${(v&&v.nextMaint)||''}" /></div> — </div> — <div class="form-group"><label>Notes</label> — <input type="text" id="flt-notes" value="${(v&&v.notes)||''}" placeholder="Insurance, registration, special equipment..." /></div> — <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:8px"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" onclick="saveVehicle('${vehicleId||''}')">SAVE VEHICLE</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveVehicle(vehicleId) { — const vehicle = { — id:        vehicleId || 'VEH-' + Date.now(), — name:      document.getElementById('flt-name').value.trim(), — type:      document.getElementById('flt-type').value, — make:      document.getElementById('flt-make').value.trim(), — model:     document.getElementById('flt-model').value.trim(), — year:      document.getElementById('flt-year').value, — plate:     document.getElementById('flt-plate').value.trim().toUpperCase(), — capacity:  document.getElementById('flt-cap').value.trim(), — maxCuFt:   parseFloat(document.getElementById('flt-maxcuft')?.value)||0, — mpg:       parseFloat(document.getElementById('flt-mpg')?.value)||0, — loadEff:   parseFloat(document.getElementById('flt-loadeff')?.value)||85, — mileage:   parseInt(document.getElementById('flt-miles').value)||0, — nextMaint: document.getElementById('flt-maint').value, — notes:     document.getElementById('flt-notes').value.trim(), — status:    vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.status||'active') : 'active', — maintLog:  vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.maintLog||[]) : [], — updatedAt: new Date().toISOString() — }; — if (!vehicle.name) { notify('⚠ Vehicle name is required', true); return; } — try { — await db.collection(COL_FLEET).doc(vehicle.id).set(vehicle); — notify((vehicleId ? 'Vehicle updated' : 'Vehicle added') + ' ✓'); — closeModal('modal-jobdetail'); — } catch(e) { notify('⚠ ' + e.message, true); } — } — async function logMaintenance(vehicleId) { — const work = prompt('Service performed:'); if (!work) return; — const cost = parseFloat(prompt('Cost ($):') || '0'); — const v    = STATE.fleet.find(x => x.id === vehicleId); — if (!v) return; — if (!v.maintLog) v.maintLog = []; — v.maintLog.push({ date: getTodayStr(), work, cost, loggedBy: currentUser.name }); — v.updatedAt = new Date().toISOString(); — await db.collection(COL_FLEET).doc(vehicleId).set(v); — notify('Maintenance logged ✓'); — } — async function updateMileage(vehicleId) { — const miles = prompt('Enter current mileage:'); if (!miles) return; — await db.collection(COL_FLEET).doc(vehicleId).update({ mileage: parseInt(miles)||0 }); — notify('Mileage updated ✓'); — } — async function updateVehicleStatus(vehicleId, status) { — await db.collection(COL_FLEET).doc(vehicleId).update({ status }); — notify('Vehicle status: ' + status.toUpperCase()); — }, function renderMargins() { — const content = document.getElementById('main-content'); — Build margin data per job — const jobMargins = STATE.jobs — .filter(j => j.amount > 0 || j.payroll) — .map(j => { — const revenue   = j.amount || 0; — const driverAccBonus = parseFloat(j.payroll?.accessorialBonus) || 0; — const crewCost  = (j.payroll?.driver || 0) + — driverAccBonus + — (j.payroll?.helpers || []).reduce((s,h) => s + calcPay(h), 0) + — (j.payroll?.driverCommission || 0); — Company revenue excludes driver accessorial bonuses (passed through to driver) — const expenses  = STATE.receipts — .filter(r => r.jobId === j.id && r.approved !== false) — .reduce((s,r) => s + (r.amount||0), 0); — const net       = revenue - crewCost - expenses; — const margin    = revenue > 0 ? (net / revenue * 100) : 0; — return { job: j, revenue, crewCost, expenses, net, margin }; — }) — .sort((a,b) => { — Default: worst margin first (ascending) — const mA = a.revenue > 0 ? a.profit/a.revenue : 0; — const mB = b.revenue > 0 ? b.profit/b.revenue : 0; — return mA - mB; — }); — Sort control — if (window._marginSort === 'date') jobMargins.sort((a,b) => b.job.date > a.job.date ? 1 : -1); — else if (window._marginSort === 'revenue') jobMargins.sort((a,b) => b.revenue - a.revenue); — ── Margin intelligence ── — const overpayJobs   = jobMargins.filter(m => m.revenue > 0 && (m.crewCost/m.revenue) > 0.40); — const lowProfitJobs = jobMargins.filter(m => m.revenue > 0 && (m.profit/m.revenue) < 0.20); — const bestJob       = [...jobMargins].sort((a,b) => (b.profit/b.revenue||0)-(a.profit/a.revenue||0))[0]; — const worstJob      = jobMargins[0]; // already sorted worst first — Overall stats — const totalRevenue  = jobMargins.reduce((s,m) => s + m.revenue, 0); — const totalCrew     = jobMargins.reduce((s,m) => s + m.crewCost, 0); — const totalExpenses = jobMargins.reduce((s,m) => s + m.expenses, 0); — const totalNet      = jobMargins.reduce((s,m) => s + m.net, 0); — const avgMargin     = totalRevenue > 0 ? (totalNet / totalRevenue * 100) : 0; — const profitable    = jobMargins.filter(m => m.net > 0).length; — const losing        = jobMargins.filter(m => m.net < 0).length; — Best and worst job — const best  = jobMargins.reduce((a,b) => b.margin > a.margin ? b : a, jobMargins[0]); — const worst = jobMargins.reduce((a,b) => b.margin < a.margin ? b : a, jobMargins[0]); — const marginColor = (n) => n >= 40 ? 'var(--green)' : n >= 20 ? 'var(--amber)' : n >= 0 ? '#ff8c00' : 'var(--red)'; — const marginBg    = (n) => n >= 40 ? 'rgba(0,255,65,0.08)' : n >= 20 ? 'rgba(255,170,0,0.08)' : n >= 0 ? 'rgba(255,140,0,0.08)' : 'rgba(255,51,51,0.08)'; — let html = ` — ${overpayJobs.length ? `<div style="background:rgba(255,30,30,0.06);border:1px solid rgba(255,30,30,0.3);border-radius:6px;padding:10px 14px;margin-bottom:12px;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px"> — <div> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;font-weight:700">⚠ LABOR OVERPAY ALERT</div> — <div style="font-size:12px;color:var(--white);margin-top:3px">${overpayJobs.length} job${overpayJobs.length>1?'s':''} with crew cost >40% of revenue</div> — <div style="font-size:11px;color:var(--gray);margin-top:2px">${overpayJobs.slice(0,3).map(m=>m.job.client+' ('+Math.round(m.crewCost/m.revenue*100)+'%)').join(' · ')}</div> — </div> — </div>` : ''} — <div style="display:flex;gap:8px;margin-bottom:12px;flex-wrap:wrap"> — <span style="font-family:var(--font-mono);font-size:10px;color:var(--gray);align-self:center">Sort:</span> — ${['worst:⬇ Worst Margin','revenue:$ Revenue','date:📅 Date'].map(o=>{const[v,l]=o.split(':');return `<button onclick="window._marginSort='${v}';renderMargins()" style="padding:5px 12px;font-family:var(--font-mono);font-size:10px;border-radius:3px;cursor:pointer;background:${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--green)':'transparent'};color:${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--navy)':'var(--gray)'};border:1px solid ${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--green)':'var(--gray2)'}">${l}</button>`;}).join('')} — </div> — <div class="section-header"> — <div> — <div class="section-title">📈 JOB MARGIN DASHBOARD</div> — <div class="section-sub">Revenue · Crew Cost · Expenses · Net per Job</div> — </div> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportJobsCSV()">⬇ EXPORT</button> — </div> — <!-- OVERALL STATS --> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(130px,1fr));gap:8px;margin-bottom:16px"> — <div class="stat-box"> — <div class="stat-label">Total Revenue</div> — <div class="stat-value">$${totalRevenue.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${jobMargins.length} jobs</div> — </div> — <div class="stat-box"> — <div class="stat-label">Crew Cost</div> — <div class="stat-value red">$${totalCrew.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${totalRevenue>0?(totalCrew/totalRevenue*100).toFixed(1):'0'}% of revenue</div> — </div> — <div class="stat-box"> — <div class="stat-label">Expenses</div> — <div class="stat-value red">$${totalExpenses.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${totalRevenue>0?(totalExpenses/totalRevenue*100).toFixed(1):'0'}% of revenue</div> — </div> — <div class="stat-box" style="border-color:${marginColor(avgMargin)}"> — <div class="stat-label">Net Profit</div> — <div class="stat-value" style="color:${marginColor(avgMargin)}">$${totalNet.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px;color:${marginColor(avgMargin)}">${avgMargin.toFixed(1)}% avg margin</div> — </div> — <div class="stat-box"> — <div class="stat-label">Profitable Jobs</div> — <div class="stat-value" style="color:var(--green)">${profitable}</div> — <div class="info-text" style="margin-top:4px;color:var(--red)">${losing} losing</div> — </div> — </div> — <!-- MARGIN BREAKDOWN BAR --> — ${totalRevenue > 0 ? ` — <div class="card" style="margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;margin-bottom:10px">REVENUE BREAKDOWN</div> — <div style="height:32px;border-radius:4px;overflow:hidden;display:flex;margin-bottom:8px"> — <div style="width:${(totalCrew/totalRevenue*100).toFixed(1)}%;background:rgba(255,51,51,0.7);transition:width 0.5s" title="Crew Cost"></div> — <div style="width:${(totalExpenses/totalRevenue*100).toFixed(1)}%;background:rgba(255,140,0,0.7);transition:width 0.5s" title="Expenses"></div> — <div style="flex:1;background:rgba(0,255,65,0.4);transition:width 0.5s" title="Net Profit"></div> — </div> — <div style="display:flex;gap:16px;flex-wrap:wrap"> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(255,51,51,0.7);border-radius:2px"></div><span class="info-text">Crew ${(totalCrew/totalRevenue*100).toFixed(1)}%</span></div> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(255,140,0,0.7);border-radius:2px"></div><span class="info-text">Expenses ${(totalExpenses/totalRevenue*100).toFixed(1)}%</span></div> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(0,255,65,0.4);border-radius:2px"></div><span class="info-text">Net ${avgMargin.toFixed(1)}%</span></div> — </div> — </div>` : ''} — <!-- BEST / WORST --> — ${jobMargins.length >= 2 ? ` — <div class="grid-2" style="margin-bottom:16px"> — ${best ? `<div class="card" style="border-color:var(--green3)"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--green);letter-spacing:2px;margin-bottom:6px">🏆 BEST MARGIN JOB</div> — <div style="font-size:15px;font-weight:700">${best.job.client} <span class="tag">${best.job.id}</span></div> — <div class="info-text">${best.job.date}</div> — <div style="font-family:var(--font-mono);font-size:22px;color:var(--green);margin-top:6px">${best.margin.toFixed(1)}%</div> — <div class="info-text">$${best.net.toFixed(2)} net on $${best.revenue.toLocaleString()}</div> — </div>` : ''} — ${worst ? `<div class="card" style="border-color:${marginColor(worst.margin)}"> — <div style="font-family:var(--font-mono);font-size:9px;color:${marginColor(worst.margin)};letter-spacing:2px;margin-bottom:6px">${worst.net < 0 ? '⚠ LOSING JOB' : '📉 LOWEST MARGIN'}</div> — <div style="font-size:15px;font-weight:700">${worst.job.client} <span class="tag">${worst.job.id}</span></div> — <div class="info-text">${worst.job.date}</div> — <div style="font-family:var(--font-mono);font-size:22px;color:${marginColor(worst.margin)};margin-top:6px">${worst.margin.toFixed(1)}%</div> — <div class="info-text">$${worst.net.toFixed(2)} net on $${worst.revenue.toLocaleString()}</div> — </div>` : ''} — </div>` : ''} — <!-- PER-JOB TABLE --> — <div class="card" style="padding:0;overflow:hidden"> — <div style="padding:12px 16px;border-bottom:1px solid var(--gray2)"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--green);letter-spacing:2px">JOB BY JOB BREAKDOWN</div> — </div> — ${jobMargins.length ? ` — <div style="overflow-x:auto"> — <table style="min-width:700px"> — <thead><tr> — <th>Job</th> — <th>Client</th> — <th>Date</th> — <th style="text-align:right">Revenue</th> — <th style="text-align:right">Crew Cost</th> — <th style="text-align:right">Expenses</th> — <th style="text-align:right">Net</th> — <th style="text-align:center">Margin</th> — <th>Status</th> — </tr></thead> — <tbody> — ${jobMargins.map(m => ` — <tr style="cursor:pointer;background:${marginBg(m.margin)}" onclick="switchTab('jobs');setTimeout(()=>openJobDetail('${m.job.id}'),300)"> — <td><span class="tag">${m.job.id}</span></td> — <td style="font-weight:600">${m.job.client}</td> — <td class="info-text">${m.job.date}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${m.revenue.toLocaleString()}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${m.crewCost.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:#ff8c00">$${m.expenses.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;font-weight:700;color:${marginColor(m.margin)}"> — ${m.net >= 0 ? '+' : ''}$${m.net.toFixed(2)} — </td> — <td style="text-align:center"> — <div style="display:inline-flex;align-items:center;justify-content:center;width:52px;height:24px;border-radius:12px;background:${marginBg(m.margin)};border:1px solid ${marginColor(m.margin)};font-family:var(--font-mono);font-size:10px;font-weight:700;color:${marginColor(m.margin)}"> — ${m.margin.toFixed(0)}% — </div> — </td> — <td><span class="status-badge status-${m.job.status}">${m.job.status}</span></td> — </tr>`).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="3" style="font-family:var(--font-mono);color:var(--green);font-size:11px">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${totalRevenue.toLocaleString()}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${totalCrew.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:#ff8c00">$${totalExpenses.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;font-weight:700;color:${marginColor(avgMargin)}"> — ${totalNet >= 0 ? '+' : ''}$${totalNet.toFixed(2)} — </td> — <td style="text-align:center"> — <div style="display:inline-flex;align-items:center;justify-content:center;width:52px;height:24px;border-radius:12px;background:${marginBg(avgMargin)};border:1px solid ${marginColor(avgMargin)};font-family:var(--font-mono);font-size:10px;font-weight:700;color:${marginColor(avgMargin)}"> — ${avgMargin.toFixed(0)}% — </div> — </td> — <td></td> — </tr> — </tbody> — </table> — </div>` : ` — <div style="text-align:center;padding:48px"> — <div style="font-size:40px;margin-bottom:12px">📈</div> — <p class="info-text">No job data yet. Create jobs with quoted amounts and crew logs to see margins.</p> — </div>`} — </div> — <!-- MARGIN LEGEND --> — <div style="display:flex;gap:16px;padding:12px 4px;flex-wrap:wrap;align-items:center"> — <span class="info-text">Margin health:</span> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(0,255,65,0.2);border:1px solid var(--green);border-radius:2px"></div><span class="info-text" style="color:var(--green)">40%+ Excellent</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,170,0,0.2);border:1px solid var(--amber);border-radius:2px"></div><span class="info-text" style="color:var(--amber)">20–39% Good</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,140,0,0.2);border:1px solid #ff8c00;border-radius:2px"></div><span class="info-text" style="color:#ff8c00">0–19% Thin</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,51,51,0.15);border:1px solid var(--red);border-radius:2px"></div><span class="info-text" style="color:var(--red)">Negative — Losing money</span></div> — </div>`; — content.innerHTML = html; — }, function renderMyPay() { — const content  = document.getElementById('main-content'); — const role     = currentUser.role; — const isHelper = role === 'helper'; — Collect all pay entries for this person — const myJobs = STATE.jobs.filter(j => { — if (role === 'driver') return nameIncludes(j.driver, currentUser.name); — if (role === 'helper') return (j.payroll?.helpers||[]).some(h => nameIncludes(h.name, currentUser.name)); — return false; — }); — let totalGross      = 0; — let totalCommission = 0; — let totalTax        = 0; — let totalTakeHome   = 0; — const payRows       = []; — myJobs.forEach(j => { — let gross = 0, commission = 0, workerType = '1099', payDetails = ''; — if (role === 'driver') { — gross       = j.payroll?.driver || 0; — commission  = j.payroll?.driverCommission || 0; — workerType  = j.payroll?.driverWorkerType || '1099'; — payDetails  = payLabel({ payType: j.payroll?.driverPayType||'flat', rate: j.payroll?.driverRate||gross, hours: j.payroll?.driverHours||0, days: j.payroll?.driverDays||1 }); — } else { — const helper = (j.payroll?.helpers||[]).find(h => — h.name?.toLowerCase().includes(currentUser.name.toLowerCase()) || — (currentUser.name.toLowerCase().split(' ')[0] && h.name?.toLowerCase().includes(currentUser.name.toLowerCase().split(' ')[0])) — ); — if (helper) { — gross      = calcPay(helper); — commission = helper.commission || 0; — workerType = helper.workerType || '1099'; — payDetails = payLabel(helper); — } — } — if (gross === 0) return; — const tax = calcTax(gross, workerType); — totalGross      += gross; — totalCommission += commission; — totalTax        += tax.totalEstTax; — totalTakeHome   += tax.takeHome; — payRows.push({ job: j, gross, commission, workerType, payDetails, tax }); — }); — let html = ` — <div class="section-header"> — <div> — <div class="section-title">📊 MY PAY SUMMARY</div> — <div class="section-sub">${currentUser.name} · ${role.toUpperCase()} · ${payRows.length} job(s)</div> — </div> — </div> — <!-- YTD SUMMARY STATS --> — <div class="grid-4" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Gross Earned</div> — <div class="stat-value">$${totalGross.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Commission</div> — <div class="stat-value" style="color:var(--amber)">$${totalCommission.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Est. Tax Owed</div> — <div class="stat-value red">$${totalTax.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Est. Take-Home</div> — <div class="stat-value">$${totalTakeHome.toFixed(2)}</div> — </div> — </div> — <!-- TAX ADVISORY BANNER --> — <div class="card" style="border-color:#664400;background:rgba(255,170,0,0.05);margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:11px;color:var(--amber);letter-spacing:2px;margin-bottom:8px">⚠ TAX ADVISORY</div> — <div class="info-text" style="line-height:1.8"> — ${totalGross > 0 ? `Based on your earnings of <strong style="color:var(--white)">$${totalGross.toFixed(2)}</strong>, you should set aside approximately <strong style="color:var(--red)">$${totalTax.toFixed(2)}</strong> (${(totalTax/totalGross*100).toFixed(1)}%) for taxes.` : 'No earnings recorded yet.'} — ${totalGross >= 600 ? ' You will likely receive a 1099 form if classified as a contractor.' : ''} — If you are a <strong style="color:var(--white)">1099 contractor</strong>, you are responsible for paying your own taxes quarterly (April, June, September, January). Consider setting aside <strong style="color:var(--red)">${totalGross > 0 ? (totalTax/totalGross*100).toFixed(0) : '~25'}%</strong> of every paycheck. — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div style="margin-top:4px"> — ${payRows.length ? payRows.map(row => ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:10px"> — <div> — <div style="display:flex;gap:8px;align-items:center;margin-bottom:4px"> — <span class="tag">${row.job.id}</span> — <span class="status-badge status-${row.job.status}">${row.job.status}</span> — <span class="tag">${row.workerType}</span> — </div> — <div style="font-size:14px;font-weight:600">${row.job.client}</div> — <div class="info-text">${row.job.date} · ${row.payDetails}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:20px;color:var(--green)">$${row.gross.toFixed(2)}</div> — ${row.commission > 0 ? `<div style="font-family:var(--font-mono);font-size:12px;color:var(--amber)">+ $${row.commission.toFixed(2)} commission</div>` : ''} — </div> — </div> — <!-- Tax breakdown per job --> — <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;margin-bottom:8px"> — <div class="stat-box" style="padding:10px"> — <div class="stat-label" style="font-size:9px">Gross Pay</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--white)">$${row.gross.toFixed(2)}</div> — </div> — <div class="stat-box" style="padding:10px"> — <div class="stat-label" style="font-size:9px">Est. Tax</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--red)">−$${row.tax.totalEstTax.toFixed(2)}</div> — </div> — <div class="stat-box" style="padding:10px;border-color:var(--green3)"> — <div class="stat-label" style="font-size:9px">Take-Home</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--green)">$${row.tax.takeHome.toFixed(2)}</div> — </div> — </div> — <!-- Detailed tax line items --> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;font-family:var(--font-mono);font-size:10px"> — ${row.tax.selfEmployTax ? `<div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>Self-Employment Tax (15.3%)</span><span style="color:var(--red)">−$${row.tax.selfEmployTax.toFixed(2)}</span></div>` : ''} — ${row.tax.ficaTax ? `<div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>FICA Employee (7.65%)</span><span style="color:var(--red)">−$${row.tax.ficaTax.toFixed(2)}</span></div>` : ''} — <div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>Federal Income (~${(row.tax.federalRate*100).toFixed(0)}%)</span><span style="color:var(--red)">−$${row.tax.federalTax.toFixed(2)}</span></div> — <div style="display:flex;justify-content:space-between;color:var(--gray)"><span>Georgia State (5.5%)</span><span style="color:var(--red)">−$${row.tax.stateTax.toFixed(2)}</span></div> — </div> — </div>`).join('') : ` — <div class="card" style="text-align:center;padding:40px"> — <p class="info-text">No pay records found for ${currentUser.name}.</p> — <p class="info-text" style="margin-top:8px">Your earnings will appear here once jobs are assigned and crew logs are saved.</p> — </div>`} — </div> — <div style="padding:12px;font-family:var(--font-mono);font-size:9px;color:var(--gray);line-height:1.8;border-top:1px solid var(--gray2);margin-top:8px"> — ⚠ DISCLAIMER: Tax estimates are for informational purposes only based on 2024 US federal rates and Georgia state rates. This is not tax advice. Individual circumstances vary. Consult a licensed tax professional or CPA for filing guidance. JPG Systems / JPG Ventures LLC is not responsible for tax obligations of independent contractors. — </div>`; — content.innerHTML = html; — }, function renderPayrollSummary() { — This is the office-facing payroll output — all crew, all jobs — const content = document.getElementById('main-content'); — const role    = currentUser.role; — if (role !== 'office') { switchTab('mypay'); return; } — Aggregate per person — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — Driver — const dKey = j.driver; — if (!payMap[dKey]) payMap[dKey] = { name: dKey, role: 'Driver', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[dKey].jobs++; — payMap[dKey].gross      += j.payroll.driver || 0; — payMap[dKey].commission += j.payroll.driverCommission || 0; — payMap[dKey].workerType  = j.payroll.driverWorkerType || '1099'; — Helpers — (j.payroll.helpers||[]).forEach(h => { — if (!h.name) return; — const hKey = h.name; — if (!payMap[hKey]) payMap[hKey] = { name: hKey, role: 'Helper', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[hKey].jobs++; — payMap[hKey].gross      += calcPay(h); — payMap[hKey].commission += h.commission || 0; — payMap[hKey].workerType  = h.workerType || '1099'; — }); — }); — const people    = Object.values(payMap).sort((a,b) => b.gross - a.gross); — const totalGross = people.reduce((s,p) => s + p.gross + p.commission, 0); — let html = ` — <div class="section-header"> — <div> — <div class="section-title">💵 PAYROLL SUMMARY</div> — <div class="section-sub">All crew · All jobs · JPG Systems</div> — </div> — <div style="display:flex;gap:8px"> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportPayrollCSV()">⬇ EXPORT CSV</button> — </div> — </div> — <div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Total Crew</div><div class="stat-value">${people.length}</div></div> — <div class="stat-box"><div class="stat-label">Total Payroll</div><div class="stat-value amber">$${totalGross.toFixed(2)}</div></div> — <div class="stat-box"><div class="stat-label">Jobs w/ Payroll</div><div class="stat-value">${STATE.jobs.filter(j=>j.payroll).length}</div></div> — </div> — <div class="card"> — <table> — <thead><tr> — <th>Name</th><th>Role</th><th>Type</th><th>Jobs</th> — <th style="text-align:right">Base Pay</th> — <th style="text-align:right">Commission</th> — <th style="text-align:right">Total Gross</th> — <th style="text-align:right">Est. Tax</th> — <th style="text-align:right">Take-Home</th> — </tr></thead> — <tbody> — ${people.map(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — return `<tr> — <td style="font-weight:600">${p.name}</td> — <td><span class="tag">${p.role}</span></td> — <td><span class="tag">${p.workerType}</span></td> — <td style="font-family:var(--font-mono);text-align:center">${p.jobs}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${p.gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">${p.commission>0?'$'+p.commission.toFixed(2):'—'}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green);font-weight:700">$${gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${tax.totalEstTax.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${tax.takeHome.toFixed(2)}</td> — </tr>`; — }).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="4" style="font-family:var(--font-mono);color:var(--green)">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${people.reduce((s,p)=>s+p.gross,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">$${people.reduce((s,p)=>s+p.commission,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${totalGross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).totalEstTax,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).takeHome,0).toFixed(2)}</td> — </tr> — </tbody> — </table> — </div>`; — content.innerHTML = html; — }

## Global bindings

`var`/`function` declarations become `window` properties in a plain (non-module) `<script>`
tag and stay reachable if later split across multiple `<script src>` files loaded in order.
**`let`/`const` do NOT** — they are scoped to the single `<script>` tag that declares them and
become invisible to any other file the moment this script is split, unless explicitly attached
to a shared object (e.g. `window.X`) or kept together. This applies to `NAV_GROUPS`, `activeTab`,
`STATE`, `currentUser`, and every other `let`/`const` binding below — each is a concrete blocker
for whichever Phase 3 split first tries to separate its declaring section from its readers.

| Name | Kind | Declared line | Declaring section | Refs | Reads | Writes | Read from inline handler |
|---|---|---|---|---|---|---|---|
| `firebaseConfig` | const | 1934 | FIREBASE INIT | 2 | 1 | 1 | no |
| `db` | const | 1944 | FIREBASE INIT | 149 | 148 | 1 | no |
| `storage` | const | 1945 | FIREBASE INIT | 3 | 2 | 1 | no |
| `CREATOR_USERNAME` | const | 1962 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 3 | 2 | 1 | no |
| `ROLE_TIER` | const | 1965 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 3 | 2 | 1 | no |
| `myTier` | function | 1967 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 3 | 3 | 0 | no |
| `tierOf` | function | 1968 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 3 | 3 | 0 | no |
| `isCreator` | function | 1969 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 12 | 12 | 0 | no |
| `isAdmin` | function | 1970 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 52 | 52 | 0 | no |
| `isCreatorAccount` | function | 1971 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 17 | 17 | 0 | no |
| `canModifyUser` | function | 1973 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 4 | 4 | 0 | no |
| `canDeleteUser` | function | 1984 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 2 | 2 | 0 | no |
| `COL_JOBS` | const | 1990 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 12 | 11 | 1 | no |
| `COL_INVENTORY` | const | 1991 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 5 | 4 | 1 | no |
| `COL_RECEIPTS` | const | 1992 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 8 | 7 | 1 | no |
| `COL_USERS` | const | 1993 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 36 | 35 | 1 | no |
| `COL_DISBURSEMENTS` | const | 1994 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 9 | 8 | 1 | no |
| `COL_NOTIFICATIONS` | const | 1995 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 6 | 5 | 1 | no |
| `COL_FLEET` | const | 1996 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 6 | 5 | 1 | no |
| `COL_LOAD_SHEETS` | const | 1997 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 7 | 6 | 1 | no |
| `COL_WAREHOUSE_LOG` | const | 1998 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 4 | 3 | 1 | no |
| `COL_COMPANY` | const | 1999 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 3 | 2 | 1 | no |
| `COL_PAYROLL_LEDGER` | const | 2000 | ROLE HIERARCHY — creator > owner > office > driver/helper/warehouse/client | 6 | 5 | 1 | no |
| `openTravelBlockModal` | function | 2009 | TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created | 2 | 2 | 0 | yes |
| `saveTravelBlock` | function | 2149 | TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created | 1 | 1 | 0 | yes |
| `updateCrewTags` | function | 2199 | TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created | 3 | 3 | 0 | yes |
| `editTravelBlock` | function | 2215 | TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created | 1 | 1 | 0 | yes |
| `updateTravelBlock` | function | 2335 | TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created | 1 | 1 | 0 | yes |
| `calcDriveTime` | function | 2381 | TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created | 4 | 4 | 0 | yes |
| `markTravelArrived` | function | 2410 | TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created | 1 | 1 | 0 | yes |
| `deleteTravelBlock` | function | 2422 | TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created | 2 | 2 | 0 | yes |
| `linkTravelToJob` | function | 2431 | TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created | 0 | 0 | 0 | no |
| `getMyTravelBlocks` | function | 2439 | TRAVEL BLOCKS — Pre-job drive time scheduling — Logs "driving to location" before job details are known — Appears on calendar, crew schedule, and driver dashboard — Can later be linked to a job once it's created | 0 | 0 | 0 | no |
| `SYMBOL_LEGEND` | const | 2454 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 7 | 6 | 1 | no |
| `getLegendBar` | function | 2484 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 1 | 1 | 0 | no |
| `toggleLegendPanel` | function | 2495 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 0 | 0 | 0 | no |
| `routeOnboarding` | function | 2555 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 0 | 0 | 0 | no |
| `setOnboardingStatus` | function | 2577 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 2 | 2 | 0 | no |
| `showSkippedBanner` | function | 2586 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 3 | 3 | 0 | no |
| `_fjStep` | let | 2598 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 10 | 8 | 3 | no |
| `FJ_STEPS` | const | 2599 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 8 | 7 | 1 | no |
| `openFirstJobFlow` | function | 2606 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 4 | 4 | 0 | yes |
| `renderFJStep` | function | 2612 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 2 | 2 | 0 | no |
| `launchFJStep` | function | 2641 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 0 | 0 | 0 | no |
| `advanceFJStep` | function | 2653 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 4 | 4 | 0 | yes |
| `exitFJFlow` | function | 2660 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 4 | 4 | 0 | yes |
| `checkBetaWelcome` | function | 2679 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 1 | 1 | 0 | no |
| `openBetaWelcome` | function | 2694 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 5 | 5 | 0 | yes |
| `dismissBetaWelcome` | function | 2844 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 0 | 0 | 0 | no |
| `submitBetaFeedback` | function | 2857 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 1 | 1 | 0 | yes |
| `openFeedbackModal` | function | 2881 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 1 | 1 | 0 | yes |
| `submitFeedbackFromModal` | function | 2913 | BETA WELCOME & ONBOARDING SCREEN — Shows once to new owner/admin accounts on first login — Gives them a 4-step quickstart, their booking URL, — and a feedback button that logs to Firestore | 1 | 1 | 0 | yes |
| `openWarehouseLogModal` | function | 2939 | WAREHOUSE WORK LOGGING — Log warehouse work by employee — shows on calendar + payroll | 1 | 1 | 0 | yes |
| `addWarehouseWorkerRow` | function | 3032 | WAREHOUSE WORK LOGGING — Log warehouse work by employee — shows on calendar + payroll | 2 | 2 | 0 | yes |
| `calcWarehouseHours` | function | 3063 | WAREHOUSE WORK LOGGING — Log warehouse work by employee — shows on calendar + payroll | 2 | 2 | 0 | yes |
| `saveWarehouseLog` | function | 3083 | WAREHOUSE WORK LOGGING — Log warehouse work by employee — shows on calendar + payroll | 1 | 1 | 0 | yes |
| `renderLoadSheets` | function | 3152 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 1 | 1 | 0 | no |
| `openNewLoadSheet` | function | 3251 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 3 | 3 | 0 | yes |
| `updateTruckCapacity` | function | 3361 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 2 | 2 | 0 | yes |
| `addLoadSheetJobRow` | function | 3380 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 3 | 3 | 0 | yes |
| `autofillJobRow` | function | 3435 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 1 | 1 | 0 | yes |
| `recalcLoadCapacity` | function | 3455 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 5 | 5 | 0 | yes |
| `saveLoadSheet` | function | 3482 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 2 | 2 | 0 | yes |
| `openLoadSheet` | function | 3557 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 2 | 2 | 0 | yes |
| `toggleJobLoaded` | function | 3652 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 0 | 0 | 0 | no |
| `updateLoadSheetStatus` | function | 3664 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 1 | 1 | 0 | yes |
| `deleteLoadSheet` | function | 3671 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 1 | 1 | 0 | yes |
| `editLoadSheet` | function | 3680 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 1 | 1 | 0 | yes |
| `printLoadSheet` | function | 3719 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 2 | 2 | 0 | yes |
| `syncCubicFt` | function | 3802 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 2 | 2 | 0 | yes |
| `calcFuelFromLinehaul` | function | 3818 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 3 | 3 | 0 | yes |
| `toggleFuelMode` | function | 3838 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 2 | 2 | 0 | yes |
| `calcCfLbsRatio` | function | 3871 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 9 | 9 | 0 | yes |
| `updateJobStatusIndicator` | function | 3925 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 2 | 2 | 0 | yes |
| `getJobStatusBadge` | function | 3933 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 0 | 0 | 0 | no |
| `populateStatusDetails` | function | 3970 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 0 | 0 | 0 | no |
| `saveStatusDetails` | function | 3983 | TRUCK LOAD SHEET — Multi-job truck loading manifest — Tracks multiple job numbers loaded into one truck — DOT compliant, weight/cuft totals, sequential order | 1 | 1 | 0 | no |
| `GAS_PRICE_DEFAULT` | const | 4003 | ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard | 7 | 6 | 1 | no |
| `calcJobFuelCost` | function | 4005 | ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard | 1 | 1 | 0 | no |
| `calcLoadScore` | function | 4011 | ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard | 2 | 2 | 0 | no |
| `getOptimizationRecommendations` | function | 4026 | ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard | 1 | 1 | 0 | no |
| `estimateDistance` | function | 4090 | ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard | 1 | 1 | 0 | no |
| `renderRouteOptimizer` | function | 4100 | ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard | 6 | 6 | 0 | yes |
| `applyTruckRecommendation` | function | 4316 | ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard | 1 | 1 | 0 | yes |
| `openFuelPriceEdit` | function | 4327 | ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard | 4 | 4 | 0 | yes |
| `openGasPriceEdit` | function | 4369 | ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard | 0 | 0 | 0 | no |
| `saveFuelPrice` | function | 4371 | ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard | 2 | 2 | 0 | yes |
| `saveGasPrice` | function | 4382 | ROUTE & LOAD OPTIMIZATION — v53 — AI truck-job matching based on cubic footage, fuel efficiency — and fleet capacity. Tells owner: "Swap Truck 2 for Truck 4 — and save $45 in fuel today." — Accessible from Fleet tab and Dashboard | 0 | 0 | 0 | no |
| `calcCrewProductivity` | function | 4391 | LABOR PRODUCTIVITY SCORES — v52 — Uses gate timestamps to calculate efficiency per crew member — Crew A moves 50 cu.ft/hr vs Crew B at 35 cu.ft/hr — Visible to owner/office — used for rewards and training | 1 | 1 | 0 | no |
| `renderProductivityScores` | function | 4464 | LABOR PRODUCTIVITY SCORES — v52 — Uses gate timestamps to calculate efficiency per crew member — Crew A moves 50 cu.ft/hr vs Crew B at 35 cu.ft/hr — Visible to owner/office — used for rewards and training | 1 | 1 | 0 | no |
| `runClaimsDefenseScan` | function | 4614 | CLAIMS DEFENSE AI — v51 — Pre-load damage detection using Claude vision — When driver takes a photo at pre-load gate, AI scans for — pre-existing damage, flags it, embeds in BOL as evidence. — Protects moving company from false damage claims. | 0 | 0 | 0 | no |
| `showClaimsDefenseResult` | function | 4693 | CLAIMS DEFENSE AI — v51 — Pre-load damage detection using Claude vision — When driver takes a photo at pre-load gate, AI scans for — pre-existing damage, flags it, embeds in BOL as evidence. — Protects moving company from false damage claims. | 0 | 0 | 0 | no |
| `renderClaimsDefenseHistory` | function | 4774 | CLAIMS DEFENSE AI — v51 — Pre-load damage detection using Claude vision — When driver takes a photo at pre-load gate, AI scans for — pre-existing damage, flags it, embeds in BOL as evidence. — Protects moving company from false damage claims. | 1 | 1 | 0 | no |
| `COL_LICENSEES` | const | 4810 | SYSTEM HEALTH DASHBOARD — Platform operator view — JPG Ventures sees all licensees, sync status, revenue flow — Accessible only to creator/owner accounts | 1 | 0 | 1 | no |
| `renderSystemHealth` | function | 4812 | SYSTEM HEALTH DASHBOARD — Platform operator view — JPG Ventures sees all licensees, sync status, revenue flow — Accessible only to creator/owner accounts | 5 | 5 | 0 | yes |
| `buildSystemHealthView` | function | 4843 | SYSTEM HEALTH DASHBOARD — Platform operator view — JPG Ventures sees all licensees, sync status, revenue flow — Accessible only to creator/owner accounts | 3 | 3 | 0 | yes |
| `showSecurityRules` | function | 5165 | SYSTEM HEALTH DASHBOARD — Platform operator view — JPG Ventures sees all licensees, sync status, revenue flow — Accessible only to creator/owner accounts | 1 | 1 | 0 | yes |
| `loadFeedbackInbox` | function | 5263 | SYSTEM HEALTH DASHBOARD — Platform operator view — JPG Ventures sees all licensees, sync status, revenue flow — Accessible only to creator/owner accounts | 1 | 1 | 0 | yes |
| `COMPANY_ID` | let | 5302 | FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill | 44 | 42 | 2 | no |
| `setCompanyId` | function | 5304 | FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill | 4 | 4 | 0 | no |
| `withCompanyId` | function | 5308 | FIRESTORE SECURITY RULES — deploy in Firebase Console → Firestore → Rules — rules_version = '2'; — Enforces company isolation at the DATABASE level (not just UI) — See migrateExistingDataToCompanyId() for one-time data backfill | 0 | 0 | 0 | no |
| `STATE` | let | 5318 | STATE & STORAGE | 346 | 344 | 2 | no |
| `DB_LOADED` | let | 5319 | STATE & STORAGE | 10 | 6 | 4 | no |
| `unsubscribers` | let | 5320 | STATE & STORAGE | 4 | 1 | 3 | no |
| `initFirestore` | function | 5323 | STATE & STORAGE | 4 | 4 | 0 | no |
| `showSyncPulse` | function | 5562 | STATE & STORAGE | 1 | 1 | 0 | no |
| `stopListeners` | function | 5570 | STATE & STORAGE | 1 | 1 | 0 | no |
| `saveJob` | function | 5578 | STATE & STORAGE | 17 | 17 | 0 | no |
| `saveInventoryItem` | function | 5590 | STATE & STORAGE | 2 | 2 | 0 | no |
| `saveReceipt` | function | 5602 | STATE & STORAGE | 6 | 6 | 0 | no |
| `uploadReceiptPhoto` | function | 5627 | STATE & STORAGE | 1 | 1 | 0 | no |
| `compressImage` | function | 5659 | STATE & STORAGE | 2 | 2 | 0 | no |
| `saveDisbursement` | function | 5695 | STATE & STORAGE | 1 | 1 | 0 | no |
| `deleteDisbursement` | function | 5707 | STATE & STORAGE | 0 | 0 | 0 | no |
| `saveState` | function | 5716 | STATE & STORAGE | 0 | 0 | 0 | no |
| `seedIfEmpty` | function | 5722 | STATE & STORAGE | 3 | 3 | 0 | no |
| `showLoadingOverlay` | function | 5766 | STATE & STORAGE | 4 | 4 | 0 | no |
| `getDefaultState` | function | 5784 | STATE & STORAGE | 1 | 1 | 0 | no |
| `getTodayStr` | function | 5835 | STATE & STORAGE | 19 | 19 | 0 | no |
| `getDateOffset` | function | 5836 | STATE & STORAGE | 2 | 2 | 0 | no |
| `currentUser` | let | 5838 | STATE & STORAGE | 219 | 213 | 6 | no |
| `currentJobForModal` | let | 5839 | STATE & STORAGE | 2 | 0 | 2 | no |
| `checkFirstRun` | function | 5849 | FIRST RUN CHECK — runs on page load before login | 1 | 1 | 0 | no |
| `createAdminAccount` | function | 5981 | FIRST RUN CHECK — runs on page load before login | 1 | 1 | 0 | yes |
| `deferredInstallPrompt` | let | 6047 | PWA — SPLASH, INSTALL PROMPT, SERVICE WORKER | 6 | 3 | 3 | no |
| `installPWA` | function | 6059 | PWA — SPLASH, INSTALL PROMPT, SERVICE WORKER | 2 | 2 | 0 | yes |
| `dismissInstall` | function | 6068 | PWA — SPLASH, INSTALL PROMPT, SERVICE WORKER | 3 | 3 | 0 | yes |
| `stampMissingCompanyIds` | function | 6080 | MIGRATION — Stamp companyId on any legacy docs missing it — Runs silently on every login — safe to run repeatedly | 1 | 1 | 0 | no |
| `OFFLINE_QUEUE_KEY` | const | 6130 | OFFLINE MODE — Full PWA with offline queue — Caches app shell on first load — Queues Firestore writes when offline — Syncs queue automatically when back online | 3 | 2 | 1 | no |
| `getOfflineQueue` | function | 6132 | OFFLINE MODE — Full PWA with offline queue — Caches app shell on first load — Queues Firestore writes when offline — Syncs queue automatically when back online | 2 | 2 | 0 | no |
| `saveOfflineQueue` | function | 6138 | OFFLINE MODE — Full PWA with offline queue — Caches app shell on first load — Queues Firestore writes when offline — Syncs queue automatically when back online | 2 | 2 | 0 | no |
| `queueOfflineWrite` | function | 6142 | OFFLINE MODE — Full PWA with offline queue — Caches app shell on first load — Queues Firestore writes when offline — Syncs queue automatically when back online | 4 | 4 | 0 | no |
| `syncOfflineQueue` | function | 6153 | OFFLINE MODE — Full PWA with offline queue — Caches app shell on first load — Queues Firestore writes when offline — Syncs queue automatically when back online | 2 | 2 | 0 | no |
| `_wasOffline` | let | 6184 | OFFLINE MODE — Full PWA with offline queue — Caches app shell on first load — Queues Firestore writes when offline — Syncs queue automatically when back online | 4 | 1 | 3 | no |
| `updateOnlineIndicator` | function | 6201 | OFFLINE MODE — Full PWA with offline queue — Caches app shell on first load — Queues Firestore writes when offline — Syncs queue automatically when back online | 2 | 2 | 0 | no |
| `isOnline` | function | 6208 | OFFLINE MODE — Full PWA with offline queue — Caches app shell on first load — Queues Firestore writes when offline — Syncs queue automatically when back online | 2 | 2 | 0 | no |
| `safeFirestoreSet` | function | 6213 | OFFLINE MODE — Full PWA with offline queue — Caches app shell on first load — Queues Firestore writes when offline — Syncs queue automatically when back online | 1 | 1 | 0 | no |
| `safeFirestoreAdd` | function | 6229 | OFFLINE MODE — Full PWA with offline queue — Caches app shell on first load — Queues Firestore writes when offline — Syncs queue automatically when back online | 1 | 1 | 0 | no |
| `enableFirestoreOffline` | function | 6247 | OFFLINE MODE — Full PWA with offline queue — Caches app shell on first load — Queues Firestore writes when offline — Syncs queue automatically when back online | 0 | 0 | 0 | no |
| `SETTINGS_KEY_PREFIX` | const | 6322 | USER SETTINGS — stored in localStorage per user | 2 | 1 | 1 | no |
| `getSettingsKey` | function | 6324 | USER SETTINGS — stored in localStorage per user | 3 | 3 | 0 | no |
| `loadSettings` | function | 6328 | USER SETTINGS — stored in localStorage per user | 5 | 5 | 0 | no |
| `saveSettings` | function | 6335 | USER SETTINGS — stored in localStorage per user | 4 | 4 | 0 | no |
| `getSetting` | function | 6344 | USER SETTINGS — stored in localStorage per user | 17 | 17 | 0 | no |
| `SESSION_KEY` | const | 6353 | SESSION PERSISTENCE — remember me across refreshes/tab closes | 5 | 4 | 1 | no |
| `saveSession` | function | 6355 | SESSION PERSISTENCE — remember me across refreshes/tab closes | 2 | 2 | 0 | no |
| `loadSession` | function | 6364 | SESSION PERSISTENCE — remember me across refreshes/tab closes | 2 | 2 | 0 | no |
| `clearSession` | function | 6379 | SESSION PERSISTENCE — remember me across refreshes/tab closes | 2 | 2 | 0 | no |
| `doLogin` | function | 6391 | SESSION PERSISTENCE — remember me across refreshes/tab closes | 3 | 3 | 0 | yes |
| `showLoginError` | function | 6501 | SESSION PERSISTENCE — remember me across refreshes/tab closes | 7 | 7 | 0 | no |
| `doLogout` | function | 6507 | SESSION PERSISTENCE — remember me across refreshes/tab closes | 2 | 2 | 0 | yes |
| `RECOVERY_CODE` | const | 6525 | SESSION PERSISTENCE — remember me across refreshes/tab closes | 2 | 1 | 1 | no |
| `openPasswordRecovery` | function | 6527 | SESSION PERSISTENCE — remember me across refreshes/tab closes | 1 | 1 | 0 | yes |
| `closePasswordRecovery` | function | 6538 | SESSION PERSISTENCE — remember me across refreshes/tab closes | 2 | 2 | 0 | yes |
| `doPasswordRecovery` | function | 6543 | SESSION PERSISTENCE — remember me across refreshes/tab closes | 5 | 5 | 0 | yes |
| `ROLE_TABS` | const | 6589 | TABS | 3 | 2 | 1 | no |
| `TAB_LABELS` | const | 6600 | TABS | 3 | 2 | 1 | no |
| `activeTab` | let | 6609 | TABS | 15 | 9 | 6 | no |
| `NAV_GROUPS` | const | 6611 | TABS | 3 | 2 | 1 | no |
| `buildTabs` | function | 6624 | TABS | 6 | 6 | 0 | no |
| `toggleNavGroup` | function | 6663 | TABS | 1 | 1 | 0 | yes |
| `closeAllNavGroups` | function | 6686 | TABS | 3 | 3 | 0 | yes |
| `switchTab` | function | 6691 | TABS | 36 | 36 | 0 | yes |
| `renderActiveTab` | function | 6700 | TABS | 6 | 6 | 0 | no |
| `renderNetwork` | function | 6711 | DASHBOARD | 1 | 1 | 0 | no |
| `buildCompactPipeline` | function | 6858 | DASHBOARD | 1 | 1 | 0 | no |
| `renderDashboard` | function | 6957 | DASHBOARD | 2 | 2 | 0 | yes |
| `renderJobs` | function | 7615 | DASHBOARD | 3 | 3 | 0 | yes |
| `renderHistory` | function | 7752 | HISTORY | 1 | 1 | 0 | no |
| `renderFinancials` | function | 7776 | FINANCIALS | 1 | 1 | 0 | no |
| `renderDriverPayRow` | function | 7851 | CREW LOG HELPERS — extracted to avoid nested template literal — Chrome syntax errors from 4+ levels of backtick nesting | 1 | 1 | 0 | no |
| `renderAccessorialSection` | function | 7920 | CREW LOG HELPERS — extracted to avoid nested template literal — Chrome syntax errors from 4+ levels of backtick nesting | 1 | 1 | 0 | no |
| `renderCrewLog` | function | 7981 | CREW LOG HELPERS — extracted to avoid nested template literal — Chrome syntax errors from 4+ levels of backtick nesting | 4 | 4 | 0 | no |
| `normalizeName` | function | 8186 | NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive | 3 | 3 | 0 | no |
| `namesMatch` | function | 8195 | NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive | 10 | 10 | 0 | no |
| `nameIncludes` | function | 8200 | NAME NORMALIZATION — "joe gedeon" === "Joe Gedeon" === "JOE GEDEON" — All names stored in Title Case, compared case-insensitive | 52 | 52 | 0 | no |
| `toggleCarrierFields` | function | 8218 | CARRIER CUBIC FEET HELPERS | 3 | 3 | 0 | yes |
| `calcCarrierTotal` | function | 8226 | CARRIER CUBIC FEET HELPERS | 4 | 4 | 0 | yes |
| `renderEstimateTab` | function | 8247 | ESTIMATE TAB — Per-job BOL line item editor — This is the source of truth for the BOL PDF | 1 | 1 | 0 | no |
| `renderEstimateForm` | function | 8286 | ESTIMATE TAB — Per-job BOL line item editor — This is the source of truth for the BOL PDF | 5 | 5 | 0 | yes |
| `saveEstimateField` | function | 8497 | ESTIMATE TAB — Per-job BOL line item editor — This is the source of truth for the BOL PDF | 6 | 6 | 0 | yes |
| `saveEstimateJobField` | function | 8507 | ESTIMATE TAB — Per-job BOL line item editor — This is the source of truth for the BOL PDF | 3 | 3 | 0 | yes |
| `saveEstimateAcc` | function | 8514 | ESTIMATE TAB — Per-job BOL line item editor — This is the source of truth for the BOL PDF | 1 | 1 | 0 | yes |
| `calcLiveTotal` | function | 8524 | ESTIMATE TAB — Per-job BOL line item editor — This is the source of truth for the BOL PDF | 2 | 2 | 0 | yes |
| `buildClientPortal` | function | 8540 | CLIENT MOVE PORTAL — Full transparency experience — This is what the client pays $100-150 for — Real-time gate tracking, item location, messaging, docs | 1 | 1 | 0 | no |
| `openClientInventory` | function | 8914 | CLIENT MOVE PORTAL — Full transparency experience — This is what the client pays $100-150 for — Real-time gate tracking, item location, messaging, docs | 1 | 1 | 0 | yes |
| `renderTax1099` | function | 8940 | 1099 / W2 YEAR-END TAX EXPORT — Aggregates all payments per contractor/employee for the year — Generates: on-screen summary + CSV export + PDF report | 2 | 2 | 0 | yes |
| `build1099Report` | function | 9156 | 1099 / W2 YEAR-END TAX EXPORT — Aggregates all payments per contractor/employee for the year — Generates: on-screen summary + CSV export + PDF report | 3 | 3 | 0 | no |
| `getAvailableYears` | function | 9228 | 1099 / W2 YEAR-END TAX EXPORT — Aggregates all payments per contractor/employee for the year — Generates: on-screen summary + CSV export + PDF report | 1 | 1 | 0 | no |
| `export1099CSV` | function | 9240 | 1099 / W2 YEAR-END TAX EXPORT — Aggregates all payments per contractor/employee for the year — Generates: on-screen summary + CSV export + PDF report | 0 | 0 | 0 | no |
| `generate1099PDF` | function | 9291 | 1099 / W2 YEAR-END TAX EXPORT — Aggregates all payments per contractor/employee for the year — Generates: on-screen summary + CSV export + PDF report | 0 | 0 | 0 | no |
| `migrateExistingDataToCompanyId` | function | 9468 | 1099 / W2 YEAR-END TAX EXPORT — Aggregates all payments per contractor/employee for the year — Generates: on-screen summary + CSV export + PDF report | 2 | 2 | 0 | yes |
| `renderAccounting` | function | 9526 | P&L ACCOUNTING MODULE — Kills QuickBooks — Chart of accounts, monthly P&L, period reports, export | 2 | 2 | 0 | yes |
| `buildPL` | function | 9695 | P&L ACCOUNTING MODULE — Kills QuickBooks — Chart of accounts, monthly P&L, period reports, export | 3 | 3 | 0 | no |
| `getAccountingPeriods` | function | 9802 | P&L ACCOUNTING MODULE — Kills QuickBooks — Chart of accounts, monthly P&L, period reports, export | 1 | 1 | 0 | no |
| `exportPLCSV` | function | 9812 | P&L ACCOUNTING MODULE — Kills QuickBooks — Chart of accounts, monthly P&L, period reports, export | 1 | 1 | 0 | yes |
| `exportPLPDF` | function | 9861 | P&L ACCOUNTING MODULE — Kills QuickBooks — Chart of accounts, monthly P&L, period reports, export | 1 | 1 | 0 | yes |
| `sendSMS` | function | 9951 | TWILIO SMS — Real text notifications — Configure in Settings → Integrations | 3 | 3 | 0 | no |
| `smsDriver` | function | 9979 | TWILIO SMS — Real text notifications — Configure in Settings → Integrations | 0 | 0 | 0 | no |
| `smsClient` | function | 9989 | TWILIO SMS — Real text notifications — Configure in Settings → Integrations | 0 | 0 | 0 | no |
| `sendGateSMS` | function | 9996 | TWILIO SMS — Real text notifications — Configure in Settings → Integrations | 1 | 1 | 0 | no |
| `STRIPE_PK` | const | 10018 | STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live | 3 | 2 | 1 | no |
| `_stripe` | let | 10019 | STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live | 5 | 2 | 3 | no |
| `_stripeCard` | let | 10020 | STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live | 6 | 4 | 2 | no |
| `getStripe` | function | 10022 | STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live | 3 | 3 | 0 | no |
| `testStripeConnection` | function | 10031 | STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live | 1 | 1 | 0 | yes |
| `openStripeUpdateKey` | function | 10041 | STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live | 1 | 1 | 0 | yes |
| `saveStripeKey` | function | 10062 | STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live | 1 | 1 | 0 | yes |
| `openStripePayment` | function | 10078 | STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live | 2 | 2 | 0 | no |
| `processStripePayment` | function | 10144 | STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live | 0 | 0 | 0 | no |
| `openPaymentGateStripe` | function | 10190 | STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live | 1 | 1 | 0 | yes |
| `openBookingDeposit` | function | 10231 | STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live | 1 | 1 | 0 | no |
| `renderPaymentGateButton` | function | 10242 | STRIPE PAYMENT INTEGRATION — pk_test_51TJ9Sy7OpLKLw4wS1pBgWlKDocDOhvAIOGmmYx0LAk2mPztD88Ph3tCybFeHpuOhRyaDQBfbXOhySaWOOErgoNIG00692nsJib — Switch to pk_live_ key in Settings when ready to go live | 0 | 0 | 0 | no |
| `COL_LEADS` | const | 10260 | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 9 | 8 | 1 | no |
| `COL_TRAVEL` | const | 10261 | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 8 | 7 | 1 | no |
| `LEAD_STAGES` | const | 10263 | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 11 | 10 | 1 | no |
| `LEAD_SOURCES` | const | 10272 | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 2 | 1 | 1 | no |
| `renderLeads` | function | 10274 | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 8 | 8 | 0 | yes |
| `openAddLead` | function | 10455 | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 4 | 4 | 0 | yes |
| `saveLead` | function | 10572 | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 1 | 1 | 0 | yes |
| `openLeadDetail` | function | 10620 | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 3 | 3 | 0 | yes |
| `openEditLead` | function | 10718 | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 1 | 1 | 0 | yes |
| `advanceLeadStage` | function | 10731 | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 1 | 1 | 0 | yes |
| `addLeadActivity` | function | 10754 | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 2 | 2 | 0 | yes |
| `openClaimsDefensePanel` | function | 10777 | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 0 | 0 | 0 | no |
| `deleteLead` | function | 10813 | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 2 | 2 | 0 | yes |
| `convertLeadToJob` | function | 10830 | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 2 | 2 | 0 | yes |
| `createJobFromLead` | function | 10856 | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 0 | 0 | 0 | no |
| `loadLeads` | function | 10875 | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 5 | 5 | 0 | no |
| `getOverdueLeadCount` | function | 10902 | LEAD CRM — Full pipeline management — Capture → Qualify → Quote → Follow-up → Convert → Job — Replaces the need for any external CRM tool | 2 | 2 | 0 | no |
| `jobDispId` | function | 10917 | JOB DISPLAY ID — broker/carrier ref number support — job.id      = FleetFlow internal ID (never changes) — job.refNum  = Broker/carrier order number (editable by office) — jobDispId() = what to show everywhere in the UI | 0 | 0 | 0 | no |
| `renderRefBadge` | function | 10922 | JOB DISPLAY ID — broker/carrier ref number support — job.id      = FleetFlow internal ID (never changes) — job.refNum  = Broker/carrier order number (editable by office) — jobDispId() = what to show everywhere in the UI | 1 | 1 | 0 | no |
| `openEditRefNum` | function | 10939 | JOB DISPLAY ID — broker/carrier ref number support — job.id      = FleetFlow internal ID (never changes) — job.refNum  = Broker/carrier order number (editable by office) — jobDispId() = what to show everywhere in the UI | 2 | 2 | 0 | yes |
| `saveRefNum` | function | 10973 | JOB DISPLAY ID — broker/carrier ref number support — job.id      = FleetFlow internal ID (never changes) — job.refNum  = Broker/carrier order number (editable by office) — jobDispId() = what to show everywhere in the UI | 1 | 1 | 0 | yes |
| `getEmployeeDisplay` | function | 10991 | EMPLOYEE ID SYSTEM — empId  = auto-assigned badge (EMP-001, EMP-002...) — handle = user-chosen nickname/alias — displayHandle() = handle if set, else empId | 0 | 0 | 0 | no |
| `assignEmpId` | function | 10997 | EMPLOYEE ID SYSTEM — empId  = auto-assigned badge (EMP-001, EMP-002...) — handle = user-chosen nickname/alias — displayHandle() = handle if set, else empId | 0 | 0 | 0 | no |
| `checkPortalRoutes` | function | 11020 | CLIENT BOOKING PORTAL — URL: fleetflowmovemasters.netlify.app/?book=1&co=COMPANY_ID — Moving company shares this link on their website — Client fills details → instant quote → pays deposit → job created | 1 | 1 | 0 | no |
| `renderBookingPortal` | function | 11034 | CLIENT BOOKING PORTAL — URL: fleetflowmovemasters.netlify.app/?book=1&co=COMPANY_ID — Moving company shares this link on their website — Client fills details → instant quote → pays deposit → job created | 1 | 1 | 0 | no |
| `calcBookingQuote` | function | 11282 | CLIENT BOOKING PORTAL — URL: fleetflowmovemasters.netlify.app/?book=1&co=COMPANY_ID — Moving company shares this link on their website — Client fills details → instant quote → pays deposit → job created | 4 | 4 | 0 | yes |
| `bookingNextStep` | function | 11316 | CLIENT BOOKING PORTAL — URL: fleetflowmovemasters.netlify.app/?book=1&co=COMPANY_ID — Moving company shares this link on their website — Client fills details → instant quote → pays deposit → job created | 4 | 4 | 0 | yes |
| `confirmBooking` | function | 11383 | CLIENT BOOKING PORTAL — URL: fleetflowmovemasters.netlify.app/?book=1&co=COMPANY_ID — Moving company shares this link on their website — Client fills details → instant quote → pays deposit → job created | 1 | 1 | 0 | yes |
| `_doConfirmBooking` | function | 11400 | CLIENT BOOKING PORTAL — URL: fleetflowmovemasters.netlify.app/?book=1&co=COMPANY_ID — Moving company shares this link on their website — Client fills details → instant quote → pays deposit → job created | 2 | 2 | 0 | no |
| `renderOnboardingPortal` | function | 11509 | CLIENT BOOKING PORTAL — URL: fleetflowmovemasters.netlify.app/?book=1&co=COMPANY_ID — Moving company shares this link on their website — Client fills details → instant quote → pays deposit → job created | 1 | 1 | 0 | no |
| `obNextStep` | function | 11745 | CLIENT BOOKING PORTAL — URL: fleetflowmovemasters.netlify.app/?book=1&co=COMPANY_ID — Moving company shares this link on their website — Client fills details → instant quote → pays deposit → job created | 6 | 6 | 0 | yes |
| `selectObPlan` | function | 11778 | CLIENT BOOKING PORTAL — URL: fleetflowmovemasters.netlify.app/?book=1&co=COMPANY_ID — Moving company shares this link on their website — Client fills details → instant quote → pays deposit → job created | 1 | 1 | 0 | yes |
| `launchCompany` | function | 11789 | CLIENT BOOKING PORTAL — URL: fleetflowmovemasters.netlify.app/?book=1&co=COMPANY_ID — Moving company shares this link on their website — Client fills details → instant quote → pays deposit → job created | 1 | 1 | 0 | yes |
| `autoInviteClient` | function | 11876 | CLIENT BOOKING PORTAL — URL: fleetflowmovemasters.netlify.app/?book=1&co=COMPANY_ID — Moving company shares this link on their website — Client fills details → instant quote → pays deposit → job created | 0 | 0 | 0 | no |
| `generateBOLPDF` | function | 11929 | PORTAL ROUTE CHECK — runs before login screen — Matches Good Friends Movers LLC document package exactly — US DOT# 3921653 / MC# 1450729 — Structure: Interstate BOL | Legal Terms | Inventory x2 | Packing | Post Contract | Valuation | 3 | 3 | 0 | yes |
| `checkUnpaidCrew` | function | 12764 | PAY SCHEDULE & UNPAID CREW CHECKER | 0 | 0 | 0 | no |
| `applyCompanyBranding` | function | 12821 | COMPANY BRANDING ENGINE | 1 | 1 | 0 | no |
| `saveCompanyProfile` | function | 12847 | COMPANY BRANDING ENGINE | 7 | 7 | 0 | no |
| `requestPushPermission` | function | 12859 | NOTIFICATION ENGINE | 0 | 0 | 0 | no |
| `sendNotification` | function | 12867 | NOTIFICATION ENGINE | 11 | 11 | 0 | no |
| `notifyAllOffice` | function | 12899 | NOTIFICATION ENGINE | 3 | 3 | 0 | no |
| `_audioCtx` | let | 12919 | SOUND ENGINE — Web Audio API, no external files needed | 4 | 2 | 2 | no |
| `getAudioCtx` | function | 12921 | SOUND ENGINE — Web Audio API, no external files needed | 2 | 2 | 0 | no |
| `playTone` | function | 12930 | SOUND ENGINE — Web Audio API, no external files needed | 10 | 10 | 0 | no |
| `SOUNDS` | const | 12960 | SOUND ENGINE — Web Audio API, no external files needed | 12 | 11 | 1 | yes |
| `playNotifSound` | function | 13029 | SOUND ENGINE — Web Audio API, no external files needed | 2 | 2 | 0 | no |
| `_audioUnlocked` | let | 13037 | SOUND ENGINE — Web Audio API, no external files needed | 4 | 1 | 3 | no |
| `unlockAudio` | function | 13038 | SOUND ENGINE — Web Audio API, no external files needed | 2 | 2 | 0 | no |
| `showPushNotification` | function | 13050 | SOUND ENGINE — Web Audio API, no external files needed | 1 | 1 | 0 | no |
| `updateNotifBadge` | function | 13072 | SOUND ENGINE — Web Audio API, no external files needed | 1 | 1 | 0 | no |
| `markNotifRead` | function | 13085 | SOUND ENGINE — Web Audio API, no external files needed | 1 | 1 | 0 | no |
| `markAllNotifsRead` | function | 13092 | SOUND ENGINE — Web Audio API, no external files needed | 1 | 1 | 0 | yes |
| `deleteNotif` | function | 13102 | SOUND ENGINE — Web Audio API, no external files needed | 1 | 1 | 0 | yes |
| `_notifPanelOpen` | let | 13109 | SOUND ENGINE — Web Audio API, no external files needed | 5 | 2 | 3 | no |
| `toggleNotifPanel` | function | 13110 | SOUND ENGINE — Web Audio API, no external files needed | 0 | 0 | 0 | no |
| `closeNotifPanel` | function | 13122 | SOUND ENGINE — Web Audio API, no external files needed | 3 | 3 | 0 | yes |
| `renderNotifPanel` | function | 13128 | SOUND ENGINE — Web Audio API, no external files needed | 1 | 1 | 0 | no |
| `handleNotifTap` | function | 13179 | SOUND ENGINE — Web Audio API, no external files needed | 0 | 0 | 0 | no |
| `calcPay` | function | 13199 | PAYROLL CALCULATION ENGINE v2 — pay + commission + tax | 18 | 18 | 0 | no |
| `calcBasePay` | function | 13215 | PAYROLL CALCULATION ENGINE v2 — pay + commission + tax | 3 | 3 | 0 | no |
| `payLabel` | function | 13229 | PAYROLL CALCULATION ENGINE v2 — pay + commission + tax | 4 | 4 | 0 | no |
| `TAX_RATES` | const | 13250 | TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 12 | 11 | 1 | no |
| `calcTax` | function | 13259 | TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 6 | 6 | 0 | no |
| `renderTaxBreakdown` | function | 13306 | TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 3 | 3 | 0 | no |
| `updateDriverPay` | function | 13334 | TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 0 | 0 | 0 | no |
| `updateDriverPayType` | function | 13341 | TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 0 | 0 | 0 | no |
| `updateDriverRate` | function | 13349 | TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 1 | 1 | 0 | yes |
| `updateDriverHours` | function | 13356 | TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 1 | 1 | 0 | yes |
| `updateDriverDays` | function | 13363 | TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 1 | 1 | 0 | yes |
| `updateDriverCommission` | function | 13370 | TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 1 | 1 | 0 | yes |
| `updateDriverWorkerType` | function | 13377 | TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 0 | 0 | 0 | no |
| `recalcDriverTotal` | function | 13384 | TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 6 | 6 | 0 | no |
| `updateHelper` | function | 13408 | TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 0 | 0 | 0 | no |
| `addHelperRow` | function | 13426 | TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 3 | 3 | 0 | yes |
| `addEjHelperRow` | function | 13447 | TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 1 | 1 | 0 | yes |
| `removeHelper` | function | 13475 | TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 0 | 0 | 0 | no |
| `recalcCrewTotal` | function | 13483 | TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 4 | 4 | 0 | no |
| `saveCrewLog` | function | 13492 | TAX CALCULATION ENGINE — Independent contractors (1099) — self-employment tax — W2 employees — standard withholding estimate | 1 | 1 | 0 | yes |
| `calYear` | let | 13515 | CALENDAR | 12 | 9 | 5 | no |
| `calMonth` | let | 13516 | CALENDAR | 15 | 10 | 6 | no |
| `DRIVER_COLORS` | const | 13519 | CALENDAR | 4 | 3 | 1 | no |
| `_driverColorMap` | let | 13530 | CALENDAR | 5 | 3 | 2 | no |
| `_driverColorIdx` | let | 13531 | CALENDAR | 4 | 2 | 3 | no |
| `getDriverColor` | function | 13533 | CALENDAR | 8 | 8 | 0 | no |
| `renderCalendar` | function | 13542 | CALENDAR | 7 | 7 | 0 | yes |
| `setCalView` | function | 13652 | CALENDAR | 2 | 2 | 0 | yes |
| `renderMonthView` | function | 13660 | MONTH VIEW — Google Calendar style with multi-day span bars | 1 | 1 | 0 | no |
| `renderWeekView` | function | 13905 | WEEK VIEW — time-based layout like Google Calendar | 1 | 1 | 0 | no |
| `calNav` | function | 14010 | WEEK VIEW — time-based layout like Google Calendar | 3 | 3 | 0 | yes |
| `openCalDay` | function | 14022 | WEEK VIEW — time-based layout like Google Calendar | 2 | 2 | 0 | yes |
| `renderMessaging` | function | 14214 | MESSAGING | 3 | 3 | 0 | no |
| `sendBroadcast` | function | 14317 | MESSAGING | 3 | 3 | 0 | yes |
| `sendMsg` | function | 14394 | MESSAGING | 2 | 2 | 0 | yes |
| `renderWarehouse` | function | 14437 | WAREHOUSE | 6 | 6 | 0 | yes |
| `saveJobWarehouseLocation` | function | 14678 | WAREHOUSE | 1 | 1 | 0 | yes |
| `notifyWarehouseShortage` | function | 14690 | WAREHOUSE | 1 | 1 | 0 | yes |
| `adjustQty` | function | 14710 | WAREHOUSE | 0 | 0 | 0 | no |
| `addInventoryItem` | function | 14716 | WAREHOUSE | 1 | 1 | 0 | yes |
| `renderBOLTab` | function | 14730 | BOL TAB | 1 | 1 | 0 | no |
| `openBOL` | function | 14768 | BOL TAB | 4 | 4 | 0 | yes |
| `renderSchedule` | function | 14962 | SCHEDULE | 1 | 1 | 0 | no |
| `renderReceipts` | function | 15017 | RECEIPTS | 11 | 11 | 0 | yes |
| `approveAllDriverReceipts` | function | 15380 | RECEIPTS | 1 | 1 | 0 | yes |
| `openReceiptModal` | function | 15402 | RECEIPTS | 2 | 2 | 0 | yes |
| `openReceiptModalFromCal` | function | 15426 | RECEIPTS | 4 | 4 | 0 | yes |
| `triggerReceiptCamera` | function | 15431 | RECEIPTS | 3 | 3 | 0 | yes |
| `handleReceiptPhoto` | function | 15462 | RECEIPTS | 1 | 1 | 0 | no |
| `scanReceiptWithAI` | function | 15486 | RECEIPTS | 1 | 1 | 0 | no |
| `saveReceiptSubmission` | function | 15634 | RECEIPTS | 1 | 1 | 0 | yes |
| `openEditReceipt` | function | 15725 | RECEIPTS | 4 | 4 | 0 | yes |
| `deleteReceipt` | function | 15758 | RECEIPTS | 2 | 2 | 0 | yes |
| `openAddPhoto` | function | 15794 | RECEIPTS | 0 | 0 | 0 | no |
| `approveReceipt` | function | 15831 | RECEIPTS | 3 | 3 | 0 | yes |
| `rejectReceipt` | function | 15852 | RECEIPTS | 2 | 2 | 0 | yes |
| `viewReceiptPhoto` | function | 15875 | RECEIPTS | 2 | 2 | 0 | yes |
| `openJobDetail` | function | 15894 | JOB DETAIL MODAL | 19 | 19 | 0 | yes |
| `saveEstimateFieldDirect` | function | 16199 | JOB DETAIL MODAL | 0 | 0 | 0 | no |
| `deleteJob` | function | 16207 | JOB DETAIL MODAL | 1 | 1 | 0 | yes |
| `advanceGate` | function | 16222 | JOB DETAIL MODAL | 3 | 3 | 0 | yes |
| `changeJobStatus` | function | 16287 | JOB DETAIL MODAL | 0 | 0 | 0 | no |
| `openEditJob` | function | 16301 | EDIT JOB  ← NEW FEATURE | 1 | 1 | 0 | no |
| `saveEditJob` | function | 16421 | EDIT JOB  ← NEW FEATURE | 1 | 1 | 0 | yes |
| `openNewJobModal` | function | 16616 | function openNewJobModal(prefillDate) { — ── HARD RESET — every field to blank/zero ────────────────── — const safe = (id, val='') => { const el = document.getElementById(id); if(el) el.value = val; }; — Core job fields — safe('nj-client'); safe('nj-phone'); safe('nj-email'); — safe('nj-origin'); safe('nj-dest'); safe('nj-pickup-from'); — safe('nj-amount'); safe('nj-notes'); safe('nj-carrier'); — safe('nj-cubic-ft'); safe('nj-cubic-ft-2'); safe('nj-rate-per-cuft'); — safe('nj-weight'); safe('nj-linehaul'); — safe('nj-end-date'); safe('nj-end-time'); — safe('nj-pickup-start'); safe('nj-pickup-end'); — safe('nj-delivery-start'); safe('nj-delivery-end'); — safe('nj-pickup-driver'); safe('nj-delivery-driver'); — safe('nj-job-id'); safe('nj-ref-num'); — safe('nj-priority'); safe('nj-followup'); safe('nj-fadd'); — safe('nj-broker-ref'); safe('nj-carrier'); — ALL estimate fields — zero out completely — safe('nj-est-fuel', ''); — safe('nj-est-fuel-pct', '0'); — safe('nj-est-overload-cuft', ''); — safe('nj-est-stairs-origin', ''); — safe('nj-est-stairs-dest', ''); — safe('nj-est-longcarry-origin', ''); — safe('nj-est-longcarry-dest', ''); — safe('nj-est-shuttle', ''); — safe('nj-est-bulky', ''); — safe('nj-est-split', ''); — safe('nj-est-pgs', ''); — safe('nj-est-packing', ''); — safe('nj-est-storage-days', ''); — safe('nj-est-storage-rate', ''); — safe('nj-est-other', ''); — safe('nj-est-deposit', ''); — safe('nj-est-partial2', ''); — safe('nj-est-tip', ''); — safe('nj-est-discount', ''); — safe('nj-est-discount-label', ''); — safe('nj-est-others1', '');  safe('nj-est-others1-label', ''); — safe('nj-est-others2', '');  safe('nj-est-others2-label', ''); — safe('nj-est-others3', '');  safe('nj-est-others3-label', ''); — safe('nj-est-extra1', '');   safe('nj-est-extra1-label', ''); — safe('nj-est-extra2', '');   safe('nj-est-extra2-label', ''); — safe('nj-est-extra3', '');   safe('nj-est-extra3-label', ''); — safe('nj-est-notes', ''); — safe('nj-est-office-remarks', ''); — safe('nj-est-slip-remarks', ''); — Reset carrier total display — const ct = document.getElementById('nj-carrier-total'); if(ct) ct.textContent = '$0.00'; — Clear helper rows — const hr = document.getElementById('nj-helper-rows'); if(hr) hr.innerHTML = ''; — Reset all selects to index 0 — ['nj-job-for','nj-est-tip-method','nj-est-valuation', — 'nj-est-fuel-mode','nj-est-tip-assign'].forEach(id => { — const el = document.getElementById(id); — if (el) el.selectedIndex = 0; — }); — try { toggleCarrierFields('nj'); } catch(e) {} — Apply defaults — const s2 = loadSettings(); — safe('nj-crew', s2.defaultCrew || '3'); — safe('nj-type', s2.defaultMove || 'local'); — safe('nj-date', prefillDate || getTodayStr()); — safe('nj-time', getSetting('defaultTime', '08:00')); — safe('nj-driver', currentUser.role === 'driver' ? (currentUser.name||'') : ''); — closeModal('modal-calday'); — openModal('modal-newjob'); — setTimeout(() => { try { applyJobDefaults('nj'); } catch(e) {} }, 50); — } — async function createJob() { — const client = document.getElementById('nj-client').value.trim(); — const date   = document.getElementById('nj-date').value; — const origin = document.getElementById('nj-origin').value.trim(); — const dest   = document.getElementById('nj-dest').value.trim(); — if (!client || !date) { notify('⚠ Client name and date required', true); return; } — Custom ID or auto-generate — const customId = (document.getElementById('nj-job-id')?.value||'').trim().toUpperCase(); — const autoId   = 'JOB-' + String((STATE.jobs||[]).length + 1).padStart(3,'0'); — const jobId    = customId || autoId; — if ((STATE.jobs||[]).find(j => j.id === jobId)) { — notify('⚠ Job ID ' + jobId + ' already exists', true); return; — } — Collect helpers — const helpers = []; — document.querySelectorAll('.nj-helper-name').forEach(inp => { — const name = inp.value.trim(); — if (!name) return; — const row = inp.closest('.nj-helper-row'); — helpers.push({ — name, — rate:    parseFloat(row?.querySelector('.nj-helper-rate')?.value) || 0, — payType: row?.querySelector('.nj-helper-type')?.value || 'hourly', — hours: 0, days: 1, workerType: '1099', paid: false — }); — }); — const g = id => (document.getElementById(id)?.value || ''); — const newJob = { — id:            jobId, — refNum:        g('nj-ref-num'), — client, — phone:         g('nj-phone'), — email:         g('nj-email'), — date, — time:          g('nj-time'), — endDate:       g('nj-end-date') || g('nj-delivery-end').slice(0,10) || '', — endTime:       g('nj-end-time'), — origin, — dest, — pickupStart:   g('nj-pickup-start'), — pickupEnd:     g('nj-pickup-end'), — pickupDriver:  g('nj-pickup-driver'), — deliveryStart: g('nj-delivery-start'), — deliveryEnd:   g('nj-delivery-end'), — deliveryDriver:g('nj-delivery-driver'), — driver:        g('nj-driver'), — driverUser:    currentUser.username || '', — crewSize:      parseInt(g('nj-crew')) || 3, — crewNames:     helpers.map(h => h.name), — amount:        parseFloat(g('nj-amount')) || 0, — cubicFt:       parseFloat(g('nj-cubic-ft-2') || g('nj-cubic-ft')) || 0, — weight:        parseFloat(g('nj-weight')) || 0, — linehaul:      parseFloat(g('nj-linehaul')) || 0, — priority:      g('nj-priority'), — followUp:      g('nj-followup'), — fadd:          g('nj-fadd'), — type:          g('nj-type') || 'local', — notes:         g('nj-notes'), — companyId:     COMPANY_ID, — status:        'pending', — gates:         { preload:false, loaded:false, delivered:false, payment:false }, — messages:      { crew:[], client:[] }, — photos:        [], — signatures:    [], — payroll:       { driver:0, helpers } — }; — try { — await db.collection(COL_JOBS).doc(newJob.id).set(newJob); — closeModal('modal-newjob'); — notify('✓ Job ' + newJob.id + ' created — ' + client); — setTimeout(() => renderCalendar(), 200); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('createJob error:', e); — } — } | 7 | 7 | 0 | yes |
| `createJob` | function | 16689 | function openNewJobModal(prefillDate) { — ── HARD RESET — every field to blank/zero ────────────────── — const safe = (id, val='') => { const el = document.getElementById(id); if(el) el.value = val; }; — Core job fields — safe('nj-client'); safe('nj-phone'); safe('nj-email'); — safe('nj-origin'); safe('nj-dest'); safe('nj-pickup-from'); — safe('nj-amount'); safe('nj-notes'); safe('nj-carrier'); — safe('nj-cubic-ft'); safe('nj-cubic-ft-2'); safe('nj-rate-per-cuft'); — safe('nj-weight'); safe('nj-linehaul'); — safe('nj-end-date'); safe('nj-end-time'); — safe('nj-pickup-start'); safe('nj-pickup-end'); — safe('nj-delivery-start'); safe('nj-delivery-end'); — safe('nj-pickup-driver'); safe('nj-delivery-driver'); — safe('nj-job-id'); safe('nj-ref-num'); — safe('nj-priority'); safe('nj-followup'); safe('nj-fadd'); — safe('nj-broker-ref'); safe('nj-carrier'); — ALL estimate fields — zero out completely — safe('nj-est-fuel', ''); — safe('nj-est-fuel-pct', '0'); — safe('nj-est-overload-cuft', ''); — safe('nj-est-stairs-origin', ''); — safe('nj-est-stairs-dest', ''); — safe('nj-est-longcarry-origin', ''); — safe('nj-est-longcarry-dest', ''); — safe('nj-est-shuttle', ''); — safe('nj-est-bulky', ''); — safe('nj-est-split', ''); — safe('nj-est-pgs', ''); — safe('nj-est-packing', ''); — safe('nj-est-storage-days', ''); — safe('nj-est-storage-rate', ''); — safe('nj-est-other', ''); — safe('nj-est-deposit', ''); — safe('nj-est-partial2', ''); — safe('nj-est-tip', ''); — safe('nj-est-discount', ''); — safe('nj-est-discount-label', ''); — safe('nj-est-others1', '');  safe('nj-est-others1-label', ''); — safe('nj-est-others2', '');  safe('nj-est-others2-label', ''); — safe('nj-est-others3', '');  safe('nj-est-others3-label', ''); — safe('nj-est-extra1', '');   safe('nj-est-extra1-label', ''); — safe('nj-est-extra2', '');   safe('nj-est-extra2-label', ''); — safe('nj-est-extra3', '');   safe('nj-est-extra3-label', ''); — safe('nj-est-notes', ''); — safe('nj-est-office-remarks', ''); — safe('nj-est-slip-remarks', ''); — Reset carrier total display — const ct = document.getElementById('nj-carrier-total'); if(ct) ct.textContent = '$0.00'; — Clear helper rows — const hr = document.getElementById('nj-helper-rows'); if(hr) hr.innerHTML = ''; — Reset all selects to index 0 — ['nj-job-for','nj-est-tip-method','nj-est-valuation', — 'nj-est-fuel-mode','nj-est-tip-assign'].forEach(id => { — const el = document.getElementById(id); — if (el) el.selectedIndex = 0; — }); — try { toggleCarrierFields('nj'); } catch(e) {} — Apply defaults — const s2 = loadSettings(); — safe('nj-crew', s2.defaultCrew || '3'); — safe('nj-type', s2.defaultMove || 'local'); — safe('nj-date', prefillDate || getTodayStr()); — safe('nj-time', getSetting('defaultTime', '08:00')); — safe('nj-driver', currentUser.role === 'driver' ? (currentUser.name||'') : ''); — closeModal('modal-calday'); — openModal('modal-newjob'); — setTimeout(() => { try { applyJobDefaults('nj'); } catch(e) {} }, 50); — } — async function createJob() { — const client = document.getElementById('nj-client').value.trim(); — const date   = document.getElementById('nj-date').value; — const origin = document.getElementById('nj-origin').value.trim(); — const dest   = document.getElementById('nj-dest').value.trim(); — if (!client || !date) { notify('⚠ Client name and date required', true); return; } — Custom ID or auto-generate — const customId = (document.getElementById('nj-job-id')?.value||'').trim().toUpperCase(); — const autoId   = 'JOB-' + String((STATE.jobs||[]).length + 1).padStart(3,'0'); — const jobId    = customId || autoId; — if ((STATE.jobs||[]).find(j => j.id === jobId)) { — notify('⚠ Job ID ' + jobId + ' already exists', true); return; — } — Collect helpers — const helpers = []; — document.querySelectorAll('.nj-helper-name').forEach(inp => { — const name = inp.value.trim(); — if (!name) return; — const row = inp.closest('.nj-helper-row'); — helpers.push({ — name, — rate:    parseFloat(row?.querySelector('.nj-helper-rate')?.value) || 0, — payType: row?.querySelector('.nj-helper-type')?.value || 'hourly', — hours: 0, days: 1, workerType: '1099', paid: false — }); — }); — const g = id => (document.getElementById(id)?.value || ''); — const newJob = { — id:            jobId, — refNum:        g('nj-ref-num'), — client, — phone:         g('nj-phone'), — email:         g('nj-email'), — date, — time:          g('nj-time'), — endDate:       g('nj-end-date') || g('nj-delivery-end').slice(0,10) || '', — endTime:       g('nj-end-time'), — origin, — dest, — pickupStart:   g('nj-pickup-start'), — pickupEnd:     g('nj-pickup-end'), — pickupDriver:  g('nj-pickup-driver'), — deliveryStart: g('nj-delivery-start'), — deliveryEnd:   g('nj-delivery-end'), — deliveryDriver:g('nj-delivery-driver'), — driver:        g('nj-driver'), — driverUser:    currentUser.username || '', — crewSize:      parseInt(g('nj-crew')) || 3, — crewNames:     helpers.map(h => h.name), — amount:        parseFloat(g('nj-amount')) || 0, — cubicFt:       parseFloat(g('nj-cubic-ft-2') || g('nj-cubic-ft')) || 0, — weight:        parseFloat(g('nj-weight')) || 0, — linehaul:      parseFloat(g('nj-linehaul')) || 0, — priority:      g('nj-priority'), — followUp:      g('nj-followup'), — fadd:          g('nj-fadd'), — type:          g('nj-type') || 'local', — notes:         g('nj-notes'), — companyId:     COMPANY_ID, — status:        'pending', — gates:         { preload:false, loaded:false, delivered:false, payment:false }, — messages:      { crew:[], client:[] }, — photos:        [], — signatures:    [], — payroll:       { driver:0, helpers } — }; — try { — await db.collection(COL_JOBS).doc(newJob.id).set(newJob); — closeModal('modal-newjob'); — notify('✓ Job ' + newJob.id + ' created — ' + client); — setTimeout(() => renderCalendar(), 200); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('createJob error:', e); — } — } | 2 | 2 | 0 | yes |
| `renderMyPay` | function | 16775 | function renderMyPay() { — const content  = document.getElementById('main-content'); — const role     = currentUser.role; — const isHelper = role === 'helper'; — Collect all pay entries for this person — const myJobs = STATE.jobs.filter(j => { — if (role === 'driver') return nameIncludes(j.driver, currentUser.name); — if (role === 'helper') return (j.payroll?.helpers||[]).some(h => nameIncludes(h.name, currentUser.name)); — return false; — }); — let totalGross      = 0; — let totalCommission = 0; — let totalTax        = 0; — let totalTakeHome   = 0; — const payRows       = []; — myJobs.forEach(j => { — let gross = 0, commission = 0, workerType = '1099', payDetails = ''; — if (role === 'driver') { — gross       = j.payroll?.driver || 0; — commission  = j.payroll?.driverCommission || 0; — workerType  = j.payroll?.driverWorkerType || '1099'; — payDetails  = payLabel({ payType: j.payroll?.driverPayType||'flat', rate: j.payroll?.driverRate||gross, hours: j.payroll?.driverHours||0, days: j.payroll?.driverDays||1 }); — } else { — const helper = (j.payroll?.helpers||[]).find(h => — h.name?.toLowerCase().includes(currentUser.name.toLowerCase()) || — (currentUser.name.toLowerCase().split(' ')[0] && h.name?.toLowerCase().includes(currentUser.name.toLowerCase().split(' ')[0])) — ); — if (helper) { — gross      = calcPay(helper); — commission = helper.commission || 0; — workerType = helper.workerType || '1099'; — payDetails = payLabel(helper); — } — } — if (gross === 0) return; — const tax = calcTax(gross, workerType); — totalGross      += gross; — totalCommission += commission; — totalTax        += tax.totalEstTax; — totalTakeHome   += tax.takeHome; — payRows.push({ job: j, gross, commission, workerType, payDetails, tax }); — }); — let html = ` — <div class="section-header"> — <div> — <div class="section-title">📊 MY PAY SUMMARY</div> — <div class="section-sub">${currentUser.name} · ${role.toUpperCase()} · ${payRows.length} job(s)</div> — </div> — </div> — <!-- YTD SUMMARY STATS --> — <div class="grid-4" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Gross Earned</div> — <div class="stat-value">$${totalGross.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Commission</div> — <div class="stat-value" style="color:var(--amber)">$${totalCommission.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Est. Tax Owed</div> — <div class="stat-value red">$${totalTax.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Est. Take-Home</div> — <div class="stat-value">$${totalTakeHome.toFixed(2)}</div> — </div> — </div> — <!-- TAX ADVISORY BANNER --> — <div class="card" style="border-color:#664400;background:rgba(255,170,0,0.05);margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:11px;color:var(--amber);letter-spacing:2px;margin-bottom:8px">⚠ TAX ADVISORY</div> — <div class="info-text" style="line-height:1.8"> — ${totalGross > 0 ? `Based on your earnings of <strong style="color:var(--white)">$${totalGross.toFixed(2)}</strong>, you should set aside approximately <strong style="color:var(--red)">$${totalTax.toFixed(2)}</strong> (${(totalTax/totalGross*100).toFixed(1)}%) for taxes.` : 'No earnings recorded yet.'} — ${totalGross >= 600 ? ' You will likely receive a 1099 form if classified as a contractor.' : ''} — If you are a <strong style="color:var(--white)">1099 contractor</strong>, you are responsible for paying your own taxes quarterly (April, June, September, January). Consider setting aside <strong style="color:var(--red)">${totalGross > 0 ? (totalTax/totalGross*100).toFixed(0) : '~25'}%</strong> of every paycheck. — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div style="margin-top:4px"> — ${payRows.length ? payRows.map(row => ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:10px"> — <div> — <div style="display:flex;gap:8px;align-items:center;margin-bottom:4px"> — <span class="tag">${row.job.id}</span> — <span class="status-badge status-${row.job.status}">${row.job.status}</span> — <span class="tag">${row.workerType}</span> — </div> — <div style="font-size:14px;font-weight:600">${row.job.client}</div> — <div class="info-text">${row.job.date} · ${row.payDetails}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:20px;color:var(--green)">$${row.gross.toFixed(2)}</div> — ${row.commission > 0 ? `<div style="font-family:var(--font-mono);font-size:12px;color:var(--amber)">+ $${row.commission.toFixed(2)} commission</div>` : ''} — </div> — </div> — <!-- Tax breakdown per job --> — <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;margin-bottom:8px"> — <div class="stat-box" style="padding:10px"> — <div class="stat-label" style="font-size:9px">Gross Pay</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--white)">$${row.gross.toFixed(2)}</div> — </div> — <div class="stat-box" style="padding:10px"> — <div class="stat-label" style="font-size:9px">Est. Tax</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--red)">−$${row.tax.totalEstTax.toFixed(2)}</div> — </div> — <div class="stat-box" style="padding:10px;border-color:var(--green3)"> — <div class="stat-label" style="font-size:9px">Take-Home</div> — <div style="font-family:var(--font-mono);font-size:16px;color:var(--green)">$${row.tax.takeHome.toFixed(2)}</div> — </div> — </div> — <!-- Detailed tax line items --> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;font-family:var(--font-mono);font-size:10px"> — ${row.tax.selfEmployTax ? `<div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>Self-Employment Tax (15.3%)</span><span style="color:var(--red)">−$${row.tax.selfEmployTax.toFixed(2)}</span></div>` : ''} — ${row.tax.ficaTax ? `<div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>FICA Employee (7.65%)</span><span style="color:var(--red)">−$${row.tax.ficaTax.toFixed(2)}</span></div>` : ''} — <div style="display:flex;justify-content:space-between;color:var(--gray);margin-bottom:4px"><span>Federal Income (~${(row.tax.federalRate*100).toFixed(0)}%)</span><span style="color:var(--red)">−$${row.tax.federalTax.toFixed(2)}</span></div> — <div style="display:flex;justify-content:space-between;color:var(--gray)"><span>Georgia State (5.5%)</span><span style="color:var(--red)">−$${row.tax.stateTax.toFixed(2)}</span></div> — </div> — </div>`).join('') : ` — <div class="card" style="text-align:center;padding:40px"> — <p class="info-text">No pay records found for ${currentUser.name}.</p> — <p class="info-text" style="margin-top:8px">Your earnings will appear here once jobs are assigned and crew logs are saved.</p> — </div>`} — </div> — <div style="padding:12px;font-family:var(--font-mono);font-size:9px;color:var(--gray);line-height:1.8;border-top:1px solid var(--gray2);margin-top:8px"> — ⚠ DISCLAIMER: Tax estimates are for informational purposes only based on 2024 US federal rates and Georgia state rates. This is not tax advice. Individual circumstances vary. Consult a licensed tax professional or CPA for filing guidance. JPG Systems / JPG Ventures LLC is not responsible for tax obligations of independent contractors. — </div>`; — content.innerHTML = html; — } | 1 | 1 | 0 | no |
| `renderExpenses` | function | 16918 | function renderExpenses() { — const content = document.getElementById('main-content'); — const role = currentUser.role; — const isDriver = role === 'driver'; — Scope to this driver's jobs/disbursements if not office — const myJobs = isDriver — ? STATE.jobs.filter(j => nameIncludes(j.driver, currentUser.name)) — : STATE.jobs; — const myDisbursements = isDriver — ? STATE.disbursements.filter(d => — d.driverUser === currentUser.username || — (d.driver && d.driver.toLowerCase().includes(currentUser.name.toLowerCase())) — ) — : STATE.disbursements; — const myReceipts = isDriver — ? STATE.receipts.filter(r => { — const nm = r.driver && nameIncludes(r.driver, currentUser.name); — const um = r.driverUser && r.driverUser === currentUser.username; — return nm || um; — }) — : STATE.receipts; — Totals — const totalDisbursed = myDisbursements.reduce((s,d) => s + (d.amount||0), 0); — const totalReceipts  = myReceipts.reduce((s,r) => s + (r.amount||0), 0); — const netOwed        = totalReceipts - totalDisbursed; // positive = driver owes, negative = company owes driver — let html = ` — <div class="section-header"> — <div> — <div class="section-title">EXPENSE TRACKER</div> — <div class="section-sub">${isDriver ? 'Your funds & receipts · ' + currentUser.name : 'All driver funds & expense reconciliation'}</div> — </div> — ${isAdmin() ? `<button class="btn-green" onclick="openDisbursementModal()">💰 SEND FUNDS</button>` : ''} — </div> — <!-- SUMMARY STATS --> — <div class="grid-3" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Funds Disbursed</div> — <div class="stat-value">$${totalDisbursed.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Receipts Submitted</div> — <div class="stat-value amber">$${totalReceipts.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">${netOwed > 0 ? 'Driver Owes Back' : netOwed < 0 ? 'Company Owes Driver' : 'Balanced'}</div> — <div class="stat-value ${netOwed > 0 ? 'red' : netOwed < 0 ? 'amber' : ''}"> — ${netOwed === 0 ? '✓ $0.00' : (netOwed > 0 ? '+' : '') + '$' + Math.abs(netOwed).toFixed(2)} — </div> — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div class="card" style="margin-bottom:16px"> — <div class="card-header"><div class="card-title">JOB EXPENSE BREAKDOWN</div></div> — <table> — <thead><tr> — <th>Job ID</th><th>Client</th><th>Driver</th> — <th style="text-align:right">Funds Sent</th> — <th style="text-align:right">Receipts</th> — <th style="text-align:right">Net</th> — <th>Status</th> — </tr></thead> — <tbody> — ${myJobs.length ? myJobs.map(j => { — const jobDisb = myDisbursements.filter(d => d.jobId === j.id); — const jobRec  = myReceipts.filter(r => r.jobId === j.id); — const sent    = jobDisb.reduce((s,d) => s + (d.amount||0), 0); — const spent   = jobRec.reduce((s,r) => s + (r.amount||0), 0); — const net     = spent - sent; — return `<tr onclick="openJobExpenseDetail('${j.id}')" style="cursor:pointer"> — <td><span class="tag">${j.id}</span></td> — <td>${j.client}</td> — <td class="info-text">${j.driver || '—'}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--green)">$${sent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--amber)">$${spent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);font-weight:700;color:${net > 0 ? 'var(--red)' : net < 0 ? 'var(--amber)' : 'var(--green)'}"> — ${net === 0 ? '✓ $0.00' : (net > 0 ? '+' : '') + '$' + Math.abs(net).toFixed(2)} — </td> — <td><span class="status-badge status-${j.status}">${j.status}</span></td> — </tr>`; — }).join('') : `<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--gray);font-family:var(--font-mono)">No jobs found</td></tr>`} — </tbody> — </table> — </div> — <!-- DISBURSEMENTS LOG --> — <div class="grid-2"> — <div class="card"> — <div class="card-header"> — <div class="card-title">💰 FUNDS DISBURSED</div> — ${isAdmin() ? `<button class="btn-green" style="padding:7px 14px;font-size:11px" onclick="openDisbursementModal()">+ SEND FUNDS</button>` : ''} — </div> — ${myDisbursements.length ? myDisbursements.map(d => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${d.driver} <span class="tag">${d.jobId}</span></div> — <div class="info-text">${d.date} · ${d.method || 'Cash'} · ${d.notes || ''}</div> — <div class="info-text" style="color:var(--gray)">${d.sentBy ? 'Sent by: ' + d.sentBy : ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--green)">$${(d.amount||0).toFixed(2)}</div> — ${role === 'office' ? `<button class="btn-sm" style="margin-top:4px;color:var(--red);border-color:#552222;font-size:10px" onclick="event.stopPropagation();deleteDisbursementEntry('${d.id}')">DELETE</button>` : ''} — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No funds disbursed yet.</p>`} — ${myDisbursements.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Disbursed</span> — <span style="font-family:var(--font-mono);color:var(--green);font-size:14px">$${totalDisbursed.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="card"> — <div class="card-header"><div class="card-title">🧾 RECEIPTS SUBMITTED</div></div> — ${myReceipts.length ? myReceipts.map(r => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${r.type} <span class="tag">${r.jobId}</span></div> — <div class="info-text">${r.driver} · ${r.date} · ${r.notes || ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--amber)">$${(r.amount||0).toFixed(2)}</div> — <div style="margin-top:2px">${r.approved — ? '<span style="color:var(--green);font-size:10px;font-family:var(--font-mono)">✓ APPROVED</span>' — : '<span style="color:var(--amber);font-size:10px;font-family:var(--font-mono)">⏳ PENDING</span>'}</div> — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No receipts submitted yet.</p>`} — ${myReceipts.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Receipts</span> — <span style="font-family:var(--font-mono);color:var(--amber);font-size:14px">$${totalReceipts.toFixed(2)}</span> — </div>` : ''} — </div> — </div>`; — content.innerHTML = html; — } — function openJobExpenseDetail(jobId) { — Switch to jobs tab and open the job detail — closeModal('modal-calday'); — openJobDetail(jobId); — } — function openDisbursementModal(prefillJobId) { — Pre-fill job ID if provided — document.getElementById('disb-jobid').value = prefillJobId || ''; — document.getElementById('disb-driver').value = ''; — document.getElementById('disb-amount').value = ''; — document.getElementById('disb-method').value = getSetting('defaultPay', 'Cash'); — document.getElementById('disb-notes').value = ''; — Populate driver dropdown from users — populateDisbDriverDropdown(); — openModal('modal-disbursement'); — } — async function populateDisbDriverDropdown() { — const sel = document.getElementById('disb-driver'); — sel.innerHTML = '<option value="">-- Select Driver --</option>'; — try { — const snap = await db.collection(COL_USERS) — .where('role', 'in', ['driver','helper']) — .get(); — snap.docs.forEach(d => { — const u = d.data(); — if (u.active) { — const opt = document.createElement('option'); — opt.value = JSON.stringify({ name: u.displayName, username: u.username }); — opt.textContent = u.displayName + ' (' + u.role + ')'; — sel.appendChild(opt); — } — }); — } catch(e) { — Fallback — type name manually — sel.innerHTML = '<option value="">Type driver name in notes</option>'; — } — } — async function saveDisbursementEntry() { — const jobId   = document.getElementById('disb-jobid').value.trim().toUpperCase(); — const driverRaw = document.getElementById('disb-driver').value; — const amount  = parseFloat(document.getElementById('disb-amount').value); — const method  = document.getElementById('disb-method').value; — const notes   = document.getElementById('disb-notes').value.trim(); — const btn     = document.getElementById('disb-submit-btn'); — if (!jobId)   { notify('⚠ Job ID is required', true); return; } — if (!driverRaw) { notify('⚠ Select a driver', true); return; } — if (isNaN(amount) || amount <= 0) { notify('⚠ Enter a valid amount', true); return; } — if (btn.disabled) return; — btn.disabled = true; — btn.textContent = 'SAVING...'; — try { — let driverName = driverRaw, driverUser = ''; — try { — const parsed = JSON.parse(driverRaw); — driverName = parsed.name; — driverUser = parsed.username; — } catch(e) { driverName = driverRaw; } — const disb = { — id:         'DISB-' + Date.now(), — jobId, — driver:     driverName, — driverUser: driverUser, — amount, — method, — notes, — sentBy:     currentUser.name, — date:       getTodayStr(), — createdAt:  new Date().toISOString() — }; — await saveDisbursement(disb); — closeModal('modal-disbursement'); — notify('$' + amount.toFixed(2) + ' disbursed to ' + driverName + ' ✓'); — Notify the driver — try { — if (driverUser) { — await sendNotification(driverUser, driverName, 'funds_sent', — 'Funds Sent 💰', — '$' + amount.toFixed(2) + ' sent via ' + method + ' for ' + jobId + (notes ? ' · ' + notes : '') + ' — from ' + currentUser.name, — { jobId, amount } — ); — } — } catch(e) {} — } catch(e) { — notify('⚠ Error: ' + e.message, true); — } finally { — btn.disabled = false; — btn.textContent = 'SEND FUNDS'; — } — } — async function deleteDisbursementEntry(disbId) { — if (!confirm('Delete this disbursement? This cannot be undone.')) return; — try { — await db.collection(COL_DISBURSEMENTS).doc(disbId).delete(); — notify('Disbursement deleted ✓'); — } catch(e) { notify('⚠ ' + e.message, true); } — } | 1 | 1 | 0 | no |
| `openJobExpenseDetail` | function | 17061 | function renderExpenses() { — const content = document.getElementById('main-content'); — const role = currentUser.role; — const isDriver = role === 'driver'; — Scope to this driver's jobs/disbursements if not office — const myJobs = isDriver — ? STATE.jobs.filter(j => nameIncludes(j.driver, currentUser.name)) — : STATE.jobs; — const myDisbursements = isDriver — ? STATE.disbursements.filter(d => — d.driverUser === currentUser.username || — (d.driver && d.driver.toLowerCase().includes(currentUser.name.toLowerCase())) — ) — : STATE.disbursements; — const myReceipts = isDriver — ? STATE.receipts.filter(r => { — const nm = r.driver && nameIncludes(r.driver, currentUser.name); — const um = r.driverUser && r.driverUser === currentUser.username; — return nm || um; — }) — : STATE.receipts; — Totals — const totalDisbursed = myDisbursements.reduce((s,d) => s + (d.amount||0), 0); — const totalReceipts  = myReceipts.reduce((s,r) => s + (r.amount||0), 0); — const netOwed        = totalReceipts - totalDisbursed; // positive = driver owes, negative = company owes driver — let html = ` — <div class="section-header"> — <div> — <div class="section-title">EXPENSE TRACKER</div> — <div class="section-sub">${isDriver ? 'Your funds & receipts · ' + currentUser.name : 'All driver funds & expense reconciliation'}</div> — </div> — ${isAdmin() ? `<button class="btn-green" onclick="openDisbursementModal()">💰 SEND FUNDS</button>` : ''} — </div> — <!-- SUMMARY STATS --> — <div class="grid-3" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Funds Disbursed</div> — <div class="stat-value">$${totalDisbursed.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Receipts Submitted</div> — <div class="stat-value amber">$${totalReceipts.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">${netOwed > 0 ? 'Driver Owes Back' : netOwed < 0 ? 'Company Owes Driver' : 'Balanced'}</div> — <div class="stat-value ${netOwed > 0 ? 'red' : netOwed < 0 ? 'amber' : ''}"> — ${netOwed === 0 ? '✓ $0.00' : (netOwed > 0 ? '+' : '') + '$' + Math.abs(netOwed).toFixed(2)} — </div> — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div class="card" style="margin-bottom:16px"> — <div class="card-header"><div class="card-title">JOB EXPENSE BREAKDOWN</div></div> — <table> — <thead><tr> — <th>Job ID</th><th>Client</th><th>Driver</th> — <th style="text-align:right">Funds Sent</th> — <th style="text-align:right">Receipts</th> — <th style="text-align:right">Net</th> — <th>Status</th> — </tr></thead> — <tbody> — ${myJobs.length ? myJobs.map(j => { — const jobDisb = myDisbursements.filter(d => d.jobId === j.id); — const jobRec  = myReceipts.filter(r => r.jobId === j.id); — const sent    = jobDisb.reduce((s,d) => s + (d.amount||0), 0); — const spent   = jobRec.reduce((s,r) => s + (r.amount||0), 0); — const net     = spent - sent; — return `<tr onclick="openJobExpenseDetail('${j.id}')" style="cursor:pointer"> — <td><span class="tag">${j.id}</span></td> — <td>${j.client}</td> — <td class="info-text">${j.driver || '—'}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--green)">$${sent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--amber)">$${spent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);font-weight:700;color:${net > 0 ? 'var(--red)' : net < 0 ? 'var(--amber)' : 'var(--green)'}"> — ${net === 0 ? '✓ $0.00' : (net > 0 ? '+' : '') + '$' + Math.abs(net).toFixed(2)} — </td> — <td><span class="status-badge status-${j.status}">${j.status}</span></td> — </tr>`; — }).join('') : `<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--gray);font-family:var(--font-mono)">No jobs found</td></tr>`} — </tbody> — </table> — </div> — <!-- DISBURSEMENTS LOG --> — <div class="grid-2"> — <div class="card"> — <div class="card-header"> — <div class="card-title">💰 FUNDS DISBURSED</div> — ${isAdmin() ? `<button class="btn-green" style="padding:7px 14px;font-size:11px" onclick="openDisbursementModal()">+ SEND FUNDS</button>` : ''} — </div> — ${myDisbursements.length ? myDisbursements.map(d => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${d.driver} <span class="tag">${d.jobId}</span></div> — <div class="info-text">${d.date} · ${d.method || 'Cash'} · ${d.notes || ''}</div> — <div class="info-text" style="color:var(--gray)">${d.sentBy ? 'Sent by: ' + d.sentBy : ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--green)">$${(d.amount||0).toFixed(2)}</div> — ${role === 'office' ? `<button class="btn-sm" style="margin-top:4px;color:var(--red);border-color:#552222;font-size:10px" onclick="event.stopPropagation();deleteDisbursementEntry('${d.id}')">DELETE</button>` : ''} — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No funds disbursed yet.</p>`} — ${myDisbursements.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Disbursed</span> — <span style="font-family:var(--font-mono);color:var(--green);font-size:14px">$${totalDisbursed.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="card"> — <div class="card-header"><div class="card-title">🧾 RECEIPTS SUBMITTED</div></div> — ${myReceipts.length ? myReceipts.map(r => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${r.type} <span class="tag">${r.jobId}</span></div> — <div class="info-text">${r.driver} · ${r.date} · ${r.notes || ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--amber)">$${(r.amount||0).toFixed(2)}</div> — <div style="margin-top:2px">${r.approved — ? '<span style="color:var(--green);font-size:10px;font-family:var(--font-mono)">✓ APPROVED</span>' — : '<span style="color:var(--amber);font-size:10px;font-family:var(--font-mono)">⏳ PENDING</span>'}</div> — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No receipts submitted yet.</p>`} — ${myReceipts.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Receipts</span> — <span style="font-family:var(--font-mono);color:var(--amber);font-size:14px">$${totalReceipts.toFixed(2)}</span> — </div>` : ''} — </div> — </div>`; — content.innerHTML = html; — } — function openJobExpenseDetail(jobId) { — Switch to jobs tab and open the job detail — closeModal('modal-calday'); — openJobDetail(jobId); — } — function openDisbursementModal(prefillJobId) { — Pre-fill job ID if provided — document.getElementById('disb-jobid').value = prefillJobId || ''; — document.getElementById('disb-driver').value = ''; — document.getElementById('disb-amount').value = ''; — document.getElementById('disb-method').value = getSetting('defaultPay', 'Cash'); — document.getElementById('disb-notes').value = ''; — Populate driver dropdown from users — populateDisbDriverDropdown(); — openModal('modal-disbursement'); — } — async function populateDisbDriverDropdown() { — const sel = document.getElementById('disb-driver'); — sel.innerHTML = '<option value="">-- Select Driver --</option>'; — try { — const snap = await db.collection(COL_USERS) — .where('role', 'in', ['driver','helper']) — .get(); — snap.docs.forEach(d => { — const u = d.data(); — if (u.active) { — const opt = document.createElement('option'); — opt.value = JSON.stringify({ name: u.displayName, username: u.username }); — opt.textContent = u.displayName + ' (' + u.role + ')'; — sel.appendChild(opt); — } — }); — } catch(e) { — Fallback — type name manually — sel.innerHTML = '<option value="">Type driver name in notes</option>'; — } — } — async function saveDisbursementEntry() { — const jobId   = document.getElementById('disb-jobid').value.trim().toUpperCase(); — const driverRaw = document.getElementById('disb-driver').value; — const amount  = parseFloat(document.getElementById('disb-amount').value); — const method  = document.getElementById('disb-method').value; — const notes   = document.getElementById('disb-notes').value.trim(); — const btn     = document.getElementById('disb-submit-btn'); — if (!jobId)   { notify('⚠ Job ID is required', true); return; } — if (!driverRaw) { notify('⚠ Select a driver', true); return; } — if (isNaN(amount) || amount <= 0) { notify('⚠ Enter a valid amount', true); return; } — if (btn.disabled) return; — btn.disabled = true; — btn.textContent = 'SAVING...'; — try { — let driverName = driverRaw, driverUser = ''; — try { — const parsed = JSON.parse(driverRaw); — driverName = parsed.name; — driverUser = parsed.username; — } catch(e) { driverName = driverRaw; } — const disb = { — id:         'DISB-' + Date.now(), — jobId, — driver:     driverName, — driverUser: driverUser, — amount, — method, — notes, — sentBy:     currentUser.name, — date:       getTodayStr(), — createdAt:  new Date().toISOString() — }; — await saveDisbursement(disb); — closeModal('modal-disbursement'); — notify('$' + amount.toFixed(2) + ' disbursed to ' + driverName + ' ✓'); — Notify the driver — try { — if (driverUser) { — await sendNotification(driverUser, driverName, 'funds_sent', — 'Funds Sent 💰', — '$' + amount.toFixed(2) + ' sent via ' + method + ' for ' + jobId + (notes ? ' · ' + notes : '') + ' — from ' + currentUser.name, — { jobId, amount } — ); — } — } catch(e) {} — } catch(e) { — notify('⚠ Error: ' + e.message, true); — } finally { — btn.disabled = false; — btn.textContent = 'SEND FUNDS'; — } — } — async function deleteDisbursementEntry(disbId) { — if (!confirm('Delete this disbursement? This cannot be undone.')) return; — try { — await db.collection(COL_DISBURSEMENTS).doc(disbId).delete(); — notify('Disbursement deleted ✓'); — } catch(e) { notify('⚠ ' + e.message, true); } — } | 1 | 1 | 0 | yes |
| `openDisbursementModal` | function | 17067 | function renderExpenses() { — const content = document.getElementById('main-content'); — const role = currentUser.role; — const isDriver = role === 'driver'; — Scope to this driver's jobs/disbursements if not office — const myJobs = isDriver — ? STATE.jobs.filter(j => nameIncludes(j.driver, currentUser.name)) — : STATE.jobs; — const myDisbursements = isDriver — ? STATE.disbursements.filter(d => — d.driverUser === currentUser.username || — (d.driver && d.driver.toLowerCase().includes(currentUser.name.toLowerCase())) — ) — : STATE.disbursements; — const myReceipts = isDriver — ? STATE.receipts.filter(r => { — const nm = r.driver && nameIncludes(r.driver, currentUser.name); — const um = r.driverUser && r.driverUser === currentUser.username; — return nm || um; — }) — : STATE.receipts; — Totals — const totalDisbursed = myDisbursements.reduce((s,d) => s + (d.amount||0), 0); — const totalReceipts  = myReceipts.reduce((s,r) => s + (r.amount||0), 0); — const netOwed        = totalReceipts - totalDisbursed; // positive = driver owes, negative = company owes driver — let html = ` — <div class="section-header"> — <div> — <div class="section-title">EXPENSE TRACKER</div> — <div class="section-sub">${isDriver ? 'Your funds & receipts · ' + currentUser.name : 'All driver funds & expense reconciliation'}</div> — </div> — ${isAdmin() ? `<button class="btn-green" onclick="openDisbursementModal()">💰 SEND FUNDS</button>` : ''} — </div> — <!-- SUMMARY STATS --> — <div class="grid-3" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Funds Disbursed</div> — <div class="stat-value">$${totalDisbursed.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Receipts Submitted</div> — <div class="stat-value amber">$${totalReceipts.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">${netOwed > 0 ? 'Driver Owes Back' : netOwed < 0 ? 'Company Owes Driver' : 'Balanced'}</div> — <div class="stat-value ${netOwed > 0 ? 'red' : netOwed < 0 ? 'amber' : ''}"> — ${netOwed === 0 ? '✓ $0.00' : (netOwed > 0 ? '+' : '') + '$' + Math.abs(netOwed).toFixed(2)} — </div> — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div class="card" style="margin-bottom:16px"> — <div class="card-header"><div class="card-title">JOB EXPENSE BREAKDOWN</div></div> — <table> — <thead><tr> — <th>Job ID</th><th>Client</th><th>Driver</th> — <th style="text-align:right">Funds Sent</th> — <th style="text-align:right">Receipts</th> — <th style="text-align:right">Net</th> — <th>Status</th> — </tr></thead> — <tbody> — ${myJobs.length ? myJobs.map(j => { — const jobDisb = myDisbursements.filter(d => d.jobId === j.id); — const jobRec  = myReceipts.filter(r => r.jobId === j.id); — const sent    = jobDisb.reduce((s,d) => s + (d.amount||0), 0); — const spent   = jobRec.reduce((s,r) => s + (r.amount||0), 0); — const net     = spent - sent; — return `<tr onclick="openJobExpenseDetail('${j.id}')" style="cursor:pointer"> — <td><span class="tag">${j.id}</span></td> — <td>${j.client}</td> — <td class="info-text">${j.driver || '—'}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--green)">$${sent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--amber)">$${spent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);font-weight:700;color:${net > 0 ? 'var(--red)' : net < 0 ? 'var(--amber)' : 'var(--green)'}"> — ${net === 0 ? '✓ $0.00' : (net > 0 ? '+' : '') + '$' + Math.abs(net).toFixed(2)} — </td> — <td><span class="status-badge status-${j.status}">${j.status}</span></td> — </tr>`; — }).join('') : `<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--gray);font-family:var(--font-mono)">No jobs found</td></tr>`} — </tbody> — </table> — </div> — <!-- DISBURSEMENTS LOG --> — <div class="grid-2"> — <div class="card"> — <div class="card-header"> — <div class="card-title">💰 FUNDS DISBURSED</div> — ${isAdmin() ? `<button class="btn-green" style="padding:7px 14px;font-size:11px" onclick="openDisbursementModal()">+ SEND FUNDS</button>` : ''} — </div> — ${myDisbursements.length ? myDisbursements.map(d => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${d.driver} <span class="tag">${d.jobId}</span></div> — <div class="info-text">${d.date} · ${d.method || 'Cash'} · ${d.notes || ''}</div> — <div class="info-text" style="color:var(--gray)">${d.sentBy ? 'Sent by: ' + d.sentBy : ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--green)">$${(d.amount||0).toFixed(2)}</div> — ${role === 'office' ? `<button class="btn-sm" style="margin-top:4px;color:var(--red);border-color:#552222;font-size:10px" onclick="event.stopPropagation();deleteDisbursementEntry('${d.id}')">DELETE</button>` : ''} — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No funds disbursed yet.</p>`} — ${myDisbursements.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Disbursed</span> — <span style="font-family:var(--font-mono);color:var(--green);font-size:14px">$${totalDisbursed.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="card"> — <div class="card-header"><div class="card-title">🧾 RECEIPTS SUBMITTED</div></div> — ${myReceipts.length ? myReceipts.map(r => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${r.type} <span class="tag">${r.jobId}</span></div> — <div class="info-text">${r.driver} · ${r.date} · ${r.notes || ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--amber)">$${(r.amount||0).toFixed(2)}</div> — <div style="margin-top:2px">${r.approved — ? '<span style="color:var(--green);font-size:10px;font-family:var(--font-mono)">✓ APPROVED</span>' — : '<span style="color:var(--amber);font-size:10px;font-family:var(--font-mono)">⏳ PENDING</span>'}</div> — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No receipts submitted yet.</p>`} — ${myReceipts.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Receipts</span> — <span style="font-family:var(--font-mono);color:var(--amber);font-size:14px">$${totalReceipts.toFixed(2)}</span> — </div>` : ''} — </div> — </div>`; — content.innerHTML = html; — } — function openJobExpenseDetail(jobId) { — Switch to jobs tab and open the job detail — closeModal('modal-calday'); — openJobDetail(jobId); — } — function openDisbursementModal(prefillJobId) { — Pre-fill job ID if provided — document.getElementById('disb-jobid').value = prefillJobId || ''; — document.getElementById('disb-driver').value = ''; — document.getElementById('disb-amount').value = ''; — document.getElementById('disb-method').value = getSetting('defaultPay', 'Cash'); — document.getElementById('disb-notes').value = ''; — Populate driver dropdown from users — populateDisbDriverDropdown(); — openModal('modal-disbursement'); — } — async function populateDisbDriverDropdown() { — const sel = document.getElementById('disb-driver'); — sel.innerHTML = '<option value="">-- Select Driver --</option>'; — try { — const snap = await db.collection(COL_USERS) — .where('role', 'in', ['driver','helper']) — .get(); — snap.docs.forEach(d => { — const u = d.data(); — if (u.active) { — const opt = document.createElement('option'); — opt.value = JSON.stringify({ name: u.displayName, username: u.username }); — opt.textContent = u.displayName + ' (' + u.role + ')'; — sel.appendChild(opt); — } — }); — } catch(e) { — Fallback — type name manually — sel.innerHTML = '<option value="">Type driver name in notes</option>'; — } — } — async function saveDisbursementEntry() { — const jobId   = document.getElementById('disb-jobid').value.trim().toUpperCase(); — const driverRaw = document.getElementById('disb-driver').value; — const amount  = parseFloat(document.getElementById('disb-amount').value); — const method  = document.getElementById('disb-method').value; — const notes   = document.getElementById('disb-notes').value.trim(); — const btn     = document.getElementById('disb-submit-btn'); — if (!jobId)   { notify('⚠ Job ID is required', true); return; } — if (!driverRaw) { notify('⚠ Select a driver', true); return; } — if (isNaN(amount) || amount <= 0) { notify('⚠ Enter a valid amount', true); return; } — if (btn.disabled) return; — btn.disabled = true; — btn.textContent = 'SAVING...'; — try { — let driverName = driverRaw, driverUser = ''; — try { — const parsed = JSON.parse(driverRaw); — driverName = parsed.name; — driverUser = parsed.username; — } catch(e) { driverName = driverRaw; } — const disb = { — id:         'DISB-' + Date.now(), — jobId, — driver:     driverName, — driverUser: driverUser, — amount, — method, — notes, — sentBy:     currentUser.name, — date:       getTodayStr(), — createdAt:  new Date().toISOString() — }; — await saveDisbursement(disb); — closeModal('modal-disbursement'); — notify('$' + amount.toFixed(2) + ' disbursed to ' + driverName + ' ✓'); — Notify the driver — try { — if (driverUser) { — await sendNotification(driverUser, driverName, 'funds_sent', — 'Funds Sent 💰', — '$' + amount.toFixed(2) + ' sent via ' + method + ' for ' + jobId + (notes ? ' · ' + notes : '') + ' — from ' + currentUser.name, — { jobId, amount } — ); — } — } catch(e) {} — } catch(e) { — notify('⚠ Error: ' + e.message, true); — } finally { — btn.disabled = false; — btn.textContent = 'SEND FUNDS'; — } — } — async function deleteDisbursementEntry(disbId) { — if (!confirm('Delete this disbursement? This cannot be undone.')) return; — try { — await db.collection(COL_DISBURSEMENTS).doc(disbId).delete(); — notify('Disbursement deleted ✓'); — } catch(e) { notify('⚠ ' + e.message, true); } — } | 2 | 2 | 0 | yes |
| `populateDisbDriverDropdown` | function | 17079 | function renderExpenses() { — const content = document.getElementById('main-content'); — const role = currentUser.role; — const isDriver = role === 'driver'; — Scope to this driver's jobs/disbursements if not office — const myJobs = isDriver — ? STATE.jobs.filter(j => nameIncludes(j.driver, currentUser.name)) — : STATE.jobs; — const myDisbursements = isDriver — ? STATE.disbursements.filter(d => — d.driverUser === currentUser.username || — (d.driver && d.driver.toLowerCase().includes(currentUser.name.toLowerCase())) — ) — : STATE.disbursements; — const myReceipts = isDriver — ? STATE.receipts.filter(r => { — const nm = r.driver && nameIncludes(r.driver, currentUser.name); — const um = r.driverUser && r.driverUser === currentUser.username; — return nm || um; — }) — : STATE.receipts; — Totals — const totalDisbursed = myDisbursements.reduce((s,d) => s + (d.amount||0), 0); — const totalReceipts  = myReceipts.reduce((s,r) => s + (r.amount||0), 0); — const netOwed        = totalReceipts - totalDisbursed; // positive = driver owes, negative = company owes driver — let html = ` — <div class="section-header"> — <div> — <div class="section-title">EXPENSE TRACKER</div> — <div class="section-sub">${isDriver ? 'Your funds & receipts · ' + currentUser.name : 'All driver funds & expense reconciliation'}</div> — </div> — ${isAdmin() ? `<button class="btn-green" onclick="openDisbursementModal()">💰 SEND FUNDS</button>` : ''} — </div> — <!-- SUMMARY STATS --> — <div class="grid-3" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Funds Disbursed</div> — <div class="stat-value">$${totalDisbursed.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Receipts Submitted</div> — <div class="stat-value amber">$${totalReceipts.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">${netOwed > 0 ? 'Driver Owes Back' : netOwed < 0 ? 'Company Owes Driver' : 'Balanced'}</div> — <div class="stat-value ${netOwed > 0 ? 'red' : netOwed < 0 ? 'amber' : ''}"> — ${netOwed === 0 ? '✓ $0.00' : (netOwed > 0 ? '+' : '') + '$' + Math.abs(netOwed).toFixed(2)} — </div> — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div class="card" style="margin-bottom:16px"> — <div class="card-header"><div class="card-title">JOB EXPENSE BREAKDOWN</div></div> — <table> — <thead><tr> — <th>Job ID</th><th>Client</th><th>Driver</th> — <th style="text-align:right">Funds Sent</th> — <th style="text-align:right">Receipts</th> — <th style="text-align:right">Net</th> — <th>Status</th> — </tr></thead> — <tbody> — ${myJobs.length ? myJobs.map(j => { — const jobDisb = myDisbursements.filter(d => d.jobId === j.id); — const jobRec  = myReceipts.filter(r => r.jobId === j.id); — const sent    = jobDisb.reduce((s,d) => s + (d.amount||0), 0); — const spent   = jobRec.reduce((s,r) => s + (r.amount||0), 0); — const net     = spent - sent; — return `<tr onclick="openJobExpenseDetail('${j.id}')" style="cursor:pointer"> — <td><span class="tag">${j.id}</span></td> — <td>${j.client}</td> — <td class="info-text">${j.driver || '—'}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--green)">$${sent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--amber)">$${spent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);font-weight:700;color:${net > 0 ? 'var(--red)' : net < 0 ? 'var(--amber)' : 'var(--green)'}"> — ${net === 0 ? '✓ $0.00' : (net > 0 ? '+' : '') + '$' + Math.abs(net).toFixed(2)} — </td> — <td><span class="status-badge status-${j.status}">${j.status}</span></td> — </tr>`; — }).join('') : `<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--gray);font-family:var(--font-mono)">No jobs found</td></tr>`} — </tbody> — </table> — </div> — <!-- DISBURSEMENTS LOG --> — <div class="grid-2"> — <div class="card"> — <div class="card-header"> — <div class="card-title">💰 FUNDS DISBURSED</div> — ${isAdmin() ? `<button class="btn-green" style="padding:7px 14px;font-size:11px" onclick="openDisbursementModal()">+ SEND FUNDS</button>` : ''} — </div> — ${myDisbursements.length ? myDisbursements.map(d => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${d.driver} <span class="tag">${d.jobId}</span></div> — <div class="info-text">${d.date} · ${d.method || 'Cash'} · ${d.notes || ''}</div> — <div class="info-text" style="color:var(--gray)">${d.sentBy ? 'Sent by: ' + d.sentBy : ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--green)">$${(d.amount||0).toFixed(2)}</div> — ${role === 'office' ? `<button class="btn-sm" style="margin-top:4px;color:var(--red);border-color:#552222;font-size:10px" onclick="event.stopPropagation();deleteDisbursementEntry('${d.id}')">DELETE</button>` : ''} — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No funds disbursed yet.</p>`} — ${myDisbursements.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Disbursed</span> — <span style="font-family:var(--font-mono);color:var(--green);font-size:14px">$${totalDisbursed.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="card"> — <div class="card-header"><div class="card-title">🧾 RECEIPTS SUBMITTED</div></div> — ${myReceipts.length ? myReceipts.map(r => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${r.type} <span class="tag">${r.jobId}</span></div> — <div class="info-text">${r.driver} · ${r.date} · ${r.notes || ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--amber)">$${(r.amount||0).toFixed(2)}</div> — <div style="margin-top:2px">${r.approved — ? '<span style="color:var(--green);font-size:10px;font-family:var(--font-mono)">✓ APPROVED</span>' — : '<span style="color:var(--amber);font-size:10px;font-family:var(--font-mono)">⏳ PENDING</span>'}</div> — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No receipts submitted yet.</p>`} — ${myReceipts.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Receipts</span> — <span style="font-family:var(--font-mono);color:var(--amber);font-size:14px">$${totalReceipts.toFixed(2)}</span> — </div>` : ''} — </div> — </div>`; — content.innerHTML = html; — } — function openJobExpenseDetail(jobId) { — Switch to jobs tab and open the job detail — closeModal('modal-calday'); — openJobDetail(jobId); — } — function openDisbursementModal(prefillJobId) { — Pre-fill job ID if provided — document.getElementById('disb-jobid').value = prefillJobId || ''; — document.getElementById('disb-driver').value = ''; — document.getElementById('disb-amount').value = ''; — document.getElementById('disb-method').value = getSetting('defaultPay', 'Cash'); — document.getElementById('disb-notes').value = ''; — Populate driver dropdown from users — populateDisbDriverDropdown(); — openModal('modal-disbursement'); — } — async function populateDisbDriverDropdown() { — const sel = document.getElementById('disb-driver'); — sel.innerHTML = '<option value="">-- Select Driver --</option>'; — try { — const snap = await db.collection(COL_USERS) — .where('role', 'in', ['driver','helper']) — .get(); — snap.docs.forEach(d => { — const u = d.data(); — if (u.active) { — const opt = document.createElement('option'); — opt.value = JSON.stringify({ name: u.displayName, username: u.username }); — opt.textContent = u.displayName + ' (' + u.role + ')'; — sel.appendChild(opt); — } — }); — } catch(e) { — Fallback — type name manually — sel.innerHTML = '<option value="">Type driver name in notes</option>'; — } — } — async function saveDisbursementEntry() { — const jobId   = document.getElementById('disb-jobid').value.trim().toUpperCase(); — const driverRaw = document.getElementById('disb-driver').value; — const amount  = parseFloat(document.getElementById('disb-amount').value); — const method  = document.getElementById('disb-method').value; — const notes   = document.getElementById('disb-notes').value.trim(); — const btn     = document.getElementById('disb-submit-btn'); — if (!jobId)   { notify('⚠ Job ID is required', true); return; } — if (!driverRaw) { notify('⚠ Select a driver', true); return; } — if (isNaN(amount) || amount <= 0) { notify('⚠ Enter a valid amount', true); return; } — if (btn.disabled) return; — btn.disabled = true; — btn.textContent = 'SAVING...'; — try { — let driverName = driverRaw, driverUser = ''; — try { — const parsed = JSON.parse(driverRaw); — driverName = parsed.name; — driverUser = parsed.username; — } catch(e) { driverName = driverRaw; } — const disb = { — id:         'DISB-' + Date.now(), — jobId, — driver:     driverName, — driverUser: driverUser, — amount, — method, — notes, — sentBy:     currentUser.name, — date:       getTodayStr(), — createdAt:  new Date().toISOString() — }; — await saveDisbursement(disb); — closeModal('modal-disbursement'); — notify('$' + amount.toFixed(2) + ' disbursed to ' + driverName + ' ✓'); — Notify the driver — try { — if (driverUser) { — await sendNotification(driverUser, driverName, 'funds_sent', — 'Funds Sent 💰', — '$' + amount.toFixed(2) + ' sent via ' + method + ' for ' + jobId + (notes ? ' · ' + notes : '') + ' — from ' + currentUser.name, — { jobId, amount } — ); — } — } catch(e) {} — } catch(e) { — notify('⚠ Error: ' + e.message, true); — } finally { — btn.disabled = false; — btn.textContent = 'SEND FUNDS'; — } — } — async function deleteDisbursementEntry(disbId) { — if (!confirm('Delete this disbursement? This cannot be undone.')) return; — try { — await db.collection(COL_DISBURSEMENTS).doc(disbId).delete(); — notify('Disbursement deleted ✓'); — } catch(e) { notify('⚠ ' + e.message, true); } — } | 1 | 1 | 0 | no |
| `saveDisbursementEntry` | function | 17101 | function renderExpenses() { — const content = document.getElementById('main-content'); — const role = currentUser.role; — const isDriver = role === 'driver'; — Scope to this driver's jobs/disbursements if not office — const myJobs = isDriver — ? STATE.jobs.filter(j => nameIncludes(j.driver, currentUser.name)) — : STATE.jobs; — const myDisbursements = isDriver — ? STATE.disbursements.filter(d => — d.driverUser === currentUser.username || — (d.driver && d.driver.toLowerCase().includes(currentUser.name.toLowerCase())) — ) — : STATE.disbursements; — const myReceipts = isDriver — ? STATE.receipts.filter(r => { — const nm = r.driver && nameIncludes(r.driver, currentUser.name); — const um = r.driverUser && r.driverUser === currentUser.username; — return nm || um; — }) — : STATE.receipts; — Totals — const totalDisbursed = myDisbursements.reduce((s,d) => s + (d.amount||0), 0); — const totalReceipts  = myReceipts.reduce((s,r) => s + (r.amount||0), 0); — const netOwed        = totalReceipts - totalDisbursed; // positive = driver owes, negative = company owes driver — let html = ` — <div class="section-header"> — <div> — <div class="section-title">EXPENSE TRACKER</div> — <div class="section-sub">${isDriver ? 'Your funds & receipts · ' + currentUser.name : 'All driver funds & expense reconciliation'}</div> — </div> — ${isAdmin() ? `<button class="btn-green" onclick="openDisbursementModal()">💰 SEND FUNDS</button>` : ''} — </div> — <!-- SUMMARY STATS --> — <div class="grid-3" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Funds Disbursed</div> — <div class="stat-value">$${totalDisbursed.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Receipts Submitted</div> — <div class="stat-value amber">$${totalReceipts.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">${netOwed > 0 ? 'Driver Owes Back' : netOwed < 0 ? 'Company Owes Driver' : 'Balanced'}</div> — <div class="stat-value ${netOwed > 0 ? 'red' : netOwed < 0 ? 'amber' : ''}"> — ${netOwed === 0 ? '✓ $0.00' : (netOwed > 0 ? '+' : '') + '$' + Math.abs(netOwed).toFixed(2)} — </div> — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div class="card" style="margin-bottom:16px"> — <div class="card-header"><div class="card-title">JOB EXPENSE BREAKDOWN</div></div> — <table> — <thead><tr> — <th>Job ID</th><th>Client</th><th>Driver</th> — <th style="text-align:right">Funds Sent</th> — <th style="text-align:right">Receipts</th> — <th style="text-align:right">Net</th> — <th>Status</th> — </tr></thead> — <tbody> — ${myJobs.length ? myJobs.map(j => { — const jobDisb = myDisbursements.filter(d => d.jobId === j.id); — const jobRec  = myReceipts.filter(r => r.jobId === j.id); — const sent    = jobDisb.reduce((s,d) => s + (d.amount||0), 0); — const spent   = jobRec.reduce((s,r) => s + (r.amount||0), 0); — const net     = spent - sent; — return `<tr onclick="openJobExpenseDetail('${j.id}')" style="cursor:pointer"> — <td><span class="tag">${j.id}</span></td> — <td>${j.client}</td> — <td class="info-text">${j.driver || '—'}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--green)">$${sent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--amber)">$${spent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);font-weight:700;color:${net > 0 ? 'var(--red)' : net < 0 ? 'var(--amber)' : 'var(--green)'}"> — ${net === 0 ? '✓ $0.00' : (net > 0 ? '+' : '') + '$' + Math.abs(net).toFixed(2)} — </td> — <td><span class="status-badge status-${j.status}">${j.status}</span></td> — </tr>`; — }).join('') : `<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--gray);font-family:var(--font-mono)">No jobs found</td></tr>`} — </tbody> — </table> — </div> — <!-- DISBURSEMENTS LOG --> — <div class="grid-2"> — <div class="card"> — <div class="card-header"> — <div class="card-title">💰 FUNDS DISBURSED</div> — ${isAdmin() ? `<button class="btn-green" style="padding:7px 14px;font-size:11px" onclick="openDisbursementModal()">+ SEND FUNDS</button>` : ''} — </div> — ${myDisbursements.length ? myDisbursements.map(d => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${d.driver} <span class="tag">${d.jobId}</span></div> — <div class="info-text">${d.date} · ${d.method || 'Cash'} · ${d.notes || ''}</div> — <div class="info-text" style="color:var(--gray)">${d.sentBy ? 'Sent by: ' + d.sentBy : ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--green)">$${(d.amount||0).toFixed(2)}</div> — ${role === 'office' ? `<button class="btn-sm" style="margin-top:4px;color:var(--red);border-color:#552222;font-size:10px" onclick="event.stopPropagation();deleteDisbursementEntry('${d.id}')">DELETE</button>` : ''} — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No funds disbursed yet.</p>`} — ${myDisbursements.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Disbursed</span> — <span style="font-family:var(--font-mono);color:var(--green);font-size:14px">$${totalDisbursed.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="card"> — <div class="card-header"><div class="card-title">🧾 RECEIPTS SUBMITTED</div></div> — ${myReceipts.length ? myReceipts.map(r => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${r.type} <span class="tag">${r.jobId}</span></div> — <div class="info-text">${r.driver} · ${r.date} · ${r.notes || ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--amber)">$${(r.amount||0).toFixed(2)}</div> — <div style="margin-top:2px">${r.approved — ? '<span style="color:var(--green);font-size:10px;font-family:var(--font-mono)">✓ APPROVED</span>' — : '<span style="color:var(--amber);font-size:10px;font-family:var(--font-mono)">⏳ PENDING</span>'}</div> — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No receipts submitted yet.</p>`} — ${myReceipts.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Receipts</span> — <span style="font-family:var(--font-mono);color:var(--amber);font-size:14px">$${totalReceipts.toFixed(2)}</span> — </div>` : ''} — </div> — </div>`; — content.innerHTML = html; — } — function openJobExpenseDetail(jobId) { — Switch to jobs tab and open the job detail — closeModal('modal-calday'); — openJobDetail(jobId); — } — function openDisbursementModal(prefillJobId) { — Pre-fill job ID if provided — document.getElementById('disb-jobid').value = prefillJobId || ''; — document.getElementById('disb-driver').value = ''; — document.getElementById('disb-amount').value = ''; — document.getElementById('disb-method').value = getSetting('defaultPay', 'Cash'); — document.getElementById('disb-notes').value = ''; — Populate driver dropdown from users — populateDisbDriverDropdown(); — openModal('modal-disbursement'); — } — async function populateDisbDriverDropdown() { — const sel = document.getElementById('disb-driver'); — sel.innerHTML = '<option value="">-- Select Driver --</option>'; — try { — const snap = await db.collection(COL_USERS) — .where('role', 'in', ['driver','helper']) — .get(); — snap.docs.forEach(d => { — const u = d.data(); — if (u.active) { — const opt = document.createElement('option'); — opt.value = JSON.stringify({ name: u.displayName, username: u.username }); — opt.textContent = u.displayName + ' (' + u.role + ')'; — sel.appendChild(opt); — } — }); — } catch(e) { — Fallback — type name manually — sel.innerHTML = '<option value="">Type driver name in notes</option>'; — } — } — async function saveDisbursementEntry() { — const jobId   = document.getElementById('disb-jobid').value.trim().toUpperCase(); — const driverRaw = document.getElementById('disb-driver').value; — const amount  = parseFloat(document.getElementById('disb-amount').value); — const method  = document.getElementById('disb-method').value; — const notes   = document.getElementById('disb-notes').value.trim(); — const btn     = document.getElementById('disb-submit-btn'); — if (!jobId)   { notify('⚠ Job ID is required', true); return; } — if (!driverRaw) { notify('⚠ Select a driver', true); return; } — if (isNaN(amount) || amount <= 0) { notify('⚠ Enter a valid amount', true); return; } — if (btn.disabled) return; — btn.disabled = true; — btn.textContent = 'SAVING...'; — try { — let driverName = driverRaw, driverUser = ''; — try { — const parsed = JSON.parse(driverRaw); — driverName = parsed.name; — driverUser = parsed.username; — } catch(e) { driverName = driverRaw; } — const disb = { — id:         'DISB-' + Date.now(), — jobId, — driver:     driverName, — driverUser: driverUser, — amount, — method, — notes, — sentBy:     currentUser.name, — date:       getTodayStr(), — createdAt:  new Date().toISOString() — }; — await saveDisbursement(disb); — closeModal('modal-disbursement'); — notify('$' + amount.toFixed(2) + ' disbursed to ' + driverName + ' ✓'); — Notify the driver — try { — if (driverUser) { — await sendNotification(driverUser, driverName, 'funds_sent', — 'Funds Sent 💰', — '$' + amount.toFixed(2) + ' sent via ' + method + ' for ' + jobId + (notes ? ' · ' + notes : '') + ' — from ' + currentUser.name, — { jobId, amount } — ); — } — } catch(e) {} — } catch(e) { — notify('⚠ Error: ' + e.message, true); — } finally { — btn.disabled = false; — btn.textContent = 'SEND FUNDS'; — } — } — async function deleteDisbursementEntry(disbId) { — if (!confirm('Delete this disbursement? This cannot be undone.')) return; — try { — await db.collection(COL_DISBURSEMENTS).doc(disbId).delete(); — notify('Disbursement deleted ✓'); — } catch(e) { notify('⚠ ' + e.message, true); } — } | 1 | 1 | 0 | yes |
| `deleteDisbursementEntry` | function | 17160 | function renderExpenses() { — const content = document.getElementById('main-content'); — const role = currentUser.role; — const isDriver = role === 'driver'; — Scope to this driver's jobs/disbursements if not office — const myJobs = isDriver — ? STATE.jobs.filter(j => nameIncludes(j.driver, currentUser.name)) — : STATE.jobs; — const myDisbursements = isDriver — ? STATE.disbursements.filter(d => — d.driverUser === currentUser.username || — (d.driver && d.driver.toLowerCase().includes(currentUser.name.toLowerCase())) — ) — : STATE.disbursements; — const myReceipts = isDriver — ? STATE.receipts.filter(r => { — const nm = r.driver && nameIncludes(r.driver, currentUser.name); — const um = r.driverUser && r.driverUser === currentUser.username; — return nm || um; — }) — : STATE.receipts; — Totals — const totalDisbursed = myDisbursements.reduce((s,d) => s + (d.amount||0), 0); — const totalReceipts  = myReceipts.reduce((s,r) => s + (r.amount||0), 0); — const netOwed        = totalReceipts - totalDisbursed; // positive = driver owes, negative = company owes driver — let html = ` — <div class="section-header"> — <div> — <div class="section-title">EXPENSE TRACKER</div> — <div class="section-sub">${isDriver ? 'Your funds & receipts · ' + currentUser.name : 'All driver funds & expense reconciliation'}</div> — </div> — ${isAdmin() ? `<button class="btn-green" onclick="openDisbursementModal()">💰 SEND FUNDS</button>` : ''} — </div> — <!-- SUMMARY STATS --> — <div class="grid-3" style="margin-bottom:20px"> — <div class="stat-box"> — <div class="stat-label">Funds Disbursed</div> — <div class="stat-value">$${totalDisbursed.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">Receipts Submitted</div> — <div class="stat-value amber">$${totalReceipts.toFixed(2)}</div> — </div> — <div class="stat-box"> — <div class="stat-label">${netOwed > 0 ? 'Driver Owes Back' : netOwed < 0 ? 'Company Owes Driver' : 'Balanced'}</div> — <div class="stat-value ${netOwed > 0 ? 'red' : netOwed < 0 ? 'amber' : ''}"> — ${netOwed === 0 ? '✓ $0.00' : (netOwed > 0 ? '+' : '') + '$' + Math.abs(netOwed).toFixed(2)} — </div> — </div> — </div> — <!-- PER JOB BREAKDOWN --> — <div class="card" style="margin-bottom:16px"> — <div class="card-header"><div class="card-title">JOB EXPENSE BREAKDOWN</div></div> — <table> — <thead><tr> — <th>Job ID</th><th>Client</th><th>Driver</th> — <th style="text-align:right">Funds Sent</th> — <th style="text-align:right">Receipts</th> — <th style="text-align:right">Net</th> — <th>Status</th> — </tr></thead> — <tbody> — ${myJobs.length ? myJobs.map(j => { — const jobDisb = myDisbursements.filter(d => d.jobId === j.id); — const jobRec  = myReceipts.filter(r => r.jobId === j.id); — const sent    = jobDisb.reduce((s,d) => s + (d.amount||0), 0); — const spent   = jobRec.reduce((s,r) => s + (r.amount||0), 0); — const net     = spent - sent; — return `<tr onclick="openJobExpenseDetail('${j.id}')" style="cursor:pointer"> — <td><span class="tag">${j.id}</span></td> — <td>${j.client}</td> — <td class="info-text">${j.driver || '—'}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--green)">$${sent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);color:var(--amber)">$${spent.toFixed(2)}</td> — <td style="text-align:right;font-family:var(--font-mono);font-weight:700;color:${net > 0 ? 'var(--red)' : net < 0 ? 'var(--amber)' : 'var(--green)'}"> — ${net === 0 ? '✓ $0.00' : (net > 0 ? '+' : '') + '$' + Math.abs(net).toFixed(2)} — </td> — <td><span class="status-badge status-${j.status}">${j.status}</span></td> — </tr>`; — }).join('') : `<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--gray);font-family:var(--font-mono)">No jobs found</td></tr>`} — </tbody> — </table> — </div> — <!-- DISBURSEMENTS LOG --> — <div class="grid-2"> — <div class="card"> — <div class="card-header"> — <div class="card-title">💰 FUNDS DISBURSED</div> — ${isAdmin() ? `<button class="btn-green" style="padding:7px 14px;font-size:11px" onclick="openDisbursementModal()">+ SEND FUNDS</button>` : ''} — </div> — ${myDisbursements.length ? myDisbursements.map(d => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${d.driver} <span class="tag">${d.jobId}</span></div> — <div class="info-text">${d.date} · ${d.method || 'Cash'} · ${d.notes || ''}</div> — <div class="info-text" style="color:var(--gray)">${d.sentBy ? 'Sent by: ' + d.sentBy : ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--green)">$${(d.amount||0).toFixed(2)}</div> — ${role === 'office' ? `<button class="btn-sm" style="margin-top:4px;color:var(--red);border-color:#552222;font-size:10px" onclick="event.stopPropagation();deleteDisbursementEntry('${d.id}')">DELETE</button>` : ''} — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No funds disbursed yet.</p>`} — ${myDisbursements.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Disbursed</span> — <span style="font-family:var(--font-mono);color:var(--green);font-size:14px">$${totalDisbursed.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="card"> — <div class="card-header"><div class="card-title">🧾 RECEIPTS SUBMITTED</div></div> — ${myReceipts.length ? myReceipts.map(r => ` — <div class="fin-row"> — <div> — <div style="font-size:13px;font-weight:600">${r.type} <span class="tag">${r.jobId}</span></div> — <div class="info-text">${r.driver} · ${r.date} · ${r.notes || ''}</div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:18px;color:var(--amber)">$${(r.amount||0).toFixed(2)}</div> — <div style="margin-top:2px">${r.approved — ? '<span style="color:var(--green);font-size:10px;font-family:var(--font-mono)">✓ APPROVED</span>' — : '<span style="color:var(--amber);font-size:10px;font-family:var(--font-mono)">⏳ PENDING</span>'}</div> — </div> — </div>`).join('') : `<p class="info-text" style="padding:20px;text-align:center">No receipts submitted yet.</p>`} — ${myReceipts.length ? ` — <div style="border-top:1px solid var(--green3);padding-top:10px;margin-top:4px;display:flex;justify-content:space-between"> — <span class="info-text">Total Receipts</span> — <span style="font-family:var(--font-mono);color:var(--amber);font-size:14px">$${totalReceipts.toFixed(2)}</span> — </div>` : ''} — </div> — </div>`; — content.innerHTML = html; — } — function openJobExpenseDetail(jobId) { — Switch to jobs tab and open the job detail — closeModal('modal-calday'); — openJobDetail(jobId); — } — function openDisbursementModal(prefillJobId) { — Pre-fill job ID if provided — document.getElementById('disb-jobid').value = prefillJobId || ''; — document.getElementById('disb-driver').value = ''; — document.getElementById('disb-amount').value = ''; — document.getElementById('disb-method').value = getSetting('defaultPay', 'Cash'); — document.getElementById('disb-notes').value = ''; — Populate driver dropdown from users — populateDisbDriverDropdown(); — openModal('modal-disbursement'); — } — async function populateDisbDriverDropdown() { — const sel = document.getElementById('disb-driver'); — sel.innerHTML = '<option value="">-- Select Driver --</option>'; — try { — const snap = await db.collection(COL_USERS) — .where('role', 'in', ['driver','helper']) — .get(); — snap.docs.forEach(d => { — const u = d.data(); — if (u.active) { — const opt = document.createElement('option'); — opt.value = JSON.stringify({ name: u.displayName, username: u.username }); — opt.textContent = u.displayName + ' (' + u.role + ')'; — sel.appendChild(opt); — } — }); — } catch(e) { — Fallback — type name manually — sel.innerHTML = '<option value="">Type driver name in notes</option>'; — } — } — async function saveDisbursementEntry() { — const jobId   = document.getElementById('disb-jobid').value.trim().toUpperCase(); — const driverRaw = document.getElementById('disb-driver').value; — const amount  = parseFloat(document.getElementById('disb-amount').value); — const method  = document.getElementById('disb-method').value; — const notes   = document.getElementById('disb-notes').value.trim(); — const btn     = document.getElementById('disb-submit-btn'); — if (!jobId)   { notify('⚠ Job ID is required', true); return; } — if (!driverRaw) { notify('⚠ Select a driver', true); return; } — if (isNaN(amount) || amount <= 0) { notify('⚠ Enter a valid amount', true); return; } — if (btn.disabled) return; — btn.disabled = true; — btn.textContent = 'SAVING...'; — try { — let driverName = driverRaw, driverUser = ''; — try { — const parsed = JSON.parse(driverRaw); — driverName = parsed.name; — driverUser = parsed.username; — } catch(e) { driverName = driverRaw; } — const disb = { — id:         'DISB-' + Date.now(), — jobId, — driver:     driverName, — driverUser: driverUser, — amount, — method, — notes, — sentBy:     currentUser.name, — date:       getTodayStr(), — createdAt:  new Date().toISOString() — }; — await saveDisbursement(disb); — closeModal('modal-disbursement'); — notify('$' + amount.toFixed(2) + ' disbursed to ' + driverName + ' ✓'); — Notify the driver — try { — if (driverUser) { — await sendNotification(driverUser, driverName, 'funds_sent', — 'Funds Sent 💰', — '$' + amount.toFixed(2) + ' sent via ' + method + ' for ' + jobId + (notes ? ' · ' + notes : '') + ' — from ' + currentUser.name, — { jobId, amount } — ); — } — } catch(e) {} — } catch(e) { — notify('⚠ Error: ' + e.message, true); — } finally { — btn.disabled = false; — btn.textContent = 'SEND FUNDS'; — } — } — async function deleteDisbursementEntry(disbId) { — if (!confirm('Delete this disbursement? This cannot be undone.')) return; — try { — await db.collection(COL_DISBURSEMENTS).doc(disbId).delete(); — notify('Disbursement deleted ✓'); — } catch(e) { notify('⚠ ' + e.message, true); } — } | 1 | 1 | 0 | yes |
| `renderFleet` | function | 17172 | function renderFleet() { — const content = document.getElementById('main-content'); — const role    = currentUser.role; — const canEdit = role === 'office'; — let html = ` — <div class="section-header"> — <div> — <div class="section-title">🚚 FLEET MANAGEMENT</div> — <div class="section-sub">${STATE.fleet.length} vehicle(s) registered · Move Masters OS</div> — </div> — ${canEdit ? `<button class="btn-green" onclick="openFleetModal()">+ ADD VEHICLE</button>` : ''} — </div>`; — if (!STATE.fleet.length) { — html += `<div class="card" style="text-align:center;padding:40px"> — <div style="font-size:48px;margin-bottom:12px">🚚</div> — <p class="info-text">No vehicles registered yet.</p> — ${canEdit ? `<button class="btn-green" style="margin-top:16px" onclick="openFleetModal()">+ ADD FIRST VEHICLE</button>` : ''} — </div>`; — } else { — Summary stats — const active  = STATE.fleet.filter(v => v.status === 'active').length; — const maint   = STATE.fleet.filter(v => v.status === 'maintenance').length; — const offline = STATE.fleet.filter(v => v.status === 'offline').length; — html += `<div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Active</div><div class="stat-value">${active}</div></div> — <div class="stat-box"><div class="stat-label">In Maintenance</div><div class="stat-value amber">${maint}</div></div> — <div class="stat-box"><div class="stat-label">Offline</div><div class="stat-value red">${offline}</div></div> — </div>`; — STATE.fleet.forEach(v => { — const assignedJob = STATE.jobs.find(j => j.vehicleId === v.id && j.status === 'active'); — const statusColor = v.status === 'active' ? 'var(--green)' : v.status === 'maintenance' ? 'var(--amber)' : 'var(--red)'; — const maintDue    = v.nextMaint ? new Date(v.nextMaint) : null; — const maintOverdue = maintDue && maintDue < new Date(); — html += ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:12px"> — <div> — <div style="display:flex;align-items:center;gap:10px;margin-bottom:6px"> — <div style="font-size:28px">${v.type === 'box_truck' ? '🚛' : v.type === 'cargo_van' ? '🚐' : '🚚'}</div> — <div> — <div style="font-family:var(--font-mono);font-size:14px;font-weight:700;color:var(--white)">${v.name || v.id}</div> — <div class="info-text">${v.make||''} ${v.model||''} ${v.year||''} · ${v.plate||'No plate'}</div> — </div> — </div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:11px;color:${statusColor};letter-spacing:2px">${(v.status||'active').toUpperCase()}</div> — ${canEdit ? `<button class="btn-sm" style="margin-top:6px" onclick="openFleetModal('${v.id}')">✎ EDIT</button>` : ''} — </div> — </div> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(110px,1fr));gap:8px;margin-bottom:12px"> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Capacity</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.capacity || '—'}</div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Mileage</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.mileage ? v.mileage.toLocaleString() + ' mi' : '—'}</div> — </div> — <div class="stat-box" style="padding:8px;${maintOverdue ? 'border-color:var(--red)' : ''}"> — <div class="stat-label" style="font-size:9px">Next Service</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${maintOverdue ? 'var(--red)' : 'var(--white)'}"> — ${v.nextMaint || 'Not set'}${maintOverdue ? ' ⚠' : ''} — </div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Assigned Job</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${assignedJob ? 'var(--green)' : 'var(--gray)'}"> — ${assignedJob ? assignedJob.id : 'Available'} — </div> — </div> — </div> — ${v.notes ? `<div class="info-text" style="margin-bottom:10px">📝 ${v.notes}</div>` : ''} — <!-- MAINTENANCE LOG --> — ${v.maintLog && v.maintLog.length ? ` — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:6px">MAINTENANCE LOG</div> — ${v.maintLog.slice(-3).reverse().map(m => ` — <div style="display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px solid var(--gray2);font-size:12px"> — <div><span class="info-text">${m.date}</span> · ${m.work}</div> — <div style="font-family:var(--font-mono);color:var(--amber)">$${(m.cost||0).toFixed(0)}</div> — </div>`).join('')}` : ''} — ${canEdit ? ` — <div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap"> — <button class="btn-sm" onclick="logMaintenance('${v.id}')">🔧 LOG SERVICE</button> — <button class="btn-sm" onclick="updateMileage('${v.id}')">📍 UPDATE MILEAGE</button> — <select style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:5px 8px;font-family:var(--font-mono);font-size:10px;border-radius:3px" — onchange="updateVehicleStatus('${v.id}',this.value)"> — <option value="active"      ${v.status==='active'      ?'selected':''}>● Active</option> — <option value="maintenance" ${v.status==='maintenance' ?'selected':''}>🔧 Maintenance</option> — <option value="offline"     ${v.status==='offline'     ?'selected':''}>○ Offline</option> — </select> — </div>` : ''} — </div>`; — }); — } — content.innerHTML = html; — } — function openFleetModal(vehicleId) { — const v = vehicleId ? STATE.fleet.find(x => x.id === vehicleId) : null; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">${v ? 'EDIT VEHICLE — ' + v.id : 'ADD VEHICLE'}</div> — <div class="grid-2"> — <div class="form-group"><label>Vehicle Name / Unit #</label> — <input type="text" id="flt-name" value="${(v&&v.name)||''}" placeholder="e.g. Truck 1 · Unit 24" /></div> — <div class="form-group"><label>Type</label> — <select id="flt-type"> — <option value="box_truck" ${v?.type==='box_truck'?'selected':''}>🚛 Box Truck</option> — <option value="cargo_van" ${v?.type==='cargo_van'?'selected':''}>🚐 Cargo Van</option> — <option value="flatbed"   ${v?.type==='flatbed'  ?'selected':''}>🚚 Flatbed</option> — <option value="other"     ${v?.type==='other'    ?'selected':''}>📋 Other</option> — </select> — </div> — </div> — <div class="grid-3"> — <div class="form-group"><label>Make</label><input type="text" id="flt-make" value="${(v&&v.make)||''}" placeholder="Ford" /></div> — <div class="form-group"><label>Model</label><input type="text" id="flt-model" value="${(v&&v.model)||''}" placeholder="E-450" /></div> — <div class="form-group"><label>Year</label><input type="number" id="flt-year" value="${(v&&v.year)||''}" placeholder="2019" /></div> — </div> — <div class="grid-2"> — <div class="form-group"><label>License Plate</label><input type="text" id="flt-plate" value="${(v&&v.plate)||''}" placeholder="ABC-1234" style="text-transform:uppercase"/></div> — <div class="form-group"><label>Capacity (text label)</label><input type="text" id="flt-cap" value="${(v&&v.capacity)||''}" placeholder="26 ft · 1500 lbs" /></div> — </div> — <div class="grid-3"> — <div class="form-group"> — <label>Max Cubic Feet <span class="info-text">(for load optimization)</span></label> — <input type="number" id="flt-maxcuft" value="${(v&&v.maxCuFt)||''}" placeholder="e.g. 1200" min="0" step="50" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Fuel Efficiency (MPG) <span class="info-text">(for cost calc)</span></label> — <input type="number" id="flt-mpg" value="${(v&&v.mpg)||''}" placeholder="e.g. 10" min="1" step="0.5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Load Efficiency % <span class="info-text">(usable space)</span></label> — <input type="number" id="flt-loadeff" value="${(v&&v.loadEff)||85}" placeholder="85" min="50" max="100" step="5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — </div> — <div class="grid-2"> — <div class="form-group"><label>Current Mileage</label><input type="number" id="flt-miles" value="${(v&&v.mileage)||''}" placeholder="85000" /></div> — <div class="form-group"><label>Next Service Due</label><input type="date" id="flt-maint" value="${(v&&v.nextMaint)||''}" /></div> — </div> — <div class="form-group"><label>Notes</label> — <input type="text" id="flt-notes" value="${(v&&v.notes)||''}" placeholder="Insurance, registration, special equipment..." /></div> — <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:8px"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" onclick="saveVehicle('${vehicleId||''}')">SAVE VEHICLE</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveVehicle(vehicleId) { — const vehicle = { — id:        vehicleId || 'VEH-' + Date.now(), — name:      document.getElementById('flt-name').value.trim(), — type:      document.getElementById('flt-type').value, — make:      document.getElementById('flt-make').value.trim(), — model:     document.getElementById('flt-model').value.trim(), — year:      document.getElementById('flt-year').value, — plate:     document.getElementById('flt-plate').value.trim().toUpperCase(), — capacity:  document.getElementById('flt-cap').value.trim(), — maxCuFt:   parseFloat(document.getElementById('flt-maxcuft')?.value)||0, — mpg:       parseFloat(document.getElementById('flt-mpg')?.value)||0, — loadEff:   parseFloat(document.getElementById('flt-loadeff')?.value)||85, — mileage:   parseInt(document.getElementById('flt-miles').value)||0, — nextMaint: document.getElementById('flt-maint').value, — notes:     document.getElementById('flt-notes').value.trim(), — status:    vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.status||'active') : 'active', — maintLog:  vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.maintLog||[]) : [], — updatedAt: new Date().toISOString() — }; — if (!vehicle.name) { notify('⚠ Vehicle name is required', true); return; } — try { — await db.collection(COL_FLEET).doc(vehicle.id).set(vehicle); — notify((vehicleId ? 'Vehicle updated' : 'Vehicle added') + ' ✓'); — closeModal('modal-jobdetail'); — } catch(e) { notify('⚠ ' + e.message, true); } — } — async function logMaintenance(vehicleId) { — const work = prompt('Service performed:'); if (!work) return; — const cost = parseFloat(prompt('Cost ($):') || '0'); — const v    = STATE.fleet.find(x => x.id === vehicleId); — if (!v) return; — if (!v.maintLog) v.maintLog = []; — v.maintLog.push({ date: getTodayStr(), work, cost, loggedBy: currentUser.name }); — v.updatedAt = new Date().toISOString(); — await db.collection(COL_FLEET).doc(vehicleId).set(v); — notify('Maintenance logged ✓'); — } — async function updateMileage(vehicleId) { — const miles = prompt('Enter current mileage:'); if (!miles) return; — await db.collection(COL_FLEET).doc(vehicleId).update({ mileage: parseInt(miles)||0 }); — notify('Mileage updated ✓'); — } — async function updateVehicleStatus(vehicleId, status) { — await db.collection(COL_FLEET).doc(vehicleId).update({ status }); — notify('Vehicle status: ' + status.toUpperCase()); — } | 1 | 1 | 0 | no |
| `openFleetModal` | function | 17280 | function renderFleet() { — const content = document.getElementById('main-content'); — const role    = currentUser.role; — const canEdit = role === 'office'; — let html = ` — <div class="section-header"> — <div> — <div class="section-title">🚚 FLEET MANAGEMENT</div> — <div class="section-sub">${STATE.fleet.length} vehicle(s) registered · Move Masters OS</div> — </div> — ${canEdit ? `<button class="btn-green" onclick="openFleetModal()">+ ADD VEHICLE</button>` : ''} — </div>`; — if (!STATE.fleet.length) { — html += `<div class="card" style="text-align:center;padding:40px"> — <div style="font-size:48px;margin-bottom:12px">🚚</div> — <p class="info-text">No vehicles registered yet.</p> — ${canEdit ? `<button class="btn-green" style="margin-top:16px" onclick="openFleetModal()">+ ADD FIRST VEHICLE</button>` : ''} — </div>`; — } else { — Summary stats — const active  = STATE.fleet.filter(v => v.status === 'active').length; — const maint   = STATE.fleet.filter(v => v.status === 'maintenance').length; — const offline = STATE.fleet.filter(v => v.status === 'offline').length; — html += `<div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Active</div><div class="stat-value">${active}</div></div> — <div class="stat-box"><div class="stat-label">In Maintenance</div><div class="stat-value amber">${maint}</div></div> — <div class="stat-box"><div class="stat-label">Offline</div><div class="stat-value red">${offline}</div></div> — </div>`; — STATE.fleet.forEach(v => { — const assignedJob = STATE.jobs.find(j => j.vehicleId === v.id && j.status === 'active'); — const statusColor = v.status === 'active' ? 'var(--green)' : v.status === 'maintenance' ? 'var(--amber)' : 'var(--red)'; — const maintDue    = v.nextMaint ? new Date(v.nextMaint) : null; — const maintOverdue = maintDue && maintDue < new Date(); — html += ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:12px"> — <div> — <div style="display:flex;align-items:center;gap:10px;margin-bottom:6px"> — <div style="font-size:28px">${v.type === 'box_truck' ? '🚛' : v.type === 'cargo_van' ? '🚐' : '🚚'}</div> — <div> — <div style="font-family:var(--font-mono);font-size:14px;font-weight:700;color:var(--white)">${v.name || v.id}</div> — <div class="info-text">${v.make||''} ${v.model||''} ${v.year||''} · ${v.plate||'No plate'}</div> — </div> — </div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:11px;color:${statusColor};letter-spacing:2px">${(v.status||'active').toUpperCase()}</div> — ${canEdit ? `<button class="btn-sm" style="margin-top:6px" onclick="openFleetModal('${v.id}')">✎ EDIT</button>` : ''} — </div> — </div> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(110px,1fr));gap:8px;margin-bottom:12px"> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Capacity</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.capacity || '—'}</div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Mileage</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.mileage ? v.mileage.toLocaleString() + ' mi' : '—'}</div> — </div> — <div class="stat-box" style="padding:8px;${maintOverdue ? 'border-color:var(--red)' : ''}"> — <div class="stat-label" style="font-size:9px">Next Service</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${maintOverdue ? 'var(--red)' : 'var(--white)'}"> — ${v.nextMaint || 'Not set'}${maintOverdue ? ' ⚠' : ''} — </div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Assigned Job</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${assignedJob ? 'var(--green)' : 'var(--gray)'}"> — ${assignedJob ? assignedJob.id : 'Available'} — </div> — </div> — </div> — ${v.notes ? `<div class="info-text" style="margin-bottom:10px">📝 ${v.notes}</div>` : ''} — <!-- MAINTENANCE LOG --> — ${v.maintLog && v.maintLog.length ? ` — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:6px">MAINTENANCE LOG</div> — ${v.maintLog.slice(-3).reverse().map(m => ` — <div style="display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px solid var(--gray2);font-size:12px"> — <div><span class="info-text">${m.date}</span> · ${m.work}</div> — <div style="font-family:var(--font-mono);color:var(--amber)">$${(m.cost||0).toFixed(0)}</div> — </div>`).join('')}` : ''} — ${canEdit ? ` — <div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap"> — <button class="btn-sm" onclick="logMaintenance('${v.id}')">🔧 LOG SERVICE</button> — <button class="btn-sm" onclick="updateMileage('${v.id}')">📍 UPDATE MILEAGE</button> — <select style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:5px 8px;font-family:var(--font-mono);font-size:10px;border-radius:3px" — onchange="updateVehicleStatus('${v.id}',this.value)"> — <option value="active"      ${v.status==='active'      ?'selected':''}>● Active</option> — <option value="maintenance" ${v.status==='maintenance' ?'selected':''}>🔧 Maintenance</option> — <option value="offline"     ${v.status==='offline'     ?'selected':''}>○ Offline</option> — </select> — </div>` : ''} — </div>`; — }); — } — content.innerHTML = html; — } — function openFleetModal(vehicleId) { — const v = vehicleId ? STATE.fleet.find(x => x.id === vehicleId) : null; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">${v ? 'EDIT VEHICLE — ' + v.id : 'ADD VEHICLE'}</div> — <div class="grid-2"> — <div class="form-group"><label>Vehicle Name / Unit #</label> — <input type="text" id="flt-name" value="${(v&&v.name)||''}" placeholder="e.g. Truck 1 · Unit 24" /></div> — <div class="form-group"><label>Type</label> — <select id="flt-type"> — <option value="box_truck" ${v?.type==='box_truck'?'selected':''}>🚛 Box Truck</option> — <option value="cargo_van" ${v?.type==='cargo_van'?'selected':''}>🚐 Cargo Van</option> — <option value="flatbed"   ${v?.type==='flatbed'  ?'selected':''}>🚚 Flatbed</option> — <option value="other"     ${v?.type==='other'    ?'selected':''}>📋 Other</option> — </select> — </div> — </div> — <div class="grid-3"> — <div class="form-group"><label>Make</label><input type="text" id="flt-make" value="${(v&&v.make)||''}" placeholder="Ford" /></div> — <div class="form-group"><label>Model</label><input type="text" id="flt-model" value="${(v&&v.model)||''}" placeholder="E-450" /></div> — <div class="form-group"><label>Year</label><input type="number" id="flt-year" value="${(v&&v.year)||''}" placeholder="2019" /></div> — </div> — <div class="grid-2"> — <div class="form-group"><label>License Plate</label><input type="text" id="flt-plate" value="${(v&&v.plate)||''}" placeholder="ABC-1234" style="text-transform:uppercase"/></div> — <div class="form-group"><label>Capacity (text label)</label><input type="text" id="flt-cap" value="${(v&&v.capacity)||''}" placeholder="26 ft · 1500 lbs" /></div> — </div> — <div class="grid-3"> — <div class="form-group"> — <label>Max Cubic Feet <span class="info-text">(for load optimization)</span></label> — <input type="number" id="flt-maxcuft" value="${(v&&v.maxCuFt)||''}" placeholder="e.g. 1200" min="0" step="50" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Fuel Efficiency (MPG) <span class="info-text">(for cost calc)</span></label> — <input type="number" id="flt-mpg" value="${(v&&v.mpg)||''}" placeholder="e.g. 10" min="1" step="0.5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Load Efficiency % <span class="info-text">(usable space)</span></label> — <input type="number" id="flt-loadeff" value="${(v&&v.loadEff)||85}" placeholder="85" min="50" max="100" step="5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — </div> — <div class="grid-2"> — <div class="form-group"><label>Current Mileage</label><input type="number" id="flt-miles" value="${(v&&v.mileage)||''}" placeholder="85000" /></div> — <div class="form-group"><label>Next Service Due</label><input type="date" id="flt-maint" value="${(v&&v.nextMaint)||''}" /></div> — </div> — <div class="form-group"><label>Notes</label> — <input type="text" id="flt-notes" value="${(v&&v.notes)||''}" placeholder="Insurance, registration, special equipment..." /></div> — <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:8px"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" onclick="saveVehicle('${vehicleId||''}')">SAVE VEHICLE</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveVehicle(vehicleId) { — const vehicle = { — id:        vehicleId || 'VEH-' + Date.now(), — name:      document.getElementById('flt-name').value.trim(), — type:      document.getElementById('flt-type').value, — make:      document.getElementById('flt-make').value.trim(), — model:     document.getElementById('flt-model').value.trim(), — year:      document.getElementById('flt-year').value, — plate:     document.getElementById('flt-plate').value.trim().toUpperCase(), — capacity:  document.getElementById('flt-cap').value.trim(), — maxCuFt:   parseFloat(document.getElementById('flt-maxcuft')?.value)||0, — mpg:       parseFloat(document.getElementById('flt-mpg')?.value)||0, — loadEff:   parseFloat(document.getElementById('flt-loadeff')?.value)||85, — mileage:   parseInt(document.getElementById('flt-miles').value)||0, — nextMaint: document.getElementById('flt-maint').value, — notes:     document.getElementById('flt-notes').value.trim(), — status:    vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.status||'active') : 'active', — maintLog:  vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.maintLog||[]) : [], — updatedAt: new Date().toISOString() — }; — if (!vehicle.name) { notify('⚠ Vehicle name is required', true); return; } — try { — await db.collection(COL_FLEET).doc(vehicle.id).set(vehicle); — notify((vehicleId ? 'Vehicle updated' : 'Vehicle added') + ' ✓'); — closeModal('modal-jobdetail'); — } catch(e) { notify('⚠ ' + e.message, true); } — } — async function logMaintenance(vehicleId) { — const work = prompt('Service performed:'); if (!work) return; — const cost = parseFloat(prompt('Cost ($):') || '0'); — const v    = STATE.fleet.find(x => x.id === vehicleId); — if (!v) return; — if (!v.maintLog) v.maintLog = []; — v.maintLog.push({ date: getTodayStr(), work, cost, loggedBy: currentUser.name }); — v.updatedAt = new Date().toISOString(); — await db.collection(COL_FLEET).doc(vehicleId).set(v); — notify('Maintenance logged ✓'); — } — async function updateMileage(vehicleId) { — const miles = prompt('Enter current mileage:'); if (!miles) return; — await db.collection(COL_FLEET).doc(vehicleId).update({ mileage: parseInt(miles)||0 }); — notify('Mileage updated ✓'); — } — async function updateVehicleStatus(vehicleId, status) { — await db.collection(COL_FLEET).doc(vehicleId).update({ status }); — notify('Vehicle status: ' + status.toUpperCase()); — } | 3 | 3 | 0 | yes |
| `saveVehicle` | function | 17339 | function renderFleet() { — const content = document.getElementById('main-content'); — const role    = currentUser.role; — const canEdit = role === 'office'; — let html = ` — <div class="section-header"> — <div> — <div class="section-title">🚚 FLEET MANAGEMENT</div> — <div class="section-sub">${STATE.fleet.length} vehicle(s) registered · Move Masters OS</div> — </div> — ${canEdit ? `<button class="btn-green" onclick="openFleetModal()">+ ADD VEHICLE</button>` : ''} — </div>`; — if (!STATE.fleet.length) { — html += `<div class="card" style="text-align:center;padding:40px"> — <div style="font-size:48px;margin-bottom:12px">🚚</div> — <p class="info-text">No vehicles registered yet.</p> — ${canEdit ? `<button class="btn-green" style="margin-top:16px" onclick="openFleetModal()">+ ADD FIRST VEHICLE</button>` : ''} — </div>`; — } else { — Summary stats — const active  = STATE.fleet.filter(v => v.status === 'active').length; — const maint   = STATE.fleet.filter(v => v.status === 'maintenance').length; — const offline = STATE.fleet.filter(v => v.status === 'offline').length; — html += `<div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Active</div><div class="stat-value">${active}</div></div> — <div class="stat-box"><div class="stat-label">In Maintenance</div><div class="stat-value amber">${maint}</div></div> — <div class="stat-box"><div class="stat-label">Offline</div><div class="stat-value red">${offline}</div></div> — </div>`; — STATE.fleet.forEach(v => { — const assignedJob = STATE.jobs.find(j => j.vehicleId === v.id && j.status === 'active'); — const statusColor = v.status === 'active' ? 'var(--green)' : v.status === 'maintenance' ? 'var(--amber)' : 'var(--red)'; — const maintDue    = v.nextMaint ? new Date(v.nextMaint) : null; — const maintOverdue = maintDue && maintDue < new Date(); — html += ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:12px"> — <div> — <div style="display:flex;align-items:center;gap:10px;margin-bottom:6px"> — <div style="font-size:28px">${v.type === 'box_truck' ? '🚛' : v.type === 'cargo_van' ? '🚐' : '🚚'}</div> — <div> — <div style="font-family:var(--font-mono);font-size:14px;font-weight:700;color:var(--white)">${v.name || v.id}</div> — <div class="info-text">${v.make||''} ${v.model||''} ${v.year||''} · ${v.plate||'No plate'}</div> — </div> — </div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:11px;color:${statusColor};letter-spacing:2px">${(v.status||'active').toUpperCase()}</div> — ${canEdit ? `<button class="btn-sm" style="margin-top:6px" onclick="openFleetModal('${v.id}')">✎ EDIT</button>` : ''} — </div> — </div> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(110px,1fr));gap:8px;margin-bottom:12px"> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Capacity</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.capacity || '—'}</div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Mileage</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.mileage ? v.mileage.toLocaleString() + ' mi' : '—'}</div> — </div> — <div class="stat-box" style="padding:8px;${maintOverdue ? 'border-color:var(--red)' : ''}"> — <div class="stat-label" style="font-size:9px">Next Service</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${maintOverdue ? 'var(--red)' : 'var(--white)'}"> — ${v.nextMaint || 'Not set'}${maintOverdue ? ' ⚠' : ''} — </div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Assigned Job</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${assignedJob ? 'var(--green)' : 'var(--gray)'}"> — ${assignedJob ? assignedJob.id : 'Available'} — </div> — </div> — </div> — ${v.notes ? `<div class="info-text" style="margin-bottom:10px">📝 ${v.notes}</div>` : ''} — <!-- MAINTENANCE LOG --> — ${v.maintLog && v.maintLog.length ? ` — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:6px">MAINTENANCE LOG</div> — ${v.maintLog.slice(-3).reverse().map(m => ` — <div style="display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px solid var(--gray2);font-size:12px"> — <div><span class="info-text">${m.date}</span> · ${m.work}</div> — <div style="font-family:var(--font-mono);color:var(--amber)">$${(m.cost||0).toFixed(0)}</div> — </div>`).join('')}` : ''} — ${canEdit ? ` — <div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap"> — <button class="btn-sm" onclick="logMaintenance('${v.id}')">🔧 LOG SERVICE</button> — <button class="btn-sm" onclick="updateMileage('${v.id}')">📍 UPDATE MILEAGE</button> — <select style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:5px 8px;font-family:var(--font-mono);font-size:10px;border-radius:3px" — onchange="updateVehicleStatus('${v.id}',this.value)"> — <option value="active"      ${v.status==='active'      ?'selected':''}>● Active</option> — <option value="maintenance" ${v.status==='maintenance' ?'selected':''}>🔧 Maintenance</option> — <option value="offline"     ${v.status==='offline'     ?'selected':''}>○ Offline</option> — </select> — </div>` : ''} — </div>`; — }); — } — content.innerHTML = html; — } — function openFleetModal(vehicleId) { — const v = vehicleId ? STATE.fleet.find(x => x.id === vehicleId) : null; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">${v ? 'EDIT VEHICLE — ' + v.id : 'ADD VEHICLE'}</div> — <div class="grid-2"> — <div class="form-group"><label>Vehicle Name / Unit #</label> — <input type="text" id="flt-name" value="${(v&&v.name)||''}" placeholder="e.g. Truck 1 · Unit 24" /></div> — <div class="form-group"><label>Type</label> — <select id="flt-type"> — <option value="box_truck" ${v?.type==='box_truck'?'selected':''}>🚛 Box Truck</option> — <option value="cargo_van" ${v?.type==='cargo_van'?'selected':''}>🚐 Cargo Van</option> — <option value="flatbed"   ${v?.type==='flatbed'  ?'selected':''}>🚚 Flatbed</option> — <option value="other"     ${v?.type==='other'    ?'selected':''}>📋 Other</option> — </select> — </div> — </div> — <div class="grid-3"> — <div class="form-group"><label>Make</label><input type="text" id="flt-make" value="${(v&&v.make)||''}" placeholder="Ford" /></div> — <div class="form-group"><label>Model</label><input type="text" id="flt-model" value="${(v&&v.model)||''}" placeholder="E-450" /></div> — <div class="form-group"><label>Year</label><input type="number" id="flt-year" value="${(v&&v.year)||''}" placeholder="2019" /></div> — </div> — <div class="grid-2"> — <div class="form-group"><label>License Plate</label><input type="text" id="flt-plate" value="${(v&&v.plate)||''}" placeholder="ABC-1234" style="text-transform:uppercase"/></div> — <div class="form-group"><label>Capacity (text label)</label><input type="text" id="flt-cap" value="${(v&&v.capacity)||''}" placeholder="26 ft · 1500 lbs" /></div> — </div> — <div class="grid-3"> — <div class="form-group"> — <label>Max Cubic Feet <span class="info-text">(for load optimization)</span></label> — <input type="number" id="flt-maxcuft" value="${(v&&v.maxCuFt)||''}" placeholder="e.g. 1200" min="0" step="50" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Fuel Efficiency (MPG) <span class="info-text">(for cost calc)</span></label> — <input type="number" id="flt-mpg" value="${(v&&v.mpg)||''}" placeholder="e.g. 10" min="1" step="0.5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Load Efficiency % <span class="info-text">(usable space)</span></label> — <input type="number" id="flt-loadeff" value="${(v&&v.loadEff)||85}" placeholder="85" min="50" max="100" step="5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — </div> — <div class="grid-2"> — <div class="form-group"><label>Current Mileage</label><input type="number" id="flt-miles" value="${(v&&v.mileage)||''}" placeholder="85000" /></div> — <div class="form-group"><label>Next Service Due</label><input type="date" id="flt-maint" value="${(v&&v.nextMaint)||''}" /></div> — </div> — <div class="form-group"><label>Notes</label> — <input type="text" id="flt-notes" value="${(v&&v.notes)||''}" placeholder="Insurance, registration, special equipment..." /></div> — <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:8px"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" onclick="saveVehicle('${vehicleId||''}')">SAVE VEHICLE</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveVehicle(vehicleId) { — const vehicle = { — id:        vehicleId || 'VEH-' + Date.now(), — name:      document.getElementById('flt-name').value.trim(), — type:      document.getElementById('flt-type').value, — make:      document.getElementById('flt-make').value.trim(), — model:     document.getElementById('flt-model').value.trim(), — year:      document.getElementById('flt-year').value, — plate:     document.getElementById('flt-plate').value.trim().toUpperCase(), — capacity:  document.getElementById('flt-cap').value.trim(), — maxCuFt:   parseFloat(document.getElementById('flt-maxcuft')?.value)||0, — mpg:       parseFloat(document.getElementById('flt-mpg')?.value)||0, — loadEff:   parseFloat(document.getElementById('flt-loadeff')?.value)||85, — mileage:   parseInt(document.getElementById('flt-miles').value)||0, — nextMaint: document.getElementById('flt-maint').value, — notes:     document.getElementById('flt-notes').value.trim(), — status:    vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.status||'active') : 'active', — maintLog:  vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.maintLog||[]) : [], — updatedAt: new Date().toISOString() — }; — if (!vehicle.name) { notify('⚠ Vehicle name is required', true); return; } — try { — await db.collection(COL_FLEET).doc(vehicle.id).set(vehicle); — notify((vehicleId ? 'Vehicle updated' : 'Vehicle added') + ' ✓'); — closeModal('modal-jobdetail'); — } catch(e) { notify('⚠ ' + e.message, true); } — } — async function logMaintenance(vehicleId) { — const work = prompt('Service performed:'); if (!work) return; — const cost = parseFloat(prompt('Cost ($):') || '0'); — const v    = STATE.fleet.find(x => x.id === vehicleId); — if (!v) return; — if (!v.maintLog) v.maintLog = []; — v.maintLog.push({ date: getTodayStr(), work, cost, loggedBy: currentUser.name }); — v.updatedAt = new Date().toISOString(); — await db.collection(COL_FLEET).doc(vehicleId).set(v); — notify('Maintenance logged ✓'); — } — async function updateMileage(vehicleId) { — const miles = prompt('Enter current mileage:'); if (!miles) return; — await db.collection(COL_FLEET).doc(vehicleId).update({ mileage: parseInt(miles)||0 }); — notify('Mileage updated ✓'); — } — async function updateVehicleStatus(vehicleId, status) { — await db.collection(COL_FLEET).doc(vehicleId).update({ status }); — notify('Vehicle status: ' + status.toUpperCase()); — } | 0 | 0 | 0 | no |
| `logMaintenance` | function | 17367 | function renderFleet() { — const content = document.getElementById('main-content'); — const role    = currentUser.role; — const canEdit = role === 'office'; — let html = ` — <div class="section-header"> — <div> — <div class="section-title">🚚 FLEET MANAGEMENT</div> — <div class="section-sub">${STATE.fleet.length} vehicle(s) registered · Move Masters OS</div> — </div> — ${canEdit ? `<button class="btn-green" onclick="openFleetModal()">+ ADD VEHICLE</button>` : ''} — </div>`; — if (!STATE.fleet.length) { — html += `<div class="card" style="text-align:center;padding:40px"> — <div style="font-size:48px;margin-bottom:12px">🚚</div> — <p class="info-text">No vehicles registered yet.</p> — ${canEdit ? `<button class="btn-green" style="margin-top:16px" onclick="openFleetModal()">+ ADD FIRST VEHICLE</button>` : ''} — </div>`; — } else { — Summary stats — const active  = STATE.fleet.filter(v => v.status === 'active').length; — const maint   = STATE.fleet.filter(v => v.status === 'maintenance').length; — const offline = STATE.fleet.filter(v => v.status === 'offline').length; — html += `<div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Active</div><div class="stat-value">${active}</div></div> — <div class="stat-box"><div class="stat-label">In Maintenance</div><div class="stat-value amber">${maint}</div></div> — <div class="stat-box"><div class="stat-label">Offline</div><div class="stat-value red">${offline}</div></div> — </div>`; — STATE.fleet.forEach(v => { — const assignedJob = STATE.jobs.find(j => j.vehicleId === v.id && j.status === 'active'); — const statusColor = v.status === 'active' ? 'var(--green)' : v.status === 'maintenance' ? 'var(--amber)' : 'var(--red)'; — const maintDue    = v.nextMaint ? new Date(v.nextMaint) : null; — const maintOverdue = maintDue && maintDue < new Date(); — html += ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:12px"> — <div> — <div style="display:flex;align-items:center;gap:10px;margin-bottom:6px"> — <div style="font-size:28px">${v.type === 'box_truck' ? '🚛' : v.type === 'cargo_van' ? '🚐' : '🚚'}</div> — <div> — <div style="font-family:var(--font-mono);font-size:14px;font-weight:700;color:var(--white)">${v.name || v.id}</div> — <div class="info-text">${v.make||''} ${v.model||''} ${v.year||''} · ${v.plate||'No plate'}</div> — </div> — </div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:11px;color:${statusColor};letter-spacing:2px">${(v.status||'active').toUpperCase()}</div> — ${canEdit ? `<button class="btn-sm" style="margin-top:6px" onclick="openFleetModal('${v.id}')">✎ EDIT</button>` : ''} — </div> — </div> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(110px,1fr));gap:8px;margin-bottom:12px"> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Capacity</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.capacity || '—'}</div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Mileage</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.mileage ? v.mileage.toLocaleString() + ' mi' : '—'}</div> — </div> — <div class="stat-box" style="padding:8px;${maintOverdue ? 'border-color:var(--red)' : ''}"> — <div class="stat-label" style="font-size:9px">Next Service</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${maintOverdue ? 'var(--red)' : 'var(--white)'}"> — ${v.nextMaint || 'Not set'}${maintOverdue ? ' ⚠' : ''} — </div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Assigned Job</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${assignedJob ? 'var(--green)' : 'var(--gray)'}"> — ${assignedJob ? assignedJob.id : 'Available'} — </div> — </div> — </div> — ${v.notes ? `<div class="info-text" style="margin-bottom:10px">📝 ${v.notes}</div>` : ''} — <!-- MAINTENANCE LOG --> — ${v.maintLog && v.maintLog.length ? ` — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:6px">MAINTENANCE LOG</div> — ${v.maintLog.slice(-3).reverse().map(m => ` — <div style="display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px solid var(--gray2);font-size:12px"> — <div><span class="info-text">${m.date}</span> · ${m.work}</div> — <div style="font-family:var(--font-mono);color:var(--amber)">$${(m.cost||0).toFixed(0)}</div> — </div>`).join('')}` : ''} — ${canEdit ? ` — <div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap"> — <button class="btn-sm" onclick="logMaintenance('${v.id}')">🔧 LOG SERVICE</button> — <button class="btn-sm" onclick="updateMileage('${v.id}')">📍 UPDATE MILEAGE</button> — <select style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:5px 8px;font-family:var(--font-mono);font-size:10px;border-radius:3px" — onchange="updateVehicleStatus('${v.id}',this.value)"> — <option value="active"      ${v.status==='active'      ?'selected':''}>● Active</option> — <option value="maintenance" ${v.status==='maintenance' ?'selected':''}>🔧 Maintenance</option> — <option value="offline"     ${v.status==='offline'     ?'selected':''}>○ Offline</option> — </select> — </div>` : ''} — </div>`; — }); — } — content.innerHTML = html; — } — function openFleetModal(vehicleId) { — const v = vehicleId ? STATE.fleet.find(x => x.id === vehicleId) : null; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">${v ? 'EDIT VEHICLE — ' + v.id : 'ADD VEHICLE'}</div> — <div class="grid-2"> — <div class="form-group"><label>Vehicle Name / Unit #</label> — <input type="text" id="flt-name" value="${(v&&v.name)||''}" placeholder="e.g. Truck 1 · Unit 24" /></div> — <div class="form-group"><label>Type</label> — <select id="flt-type"> — <option value="box_truck" ${v?.type==='box_truck'?'selected':''}>🚛 Box Truck</option> — <option value="cargo_van" ${v?.type==='cargo_van'?'selected':''}>🚐 Cargo Van</option> — <option value="flatbed"   ${v?.type==='flatbed'  ?'selected':''}>🚚 Flatbed</option> — <option value="other"     ${v?.type==='other'    ?'selected':''}>📋 Other</option> — </select> — </div> — </div> — <div class="grid-3"> — <div class="form-group"><label>Make</label><input type="text" id="flt-make" value="${(v&&v.make)||''}" placeholder="Ford" /></div> — <div class="form-group"><label>Model</label><input type="text" id="flt-model" value="${(v&&v.model)||''}" placeholder="E-450" /></div> — <div class="form-group"><label>Year</label><input type="number" id="flt-year" value="${(v&&v.year)||''}" placeholder="2019" /></div> — </div> — <div class="grid-2"> — <div class="form-group"><label>License Plate</label><input type="text" id="flt-plate" value="${(v&&v.plate)||''}" placeholder="ABC-1234" style="text-transform:uppercase"/></div> — <div class="form-group"><label>Capacity (text label)</label><input type="text" id="flt-cap" value="${(v&&v.capacity)||''}" placeholder="26 ft · 1500 lbs" /></div> — </div> — <div class="grid-3"> — <div class="form-group"> — <label>Max Cubic Feet <span class="info-text">(for load optimization)</span></label> — <input type="number" id="flt-maxcuft" value="${(v&&v.maxCuFt)||''}" placeholder="e.g. 1200" min="0" step="50" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Fuel Efficiency (MPG) <span class="info-text">(for cost calc)</span></label> — <input type="number" id="flt-mpg" value="${(v&&v.mpg)||''}" placeholder="e.g. 10" min="1" step="0.5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Load Efficiency % <span class="info-text">(usable space)</span></label> — <input type="number" id="flt-loadeff" value="${(v&&v.loadEff)||85}" placeholder="85" min="50" max="100" step="5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — </div> — <div class="grid-2"> — <div class="form-group"><label>Current Mileage</label><input type="number" id="flt-miles" value="${(v&&v.mileage)||''}" placeholder="85000" /></div> — <div class="form-group"><label>Next Service Due</label><input type="date" id="flt-maint" value="${(v&&v.nextMaint)||''}" /></div> — </div> — <div class="form-group"><label>Notes</label> — <input type="text" id="flt-notes" value="${(v&&v.notes)||''}" placeholder="Insurance, registration, special equipment..." /></div> — <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:8px"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" onclick="saveVehicle('${vehicleId||''}')">SAVE VEHICLE</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveVehicle(vehicleId) { — const vehicle = { — id:        vehicleId || 'VEH-' + Date.now(), — name:      document.getElementById('flt-name').value.trim(), — type:      document.getElementById('flt-type').value, — make:      document.getElementById('flt-make').value.trim(), — model:     document.getElementById('flt-model').value.trim(), — year:      document.getElementById('flt-year').value, — plate:     document.getElementById('flt-plate').value.trim().toUpperCase(), — capacity:  document.getElementById('flt-cap').value.trim(), — maxCuFt:   parseFloat(document.getElementById('flt-maxcuft')?.value)||0, — mpg:       parseFloat(document.getElementById('flt-mpg')?.value)||0, — loadEff:   parseFloat(document.getElementById('flt-loadeff')?.value)||85, — mileage:   parseInt(document.getElementById('flt-miles').value)||0, — nextMaint: document.getElementById('flt-maint').value, — notes:     document.getElementById('flt-notes').value.trim(), — status:    vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.status||'active') : 'active', — maintLog:  vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.maintLog||[]) : [], — updatedAt: new Date().toISOString() — }; — if (!vehicle.name) { notify('⚠ Vehicle name is required', true); return; } — try { — await db.collection(COL_FLEET).doc(vehicle.id).set(vehicle); — notify((vehicleId ? 'Vehicle updated' : 'Vehicle added') + ' ✓'); — closeModal('modal-jobdetail'); — } catch(e) { notify('⚠ ' + e.message, true); } — } — async function logMaintenance(vehicleId) { — const work = prompt('Service performed:'); if (!work) return; — const cost = parseFloat(prompt('Cost ($):') || '0'); — const v    = STATE.fleet.find(x => x.id === vehicleId); — if (!v) return; — if (!v.maintLog) v.maintLog = []; — v.maintLog.push({ date: getTodayStr(), work, cost, loggedBy: currentUser.name }); — v.updatedAt = new Date().toISOString(); — await db.collection(COL_FLEET).doc(vehicleId).set(v); — notify('Maintenance logged ✓'); — } — async function updateMileage(vehicleId) { — const miles = prompt('Enter current mileage:'); if (!miles) return; — await db.collection(COL_FLEET).doc(vehicleId).update({ mileage: parseInt(miles)||0 }); — notify('Mileage updated ✓'); — } — async function updateVehicleStatus(vehicleId, status) { — await db.collection(COL_FLEET).doc(vehicleId).update({ status }); — notify('Vehicle status: ' + status.toUpperCase()); — } | 1 | 1 | 0 | yes |
| `updateMileage` | function | 17379 | function renderFleet() { — const content = document.getElementById('main-content'); — const role    = currentUser.role; — const canEdit = role === 'office'; — let html = ` — <div class="section-header"> — <div> — <div class="section-title">🚚 FLEET MANAGEMENT</div> — <div class="section-sub">${STATE.fleet.length} vehicle(s) registered · Move Masters OS</div> — </div> — ${canEdit ? `<button class="btn-green" onclick="openFleetModal()">+ ADD VEHICLE</button>` : ''} — </div>`; — if (!STATE.fleet.length) { — html += `<div class="card" style="text-align:center;padding:40px"> — <div style="font-size:48px;margin-bottom:12px">🚚</div> — <p class="info-text">No vehicles registered yet.</p> — ${canEdit ? `<button class="btn-green" style="margin-top:16px" onclick="openFleetModal()">+ ADD FIRST VEHICLE</button>` : ''} — </div>`; — } else { — Summary stats — const active  = STATE.fleet.filter(v => v.status === 'active').length; — const maint   = STATE.fleet.filter(v => v.status === 'maintenance').length; — const offline = STATE.fleet.filter(v => v.status === 'offline').length; — html += `<div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Active</div><div class="stat-value">${active}</div></div> — <div class="stat-box"><div class="stat-label">In Maintenance</div><div class="stat-value amber">${maint}</div></div> — <div class="stat-box"><div class="stat-label">Offline</div><div class="stat-value red">${offline}</div></div> — </div>`; — STATE.fleet.forEach(v => { — const assignedJob = STATE.jobs.find(j => j.vehicleId === v.id && j.status === 'active'); — const statusColor = v.status === 'active' ? 'var(--green)' : v.status === 'maintenance' ? 'var(--amber)' : 'var(--red)'; — const maintDue    = v.nextMaint ? new Date(v.nextMaint) : null; — const maintOverdue = maintDue && maintDue < new Date(); — html += ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:12px"> — <div> — <div style="display:flex;align-items:center;gap:10px;margin-bottom:6px"> — <div style="font-size:28px">${v.type === 'box_truck' ? '🚛' : v.type === 'cargo_van' ? '🚐' : '🚚'}</div> — <div> — <div style="font-family:var(--font-mono);font-size:14px;font-weight:700;color:var(--white)">${v.name || v.id}</div> — <div class="info-text">${v.make||''} ${v.model||''} ${v.year||''} · ${v.plate||'No plate'}</div> — </div> — </div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:11px;color:${statusColor};letter-spacing:2px">${(v.status||'active').toUpperCase()}</div> — ${canEdit ? `<button class="btn-sm" style="margin-top:6px" onclick="openFleetModal('${v.id}')">✎ EDIT</button>` : ''} — </div> — </div> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(110px,1fr));gap:8px;margin-bottom:12px"> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Capacity</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.capacity || '—'}</div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Mileage</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.mileage ? v.mileage.toLocaleString() + ' mi' : '—'}</div> — </div> — <div class="stat-box" style="padding:8px;${maintOverdue ? 'border-color:var(--red)' : ''}"> — <div class="stat-label" style="font-size:9px">Next Service</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${maintOverdue ? 'var(--red)' : 'var(--white)'}"> — ${v.nextMaint || 'Not set'}${maintOverdue ? ' ⚠' : ''} — </div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Assigned Job</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${assignedJob ? 'var(--green)' : 'var(--gray)'}"> — ${assignedJob ? assignedJob.id : 'Available'} — </div> — </div> — </div> — ${v.notes ? `<div class="info-text" style="margin-bottom:10px">📝 ${v.notes}</div>` : ''} — <!-- MAINTENANCE LOG --> — ${v.maintLog && v.maintLog.length ? ` — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:6px">MAINTENANCE LOG</div> — ${v.maintLog.slice(-3).reverse().map(m => ` — <div style="display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px solid var(--gray2);font-size:12px"> — <div><span class="info-text">${m.date}</span> · ${m.work}</div> — <div style="font-family:var(--font-mono);color:var(--amber)">$${(m.cost||0).toFixed(0)}</div> — </div>`).join('')}` : ''} — ${canEdit ? ` — <div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap"> — <button class="btn-sm" onclick="logMaintenance('${v.id}')">🔧 LOG SERVICE</button> — <button class="btn-sm" onclick="updateMileage('${v.id}')">📍 UPDATE MILEAGE</button> — <select style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:5px 8px;font-family:var(--font-mono);font-size:10px;border-radius:3px" — onchange="updateVehicleStatus('${v.id}',this.value)"> — <option value="active"      ${v.status==='active'      ?'selected':''}>● Active</option> — <option value="maintenance" ${v.status==='maintenance' ?'selected':''}>🔧 Maintenance</option> — <option value="offline"     ${v.status==='offline'     ?'selected':''}>○ Offline</option> — </select> — </div>` : ''} — </div>`; — }); — } — content.innerHTML = html; — } — function openFleetModal(vehicleId) { — const v = vehicleId ? STATE.fleet.find(x => x.id === vehicleId) : null; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">${v ? 'EDIT VEHICLE — ' + v.id : 'ADD VEHICLE'}</div> — <div class="grid-2"> — <div class="form-group"><label>Vehicle Name / Unit #</label> — <input type="text" id="flt-name" value="${(v&&v.name)||''}" placeholder="e.g. Truck 1 · Unit 24" /></div> — <div class="form-group"><label>Type</label> — <select id="flt-type"> — <option value="box_truck" ${v?.type==='box_truck'?'selected':''}>🚛 Box Truck</option> — <option value="cargo_van" ${v?.type==='cargo_van'?'selected':''}>🚐 Cargo Van</option> — <option value="flatbed"   ${v?.type==='flatbed'  ?'selected':''}>🚚 Flatbed</option> — <option value="other"     ${v?.type==='other'    ?'selected':''}>📋 Other</option> — </select> — </div> — </div> — <div class="grid-3"> — <div class="form-group"><label>Make</label><input type="text" id="flt-make" value="${(v&&v.make)||''}" placeholder="Ford" /></div> — <div class="form-group"><label>Model</label><input type="text" id="flt-model" value="${(v&&v.model)||''}" placeholder="E-450" /></div> — <div class="form-group"><label>Year</label><input type="number" id="flt-year" value="${(v&&v.year)||''}" placeholder="2019" /></div> — </div> — <div class="grid-2"> — <div class="form-group"><label>License Plate</label><input type="text" id="flt-plate" value="${(v&&v.plate)||''}" placeholder="ABC-1234" style="text-transform:uppercase"/></div> — <div class="form-group"><label>Capacity (text label)</label><input type="text" id="flt-cap" value="${(v&&v.capacity)||''}" placeholder="26 ft · 1500 lbs" /></div> — </div> — <div class="grid-3"> — <div class="form-group"> — <label>Max Cubic Feet <span class="info-text">(for load optimization)</span></label> — <input type="number" id="flt-maxcuft" value="${(v&&v.maxCuFt)||''}" placeholder="e.g. 1200" min="0" step="50" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Fuel Efficiency (MPG) <span class="info-text">(for cost calc)</span></label> — <input type="number" id="flt-mpg" value="${(v&&v.mpg)||''}" placeholder="e.g. 10" min="1" step="0.5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Load Efficiency % <span class="info-text">(usable space)</span></label> — <input type="number" id="flt-loadeff" value="${(v&&v.loadEff)||85}" placeholder="85" min="50" max="100" step="5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — </div> — <div class="grid-2"> — <div class="form-group"><label>Current Mileage</label><input type="number" id="flt-miles" value="${(v&&v.mileage)||''}" placeholder="85000" /></div> — <div class="form-group"><label>Next Service Due</label><input type="date" id="flt-maint" value="${(v&&v.nextMaint)||''}" /></div> — </div> — <div class="form-group"><label>Notes</label> — <input type="text" id="flt-notes" value="${(v&&v.notes)||''}" placeholder="Insurance, registration, special equipment..." /></div> — <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:8px"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" onclick="saveVehicle('${vehicleId||''}')">SAVE VEHICLE</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveVehicle(vehicleId) { — const vehicle = { — id:        vehicleId || 'VEH-' + Date.now(), — name:      document.getElementById('flt-name').value.trim(), — type:      document.getElementById('flt-type').value, — make:      document.getElementById('flt-make').value.trim(), — model:     document.getElementById('flt-model').value.trim(), — year:      document.getElementById('flt-year').value, — plate:     document.getElementById('flt-plate').value.trim().toUpperCase(), — capacity:  document.getElementById('flt-cap').value.trim(), — maxCuFt:   parseFloat(document.getElementById('flt-maxcuft')?.value)||0, — mpg:       parseFloat(document.getElementById('flt-mpg')?.value)||0, — loadEff:   parseFloat(document.getElementById('flt-loadeff')?.value)||85, — mileage:   parseInt(document.getElementById('flt-miles').value)||0, — nextMaint: document.getElementById('flt-maint').value, — notes:     document.getElementById('flt-notes').value.trim(), — status:    vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.status||'active') : 'active', — maintLog:  vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.maintLog||[]) : [], — updatedAt: new Date().toISOString() — }; — if (!vehicle.name) { notify('⚠ Vehicle name is required', true); return; } — try { — await db.collection(COL_FLEET).doc(vehicle.id).set(vehicle); — notify((vehicleId ? 'Vehicle updated' : 'Vehicle added') + ' ✓'); — closeModal('modal-jobdetail'); — } catch(e) { notify('⚠ ' + e.message, true); } — } — async function logMaintenance(vehicleId) { — const work = prompt('Service performed:'); if (!work) return; — const cost = parseFloat(prompt('Cost ($):') || '0'); — const v    = STATE.fleet.find(x => x.id === vehicleId); — if (!v) return; — if (!v.maintLog) v.maintLog = []; — v.maintLog.push({ date: getTodayStr(), work, cost, loggedBy: currentUser.name }); — v.updatedAt = new Date().toISOString(); — await db.collection(COL_FLEET).doc(vehicleId).set(v); — notify('Maintenance logged ✓'); — } — async function updateMileage(vehicleId) { — const miles = prompt('Enter current mileage:'); if (!miles) return; — await db.collection(COL_FLEET).doc(vehicleId).update({ mileage: parseInt(miles)||0 }); — notify('Mileage updated ✓'); — } — async function updateVehicleStatus(vehicleId, status) { — await db.collection(COL_FLEET).doc(vehicleId).update({ status }); — notify('Vehicle status: ' + status.toUpperCase()); — } | 1 | 1 | 0 | yes |
| `updateVehicleStatus` | function | 17385 | function renderFleet() { — const content = document.getElementById('main-content'); — const role    = currentUser.role; — const canEdit = role === 'office'; — let html = ` — <div class="section-header"> — <div> — <div class="section-title">🚚 FLEET MANAGEMENT</div> — <div class="section-sub">${STATE.fleet.length} vehicle(s) registered · Move Masters OS</div> — </div> — ${canEdit ? `<button class="btn-green" onclick="openFleetModal()">+ ADD VEHICLE</button>` : ''} — </div>`; — if (!STATE.fleet.length) { — html += `<div class="card" style="text-align:center;padding:40px"> — <div style="font-size:48px;margin-bottom:12px">🚚</div> — <p class="info-text">No vehicles registered yet.</p> — ${canEdit ? `<button class="btn-green" style="margin-top:16px" onclick="openFleetModal()">+ ADD FIRST VEHICLE</button>` : ''} — </div>`; — } else { — Summary stats — const active  = STATE.fleet.filter(v => v.status === 'active').length; — const maint   = STATE.fleet.filter(v => v.status === 'maintenance').length; — const offline = STATE.fleet.filter(v => v.status === 'offline').length; — html += `<div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Active</div><div class="stat-value">${active}</div></div> — <div class="stat-box"><div class="stat-label">In Maintenance</div><div class="stat-value amber">${maint}</div></div> — <div class="stat-box"><div class="stat-label">Offline</div><div class="stat-value red">${offline}</div></div> — </div>`; — STATE.fleet.forEach(v => { — const assignedJob = STATE.jobs.find(j => j.vehicleId === v.id && j.status === 'active'); — const statusColor = v.status === 'active' ? 'var(--green)' : v.status === 'maintenance' ? 'var(--amber)' : 'var(--red)'; — const maintDue    = v.nextMaint ? new Date(v.nextMaint) : null; — const maintOverdue = maintDue && maintDue < new Date(); — html += ` — <div class="card" style="margin-bottom:12px"> — <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:12px"> — <div> — <div style="display:flex;align-items:center;gap:10px;margin-bottom:6px"> — <div style="font-size:28px">${v.type === 'box_truck' ? '🚛' : v.type === 'cargo_van' ? '🚐' : '🚚'}</div> — <div> — <div style="font-family:var(--font-mono);font-size:14px;font-weight:700;color:var(--white)">${v.name || v.id}</div> — <div class="info-text">${v.make||''} ${v.model||''} ${v.year||''} · ${v.plate||'No plate'}</div> — </div> — </div> — </div> — <div style="text-align:right"> — <div style="font-family:var(--font-mono);font-size:11px;color:${statusColor};letter-spacing:2px">${(v.status||'active').toUpperCase()}</div> — ${canEdit ? `<button class="btn-sm" style="margin-top:6px" onclick="openFleetModal('${v.id}')">✎ EDIT</button>` : ''} — </div> — </div> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(110px,1fr));gap:8px;margin-bottom:12px"> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Capacity</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.capacity || '—'}</div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Mileage</div> — <div style="font-family:var(--font-mono);font-size:13px">${v.mileage ? v.mileage.toLocaleString() + ' mi' : '—'}</div> — </div> — <div class="stat-box" style="padding:8px;${maintOverdue ? 'border-color:var(--red)' : ''}"> — <div class="stat-label" style="font-size:9px">Next Service</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${maintOverdue ? 'var(--red)' : 'var(--white)'}"> — ${v.nextMaint || 'Not set'}${maintOverdue ? ' ⚠' : ''} — </div> — </div> — <div class="stat-box" style="padding:8px"> — <div class="stat-label" style="font-size:9px">Assigned Job</div> — <div style="font-family:var(--font-mono);font-size:11px;color:${assignedJob ? 'var(--green)' : 'var(--gray)'}"> — ${assignedJob ? assignedJob.id : 'Available'} — </div> — </div> — </div> — ${v.notes ? `<div class="info-text" style="margin-bottom:10px">📝 ${v.notes}</div>` : ''} — <!-- MAINTENANCE LOG --> — ${v.maintLog && v.maintLog.length ? ` — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:6px">MAINTENANCE LOG</div> — ${v.maintLog.slice(-3).reverse().map(m => ` — <div style="display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px solid var(--gray2);font-size:12px"> — <div><span class="info-text">${m.date}</span> · ${m.work}</div> — <div style="font-family:var(--font-mono);color:var(--amber)">$${(m.cost||0).toFixed(0)}</div> — </div>`).join('')}` : ''} — ${canEdit ? ` — <div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap"> — <button class="btn-sm" onclick="logMaintenance('${v.id}')">🔧 LOG SERVICE</button> — <button class="btn-sm" onclick="updateMileage('${v.id}')">📍 UPDATE MILEAGE</button> — <select style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:5px 8px;font-family:var(--font-mono);font-size:10px;border-radius:3px" — onchange="updateVehicleStatus('${v.id}',this.value)"> — <option value="active"      ${v.status==='active'      ?'selected':''}>● Active</option> — <option value="maintenance" ${v.status==='maintenance' ?'selected':''}>🔧 Maintenance</option> — <option value="offline"     ${v.status==='offline'     ?'selected':''}>○ Offline</option> — </select> — </div>` : ''} — </div>`; — }); — } — content.innerHTML = html; — } — function openFleetModal(vehicleId) { — const v = vehicleId ? STATE.fleet.find(x => x.id === vehicleId) : null; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">${v ? 'EDIT VEHICLE — ' + v.id : 'ADD VEHICLE'}</div> — <div class="grid-2"> — <div class="form-group"><label>Vehicle Name / Unit #</label> — <input type="text" id="flt-name" value="${(v&&v.name)||''}" placeholder="e.g. Truck 1 · Unit 24" /></div> — <div class="form-group"><label>Type</label> — <select id="flt-type"> — <option value="box_truck" ${v?.type==='box_truck'?'selected':''}>🚛 Box Truck</option> — <option value="cargo_van" ${v?.type==='cargo_van'?'selected':''}>🚐 Cargo Van</option> — <option value="flatbed"   ${v?.type==='flatbed'  ?'selected':''}>🚚 Flatbed</option> — <option value="other"     ${v?.type==='other'    ?'selected':''}>📋 Other</option> — </select> — </div> — </div> — <div class="grid-3"> — <div class="form-group"><label>Make</label><input type="text" id="flt-make" value="${(v&&v.make)||''}" placeholder="Ford" /></div> — <div class="form-group"><label>Model</label><input type="text" id="flt-model" value="${(v&&v.model)||''}" placeholder="E-450" /></div> — <div class="form-group"><label>Year</label><input type="number" id="flt-year" value="${(v&&v.year)||''}" placeholder="2019" /></div> — </div> — <div class="grid-2"> — <div class="form-group"><label>License Plate</label><input type="text" id="flt-plate" value="${(v&&v.plate)||''}" placeholder="ABC-1234" style="text-transform:uppercase"/></div> — <div class="form-group"><label>Capacity (text label)</label><input type="text" id="flt-cap" value="${(v&&v.capacity)||''}" placeholder="26 ft · 1500 lbs" /></div> — </div> — <div class="grid-3"> — <div class="form-group"> — <label>Max Cubic Feet <span class="info-text">(for load optimization)</span></label> — <input type="number" id="flt-maxcuft" value="${(v&&v.maxCuFt)||''}" placeholder="e.g. 1200" min="0" step="50" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Fuel Efficiency (MPG) <span class="info-text">(for cost calc)</span></label> — <input type="number" id="flt-mpg" value="${(v&&v.mpg)||''}" placeholder="e.g. 10" min="1" step="0.5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Load Efficiency % <span class="info-text">(usable space)</span></label> — <input type="number" id="flt-loadeff" value="${(v&&v.loadEff)||85}" placeholder="85" min="50" max="100" step="5" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:8px 10px;font-family:var(--font-mono);font-size:14px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — </div> — <div class="grid-2"> — <div class="form-group"><label>Current Mileage</label><input type="number" id="flt-miles" value="${(v&&v.mileage)||''}" placeholder="85000" /></div> — <div class="form-group"><label>Next Service Due</label><input type="date" id="flt-maint" value="${(v&&v.nextMaint)||''}" /></div> — </div> — <div class="form-group"><label>Notes</label> — <input type="text" id="flt-notes" value="${(v&&v.notes)||''}" placeholder="Insurance, registration, special equipment..." /></div> — <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:8px"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" onclick="saveVehicle('${vehicleId||''}')">SAVE VEHICLE</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveVehicle(vehicleId) { — const vehicle = { — id:        vehicleId || 'VEH-' + Date.now(), — name:      document.getElementById('flt-name').value.trim(), — type:      document.getElementById('flt-type').value, — make:      document.getElementById('flt-make').value.trim(), — model:     document.getElementById('flt-model').value.trim(), — year:      document.getElementById('flt-year').value, — plate:     document.getElementById('flt-plate').value.trim().toUpperCase(), — capacity:  document.getElementById('flt-cap').value.trim(), — maxCuFt:   parseFloat(document.getElementById('flt-maxcuft')?.value)||0, — mpg:       parseFloat(document.getElementById('flt-mpg')?.value)||0, — loadEff:   parseFloat(document.getElementById('flt-loadeff')?.value)||85, — mileage:   parseInt(document.getElementById('flt-miles').value)||0, — nextMaint: document.getElementById('flt-maint').value, — notes:     document.getElementById('flt-notes').value.trim(), — status:    vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.status||'active') : 'active', — maintLog:  vehicleId ? (STATE.fleet.find(v=>v.id===vehicleId)?.maintLog||[]) : [], — updatedAt: new Date().toISOString() — }; — if (!vehicle.name) { notify('⚠ Vehicle name is required', true); return; } — try { — await db.collection(COL_FLEET).doc(vehicle.id).set(vehicle); — notify((vehicleId ? 'Vehicle updated' : 'Vehicle added') + ' ✓'); — closeModal('modal-jobdetail'); — } catch(e) { notify('⚠ ' + e.message, true); } — } — async function logMaintenance(vehicleId) { — const work = prompt('Service performed:'); if (!work) return; — const cost = parseFloat(prompt('Cost ($):') || '0'); — const v    = STATE.fleet.find(x => x.id === vehicleId); — if (!v) return; — if (!v.maintLog) v.maintLog = []; — v.maintLog.push({ date: getTodayStr(), work, cost, loggedBy: currentUser.name }); — v.updatedAt = new Date().toISOString(); — await db.collection(COL_FLEET).doc(vehicleId).set(v); — notify('Maintenance logged ✓'); — } — async function updateMileage(vehicleId) { — const miles = prompt('Enter current mileage:'); if (!miles) return; — await db.collection(COL_FLEET).doc(vehicleId).update({ mileage: parseInt(miles)||0 }); — notify('Mileage updated ✓'); — } — async function updateVehicleStatus(vehicleId, status) { — await db.collection(COL_FLEET).doc(vehicleId).update({ status }); — notify('Vehicle status: ' + status.toUpperCase()); — } | 1 | 1 | 0 | yes |
| `renderPayrollSummary` | function | 17394 | function renderPayrollSummary() { — This is the office-facing payroll output — all crew, all jobs — const content = document.getElementById('main-content'); — const role    = currentUser.role; — if (role !== 'office') { switchTab('mypay'); return; } — Aggregate per person — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — Driver — const dKey = j.driver; — if (!payMap[dKey]) payMap[dKey] = { name: dKey, role: 'Driver', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[dKey].jobs++; — payMap[dKey].gross      += j.payroll.driver || 0; — payMap[dKey].commission += j.payroll.driverCommission || 0; — payMap[dKey].workerType  = j.payroll.driverWorkerType || '1099'; — Helpers — (j.payroll.helpers||[]).forEach(h => { — if (!h.name) return; — const hKey = h.name; — if (!payMap[hKey]) payMap[hKey] = { name: hKey, role: 'Helper', jobs: 0, gross: 0, commission: 0, workerType: '1099' }; — payMap[hKey].jobs++; — payMap[hKey].gross      += calcPay(h); — payMap[hKey].commission += h.commission || 0; — payMap[hKey].workerType  = h.workerType || '1099'; — }); — }); — const people    = Object.values(payMap).sort((a,b) => b.gross - a.gross); — const totalGross = people.reduce((s,p) => s + p.gross + p.commission, 0); — let html = ` — <div class="section-header"> — <div> — <div class="section-title">💵 PAYROLL SUMMARY</div> — <div class="section-sub">All crew · All jobs · JPG Systems</div> — </div> — <div style="display:flex;gap:8px"> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportPayrollCSV()">⬇ EXPORT CSV</button> — </div> — </div> — <div class="grid-3" style="margin-bottom:16px"> — <div class="stat-box"><div class="stat-label">Total Crew</div><div class="stat-value">${people.length}</div></div> — <div class="stat-box"><div class="stat-label">Total Payroll</div><div class="stat-value amber">$${totalGross.toFixed(2)}</div></div> — <div class="stat-box"><div class="stat-label">Jobs w/ Payroll</div><div class="stat-value">${STATE.jobs.filter(j=>j.payroll).length}</div></div> — </div> — <div class="card"> — <table> — <thead><tr> — <th>Name</th><th>Role</th><th>Type</th><th>Jobs</th> — <th style="text-align:right">Base Pay</th> — <th style="text-align:right">Commission</th> — <th style="text-align:right">Total Gross</th> — <th style="text-align:right">Est. Tax</th> — <th style="text-align:right">Take-Home</th> — </tr></thead> — <tbody> — ${people.map(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — return `<tr> — <td style="font-weight:600">${p.name}</td> — <td><span class="tag">${p.role}</span></td> — <td><span class="tag">${p.workerType}</span></td> — <td style="font-family:var(--font-mono);text-align:center">${p.jobs}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${p.gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">${p.commission>0?'$'+p.commission.toFixed(2):'—'}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green);font-weight:700">$${gross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${tax.totalEstTax.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${tax.takeHome.toFixed(2)}</td> — </tr>`; — }).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="4" style="font-family:var(--font-mono);color:var(--green)">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${people.reduce((s,p)=>s+p.gross,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--amber)">$${people.reduce((s,p)=>s+p.commission,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${totalGross.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).totalEstTax,0).toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--green)">$${people.reduce((s,p)=>s+calcTax(p.gross+p.commission,p.workerType).takeHome,0).toFixed(2)}</td> — </tr> — </tbody> — </table> — </div>`; — content.innerHTML = html; — } | 2 | 2 | 0 | yes |
| `exportPayrollCSV` | function | 17488 | function exportPayrollCSV() { — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — const addPerson = (name, role, gross, commission, workerType) => { — if (!name) return; — if (!payMap[name]) payMap[name] = { name, role, jobs: 0, gross: 0, commission: 0, workerType }; — payMap[name].jobs++; — payMap[name].gross      += gross; — payMap[name].commission += commission; — }; — addPerson(j.driver, 'Driver', j.payroll.driver||0, j.payroll.driverCommission||0, j.payroll.driverWorkerType||'1099'); — (j.payroll.helpers||[]).forEach(h => addPerson(h.name, 'Helper', calcPay(h), h.commission||0, h.workerType||'1099')); — }); — const rows = [['Name','Role','Worker Type','Jobs','Base Pay','Commission','Total Gross','Est Tax','Est Take-Home']]; — Object.values(payMap).forEach(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — rows.push([p.name, p.role, p.workerType, p.jobs, p.gross.toFixed(2), p.commission.toFixed(2), — gross.toFixed(2), tax.totalEstTax.toFixed(2), tax.takeHome.toFixed(2)]); — }); — downloadCSV(rows, 'jpgsystems-payroll-' + getTodayStr() + '.csv'); — notify('Payroll CSV exported ✓'); — } — function exportJobsCSV() { — const rows = [['Job ID','Client','Date','Time','Driver','Type','Status','Amount','Gates Passed','Origin','Destination']]; — STATE.jobs.forEach(j => { — const gates = Object.values(j.gates||{}).filter(Boolean).length; — rows.push([j.id, j.client, j.date, j.time||'', j.driver||'', j.type||'', — j.status, (j.amount||0).toFixed(2), gates + '/' + Object.keys(j.gates||{}).length, — j.origin||'', j.dest||'']); — }); — downloadCSV(rows, 'jpgsystems-jobs-' + getTodayStr() + '.csv'); — notify('Jobs CSV exported ✓'); — } — function exportReceiptsCSV() { — const rows = [['Receipt ID','Job ID','Driver','Date','Type','Amount','Status','Notes']]; — STATE.receipts.forEach(r => { — rows.push([r.id, r.jobId, r.driver||'', r.date||'', r.type||'', — (r.amount||0).toFixed(2), r.approved ? 'APPROVED' : r.rejected ? 'REJECTED' : 'PENDING', r.notes||'']); — }); — downloadCSV(rows, 'jpgsystems-receipts-' + getTodayStr() + '.csv'); — notify('Receipts CSV exported ✓'); — } — function downloadCSV(rows, filename) { — const csv  = rows.map(r => r.map(c => '"' + String(c).replace(/"/g,'""') + '"').join(',')).join('\n'); — const blob = new Blob([csv], { type: 'text/csv' }); — const url  = URL.createObjectURL(blob); — const a    = document.createElement('a'); — a.href     = url; — a.download = filename; — a.click(); — URL.revokeObjectURL(url); — } — Add export buttons to financials tab — function renderExportBar() { — return ` — <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;align-self:center">EXPORT:</div> — <button class="btn-sm" onclick="exportJobsCSV()">⬇ Jobs CSV</button> — <button class="btn-sm" onclick="exportReceiptsCSV()">⬇ Receipts CSV</button> — <button class="btn-sm" onclick="exportPayrollCSV()">⬇ Payroll CSV</button> — <button class="btn-sm" onclick="renderPayrollSummary()">💵 Payroll Report</button> — </div>`; — } | 4 | 4 | 0 | yes |
| `exportJobsCSV` | function | 17514 | function exportPayrollCSV() { — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — const addPerson = (name, role, gross, commission, workerType) => { — if (!name) return; — if (!payMap[name]) payMap[name] = { name, role, jobs: 0, gross: 0, commission: 0, workerType }; — payMap[name].jobs++; — payMap[name].gross      += gross; — payMap[name].commission += commission; — }; — addPerson(j.driver, 'Driver', j.payroll.driver||0, j.payroll.driverCommission||0, j.payroll.driverWorkerType||'1099'); — (j.payroll.helpers||[]).forEach(h => addPerson(h.name, 'Helper', calcPay(h), h.commission||0, h.workerType||'1099')); — }); — const rows = [['Name','Role','Worker Type','Jobs','Base Pay','Commission','Total Gross','Est Tax','Est Take-Home']]; — Object.values(payMap).forEach(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — rows.push([p.name, p.role, p.workerType, p.jobs, p.gross.toFixed(2), p.commission.toFixed(2), — gross.toFixed(2), tax.totalEstTax.toFixed(2), tax.takeHome.toFixed(2)]); — }); — downloadCSV(rows, 'jpgsystems-payroll-' + getTodayStr() + '.csv'); — notify('Payroll CSV exported ✓'); — } — function exportJobsCSV() { — const rows = [['Job ID','Client','Date','Time','Driver','Type','Status','Amount','Gates Passed','Origin','Destination']]; — STATE.jobs.forEach(j => { — const gates = Object.values(j.gates||{}).filter(Boolean).length; — rows.push([j.id, j.client, j.date, j.time||'', j.driver||'', j.type||'', — j.status, (j.amount||0).toFixed(2), gates + '/' + Object.keys(j.gates||{}).length, — j.origin||'', j.dest||'']); — }); — downloadCSV(rows, 'jpgsystems-jobs-' + getTodayStr() + '.csv'); — notify('Jobs CSV exported ✓'); — } — function exportReceiptsCSV() { — const rows = [['Receipt ID','Job ID','Driver','Date','Type','Amount','Status','Notes']]; — STATE.receipts.forEach(r => { — rows.push([r.id, r.jobId, r.driver||'', r.date||'', r.type||'', — (r.amount||0).toFixed(2), r.approved ? 'APPROVED' : r.rejected ? 'REJECTED' : 'PENDING', r.notes||'']); — }); — downloadCSV(rows, 'jpgsystems-receipts-' + getTodayStr() + '.csv'); — notify('Receipts CSV exported ✓'); — } — function downloadCSV(rows, filename) { — const csv  = rows.map(r => r.map(c => '"' + String(c).replace(/"/g,'""') + '"').join(',')).join('\n'); — const blob = new Blob([csv], { type: 'text/csv' }); — const url  = URL.createObjectURL(blob); — const a    = document.createElement('a'); — a.href     = url; — a.download = filename; — a.click(); — URL.revokeObjectURL(url); — } — Add export buttons to financials tab — function renderExportBar() { — return ` — <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;align-self:center">EXPORT:</div> — <button class="btn-sm" onclick="exportJobsCSV()">⬇ Jobs CSV</button> — <button class="btn-sm" onclick="exportReceiptsCSV()">⬇ Receipts CSV</button> — <button class="btn-sm" onclick="exportPayrollCSV()">⬇ Payroll CSV</button> — <button class="btn-sm" onclick="renderPayrollSummary()">💵 Payroll Report</button> — </div>`; — } | 3 | 3 | 0 | yes |
| `exportReceiptsCSV` | function | 17526 | function exportPayrollCSV() { — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — const addPerson = (name, role, gross, commission, workerType) => { — if (!name) return; — if (!payMap[name]) payMap[name] = { name, role, jobs: 0, gross: 0, commission: 0, workerType }; — payMap[name].jobs++; — payMap[name].gross      += gross; — payMap[name].commission += commission; — }; — addPerson(j.driver, 'Driver', j.payroll.driver||0, j.payroll.driverCommission||0, j.payroll.driverWorkerType||'1099'); — (j.payroll.helpers||[]).forEach(h => addPerson(h.name, 'Helper', calcPay(h), h.commission||0, h.workerType||'1099')); — }); — const rows = [['Name','Role','Worker Type','Jobs','Base Pay','Commission','Total Gross','Est Tax','Est Take-Home']]; — Object.values(payMap).forEach(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — rows.push([p.name, p.role, p.workerType, p.jobs, p.gross.toFixed(2), p.commission.toFixed(2), — gross.toFixed(2), tax.totalEstTax.toFixed(2), tax.takeHome.toFixed(2)]); — }); — downloadCSV(rows, 'jpgsystems-payroll-' + getTodayStr() + '.csv'); — notify('Payroll CSV exported ✓'); — } — function exportJobsCSV() { — const rows = [['Job ID','Client','Date','Time','Driver','Type','Status','Amount','Gates Passed','Origin','Destination']]; — STATE.jobs.forEach(j => { — const gates = Object.values(j.gates||{}).filter(Boolean).length; — rows.push([j.id, j.client, j.date, j.time||'', j.driver||'', j.type||'', — j.status, (j.amount||0).toFixed(2), gates + '/' + Object.keys(j.gates||{}).length, — j.origin||'', j.dest||'']); — }); — downloadCSV(rows, 'jpgsystems-jobs-' + getTodayStr() + '.csv'); — notify('Jobs CSV exported ✓'); — } — function exportReceiptsCSV() { — const rows = [['Receipt ID','Job ID','Driver','Date','Type','Amount','Status','Notes']]; — STATE.receipts.forEach(r => { — rows.push([r.id, r.jobId, r.driver||'', r.date||'', r.type||'', — (r.amount||0).toFixed(2), r.approved ? 'APPROVED' : r.rejected ? 'REJECTED' : 'PENDING', r.notes||'']); — }); — downloadCSV(rows, 'jpgsystems-receipts-' + getTodayStr() + '.csv'); — notify('Receipts CSV exported ✓'); — } — function downloadCSV(rows, filename) { — const csv  = rows.map(r => r.map(c => '"' + String(c).replace(/"/g,'""') + '"').join(',')).join('\n'); — const blob = new Blob([csv], { type: 'text/csv' }); — const url  = URL.createObjectURL(blob); — const a    = document.createElement('a'); — a.href     = url; — a.download = filename; — a.click(); — URL.revokeObjectURL(url); — } — Add export buttons to financials tab — function renderExportBar() { — return ` — <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;align-self:center">EXPORT:</div> — <button class="btn-sm" onclick="exportJobsCSV()">⬇ Jobs CSV</button> — <button class="btn-sm" onclick="exportReceiptsCSV()">⬇ Receipts CSV</button> — <button class="btn-sm" onclick="exportPayrollCSV()">⬇ Payroll CSV</button> — <button class="btn-sm" onclick="renderPayrollSummary()">💵 Payroll Report</button> — </div>`; — } | 2 | 2 | 0 | yes |
| `downloadCSV` | function | 17536 | function exportPayrollCSV() { — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — const addPerson = (name, role, gross, commission, workerType) => { — if (!name) return; — if (!payMap[name]) payMap[name] = { name, role, jobs: 0, gross: 0, commission: 0, workerType }; — payMap[name].jobs++; — payMap[name].gross      += gross; — payMap[name].commission += commission; — }; — addPerson(j.driver, 'Driver', j.payroll.driver||0, j.payroll.driverCommission||0, j.payroll.driverWorkerType||'1099'); — (j.payroll.helpers||[]).forEach(h => addPerson(h.name, 'Helper', calcPay(h), h.commission||0, h.workerType||'1099')); — }); — const rows = [['Name','Role','Worker Type','Jobs','Base Pay','Commission','Total Gross','Est Tax','Est Take-Home']]; — Object.values(payMap).forEach(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — rows.push([p.name, p.role, p.workerType, p.jobs, p.gross.toFixed(2), p.commission.toFixed(2), — gross.toFixed(2), tax.totalEstTax.toFixed(2), tax.takeHome.toFixed(2)]); — }); — downloadCSV(rows, 'jpgsystems-payroll-' + getTodayStr() + '.csv'); — notify('Payroll CSV exported ✓'); — } — function exportJobsCSV() { — const rows = [['Job ID','Client','Date','Time','Driver','Type','Status','Amount','Gates Passed','Origin','Destination']]; — STATE.jobs.forEach(j => { — const gates = Object.values(j.gates||{}).filter(Boolean).length; — rows.push([j.id, j.client, j.date, j.time||'', j.driver||'', j.type||'', — j.status, (j.amount||0).toFixed(2), gates + '/' + Object.keys(j.gates||{}).length, — j.origin||'', j.dest||'']); — }); — downloadCSV(rows, 'jpgsystems-jobs-' + getTodayStr() + '.csv'); — notify('Jobs CSV exported ✓'); — } — function exportReceiptsCSV() { — const rows = [['Receipt ID','Job ID','Driver','Date','Type','Amount','Status','Notes']]; — STATE.receipts.forEach(r => { — rows.push([r.id, r.jobId, r.driver||'', r.date||'', r.type||'', — (r.amount||0).toFixed(2), r.approved ? 'APPROVED' : r.rejected ? 'REJECTED' : 'PENDING', r.notes||'']); — }); — downloadCSV(rows, 'jpgsystems-receipts-' + getTodayStr() + '.csv'); — notify('Receipts CSV exported ✓'); — } — function downloadCSV(rows, filename) { — const csv  = rows.map(r => r.map(c => '"' + String(c).replace(/"/g,'""') + '"').join(',')).join('\n'); — const blob = new Blob([csv], { type: 'text/csv' }); — const url  = URL.createObjectURL(blob); — const a    = document.createElement('a'); — a.href     = url; — a.download = filename; — a.click(); — URL.revokeObjectURL(url); — } — Add export buttons to financials tab — function renderExportBar() { — return ` — <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;align-self:center">EXPORT:</div> — <button class="btn-sm" onclick="exportJobsCSV()">⬇ Jobs CSV</button> — <button class="btn-sm" onclick="exportReceiptsCSV()">⬇ Receipts CSV</button> — <button class="btn-sm" onclick="exportPayrollCSV()">⬇ Payroll CSV</button> — <button class="btn-sm" onclick="renderPayrollSummary()">💵 Payroll Report</button> — </div>`; — } | 3 | 3 | 0 | no |
| `renderExportBar` | function | 17548 | function exportPayrollCSV() { — const payMap = {}; — STATE.jobs.filter(j => j.payroll).forEach(j => { — const addPerson = (name, role, gross, commission, workerType) => { — if (!name) return; — if (!payMap[name]) payMap[name] = { name, role, jobs: 0, gross: 0, commission: 0, workerType }; — payMap[name].jobs++; — payMap[name].gross      += gross; — payMap[name].commission += commission; — }; — addPerson(j.driver, 'Driver', j.payroll.driver||0, j.payroll.driverCommission||0, j.payroll.driverWorkerType||'1099'); — (j.payroll.helpers||[]).forEach(h => addPerson(h.name, 'Helper', calcPay(h), h.commission||0, h.workerType||'1099')); — }); — const rows = [['Name','Role','Worker Type','Jobs','Base Pay','Commission','Total Gross','Est Tax','Est Take-Home']]; — Object.values(payMap).forEach(p => { — const gross = p.gross + p.commission; — const tax   = calcTax(gross, p.workerType); — rows.push([p.name, p.role, p.workerType, p.jobs, p.gross.toFixed(2), p.commission.toFixed(2), — gross.toFixed(2), tax.totalEstTax.toFixed(2), tax.takeHome.toFixed(2)]); — }); — downloadCSV(rows, 'jpgsystems-payroll-' + getTodayStr() + '.csv'); — notify('Payroll CSV exported ✓'); — } — function exportJobsCSV() { — const rows = [['Job ID','Client','Date','Time','Driver','Type','Status','Amount','Gates Passed','Origin','Destination']]; — STATE.jobs.forEach(j => { — const gates = Object.values(j.gates||{}).filter(Boolean).length; — rows.push([j.id, j.client, j.date, j.time||'', j.driver||'', j.type||'', — j.status, (j.amount||0).toFixed(2), gates + '/' + Object.keys(j.gates||{}).length, — j.origin||'', j.dest||'']); — }); — downloadCSV(rows, 'jpgsystems-jobs-' + getTodayStr() + '.csv'); — notify('Jobs CSV exported ✓'); — } — function exportReceiptsCSV() { — const rows = [['Receipt ID','Job ID','Driver','Date','Type','Amount','Status','Notes']]; — STATE.receipts.forEach(r => { — rows.push([r.id, r.jobId, r.driver||'', r.date||'', r.type||'', — (r.amount||0).toFixed(2), r.approved ? 'APPROVED' : r.rejected ? 'REJECTED' : 'PENDING', r.notes||'']); — }); — downloadCSV(rows, 'jpgsystems-receipts-' + getTodayStr() + '.csv'); — notify('Receipts CSV exported ✓'); — } — function downloadCSV(rows, filename) { — const csv  = rows.map(r => r.map(c => '"' + String(c).replace(/"/g,'""') + '"').join(',')).join('\n'); — const blob = new Blob([csv], { type: 'text/csv' }); — const url  = URL.createObjectURL(blob); — const a    = document.createElement('a'); — a.href     = url; — a.download = filename; — a.click(); — URL.revokeObjectURL(url); — } — Add export buttons to financials tab — function renderExportBar() { — return ` — <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;align-self:center">EXPORT:</div> — <button class="btn-sm" onclick="exportJobsCSV()">⬇ Jobs CSV</button> — <button class="btn-sm" onclick="exportReceiptsCSV()">⬇ Receipts CSV</button> — <button class="btn-sm" onclick="exportPayrollCSV()">⬇ Payroll CSV</button> — <button class="btn-sm" onclick="renderPayrollSummary()">💵 Payroll Report</button> — </div>`; — } | 0 | 0 | 0 | no |
| `renderMargins` | function | 17564 | function renderMargins() { — const content = document.getElementById('main-content'); — Build margin data per job — const jobMargins = STATE.jobs — .filter(j => j.amount > 0 || j.payroll) — .map(j => { — const revenue   = j.amount || 0; — const driverAccBonus = parseFloat(j.payroll?.accessorialBonus) || 0; — const crewCost  = (j.payroll?.driver || 0) + — driverAccBonus + — (j.payroll?.helpers || []).reduce((s,h) => s + calcPay(h), 0) + — (j.payroll?.driverCommission || 0); — Company revenue excludes driver accessorial bonuses (passed through to driver) — const expenses  = STATE.receipts — .filter(r => r.jobId === j.id && r.approved !== false) — .reduce((s,r) => s + (r.amount||0), 0); — const net       = revenue - crewCost - expenses; — const margin    = revenue > 0 ? (net / revenue * 100) : 0; — return { job: j, revenue, crewCost, expenses, net, margin }; — }) — .sort((a,b) => { — Default: worst margin first (ascending) — const mA = a.revenue > 0 ? a.profit/a.revenue : 0; — const mB = b.revenue > 0 ? b.profit/b.revenue : 0; — return mA - mB; — }); — Sort control — if (window._marginSort === 'date') jobMargins.sort((a,b) => b.job.date > a.job.date ? 1 : -1); — else if (window._marginSort === 'revenue') jobMargins.sort((a,b) => b.revenue - a.revenue); — ── Margin intelligence ── — const overpayJobs   = jobMargins.filter(m => m.revenue > 0 && (m.crewCost/m.revenue) > 0.40); — const lowProfitJobs = jobMargins.filter(m => m.revenue > 0 && (m.profit/m.revenue) < 0.20); — const bestJob       = [...jobMargins].sort((a,b) => (b.profit/b.revenue||0)-(a.profit/a.revenue||0))[0]; — const worstJob      = jobMargins[0]; // already sorted worst first — Overall stats — const totalRevenue  = jobMargins.reduce((s,m) => s + m.revenue, 0); — const totalCrew     = jobMargins.reduce((s,m) => s + m.crewCost, 0); — const totalExpenses = jobMargins.reduce((s,m) => s + m.expenses, 0); — const totalNet      = jobMargins.reduce((s,m) => s + m.net, 0); — const avgMargin     = totalRevenue > 0 ? (totalNet / totalRevenue * 100) : 0; — const profitable    = jobMargins.filter(m => m.net > 0).length; — const losing        = jobMargins.filter(m => m.net < 0).length; — Best and worst job — const best  = jobMargins.reduce((a,b) => b.margin > a.margin ? b : a, jobMargins[0]); — const worst = jobMargins.reduce((a,b) => b.margin < a.margin ? b : a, jobMargins[0]); — const marginColor = (n) => n >= 40 ? 'var(--green)' : n >= 20 ? 'var(--amber)' : n >= 0 ? '#ff8c00' : 'var(--red)'; — const marginBg    = (n) => n >= 40 ? 'rgba(0,255,65,0.08)' : n >= 20 ? 'rgba(255,170,0,0.08)' : n >= 0 ? 'rgba(255,140,0,0.08)' : 'rgba(255,51,51,0.08)'; — let html = ` — ${overpayJobs.length ? `<div style="background:rgba(255,30,30,0.06);border:1px solid rgba(255,30,30,0.3);border-radius:6px;padding:10px 14px;margin-bottom:12px;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px"> — <div> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;font-weight:700">⚠ LABOR OVERPAY ALERT</div> — <div style="font-size:12px;color:var(--white);margin-top:3px">${overpayJobs.length} job${overpayJobs.length>1?'s':''} with crew cost >40% of revenue</div> — <div style="font-size:11px;color:var(--gray);margin-top:2px">${overpayJobs.slice(0,3).map(m=>m.job.client+' ('+Math.round(m.crewCost/m.revenue*100)+'%)').join(' · ')}</div> — </div> — </div>` : ''} — <div style="display:flex;gap:8px;margin-bottom:12px;flex-wrap:wrap"> — <span style="font-family:var(--font-mono);font-size:10px;color:var(--gray);align-self:center">Sort:</span> — ${['worst:⬇ Worst Margin','revenue:$ Revenue','date:📅 Date'].map(o=>{const[v,l]=o.split(':');return `<button onclick="window._marginSort='${v}';renderMargins()" style="padding:5px 12px;font-family:var(--font-mono);font-size:10px;border-radius:3px;cursor:pointer;background:${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--green)':'transparent'};color:${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--navy)':'var(--gray)'};border:1px solid ${window._marginSort===v||(!window._marginSort&&v==='worst')?'var(--green)':'var(--gray2)'}">${l}</button>`;}).join('')} — </div> — <div class="section-header"> — <div> — <div class="section-title">📈 JOB MARGIN DASHBOARD</div> — <div class="section-sub">Revenue · Crew Cost · Expenses · Net per Job</div> — </div> — <button class="btn-outline" style="padding:8px 14px;font-size:11px" onclick="exportJobsCSV()">⬇ EXPORT</button> — </div> — <!-- OVERALL STATS --> — <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(130px,1fr));gap:8px;margin-bottom:16px"> — <div class="stat-box"> — <div class="stat-label">Total Revenue</div> — <div class="stat-value">$${totalRevenue.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${jobMargins.length} jobs</div> — </div> — <div class="stat-box"> — <div class="stat-label">Crew Cost</div> — <div class="stat-value red">$${totalCrew.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${totalRevenue>0?(totalCrew/totalRevenue*100).toFixed(1):'0'}% of revenue</div> — </div> — <div class="stat-box"> — <div class="stat-label">Expenses</div> — <div class="stat-value red">$${totalExpenses.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px">${totalRevenue>0?(totalExpenses/totalRevenue*100).toFixed(1):'0'}% of revenue</div> — </div> — <div class="stat-box" style="border-color:${marginColor(avgMargin)}"> — <div class="stat-label">Net Profit</div> — <div class="stat-value" style="color:${marginColor(avgMargin)}">$${totalNet.toLocaleString()}</div> — <div class="info-text" style="margin-top:4px;color:${marginColor(avgMargin)}">${avgMargin.toFixed(1)}% avg margin</div> — </div> — <div class="stat-box"> — <div class="stat-label">Profitable Jobs</div> — <div class="stat-value" style="color:var(--green)">${profitable}</div> — <div class="info-text" style="margin-top:4px;color:var(--red)">${losing} losing</div> — </div> — </div> — <!-- MARGIN BREAKDOWN BAR --> — ${totalRevenue > 0 ? ` — <div class="card" style="margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--gray);letter-spacing:2px;margin-bottom:10px">REVENUE BREAKDOWN</div> — <div style="height:32px;border-radius:4px;overflow:hidden;display:flex;margin-bottom:8px"> — <div style="width:${(totalCrew/totalRevenue*100).toFixed(1)}%;background:rgba(255,51,51,0.7);transition:width 0.5s" title="Crew Cost"></div> — <div style="width:${(totalExpenses/totalRevenue*100).toFixed(1)}%;background:rgba(255,140,0,0.7);transition:width 0.5s" title="Expenses"></div> — <div style="flex:1;background:rgba(0,255,65,0.4);transition:width 0.5s" title="Net Profit"></div> — </div> — <div style="display:flex;gap:16px;flex-wrap:wrap"> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(255,51,51,0.7);border-radius:2px"></div><span class="info-text">Crew ${(totalCrew/totalRevenue*100).toFixed(1)}%</span></div> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(255,140,0,0.7);border-radius:2px"></div><span class="info-text">Expenses ${(totalExpenses/totalRevenue*100).toFixed(1)}%</span></div> — <div style="display:flex;align-items:center;gap:6px"><div style="width:12px;height:12px;background:rgba(0,255,65,0.4);border-radius:2px"></div><span class="info-text">Net ${avgMargin.toFixed(1)}%</span></div> — </div> — </div>` : ''} — <!-- BEST / WORST --> — ${jobMargins.length >= 2 ? ` — <div class="grid-2" style="margin-bottom:16px"> — ${best ? `<div class="card" style="border-color:var(--green3)"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--green);letter-spacing:2px;margin-bottom:6px">🏆 BEST MARGIN JOB</div> — <div style="font-size:15px;font-weight:700">${best.job.client} <span class="tag">${best.job.id}</span></div> — <div class="info-text">${best.job.date}</div> — <div style="font-family:var(--font-mono);font-size:22px;color:var(--green);margin-top:6px">${best.margin.toFixed(1)}%</div> — <div class="info-text">$${best.net.toFixed(2)} net on $${best.revenue.toLocaleString()}</div> — </div>` : ''} — ${worst ? `<div class="card" style="border-color:${marginColor(worst.margin)}"> — <div style="font-family:var(--font-mono);font-size:9px;color:${marginColor(worst.margin)};letter-spacing:2px;margin-bottom:6px">${worst.net < 0 ? '⚠ LOSING JOB' : '📉 LOWEST MARGIN'}</div> — <div style="font-size:15px;font-weight:700">${worst.job.client} <span class="tag">${worst.job.id}</span></div> — <div class="info-text">${worst.job.date}</div> — <div style="font-family:var(--font-mono);font-size:22px;color:${marginColor(worst.margin)};margin-top:6px">${worst.margin.toFixed(1)}%</div> — <div class="info-text">$${worst.net.toFixed(2)} net on $${worst.revenue.toLocaleString()}</div> — </div>` : ''} — </div>` : ''} — <!-- PER-JOB TABLE --> — <div class="card" style="padding:0;overflow:hidden"> — <div style="padding:12px 16px;border-bottom:1px solid var(--gray2)"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--green);letter-spacing:2px">JOB BY JOB BREAKDOWN</div> — </div> — ${jobMargins.length ? ` — <div style="overflow-x:auto"> — <table style="min-width:700px"> — <thead><tr> — <th>Job</th> — <th>Client</th> — <th>Date</th> — <th style="text-align:right">Revenue</th> — <th style="text-align:right">Crew Cost</th> — <th style="text-align:right">Expenses</th> — <th style="text-align:right">Net</th> — <th style="text-align:center">Margin</th> — <th>Status</th> — </tr></thead> — <tbody> — ${jobMargins.map(m => ` — <tr style="cursor:pointer;background:${marginBg(m.margin)}" onclick="switchTab('jobs');setTimeout(()=>openJobDetail('${m.job.id}'),300)"> — <td><span class="tag">${m.job.id}</span></td> — <td style="font-weight:600">${m.job.client}</td> — <td class="info-text">${m.job.date}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--white)">$${m.revenue.toLocaleString()}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${m.crewCost.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:#ff8c00">$${m.expenses.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;font-weight:700;color:${marginColor(m.margin)}"> — ${m.net >= 0 ? '+' : ''}$${m.net.toFixed(2)} — </td> — <td style="text-align:center"> — <div style="display:inline-flex;align-items:center;justify-content:center;width:52px;height:24px;border-radius:12px;background:${marginBg(m.margin)};border:1px solid ${marginColor(m.margin)};font-family:var(--font-mono);font-size:10px;font-weight:700;color:${marginColor(m.margin)}"> — ${m.margin.toFixed(0)}% — </div> — </td> — <td><span class="status-badge status-${m.job.status}">${m.job.status}</span></td> — </tr>`).join('')} — <tr style="border-top:2px solid var(--green3);font-weight:700"> — <td colspan="3" style="font-family:var(--font-mono);color:var(--green);font-size:11px">TOTALS</td> — <td style="font-family:var(--font-mono);text-align:right">$${totalRevenue.toLocaleString()}</td> — <td style="font-family:var(--font-mono);text-align:right;color:var(--red)">$${totalCrew.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;color:#ff8c00">$${totalExpenses.toFixed(2)}</td> — <td style="font-family:var(--font-mono);text-align:right;font-weight:700;color:${marginColor(avgMargin)}"> — ${totalNet >= 0 ? '+' : ''}$${totalNet.toFixed(2)} — </td> — <td style="text-align:center"> — <div style="display:inline-flex;align-items:center;justify-content:center;width:52px;height:24px;border-radius:12px;background:${marginBg(avgMargin)};border:1px solid ${marginColor(avgMargin)};font-family:var(--font-mono);font-size:10px;font-weight:700;color:${marginColor(avgMargin)}"> — ${avgMargin.toFixed(0)}% — </div> — </td> — <td></td> — </tr> — </tbody> — </table> — </div>` : ` — <div style="text-align:center;padding:48px"> — <div style="font-size:40px;margin-bottom:12px">📈</div> — <p class="info-text">No job data yet. Create jobs with quoted amounts and crew logs to see margins.</p> — </div>`} — </div> — <!-- MARGIN LEGEND --> — <div style="display:flex;gap:16px;padding:12px 4px;flex-wrap:wrap;align-items:center"> — <span class="info-text">Margin health:</span> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(0,255,65,0.2);border:1px solid var(--green);border-radius:2px"></div><span class="info-text" style="color:var(--green)">40%+ Excellent</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,170,0,0.2);border:1px solid var(--amber);border-radius:2px"></div><span class="info-text" style="color:var(--amber)">20–39% Good</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,140,0,0.2);border:1px solid #ff8c00;border-radius:2px"></div><span class="info-text" style="color:#ff8c00">0–19% Thin</span></div> — <div style="display:flex;align-items:center;gap:5px"><div style="width:12px;height:12px;background:rgba(255,51,51,0.15);border:1px solid var(--red);border-radius:2px"></div><span class="info-text" style="color:var(--red)">Negative — Losing money</span></div> — </div>`; — content.innerHTML = html; — } | 2 | 2 | 0 | yes |
| `openPayrollCorrectByName` | function | 17781 | Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — } | 1 | 1 | 0 | yes |
| `openManualPayrollCorrection` | function | 17826 | Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — } | 2 | 2 | 0 | yes |
| `saveManualPayrollCorrection` | function | 17890 | Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — } | 1 | 1 | 0 | yes |
| `editPayrollEntry` | function | 17926 | Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — } | 6 | 6 | 0 | yes |
| `savePayrollEdit` | function | 18007 | Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — } | 1 | 1 | 0 | yes |
| `voidPayrollEntry` | function | 18074 | Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — } | 0 | 0 | 0 | no |
| `restorePayrollEntry` | function | 18079 | Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — } | 0 | 0 | 0 | no |
| `runDuplicateScan` | function | 18086 | Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — } | 2 | 2 | 0 | no |
| `showDuplicateScanResults` | function | 18159 | Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — } | 1 | 1 | 0 | yes |
| `checkForDuplicatesOnLoad` | function | 18185 | Opens a correction picker for all payroll records for a person — function openPayrollCorrectByName(crewName) { — const records = STATE.payrollLedger — .filter(p => namesMatch(p.crewName, crewName)) — .sort((a,b) => b.paidAt > a.paidAt ? 1 : -1); — if (!records.length) { — openManualPayrollCorrection(crewName); — return; — } — if (records.length === 1) { — editPayrollEntry(records[0]._docId); — return; — } — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYROLL — ${crewName}</div> — <div class="info-text" style="margin-bottom:14px">Select the payment record to correct:</div> — ${records.map(r => ` — <div style="display:flex;justify-content:space-between;align-items:center; — padding:12px 14px;margin-bottom:8px;border:2px solid ${r.voided?'var(--red)':'var(--gray2)'}; — border-radius:4px;background:${r.voided?'rgba(255,51,51,0.05)':'rgba(0,255,65,0.02)'}"> — <div> — <div style="font-weight:700;font-family:var(--font-mono);font-size:14px"> — $${(r.amount||0).toFixed(2)} via ${r.method||'—'} — ${r.voided?'<span style="color:var(--red);font-size:10px;margin-left:8px">⚠ MARKED NOT PAID</span>':''} — </div> — <div class="info-text">${r.paidAt?.slice(0,10)||'No date'} · ${r.periodJobs||0} job(s) · logged by ${r.paidBy||'System'}</div> — ${r.correctionNote?`<div class="info-text" style="color:var(--amber)">📝 ${r.correctionNote}</div>`:''} — </div> — <button class="btn-outline" style="padding:8px 16px;font-size:11px;color:var(--amber);border-color:#664400;font-weight:700;white-space:nowrap;margin-left:12px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>editPayrollEntry('${r._docId}'),150)"> — ✎ CORRECT — </button> — </div>`).join('')} — <div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--gray2)"> — <button class="btn-outline" style="width:100%;padding:10px;font-size:11px" — onclick="closeModal('modal-jobdetail');setTimeout(()=>openManualPayrollCorrection('${crewName}'),150)"> — + ADD MANUAL CORRECTION ENTRY — </button> — </div>`; — openModal('modal-jobdetail'); — } — For cases where the balance shows owed but no ledger record exists — function openManualPayrollCorrection(crewName) { — const ledger = buildCrewLedger(); — const person = ledger[crewName] || {}; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ MANUAL PAYROLL CORRECTION — ${crewName}</div> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:8px">⚠ CORRECTION ENTRY FOR ${crewName.toUpperCase()}</div> — ${person.earned > 0 ? ` — <div style="font-family:var(--font-mono);font-size:12px;margin-top:6px"> — Earned this cycle: <span style="color:var(--green)">$${person.earned.toFixed(2)}</span> · — Logged as paid: <span style="color:var(--amber)">$${person.paidThisCycle.toFixed(2)}</span> · — Balance: <span style="color:${person.balance>0?'var(--red)':'var(--green)'}">$${person.balance.toFixed(2)}</span> — </div>` : ''} — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount (enter 0 if nothing was paid)</label> — <input type="number" id="manual-amount" value="0" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Method</label> — <select id="manual-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — <option value="None / Not Paid">None / Not Paid</option> — <option value="Cash">Cash</option> — <option value="Zelle">Zelle</option> — <option value="CashApp">CashApp</option> — <option value="Check">Check</option> — <option value="Venmo">Venmo</option> — </select> — </div> — </div> — <div class="form-group"> — <label>Date</label> — <input type="date" id="manual-date" value="${new Date().toISOString().slice(0,10)}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <label style="display:flex;align-items:center;gap:10px;cursor:pointer;padding:12px 14px;border:2px solid var(--red);border-radius:4px;background:rgba(255,51,51,0.06);margin-bottom:14px"> — <input type="checkbox" id="manual-notpaid" checked style="accent-color:var(--red);width:18px;height:18px" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red);font-weight:700"> — ✕ THIS PAYMENT WAS NOT MADE — keep balance open as owed — </span> — </label> — <div class="form-group"> — <label>Correction Note</label> — <input type="text" id="manual-note" value="Payment not made — correcting record" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px" — onclick="saveManualPayrollCorrection('${crewName}')">✓ SAVE CORRECTION</button> — </div>`; — openModal('modal-jobdetail'); — } — async function saveManualPayrollCorrection(crewName) { — const amount  = parseFloat(document.getElementById('manual-amount')?.value) || 0; — const method  = document.getElementById('manual-method')?.value; — const date    = document.getElementById('manual-date')?.value; — const notPaid = document.getElementById('manual-notpaid')?.checked; — const note    = document.getElementById('manual-note')?.value.trim(); — const ledger  = buildCrewLedger(); — const person  = ledger[crewName] || {}; — try { — const record = { — crewName, role: person.role||'crew', username: person.username||'', — amount, method, note, correctionNote: note, — periodJobs: person.jobCount||0, periodEarned: person.earned||0, — paidBy: currentUser.name, — paidAt: date ? date+'T12:00:00.000Z' : new Date().toISOString(), — cycleClosedAt: new Date().toISOString(), — voided: notPaid, — voidReason: notPaid ? note : null, — voidedBy: notPaid ? currentUser.name : null, — voidedAt: notPaid ? new Date().toISOString() : null, — isManualCorrection: true, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }; — await db.collection(COL_PAYROLL_LEDGER).add(record); — closeModal('modal-jobdetail'); — notify(notPaid — ? `✓ Correction recorded — ${crewName} marked as NOT paid · balance stays open` — : `✓ Correction entry saved for ${crewName}`); — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('saveManualPayrollCorrection error:', e); — } — } — async function editPayrollEntry(docId) { — const entry = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — document.getElementById('jobdetail-content').innerHTML = ` — <button class="modal-close" onclick="closeModal('modal-jobdetail')">✕</button> — <div class="modal-title">✎ CORRECT PAYMENT RECORD — ${entry.crewName}</div> — <!-- Current record display --> — <div style="background:rgba(0,0,0,0.25);border:1px solid var(--gray2);border-radius:4px;padding:12px 14px;margin-bottom:16px"> — <div style="font-family:var(--font-mono);font-size:9px;color:var(--gray);letter-spacing:2px;margin-bottom:8px">CURRENT RECORD</div> — <div style="display:flex;gap:20px;flex-wrap:wrap"> — <div><div style="font-size:11px;color:var(--gray)">Amount</div><div style="font-family:var(--font-mono);font-size:18px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'<s>':''}$${(entry.amount||0).toFixed(2)}${entry.voided?'</s>':''}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Method</div><div style="font-family:var(--font-mono);font-size:14px">${entry.method||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Date</div><div style="font-family:var(--font-mono);font-size:14px">${entry.paidAt?.slice(0,10)||'—'}</div></div> — <div><div style="font-size:11px;color:var(--gray)">Status</div><div style="font-family:var(--font-mono);font-size:11px;color:${entry.voided?'var(--red)':'var(--green)'}">${entry.voided?'⚠ VOIDED — BALANCE RESTORED':'✓ MARKED PAID'}</div></div> — </div> — ${entry.correctionNote ? `<div style="margin-top:8px;font-size:11px;color:var(--amber)">📝 Previous correction: ${entry.correctionNote}</div>` : ''} — </div> — <!-- PAID STATUS TOGGLE — most important field --> — <div style="background:rgba(255,51,51,0.06);border:2px solid #552222;border-radius:4px;padding:14px;margin-bottom:14px"> — <div style="font-family:var(--font-mono);font-size:10px;color:var(--red);letter-spacing:2px;margin-bottom:10px">⚠ PAYMENT STATUS</div> — <div style="display:flex;gap:10px;flex-wrap:wrap"> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${!entry.voided?'var(--green)':'var(--gray2)'};border-radius:4px;background:${!entry.voided?'rgba(0,255,65,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-paid" value="paid" ${!entry.voided?'checked':''} style="accent-color:var(--green)" /> — <span style="font-family:var(--font-mono);font-size:12px">✓ PAYMENT WAS MADE — keep as paid</span> — </label> — <label style="display:flex;align-items:center;gap:8px;cursor:pointer;padding:10px 16px;border:2px solid ${entry.voided?'var(--red)':'var(--gray2)'};border-radius:4px;background:${entry.voided?'rgba(255,51,51,0.08)':'transparent'}"> — <input type="radio" name="pay-status" id="status-notpaid" value="notpaid" ${entry.voided?'checked':''} style="accent-color:var(--red)" /> — <span style="font-family:var(--font-mono);font-size:12px;color:var(--red)">✕ PAYMENT WAS NOT MADE — mark as unpaid, restore balance</span> — </label> — </div> — </div> — <div class="grid-2"> — <div class="form-group"> — <label>Amount</label> — <input type="number" id="edit-pay-amount" value="${entry.amount||0}" step="0.01" min="0" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:18px;border-radius:3px;outline:none;width:100%;font-weight:700" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Payment Method</label> — <select id="edit-pay-method" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%"> — ${['Cash','Zelle','CashApp','Check','Venmo','Direct Deposit','None / Not Paid'].map(m => — `<option ${entry.method===m?'selected':''}>${m}</option>`).join('')} — </select> — </div> — </div> — <div class="form-group"> — <label>Payment Date</label> — <input type="date" id="edit-pay-date" value="${(entry.paidAt&&entry.paidAt.slice(0,10))||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:13px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div class="form-group"> — <label>Correction Note <span style="color:var(--gray);font-size:11px">(optional — describe what was wrong)</span></label> — <input type="text" id="edit-pay-note" — placeholder="e.g. Payment never sent — employee did not receive funds" — value="${entry.correctionNote||''}" — style="background:var(--navy);border:1px solid var(--gray2);color:var(--white);padding:10px;font-family:var(--font-mono);font-size:12px;border-radius:3px;outline:none;width:100%" — onfocus="this.style.borderColor='var(--green)'" onblur="this.style.borderColor='var(--gray2)'" /> — </div> — <div style="background:rgba(0,0,0,0.2);border-radius:3px;padding:10px 12px;margin-bottom:14px;font-family:var(--font-mono);font-size:10px;color:var(--gray);line-height:1.8"> — If you select <span style="color:var(--red)">NOT MADE</span> — this payment cycle is voided, the employee's balance is fully restored, and they will appear as unpaid in the Payroll Ledger. The record stays in history but is flagged as corrected. — </div> — <div style="display:flex;gap:10px;justify-content:flex-end"> — <button class="btn-outline" onclick="closeModal('modal-jobdetail')">CANCEL</button> — <button class="btn-green" style="padding:12px 24px;font-size:13px;font-weight:700" onclick="savePayrollEdit('${docId}')"> — ✓ SAVE CORRECTION — </button> — </div>`; — openModal('modal-jobdetail'); — } — async function savePayrollEdit(docId) { — const entry  = STATE.payrollLedger.find(p => p._docId === docId); — if (!entry) return; — const status  = document.querySelector('input[name="pay-status"]:checked')?.value; — const amount  = parseFloat(document.getElementById('edit-pay-amount')?.value) || 0; — const method  = document.getElementById('edit-pay-method')?.value; — const date    = document.getElementById('edit-pay-date')?.value; — const note    = document.getElementById('edit-pay-note')?.value.trim(); — const notPaid = status === 'notpaid'; — try { — const update = { — amount, — method, — paidAt:         date ? date + 'T12:00:00.000Z' : entry.paidAt, — correctionNote: note || null, — correctedBy:    currentUser.name, — correctedAt:    new Date().toISOString(), — voided:         notPaid, — }; — if (notPaid) { — update.voidReason = note || 'Payment marked as not made — balance restored'; — update.voidedBy   = currentUser.name; — update.voidedAt   = new Date().toISOString(); — } else { — Restoring to paid — clear void fields — update.voidReason = null; — update.voidedBy   = null; — update.voidedAt   = null; — } — await db.collection(COL_PAYROLL_LEDGER).doc(docId).update(update); — Also update any matching disbursement record — const matchDisb = STATE.disbursements.find(d => — d.driver === entry.crewName && d.isPayroll && — Math.abs(new Date(d.createdAt) - new Date(entry.paidAt)) < 24*60*60*1000 — ); — if (matchDisb?._docId) { — await db.collection(COL_DISBURSEMENTS).doc(matchDisb._docId).update({ — amount, — method, — notes: note || matchDisb.notes, — voided: notPaid, — correctedBy: currentUser.name, — correctedAt: new Date().toISOString() — }); — } — closeModal('modal-jobdetail'); — if (notPaid) { — notify(`⚠ Payment record voided — ${entry.crewName}'s balance restored. They will show as unpaid.`); — } else { — notify(`✓ Payment record corrected for ${entry.crewName}`); — } — renderPayrollLedger(); — } catch(e) { — notify('⚠ ' + e.message, true); — console.error('savePayrollEdit error:', e); — } — } — async function voidPayrollEntry(docId, crewName) { — Redirect to the full edit modal for a better UX — editPayrollEntry(docId); — } — async function restorePayrollEntry(docId) { — editPayrollEntry(docId); — } | 0 | 0 | 0 | no |
| `renderPayrollLedger` | function | 18200 | PAYROLL LEDGER — Running balance per crew member — Balance = earned - paid. When paid → resets to 0 → new cycle | 5 | 5 | 0 | no |
| `buildCrewLedger` | function | 18360 | PAYROLL LEDGER — Build running balance from jobs + payments | 8 | 8 | 0 | no |
| `getNextPayday` | function | 18542 | PAYROLL LEDGER — Build running balance from jobs + payments | 1 | 1 | 0 | no |
| `openMarkPaidModal` | function | 18557 | PAYROLL LEDGER — Mark Paid modal | 0 | 0 | 0 | no |
| `processPayment` | function | 18610 | PAYROLL LEDGER — Mark Paid modal | 0 | 0 | 0 | no |
| `openPayAllModal` | function | 18698 | PAYROLL LEDGER — Mark Paid modal | 1 | 1 | 0 | yes |
| `processPayAll` | function | 18736 | PAYROLL LEDGER — Mark Paid modal | 1 | 1 | 0 | yes |
| `openCrewLedgerDetail` | function | 18768 | PAYROLL LEDGER — Mark Paid modal | 2 | 2 | 0 | yes |
| `renderLineLabelSettings` | function | 18838 | SETTINGS TAB | 1 | 1 | 0 | no |
| `renderSettings` | function | 18851 | SETTINGS TAB | 10 | 10 | 0 | no |
| `toggleSetting` | function | 19443 | SETTINGS ACTION FUNCTIONS | 0 | 0 | 0 | no |
| `saveSetting` | function | 19449 | SETTINGS ACTION FUNCTIONS | 3 | 3 | 0 | yes |
| `switchRole` | function | 19458 | ROLE SWITCHER — instant role switch without logout | 1 | 1 | 0 | yes |
| `saveProfileSettings` | function | 19496 | ROLE SWITCHER — instant role switch without logout | 1 | 1 | 0 | yes |
| `saveTwilioConfig` | function | 19505 | ROLE SWITCHER — instant role switch without logout | 1 | 1 | 0 | yes |
| `saveCompanySettings` | function | 19516 | ROLE SWITCHER — instant role switch without logout | 1 | 1 | 0 | yes |
| `handleLogoUpload` | function | 19561 | ROLE SWITCHER — instant role switch without logout | 1 | 1 | 0 | yes |
| `removeCompanyLogo` | function | 19575 | ROLE SWITCHER — instant role switch without logout | 1 | 1 | 0 | yes |
| `setAccentColor` | function | 19582 | ROLE SWITCHER — instant role switch without logout | 2 | 2 | 0 | yes |
| `changePassword` | function | 19589 | ROLE SWITCHER — instant role switch without logout | 1 | 1 | 0 | yes |
| `changeAvatar` | function | 19608 | ROLE SWITCHER — instant role switch without logout | 1 | 1 | 0 | yes |
| `testPushPermission` | function | 19621 | ROLE SWITCHER — instant role switch without logout | 1 | 1 | 0 | yes |
| `clearAllSettings` | function | 19638 | ROLE SWITCHER — instant role switch without logout | 1 | 1 | 0 | yes |
| `renderUsers` | function | 19651 | USER MANAGEMENT (Office only) | 5 | 5 | 0 | no |
| `openEditHandle` | function | 19764 | USER MANAGEMENT (Office only) | 0 | 0 | 0 | no |
| `saveEmployeeId` | function | 19796 | USER MANAGEMENT (Office only) | 0 | 0 | 0 | no |
| `openAddUser` | function | 19820 | USER MANAGEMENT (Office only) | 1 | 1 | 0 | yes |
| `openEditUser` | function | 19837 | USER MANAGEMENT (Office only) | 1 | 1 | 0 | yes |
| `saveUser` | function | 19881 | USER MANAGEMENT (Office only) | 1 | 1 | 0 | yes |
| `toggleUserActive` | function | 19931 | USER MANAGEMENT (Office only) | 0 | 0 | 0 | no |
| `deleteUser` | function | 19946 | USER MANAGEMENT (Office only) | 1 | 1 | 0 | yes |
| `resetPassword` | function | 19958 | USER MANAGEMENT (Office only) | 1 | 1 | 0 | yes |
| `openModal` | function | 19971 | MODAL HELPERS | 40 | 40 | 0 | no |
| `closeModal` | function | 19983 | MODAL HELPERS | 119 | 119 | 0 | yes |
| `openEditJobFromCal` | function | 19989 | MODAL HELPERS | 1 | 1 | 0 | yes |
| `openJobDetailFromCal` | function | 19995 | MODAL HELPERS | 1 | 1 | 0 | yes |
| `notifyTimer` | let | 20012 | NOTIFICATION | 4 | 2 | 2 | no |
| `notify` | function | 20013 | NOTIFICATION | 232 | 232 | 0 | yes |
| `_scribbleCtx` | var | 20024 | NOTIFICATION | 22 | 20 | 2 | no |
| `_scribbleStrokes` | var | 20024 | NOTIFICATION | 8 | 5 | 3 | no |
| `_scribbleCur` | var | 20024 | NOTIFICATION | 6 | 3 | 3 | no |
| `_scribbleTarget` | var | 20024 | NOTIFICATION | 5 | 2 | 3 | no |
| `_scribbleDraw` | var | 20024 | NOTIFICATION | 5 | 2 | 3 | no |
| `openScribble` | function | 20025 | NOTIFICATION | 1 | 1 | 0 | yes |
| `_sPos` | function | 20026 | NOTIFICATION | 6 | 6 | 0 | no |
| `closeScribble` | function | 20027 | NOTIFICATION | 2 | 2 | 0 | yes |
| `scribbleClear` | function | 20028 | NOTIFICATION | 1 | 1 | 0 | yes |
| `scribbleUndo` | function | 20029 | NOTIFICATION | 1 | 1 | 0 | yes |
| `scribbleTranscribe` | function | 20030 | NOTIFICATION | 2 | 2 | 0 | yes |
| `scribbleApply` | function | 20031 | NOTIFICATION | 1 | 1 | 0 | yes |
| `PLANS` | const | 20041 | TRIAL + TIER SYSTEM | 14 | 13 | 1 | no |
| `TRIAL_DAYS` | const | 20046 | TRIAL + TIER SYSTEM | 3 | 2 | 1 | no |
| `checkCompanyAccess` | function | 20048 | TRIAL + TIER SYSTEM | 1 | 1 | 0 | no |
| `getCompanyPlan` | function | 20068 | TRIAL + TIER SYSTEM | 1 | 1 | 0 | no |
| `toggleTierFirewall` | function | 20074 | TRIAL + TIER SYSTEM | 0 | 0 | 0 | no |
| `toggleCompanyFeature` | function | 20079 | TRIAL + TIER SYSTEM | 0 | 0 | 0 | no |
| `_platformConfig` | let | 20090 | TRIAL + TIER SYSTEM | 2 | 0 | 2 | no |
| `loadPlatformConfig` | function | 20091 | TRIAL + TIER SYSTEM | 1 | 1 | 0 | no |
| `planAllowsTab` | function | 20093 | TRIAL + TIER SYSTEM | 1 | 1 | 0 | no |
| `showTrialBanner` | function | 20119 | TRIAL + TIER SYSTEM | 1 | 1 | 0 | no |
| `showUpgradeModal` | function | 20131 | TRIAL + TIER SYSTEM | 3 | 3 | 0 | yes |
| `selectUpgradePlan` | function | 20143 | TRIAL + TIER SYSTEM | 2 | 2 | 0 | yes |
| `requestUpgrade` | function | 20149 | TRIAL + TIER SYSTEM | 1 | 1 | 0 | yes |
| `showTabLockedPrompt` | function | 20156 | TRIAL + TIER SYSTEM | 1 | 1 | 0 | no |
| `showPaywall` | function | 20163 | TRIAL + TIER SYSTEM | 1 | 1 | 0 | no |
| `toggleCompanyAccess` | function | 20178 | TRIAL + TIER SYSTEM | 0 | 0 | 0 | no |
| `setCompanyPlan` | function | 20187 | TRIAL + TIER SYSTEM | 1 | 1 | 0 | yes |
| `_photoViewerPhotos` | let | 20199 | PHOTO VIEWER | 13 | 9 | 4 | no |
| `_photoViewerIdx` | let | 20200 | PHOTO VIEWER | 13 | 8 | 5 | no |
| `_photoViewerJobId` | let | 20201 | PHOTO VIEWER | 5 | 3 | 2 | no |
| `_photoViewerField` | let | 20202 | PHOTO VIEWER | 8 | 6 | 2 | no |
| `_photoZoomLevel` | let | 20203 | PHOTO VIEWER | 9 | 3 | 6 | no |
| `openPhotoViewer` | function | 20205 | PHOTO VIEWER | 2 | 2 | 0 | yes |
| `_refreshPhotoViewer` | function | 20218 | PHOTO VIEWER | 5 | 5 | 0 | no |
| `closePhotoViewer` | function | 20231 | PHOTO VIEWER | 2 | 2 | 0 | yes |
| `photoNav` | function | 20238 | PHOTO VIEWER | 2 | 2 | 0 | yes |
| `photoZoom` | function | 20246 | PHOTO VIEWER | 2 | 2 | 0 | yes |
| `photoZoomReset` | function | 20251 | PHOTO VIEWER | 1 | 1 | 0 | yes |
| `deleteViewedPhoto` | function | 20256 | PHOTO VIEWER | 1 | 1 | 0 | yes |
| `downloadViewedPhoto` | function | 20274 | PHOTO VIEWER | 1 | 1 | 0 | yes |
| `openPhotoModal` | function | 20282 | PHOTO VIEWER | 2 | 2 | 0 | yes |
| `_sigJobId` | let | 20289 | SIGNATURE MODAL | 5 | 3 | 2 | no |
| `_sigCanvas` | let | 20290 | SIGNATURE MODAL | 22 | 20 | 2 | no |
| `_sigCtx` | let | 20291 | SIGNATURE MODAL | 17 | 15 | 2 | no |
| `_sigDrawing` | let | 20292 | SIGNATURE MODAL | 8 | 2 | 6 | no |
| `openSigModal` | function | 20294 | SIGNATURE MODAL | 2 | 2 | 0 | yes |
| `clearSig` | function | 20316 | SIGNATURE MODAL | 1 | 1 | 0 | yes |
| `saveSig` | function | 20320 | SIGNATURE MODAL | 1 | 1 | 0 | yes |
| `openFieldNotes` | function | 20335 | FIELD NOTES | 0 | 0 | 0 | no |
| `setTheme` | function | 20348 | THEME TOGGLE | 3 | 3 | 0 | yes |
| `_updateThemeBtns` | function | 20359 | THEME TOGGLE | 3 | 3 | 0 | no |

## Unresolved identifiers (not declared anywhere in the main script)

External globals referenced but not declared here — e.g. `firebase`, `document`, `window`,
browser APIs, or CDN-provided names. Not a problem by itself; listed for completeness.

`Array`, `BLUE`, `Blob`, `Boolean`, `Date`, `Error`, `FileReader`, `GRAY`, `Image`, `JSON`, `Math`, `Notification`, `Number`, `Object`, `Promise`, `Set`, `String`, `Stripe`, `URL`, `URLSearchParams`, `alert`, `applyJobDefaults`, `btn`, `clearTimeout`, `confirm`, `console`, `document`, `fetch`, `firebase`, `getComputedStyle`, `isNaN`, `localStorage`, `navigator`, `parseFloat`, `parseInt`, `populateCrewDatalist`, `prompt`, `setTimeout`, `undefined`, `window`

## Inline HTML event handlers

Found 808 total. Classification distinguishes a direct function call
(`toggleNavGroup('command')`) from an assignment, a multi-statement handler, or anything
that did not parse as a standalone JS snippet.

| Event | Count |
|---|---|
| onclick | 412 |
| onfocus | 152 |
| onblur | 151 |
| onchange | 66 |
| onkeypress | 9 |
| onerror | 6 |
| oninput | 5 |
| onkeyup | 3 |
| onmouseover | 1 |
| onmouseout | 1 |
| onmouseenter | 1 |
| onmouseleave | 1 |

| Classification | Count |
|---|---|
| direct-call | 340 |
| assignment | 308 |
| multi-statement | 72 |
| unparsed | 60 |
| other-expression | 28 |

Sample (first 15, by file line):

| Line | Event | Classification | Calls | In main-script-generated markup |
|---|---|---|---|---|
| 672 | onclick | direct-call | installPWA | no (static HTML) |
| 673 | onclick | direct-call | dismissInstall | no (static HTML) |
| 723 | onclick | direct-call | installPWA | no (static HTML) |
| 724 | onclick | direct-call | dismissInstall | no (static HTML) |
| 759 | onclick | direct-call | createAdminAccount | no (static HTML) |
| 766 | onkeypress | other-expression | doLogin | no (static HTML) |
| 769 | onkeypress | other-expression | doLogin | no (static HTML) |
| 770 | onclick | multi-statement | — | no (static HTML) |
| 773 | onclick | direct-call | doLogin | no (static HTML) |
| 775 | onclick | direct-call | openPasswordRecovery | no (static HTML) |
| 785 | onkeypress | other-expression | doPasswordRecovery | no (static HTML) |
| 788 | onkeypress | other-expression | doPasswordRecovery | no (static HTML) |
| 789 | onclick | multi-statement | — | no (static HTML) |
| 792 | onkeypress | other-expression | doPasswordRecovery | no (static HTML) |
| 794 | onkeypress | other-expression | doPasswordRecovery | no (static HTML) |

## Phase 2 `fleetflow:ready` candidate dispatch points

Findings only — **no runtime code is added in Phase 0.** These are the identifiers most
likely to mark "authentication resolved + dashboard ready" across the different startup
paths (fresh login, restored session, role-specific render, logout/login cycle, local testing).
Phase 2 planning should trace all of these before choosing the exact-once guarded dispatch site.

Auth/startup-named bindings:

| Name | Kind | Declared line | Section |
|---|---|---|---|
| `doLogin` | function | 6391 | SESSION PERSISTENCE — remember me across refreshes/tab closes |
| `showLoginError` | function | 6501 | SESSION PERSISTENCE — remember me across refreshes/tab closes |
| `doLogout` | function | 6507 | SESSION PERSISTENCE — remember me across refreshes/tab closes |

`currentUser` write sites (candidate "auth resolved" points): lines 5838, 5862, 5903, 6415, 6458, 6511

Dashboard/nav entry points (candidate "app rendered" points):

- `buildTabs` declared at line 6624
- `renderActiveTab` declared at line 6700
- `renderDashboard` declared at line 6957

